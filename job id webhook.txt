local P = {
    SelectedPetData = nil,
    AllAnimalsCache = nil,
    DisableStealSpeed = nil,
    ListNeedsRedraw = true,
    AdminButtonCache = {},
    StealSpeedToggleFunc = nil,
    _ssUpdateBtn = nil,
    AdminProxBtn = nil,
    BalloonedPlayers = {},
    MobileScaleObjects = {},
    RefreshMobileScale = nil,
    MobileActionButtons = {},
    RefreshMobileActionButtons = nil,
}
do
    local C = { AUTO_STEAL = false, RADIUS = 12 }
    local r =
        {
            {
                min = Vector3.new(-337.448303, -3.898971, -122.397758),
                max = Vector3.new(-328.004578, -3.898971, 242.625626),
            },
            {
                min = Vector3.new(-327.25766, -3.899109, -122.228622),
                max = Vector3.new(-320.600891, -3.899109, 242.612259),
            },
            {
                min = Vector3.new(-319.783386, -3.89897, -122.227089),
                max = Vector3.new(-312.908325, -3.89897, 242.585617),
            },
            {
                min = Vector3.new(-312.445648, -3.899108, -122.389832),
                max = Vector3.new(-305.489899, -3.899108, 242.456818),
            },
            {
                min = Vector3.new(-305.037048, -3.89897, -122.230743),
                max = Vector3.new(-293.957489, -3.89897, 242.606873),
            },
            {
                min = Vector3.new(-491.448608, -3.898972, -122.253258),
                max = Vector3.new(-481.811737, -3.898972, 242.615005),
            },
            {
                min = Vector3.new(-498.971069, -3.89897, -122.382767),
                max = Vector3.new(-491.74884, -3.89897, 242.612061),
            },
            {
                min = Vector3.new(-506.436737, -3.898972, -122.411476),
                max = Vector3.new(-499.318542, -3.898972, 242.615982),
            },
            {
                min = Vector3.new(-513.783569, -3.898972, -122.223297),
                max = Vector3.new(-506.801849, -3.898972, 242.62709),
            },
            {
                min = Vector3.new(-525.236938, -3.898972, -122.409813),
                max = Vector3.new(-514.265015, -3.898972, 242.608932),
            },
        }
    local R = game:GetService("Players")
    local d = game:GetService("RunService")
    local n = R.LocalPlayer
    do
        local R
        local function f()
            n.DevEnableMouseLock = true
            n.DevCameraOcclusionMode = Enum.DevCameraOcclusionMode.Invisicam
            if R then
                R:Disconnect()
            end
            R = d.RenderStepped:Connect(function()
                local R = workspace.CurrentCamera
                if R and R.CameraSubject and R.CameraType == Enum.CameraType.Custom then
                    R.CFrame = R.CFrame
                end
            end)
        end
        f()
        n.CharacterAdded:Connect(function()
            task.wait(0.5)
            f()
        end)
    end
    local R = {}
    local d = {}
    local f = 0
    function _G.getSafePollRate()
        if os.clock() < f then
            return 0.27
        end
        return 0.1
    end
    function _G.triggerSafePollBoost()
        f = os.clock() + 3
    end
    local f = {}
    local function J()
        local a = n.Character
        return a and a:FindFirstChild("HumanoidRootPart")
    end
    local function n(a)
        for e, x in ipairs(r) do
            if
                a.X >= math.min(x.min.X, x.max.X)
                and a.X <= math.max(x.min.X, x.max.X)
                and a.Z >= math.min(x.min.Z, x.max.Z)
                and a.Z <= math.max(x.min.Z, x.max.Z)
            then
                return e
            end
        end
    end
    local function r(a)
        local e = a.Parent
        if not e then
            return
        end
        if e:IsA("Attachment") and e.Parent then
            e = e.Parent
        end
        if e:IsA("BasePart") then
            return e.Position
        elseif e:IsA("Model") then
            return e:GetPivot().Position
        end
    end
    local function a(e)
        if not P then
            return false
        end
        local x = P.SelectedPetData
        if not x then
            return false
        end
        local Q = e:FindFirstAncestorOfClass("Model")
        if not Q then
            return false
        end
        if x.plot then
            local V = e:FindFirstAncestor(x.plot)
            if not V then
                return false
            end
        end
        if x.slot then
            local V = e:FindFirstAncestor(x.slot)
            if V then
                return true
            end
            if Q.Name == x.slot then
                return true
            end
            if Q.Parent and Q.Parent.Name == x.slot then
                return true
            end
        end
        if x.name then
            local e = string.lower(x.name)
            local x = Q
            while x do
                if x.Name and string.lower(x.Name) == e then
                    return true
                end
                x = x.Parent
            end
        end
        return false
    end
    local function e(x, Q)
        if not x or not x.Parent then
            return false
        end
        if not x.Enabled then
            return false
        end
        local V = r(x)
        if not V then
            return false
        end
        local r = x:FindFirstAncestorOfClass("Model")
        if r then
            local r = workspace:FindFirstChild("Plots")
            if r then
                local i = x:FindFirstAncestorWhichIsA("Model")
                while i and i.Parent ~= r do
                    i = i.Parent
                end
                if i then
                    local r = i:FindFirstChild("PlotSign")
                    if r then
                        local i = r:FindFirstChildWhichIsA("SurfaceGui", true)
                        local r = i and i:FindFirstChildWhichIsA("TextLabel", true)
                        if r then
                            local i = r.Text:lower()
                            if
                                i:find(game.Players.LocalPlayer.Name:lower(), 1, true)
                                or i:find(game.Players.LocalPlayer.DisplayName:lower(), 1, true)
                            then
                                return false
                            end
                        end
                    end
                end
            end
        end
        if _G.NEAREST_INSTANT_MODE == true then
            local r = n(Q)
            local i = n(V)
            if not r or r ~= i then
                return false
            end
        end
        if _G.NEAREST_INSTANT_MODE ~= true then
            if not a(x) then
                return false
            end
        end
        local r = typeof(x.MaxActivationDistance) == "number"
                and x.MaxActivationDistance > 0
                and x.MaxActivationDistance
            or C.RADIUS
        local n = math.min(C.RADIUS, r)
        return (V - Q).Magnitude <= n
    end
    local function r(n, a)
        local x = os.clock()
        local Q = d[n]
        if Q and x - Q < a then
            return false
        end
        d[n] = x
        return true
    end
    local function n(a, x, Q)
        if not a or not a.Parent then
            return
        end
        if not a.Enabled then
            return
        end
        if not r(a, Q) then
            return
        end
        for r = 1, x do
            pcall(function()
                fireproximityprompt(a, 0)
            end)
        end
    end
    local function r(a)
        if R[a] then
            return
        end
        R[a] = true
        local function x()
            local Q = J()
            if not Q then
                return
            end
            local V = Q.Position
            if e(a, V) then
                C.AUTO_STEAL = true
                local Q = os.clock()
                local V = f[a]
                if not V or Q - V >= 0.08 then
                    f[a] = Q
                    n(a, 25, 0)
                end
            end
        end
        task.defer(function()
            x()
        end)
        pcall(function()
            a:GetPropertyChangedSignal("Enabled"):Connect(function()
                if a.Enabled then
                    x()
                end
            end)
        end)
        a.AncestryChanged:Connect(function()
            if not a:IsDescendantOf(workspace) then
                R[a] = nil
                d[a] = nil
                f[a] = nil
            end
        end)
    end
    local function d()
        local f = workspace:FindFirstChild("Plots")
        if not f then
            return
        end
        for a, a in ipairs(f:GetChildren()) do
            local f = a:FindFirstChild("AnimalPodiums")
            if f then
                for a, a in ipairs(f:GetDescendants()) do
                    if a:IsA("ProximityPrompt") then
                        r(a)
                    end
                end
            end
        end
    end
    d()
    workspace.DescendantAdded:Connect(function(d)
        if d:IsA("ProximityPrompt") and d:FindFirstAncestor("AnimalPodiums") then
            r(d)
        end
    end)
    task.spawn(function()
        while task.wait(_G.getSafePollRate()) do
            local r = J()
            if not r then
                C.AUTO_STEAL = false
                continue
            end
            local d = r.Position
            local r = false
            for f in pairs(R) do
                if e(f, d) then
                    r = true
                    if C.AUTO_STEAL then
                        n(f, 3, 0.12)
                    end
                end
            end
            C.AUTO_STEAL = r
        end
    end)
end
local C = {
    Players = game:GetService("Players"),
    RunService = game:GetService("RunService"),
    UserInputService = game:GetService("UserInputService"),
    ReplicatedStorage = game:GetService("ReplicatedStorage"),
    TweenService = game:GetService("TweenService"),
    HttpService = game:GetService("HttpService"),
    Workspace = game:GetService("Workspace"),
    Lighting = game:GetService("Lighting"),
    GuiService = game:GetService("GuiService"),
    TeleportService = game:GetService("TeleportService"),
}
local r = C.Players
local R = C.RunService
local d = C.UserInputService
local n = C.ReplicatedStorage
local f = C.TweenService
local J = C.HttpService
local a = C.Workspace
local e = C.Lighting
local e = C.GuiService
local e = C.TeleportService
local C = r.LocalPlayer
local x = C:WaitForChild("PlayerGui")
local Q
Q = setmetatable({}, {
    __index = function(V, V)
        local i = n.Packages.Net
        local O, U
        if V:sub(1, 3) == "RE/" then
            O = "RE/"
            U = V:sub(4)
        elseif V:sub(1, 3) == "RF/" then
            O = "RF/"
            U = V:sub(4)
        else
            return nil
        end
        local O
        for U, y in i:GetChildren() do
            if y.Name == V then
                O = i:GetChildren()[U + 1]
                break
            end
        end
        if O and not rawget(Q, V) then
            rawset(Q, V, O)
        end
        return rawget(Q, V)
    end,
})
local V = {}
function V:LarpNet(V)
    return Q[V]
end
local Q = a.CurrentCamera
local V = C:GetMouse()
local function V()
    return d.TouchEnabled and not d.KeyboardEnabled and not d.MouseEnabled
end
local d = V()
local V = {
    Positions = {
        SafePanel = { X = 0.02, Y = 0.5, OffsetX = 0, OffsetY = 0 },
        StealSpeed = { X = 0.02, Y = 0.18 },
        Settings = { X = 0.5, Y = 0.5, OffsetX = 0, OffsetY = 0 },
        InvisPanel = { X = 0.8578125, Y = 0.17260276361454258, OffsetX = 0, OffsetY = 0 },
        AutoSteal = { X = 0.02, Y = 0.35, OffsetX = 0, OffsetY = 0 },
        MobileControls = { X = 0.9, Y = 0.4 },
        MobileBtn_TP = { X = 0.5, Y = 0.4 },
        MobileBtn_CL = { X = 0.5, Y = 0.4 },
        MobileBtn_SP = { X = 0.5, Y = 0.4 },
        MobileBtn_IV = { X = 0.5, Y = 0.4 },
        MobileBtn_UI = { X = 0.5, Y = 0.4 },
        MobileBtn_KICK = { X = 0.5, Y = 0.4 },
        JobJoiner = { X = 0.5, Y = 0.85 },
        TargetControls = { X = 0.26, Y = 0.35, OffsetX = 0, OffsetY = 0 },
    },
    TpSettings = {
        Tool = "Flying Carpet",
        Speed = 8,
        TpKey = "T",
        CloneKey = "V",
        TpOnLoad = false,
        MinGenForTp = "",
        CarpetSpeedKey = "Q",
        InfiniteJump = false,
        TeleportV2 = false,
    },
    StealSpeed = 20,
    ShowStealSpeedPanel = true,
    MenuKey = "LeftControl",
    MobileGuiScale = 0.5,
    XrayEnabled = false,
    AntiRagdoll = 0,
    AntiRagdollV2 = false,
    FPSBoost = false,
    UnwalkEnabled = false,
    FPSBoostV2 = false,
    TracerEnabled = true,
    BrainrotESP = true,
    Float = false,
    FloatKeybind = "Z",
    LineToBase = false,
    StealNearest = false,
    StealHighest = true,
    StealPriority = false,
    DefaultToNearest = false,
    DefaultToHighest = false,
    DefaultToPriority = false,
    UILocked = false,
    HideSafePanel = false,
    HideAutoSteal = false,
    CompactAutoSteal = false,
    AutoKickOnSteal = false,
    InstantSteal = false,
    InvisStealAngle = 233,
    SinkSliderValue = 5,
    AutoRecoverLagback = true,
    AutoInvisDuringSteal = false,
    InvisToggleKey = "I",
    ClickToAP = false,
    ClickToAPKeybind = "L",
    DisableClickToAPOnMoby = false,
    ProximityAP = false,
    ProximityAPKeybind = "P",
    ProximityRange = 15,
    StealSpeedKey = "C",
    ShowInvisPanel = true,
    ResetKey = "X",
    AutoResetOnBalloon = false,
    AntiBeeDisco = false,
    AutoDestroyTurrets = false,
    FOV = 70,
    SubspaceMineESP = false,
    AutoUnlockOnSteal = false,
    ShowUnlockButtonsHUD = false,
    AutoTPOnFailedSteal = false,
    AutoKickOnSteal = false,
    AutoTPPriority = true,
    KickKey = "",
    CleanErrorGUIs = false,
    ClickToAPSingleCommand = false,
    RagdollSelfKey = "B",
    AlertsEnabled = true,
    AlertSoundID = "rbxassetid://6518811702",
    DisableProximitySpamOnMoby = false,
    CancelAPPanelOnMoby = true,
    DisableClickToAPOnKawaifu = false,
    DisableProximitySpamOnKawaifu = false,
    CancelAPPanelOnKawaifu = true,
    AutoStealSpeed = false,
    ShowJobJoiner = false,
    XrayBase = false,
    ShowMobileActionButtons = true,
    JobJoinerKey = "J",
    PriorityList = {
        "Strawberry Elephant",
        "Meowl",
        "Skibidi Toilet",
        "Headless Horseman",
        "Dragon Gingerini",
        "Dragon Cannelloni",
        "Ketupat Bros",
        "Hydra Dragon Cannelloni",
        "La Supreme Combinasion",
        "Love Love Bear",
        "Ginger Gerat",
        "Cerberus",
        "Capitano Moby",
        "La Casa Boo",
        "Burguro and Fryuro",
        "Spooky and Pumpky",
        "Cooki and Milki",
        "Rosey and Teddy",
        "Popcuru and Fizzuru",
        "Reinito Sleighito",
        "Fragrama and Chocrama",
        "Garama and Madundung",
        "Ketchuru and Musturu",
        "La Secret Combinasion",
        "Tralaledon",
        "Tictac Sahur",
        "Ketupat Kepat",
        "Tang Tang Keletang",
        "Orcaledon",
        "La Ginger Sekolah",
        "Los Spaghettis",
        "Lavadorito Spinito",
        "Swaggy Bros",
        "La Taco Combinasion",
        "Los Primos",
        "Chillin Chili",
        "Tuff Toucan",
        "W or L",
        "Chillin Chili",
        "Chipso and Queso",
    },
}
function DeepCopy(i)
    if type(i) ~= "table" then
        return i
    end
    local O = {}
    for U, y in pairs(i) do
        O[U] = DeepCopy(y)
    end
    return O
end
function MergeDefaults(i, O)
    for U, y in pairs(O) do
        if type(y) == "table" then
            if type(i[U]) ~= "table" then
                i[U] = DeepCopy(y)
            else
                MergeDefaults(i[U], y)
            end
        elseif i[U] == nil then
            i[U] = y
        end
    end
end
local i = DeepCopy(V)
function NormalizeKeyName(O, U)
    if type(O) ~= "string" or O == "" then
        return U
    end
    local y = {
        ALT = "LeftAlt",
        LALT = "LeftAlt",
        RALT = "RightAlt",
        CTRL = "LeftControl",
        CONTROL = "LeftControl",
        LCTRL = "LeftControl",
        RCTRL = "RightControl",
        SHIFT = "LeftShift",
        LSHIFT = "LeftShift",
        RSHIFT = "RightShift",
        WIN = "LeftSuper",
        CMD = "LeftSuper",
        META = "LeftSuper",
    }
    local K = string.upper(O)
    local q = y[K] or O
    return Enum.KeyCode[q] and q or U
end
function PrettyKeyName(O)
    local U = {
        LeftAlt = "ALT",
        RightAlt = "RALT",
        LeftControl = "CTRL",
        RightControl = "RCTRL",
        LeftShift = "SHIFT",
        RightShift = "RSHIFT",
        LeftSuper = "WIN",
        RightSuper = "RWIN",
    }
    return U[O] or tostring(O or "")
end
if isfile and isfile("moradLACASABOO.json") then
    pcall(function()
        local O = readfile("moradLACASABOO.json")
        if not O or O == "" then
            return
        end
        local U = J:JSONDecode(O)
        if type(U) ~= "table" then
            return
        end
        local O = nil
        if type(U.PriorityList) == "table" then
            O = DeepCopy(U.PriorityList)
        end
        MergeDefaults(U, V)
        if O ~= nil then
            U.PriorityList = O
        end
        i = U
    end)
end
i.ProximityAP = false
if i.Positions then
    i.Positions.Settings = { X = 0.5, Y = 0.5, OffsetX = 0, OffsetY = 0 }
end
if type(i.CancelAPPanelOnMoby) ~= "boolean" then
    i.CancelAPPanelOnMoby = true
end
if type(i.CancelAPPanelOnKawaifu) ~= "boolean" then
    i.CancelAPPanelOnKawaifu = true
end
function EnsureSavedKeybinds()
    i.SavedKeybinds = i.SavedKeybinds or {}
    i.SavedKeybinds.MenuKey =
        NormalizeKeyName(i.SavedKeybinds.MenuKey or i.MenuKey, V.MenuKey or "LeftControl")
    i.SavedKeybinds.ClickToAPKeybind =
        NormalizeKeyName(i.SavedKeybinds.ClickToAPKeybind or i.ClickToAPKeybind, V.ClickToAPKeybind or "L")
    i.SavedKeybinds.ProximityAPKeybind = NormalizeKeyName(
        i.SavedKeybinds.ProximityAPKeybind or i.ProximityAPKeybind,
        V.ProximityAPKeybind or "P"
    )
    i.SavedKeybinds.StealSpeedKey =
        NormalizeKeyName(i.SavedKeybinds.StealSpeedKey or i.StealSpeedKey, V.StealSpeedKey or "C")
    i.SavedKeybinds.FloatKeybind =
        NormalizeKeyName(i.SavedKeybinds.FloatKeybind or i.FloatKeybind, V.FloatKeybind or "Z")
    i.SavedKeybinds.ResetKey = NormalizeKeyName(i.SavedKeybinds.ResetKey or i.ResetKey, V.ResetKey or "X")
    i.SavedKeybinds.JobJoinerKey =
        NormalizeKeyName(i.SavedKeybinds.JobJoinerKey or i.JobJoinerKey, V.JobJoinerKey or "J")
    i.SavedKeybinds.InvisToggleKey =
        NormalizeKeyName(i.SavedKeybinds.InvisToggleKey or i.InvisToggleKey, V.InvisToggleKey or "I")
    i.SavedKeybinds.KickKey = NormalizeKeyName(i.SavedKeybinds.KickKey or i.KickKey, "")
    i.SavedKeybinds.RagdollSelfKey = NormalizeKeyName(i.SavedKeybinds.RagdollSelfKey or i.RagdollSelfKey, "B")
    i.SavedKeybinds.TpKey =
        NormalizeKeyName(i.SavedKeybinds.TpKey or i.TpSettings.TpKey, V.TpSettings.TpKey or "T")
    i.SavedKeybinds.CloneKey =
        NormalizeKeyName(i.SavedKeybinds.CloneKey or i.TpSettings.CloneKey, V.TpSettings.CloneKey or "V")
    i.SavedKeybinds.CarpetSpeedKey = NormalizeKeyName(
        i.SavedKeybinds.CarpetSpeedKey or i.TpSettings.CarpetSpeedKey,
        V.TpSettings.CarpetSpeedKey or "Q"
    )
end
function ApplySavedKeybindsToConfig()
    EnsureSavedKeybinds()
    i.MenuKey = i.SavedKeybinds.MenuKey
    i.ClickToAPKeybind = i.SavedKeybinds.ClickToAPKeybind
    i.ProximityAPKeybind = i.SavedKeybinds.ProximityAPKeybind
    i.StealSpeedKey = i.SavedKeybinds.StealSpeedKey
    i.FloatKeybind = i.SavedKeybinds.FloatKeybind
    i.ResetKey = i.SavedKeybinds.ResetKey
    i.JobJoinerKey = i.SavedKeybinds.JobJoinerKey
    i.InvisToggleKey = i.SavedKeybinds.InvisToggleKey
    i.KickKey = i.SavedKeybinds.KickKey
    i.RagdollSelfKey = i.SavedKeybinds.RagdollSelfKey
    i.TpSettings.TpKey = i.SavedKeybinds.TpKey
    i.TpSettings.CloneKey = i.SavedKeybinds.CloneKey
    i.TpSettings.CarpetSpeedKey = i.SavedKeybinds.CarpetSpeedKey
end
local function O(U)
    local y = { [1] = 0.1, [2] = 0.2, [3] = 0.3, [4] = 0.4 }
    return y[tonumber(U)] or 0.1
end
function NormalizeAllKeybinds()
    EnsureSavedKeybinds()
    ApplySavedKeybindsToConfig()
end
NormalizeAllKeybinds()
local function U()
    if writefile then
        pcall(function()
            NormalizeAllKeybinds()
            local y = DeepCopy(i)
            y.ProximityAP = false
            y.MenuKey = i.SavedKeybinds.MenuKey
            y.ClickToAPKeybind = i.SavedKeybinds.ClickToAPKeybind
            y.ProximityAPKeybind = i.SavedKeybinds.ProximityAPKeybind
            y.StealSpeedKey = i.SavedKeybinds.StealSpeedKey
            y.FloatKeybind = i.SavedKeybinds.FloatKeybind
            y.ResetKey = i.SavedKeybinds.ResetKey
            y.JobJoinerKey = i.SavedKeybinds.JobJoinerKey
            y.InvisToggleKey = i.SavedKeybinds.InvisToggleKey
            y.KickKey = i.SavedKeybinds.KickKey
            y.RagdollSelfKey = i.SavedKeybinds.RagdollSelfKey
            y.TpSettings.TpKey = i.SavedKeybinds.TpKey
            y.TpSettings.CloneKey = i.SavedKeybinds.CloneKey
            y.TpSettings.CarpetSpeedKey = i.SavedKeybinds.CarpetSpeedKey
            writefile("moradLACASABOO.json", J:JSONEncode(y))
        end)
    end
end
if _needMigrationSave then
    U()
end
local function y(K)
    if not K or not K.Character then
        return false
    end
    return K.Character:FindFirstChild("_moby_highlight") ~= nil
end
local function K(q)
    if not q or not q.Character then
        return false
    end
    return q.Character:FindFirstChild("KaWaifu_NeonHighlight") ~= nil
end
_G.InvisStealAngle = i.InvisStealAngle
_G.SinkSliderValue = i.SinkSliderValue
_G.AutoRecoverLagback = i.AutoRecoverLagback
_G.AutoInvisDuringSteal = i.AutoInvisDuringSteal
_G.INVISIBLE_STEAL_KEY = Enum.KeyCode[i.InvisToggleKey] or Enum.KeyCode.I
_G.invisibleStealEnabled = false
_G.RecoveryInProgress = false
local function q()
    local c = C:WaitForChild("PlayerScripts")
    local z = require(c:WaitForChild("PlayerModule"))
    return z:GetControls()
end
local c = q()
local function c()
    local z = pcall(function()
        game:Shutdown()
    end)
    if z then
        return
    end
    pcall(function()
        C:Kick("")
    end)
end
local function z(B)
    local o = C.Character
    local Y = o:FindFirstChild("Humanoid")
    local T = o:FindFirstChild("HumanoidRootPart")
    local o = q()
    local q = T.CFrame.LookVector
    o:Disable()
    local T = os.clock()
    local I
    I = R.RenderStepped:Connect(function()
        if os.clock() - T >= B then
            I:Disconnect()
            Y:Move(Vector3.zero, false)
            o:Enable()
            return
        end
        Y:Move(q, false)
    end)
end
local function q()
    local B = game:GetService("Players")
    local o = B.LocalPlayer
    if not o then
        return
    end
    local B = o.Character or o.CharacterAdded:Wait()
    local Y = B:FindFirstChildOfClass("Humanoid")
    if not Y then
        return
    end
    local T = o.Backpack:FindFirstChild("Quantum Cloner") or B:FindFirstChild("Quantum Cloner")
    if not T then
        return
    end
    if T.Parent ~= B then
        Y:EquipTool(T)
        task.wait()
    end
    local B = o:WaitForChild("PlayerGui")
    local o = B:FindFirstChild("ToolsFrames")
    local B = o and o:FindFirstChild("QuantumCloner")
    local o = B and B:FindFirstChild("TeleportToClone")
    if not o then
        return
    end
    T:Activate()
    task.wait(0.05)
    o.Visible = true
    if typeof(firesignal) == "function" then
        firesignal(o.MouseButton1Up)
        if _G.triggerSafePollBoost then
            _G.triggerSafePollBoost()
        end
    end
end
local function B(o)
    local Y = 0
    while Y < o do
        Y += R.Heartbeat:Wait()
    end
end
local function o(Y, T)
    local I = 0
    while true do
        if Y() then
            return true
        end
        local Y = R.Heartbeat:Wait()
        I += Y
        if T and I >= T then
            return false
        end
    end
end
local function Y(T)
    if not T or not T:IsA("BasePart") then
        return nil
    end
    local I = nil
    local p = math.huge
    for j, j in ipairs(a:GetDescendants()) do
        if j:IsA("TextLabel") then
            local l = tostring(j.Text or "")
            if l ~= "" and l:lower():find("base", 1, true) then
                local l = j:FindFirstAncestorWhichIsA("SurfaceGui")
                if l then
                    local j = l.Adornee or l.Parent
                    if j and j:IsA("BasePart") then
                        local l = (j.Position - T.Position).Magnitude
                        if l < p then
                            p = l
                            I = j
                        end
                    end
                end
            end
        end
    end
    return I
end
local function T(I, p)
    if not I then
        return
    end
    local j = os.clock()
    while I.Parent and I.Position.Y < p do
        local l = p - I.Position.Y
        local p = math.clamp(l * 20, 280, 310)
        I.Velocity = Vector3.new(I.Velocity.X, p, I.Velocity.Z)
        if os.clock() - j > 3 then
            break
        end
        R.Heartbeat:Wait()
    end
    I.Velocity = Vector3.new(I.Velocity.X, 0, I.Velocity.Z)
end
local function I(p)
    if not p then
        return nil
    end
    local j = p.Parent
    local l = i.TpSettings.Tool or "Flying Carpet"
    local W = C.Backpack:FindFirstChild(l) or j and j:FindFirstChild(l)
    if W then
        if W.Parent ~= j then
            p:EquipTool(W)
        else
            p:EquipTool(W)
        end
        task.wait(0.02)
    end
    return W
end
local function p(j, l)
    if not j or not l then
        return
    end
    local l = j.Parent
    local j = i.TpSettings.Tool or "Flying Carpet"
    local W = C.Backpack:FindFirstChild(j) or l and l:FindFirstChild(j)
    if not W then
        return
    end
end
local j = game:GetService("UserInputService")
local l = {
    { name = "MED1", pos = Vector3.new(-410.65, -5.68, -46.1) },
    { name = "MED2", pos = Vector3.new(-410.91, -5.68, 168.89) },
}
local W = {
    { name = "TP1", pos = Vector3.new(-488.88, 15, 196.38), facing = "back" },
    { name = "TP2", pos = Vector3.new(-487.79, 15, 138.13), facing = "front" },
    { name = "TP3", pos = Vector3.new(-489.38, 15, 89.23), facing = "back" },
    { name = "TP4", pos = Vector3.new(-489.69, 15, 30.98), facing = "front" },
    { name = "TP5", pos = Vector3.new(-488.75, 15, -17.95), facing = "back" },
    { name = "TP6", pos = Vector3.new(-490.0, 15, -75.9), facing = "front" },
    { name = "TP7", pos = Vector3.new(-331.75, 15, -75.8), facing = "back" },
    { name = "TP8", pos = Vector3.new(-329.98, 15, -18.16), facing = "front" },
    { name = "TP9", pos = Vector3.new(-330.04, 15, 31.14), facing = "back" },
    { name = "TP10", pos = Vector3.new(-331.28, 15, 88.92), facing = "front" },
    { name = "TP11", pos = Vector3.new(-330.57, 15, 138.1), facing = "back" },
    { name = "TP12", pos = Vector3.new(-330.01, 15, 195.96), facing = "front" },
}
local function X(t, G)
    return (Vector3.new(t.X, 0, t.Z) - Vector3.new(G.X, 0, G.Z)).Magnitude
end
local function t(G)
    local v = nil
    local M = math.huge
    for w, w in ipairs(a:GetDescendants()) do
        if w:IsA("TextLabel") then
            local b = tostring(w.Text or "")
            if b ~= "" and b:lower():find("base", 1, true) then
                local b = w:FindFirstAncestorWhichIsA("SurfaceGui")
                if b then
                    local w = b.Adornee or b.Parent
                    if w and w:IsA("BasePart") then
                        local b = (w.Position - G).Magnitude
                        if b < M then
                            M = b
                            v = w
                        end
                    end
                end
            end
        end
    end
    return v
end
local function G(v)
    local M = nil
    local w = math.huge
    for b, b in ipairs(l) do
        local l = X(v, b.pos)
        if l < w then
            w = l
            M = b
        end
    end
    return M, w
end
local l = {
    MED1 = { TP6 = true, TP7 = true, TP8 = true, TP10 = true, TP12 = true, TP5 = true, TP3 = true, TP1 = true },
    MED2 = { TP1 = true, TP2 = true, TP4 = true, TP6 = true, TP7 = true, TP9 = true, TP11 = true, TP12 = true },
}
local function v(M, w)
    local b = M and M.pos
    local D = M and M.name
    if not b or not D then
        return nil
    end
    local M = l[D]
    if not M then
        return nil
    end
    local l = nil
    local D = math.huge
    local F = math.huge
    for E, E in ipairs(W) do
        if M[E.name] then
            local W = X(b, E.pos)
            local M = X(w, E.pos)
            if M < F or math.abs(M - F) <= 0.001 and W < D then
                l = E
                D = W
                F = M
            end
        end
    end
    return l, D, F
end
local function l(W, M, w)
    if not W or not M or not w then
        return false, "missing target/hum/hrp"
    end
    local b = w.Position
    local D = W.Position
    local D = G(b)
    if not D then
        return false, "no MED point found"
    end
    local b = W.Position
    local F = v(D, b)
    if not F then
        return false, "no TP1-TP12 point found"
    end
    local v = b.Y <= 6.313370704650879
    local b = v and Vector3.new(F.pos.X, 1, F.pos.Z) or F.pos
    local v = t(b) or Y(W)
    local W = nil
    if v then
        W = F.facing == "front" and v.CFrame.LookVector or -v.CFrame.LookVector
        if W.Magnitude <= 0 then
            W = nil
        end
    end
    local t = a.CurrentCamera
    local v = t and t.CameraType
    local E = t and t.FieldOfView
    local m = nil
    if W then
        w.Velocity = Vector3.zero
        w.CFrame = CFrame.new(w.Position, w.Position + W)
        w.Velocity = Vector3.zero
    end
    if t then
        t.CameraType = Enum.CameraType.Scriptable
        t.FieldOfView = 95
        m = R.RenderStepped:Connect(function()
            local A = C.Character and C.Character:FindFirstChild("HumanoidRootPart")
            if A and t then
                t.CFrame = A.CFrame * CFrame.new(0, 2, 10) * CFrame.Angles(-0.08726646259971647, 0, 0)
            end
        end)
    end
    local function A()
        if m then
            m:Disconnect()
            m = nil
        end
        if t then
            if v then
                t.CameraType = v
            end
            if E then
                t.FieldOfView = E
            end
        end
    end
    local function t(v, E)
        A()
        return v, E
    end
    w.Velocity = Vector3.zero
    if M and M.Parent then
        M:ChangeState(Enum.HumanoidStateType.Jumping)
    end
    task.wait(0.01)
    if not w or not w.Parent then
        return t(false, "hrp lost during jump start")
    end
    local v = Vector3.new(D.pos.X, w.Position.Y, D.pos.Z)
    w.Velocity = Vector3.zero
    w.CFrame = W and CFrame.new(v, v + W) or CFrame.new(v)
    w.Velocity = Vector3.zero
    task.wait(0.1)
    if not w or not w.Parent then
        return t(false, "hrp lost after MED reposition")
    end
    local v = o(function()
        return w and w.Parent and X(w.Position, D.pos) <= 2.5
    end, 1.5)
    if not v then
        return t(false, "failed to reach MED point")
    end
    T(w, 45)
    w.Velocity = Vector3.zero
    w.CFrame = W and CFrame.new(b, b + W) or CFrame.new(b)
    w.Velocity = Vector3.zero
    local W = o(function()
        return w and w.Parent and (w.Position - b).Magnitude <= 2.5
    end, 3)
    if not W then
        return t(false, "failed to reach TP1-TP12")
    end
    B(0.1)
    B(0.05)
    local W = o(function()
        return w and w.Parent and (w.Position - b).Magnitude <= 2.5
    end, 0.75)
    if not W then
        return t(false, "moved away from TP1-TP12 before clone")
    end
    w.Velocity = Vector3.zero
    w.AssemblyAngularVelocity = Vector3.zero
    local W = w.Position
    task.wait(0.1)
    q()
    local X = o(function()
        return w and w.Parent and (w.Position - W).Magnitude >= 0.5
    end, 5)
    while _G.isCloning do
        task.wait()
    end
    task.wait(0.2)
    return t(true, F.name)
end
local W = game:GetService("CoreGui")
local X = nil
local t = nil
local v = nil
local M = nil
local w = nil
local function b(D, F)
    pcall(function()
        if writefile then
            writefile(
                "HazeButtonsClean2_Pos.json",
                J:JSONEncode({
                    xScale = D.X.Scale,
                    xOffset = D.X.Offset,
                    yScale = D.Y.Scale,
                    yOffset = D.Y.Offset,
                    locked = F,
                })
            )
        end
    end)
end
local function D()
    local F = UDim2.new(0.5, -108.0, 0.5, -21.0)
    local E, m = pcall(function()
        if readfile and isfile and isfile("HazeButtonsClean2_Pos.json") then
            return J:JSONDecode(readfile("HazeButtonsClean2_Pos.json"))
        end
    end)
    if E and m then
        return UDim2.new(m.xScale or 0.5, m.xOffset or -108.0, m.yScale or 0.5, m.yOffset or -21.0),
            m.locked == true
    end
    return F, false
end
local function J()
    local F = C.Character
    if not F then
        return
    end
    return F:FindFirstChild("HumanoidRootPart") or F:FindFirstChild("UpperTorso")
end
local function F(E)
    local m = C.Character
    local A = J()
    if not m or not A then
        return
    end
    local m = a:FindFirstChild("Plots")
    if not m then
        return
    end
    local H, _ = nil, 40
    for Z, Z in pairs(m:GetChildren()) do
        local m
        if Z:IsA("Model") then
            m = Z.PrimaryPart and Z.PrimaryPart.Position or Z:GetPivot().Position
        else
            m = Z.Position
        end
        local k = (A.Position - m).Magnitude
        if k < _ then
            H = Z
            _ = k
        end
    end
    if H and H:FindFirstChild("Unlock") then
        local m = {}
        for A, A in pairs(H.Unlock:GetChildren()) do
            local H = A:IsA("Model") and A:GetPivot().Position or A.Position
            table.insert(m, { Obj = A, Y = H.Y })
        end
        table.sort(m, function(A, H)
            return A.Y < H.Y
        end)
        if m[E] then
            for A, A in pairs(m[E].Obj:GetDescendants()) do
                if A:IsA("ProximityPrompt") then
                    pcall(function()
                        fireproximityprompt(A)
                    end)
                end
            end
        end
    end
end
local function E()
    local m = J()
    if not m then
        return 1
    end
    local J = m.Position.Y
    if J < 12 then
        return 1
    elseif J < 30 then
        return 2
    else
        return 3
    end
end
local function J()
    local m = E()
    F(m)
    ShowNotification("AUTO UNLOCK", "Level " .. tostring(m))
end
local function E()
    if X then
        pcall(function()
            X:Destroy()
        end)
    end
    X = nil
    t = nil
    v = nil
    M = nil
    w = nil
end
function toggleUnlockBaseMenu(m)
    if not m then
        E()
        return
    end
    E()
    local E, m = D()
    local function D(A, H)
        local _ = Instance.new("UICorner")
        _.CornerRadius = UDim.new(0, H)
        _.Parent = A
        return _
    end
    local function A(H, _, Z, k)
        local u = Instance.new("UIStroke")
        u.Color = _
        u.Thickness = Z or 1
        u.Transparency = k or 0
        u.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        u.Parent = H
        return u
    end
    local function H(_, Z, k, u, g)
        f:Create(_, TweenInfo.new(Z or 0.16, u or Enum.EasingStyle.Quint, g or Enum.EasingDirection.Out), k)
            :Play()
    end
    local function _(Z, k, u, g)
        local h = Instance.new("UIGradient")
        h.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, k), ColorSequenceKeypoint.new(1, u) })
        h.Rotation = g or 0
        h.Parent = Z
        return h
    end
    local Z = {
        BG = Color3.fromRGB(10, 20, 35),
        SURF = Color3.fromRGB(15, 30, 50),
        TEXT = Color3.fromRGB(220, 240, 255),
        AQUA_STROKE = Color3.fromRGB(65, 120, 225),
        ACTIVE1 = Color3.fromRGB(30, 144, 255),
        ACTIVE2 = Color3.fromRGB(0, 102, 204),
        LOCK_RED_1 = Color3.fromRGB(80, 119, 170),
        LOCK_RED_2 = Color3.fromRGB(65, 82, 140),
        LOCK_GREEN_1 = Color3.fromRGB(70, 160, 115),
        LOCK_GREEN_2 = Color3.fromRGB(55, 135, 95),
    }
    local k = Instance.new("ScreenGui")
    k.Name = "SECDcxOHMxFe"
    k.ResetOnSpawn = false
    k.IgnoreGuiInset = true
    k.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    k.Parent = W
    X = k
    local W = Instance.new("Frame")
    W.Name = "UnlockRoot"
    W.BackgroundTransparency = 1
    W.Size = UDim2.fromOffset(204, 42)
    W.Position = E
    W.Parent = k
    v = W
    local X = Instance.new("TextButton")
    X.Name = "LockFrame"
    X.AutoButtonColor = false
    X.Text = ""
    X.Size = UDim2.fromOffset(32, 32)
    X.Position = UDim2.fromOffset(0, 5)
    X.BackgroundColor3 = Z.LOCK_RED_1
    X.BorderSizePixel = 0
    X.Parent = W
    M = X
    D(X, 10)
    local v = Instance.new("Frame")
    v.Name = "Fill"
    v.BackgroundColor3 = Z.LOCK_RED_1
    v.BorderSizePixel = 0
    v.Size = UDim2.fromScale(1, 1)
    v.Position = UDim2.fromScale(0, 0)
    v.Parent = X
    D(v, 10)
    local M = _(v, Z.LOCK_RED_1, Z.LOCK_RED_2, 90)
    local function E()
        if m then
            X.BackgroundColor3 = Z.LOCK_RED_1
            v.BackgroundColor3 = Z.LOCK_RED_1
            M.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Z.LOCK_RED_1),
                ColorSequenceKeypoint.new(1, Z.LOCK_RED_2),
            })
        else
            X.BackgroundColor3 = Z.LOCK_GREEN_1
            v.BackgroundColor3 = Z.LOCK_GREEN_1
            M.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Z.LOCK_GREEN_1),
                ColorSequenceKeypoint.new(1, Z.LOCK_GREEN_2),
            })
        end
    end
    E()
    X.MouseButton1Click:Connect(function()
        m = not m
        E()
        b(W.Position, m)
    end)
    local X = Instance.new("Frame")
    X.Name = "UnlockButtonsBar"
    X.Size = UDim2.fromOffset(168, 42)
    X.Position = UDim2.fromOffset(36, 0)
    X.BackgroundColor3 = Z.BG
    X.BorderSizePixel = 0
    X.Parent = W
    t = X
    w = X
    D(X, 11)
    A(X, Z.AQUA_STROKE, 1.2, 0.3)
    local t = Instance.new("Frame")
    t.Name = "Inner"
    t.BackgroundColor3 = Z.SURF
    t.BorderSizePixel = 0
    t.Size = UDim2.new(1, 0, 1, 0)
    t.Parent = X
    D(t, 11)
    local X = Instance.new("UIPadding")
    X.Parent = t
    X.PaddingLeft = UDim.new(0, 5)
    X.PaddingRight = UDim.new(0, 5)
    X.PaddingTop = UDim.new(0, 5)
    X.PaddingBottom = UDim.new(0, 5)
    local X = Instance.new("UIListLayout")
    X.Parent = t
    X.FillDirection = Enum.FillDirection.Horizontal
    X.HorizontalAlignment = Enum.HorizontalAlignment.Center
    X.VerticalAlignment = Enum.VerticalAlignment.Center
    X.Padding = UDim.new(0, 4)
    local function X(v)
        local M = Instance.new("TextButton")
        M.Name = "Unlock" .. tostring(v)
        M.AutoButtonColor = false
        M.Text = ""
        M.Size = UDim2.fromOffset(50, 30)
        M.BackgroundColor3 = Z.ACTIVE1
        M.BorderSizePixel = 0
        M.Parent = t
        D(M, 8)
        local t = A(M, Z.AQUA_STROKE, 1, 0.12)
        local w = Instance.new("Frame")
        w.BackgroundTransparency = 0
        w.BorderSizePixel = 0
        w.Size = UDim2.fromScale(1, 1)
        w.Parent = M
        D(w, 8)
        _(w, Z.ACTIVE1, Z.ACTIVE2, 90)
        local w = Instance.new("TextLabel")
        w.BackgroundTransparency = 1
        w.Size = UDim2.fromScale(1, 1)
        w.Font = Enum.Font.FredokaOne
        w.Text = tostring(v)
        w.TextSize = 13
        w.TextColor3 = Z.TEXT
        w.Parent = M
        M.MouseEnter:Connect(function()
            H(t, 0.12, { Transparency = 0.02 })
        end)
        M.MouseLeave:Connect(function()
            H(t, 0.12, { Transparency = 0.12 })
        end)
        M.MouseButton1Down:Connect(function()
            M.Size = UDim2.fromOffset(48, 28)
        end)
        M.MouseButton1Up:Connect(function()
            M.Size = UDim2.fromOffset(50, 30)
        end)
        return M
    end
    local t = X("1")
    local v = X("2")
    local M = X("3")
    t.MouseButton1Click:Connect(function()
        F(1)
        ShowNotification("UNLOCK", "Level 1")
    end)
    v.MouseButton1Click:Connect(function()
        F(2)
        ShowNotification("UNLOCK", "Level 2")
    end)
    M.MouseButton1Click:Connect(function()
        F(3)
        ShowNotification("UNLOCK", "Level 3")
    end)
    local X = false
    local t
    local v
    W.InputBegan:Connect(function(M)
        if m then
            return
        end
        if M.UserInputType == Enum.UserInputType.MouseButton1 then
            X = true
            t = M.Position
            v = W.Position
            M.Changed:Connect(function()
                if M.UserInputState == Enum.UserInputState.End then
                    X = false
                    b(W.Position, m)
                end
            end)
        end
    end)
    j.InputChanged:Connect(function(M)
        if m then
            return
        end
        if X and M.UserInputType == Enum.UserInputType.MouseMovement then
            local X = M.Position - t
            W.Position = UDim2.new(v.X.Scale, v.X.Offset + X.X, v.Y.Scale, v.Y.Offset + X.Y)
        end
    end)
end
local W = {
    Background = Color3.fromRGB(8, 10, 18),
    Surface = Color3.fromRGB(10, 15, 28),
    SurfaceHighlight = Color3.fromRGB(16, 26, 48),
    Accent1 = Color3.fromRGB(60, 154, 220),
    Accent2 = Color3.fromRGB(30, 119, 170),
    TextPrimary = Color3.fromRGB(240, 240, 240),
    TextSecondary = Color3.fromRGB(140, 140, 150),
    Success = Color3.fromRGB(30, 150, 90),
    Error = Color3.fromRGB(80, 178, 255),
}
local X = {}
local function t()
    for v = #X, 1, -1.0 do
        X[v] = nil
    end
    for v, M in ipairs(i.PriorityList or {}) do
        X[v] = M
    end
end
local function v()
    i.PriorityList = {}
    for M, w in ipairs(X) do
        i.PriorityList[M] = w
    end
end
t()
local function M(w)
    if not w then
        return nil
    end
    local b = a:FindFirstChild("Plots") and a.Plots:FindFirstChild(w.plot)
    if b then
        local D = b:FindFirstChild("AnimalPodiums")
        if D then
            local b = D:FindFirstChild(w.slot)
            if b then
                local w = b:FindFirstChild("Base")
                if w then
                    local b = w:FindFirstChild("Spawn")
                    if b then
                        return b
                    end
                    return w:FindFirstChildWhichIsA("BasePart") or w
                end
            end
        end
    end
    return nil
end
local function w(b)
    if not b then
        return
    end
    if not d then
        return
    end
    local D = b:FindFirstChildOfClass("UIScale")
    if D then
        D:Destroy()
    end
    local D = Instance.new("UIScale")
    D.Parent = b
    P.MobileScaleObjects[b] = D
    if P.RefreshMobileScale then
        P.RefreshMobileScale()
    else
        D.Scale = math.clamp(tonumber(i.MobileGuiScale) or 0.5, 0, 1)
    end
end
P.RefreshMobileScale = function()
    local b = math.clamp(tonumber(i.MobileGuiScale) or 0.5, 0, 1)
    for D, F in pairs(P.MobileScaleObjects) do
        if D and D.Parent and F and F.Parent == D then
            F.Scale = b
        else
            P.MobileScaleObjects[D] = nil
        end
    end
end
local function b(D, F)
    if not d then
        return
    end
    if not D or not D.Parent then
        return
    end
    local E = D.Parent
    local m = D:FindFirstChildWhichIsA("Frame")
    if not m then
        return
    end
    local A = Instance.new("TextButton")
    A.Size = UDim2.new(0, 26, 0, 26)
    A.Position = UDim2.new(1, -30.0, 0, 6)
    A.BackgroundColor3 = W.SurfaceHighlight
    A.Text = "-"
    A.Font = Enum.Font.FredokaOne
    A.TextSize = 18
    A.TextColor3 = W.TextPrimary
    A.AutoButtonColor = false
    A.Parent = m
    Instance.new("UICorner", A).CornerRadius = UDim.new(0, 8)
    local m = Instance.new("TextButton")
    m.Size = UDim2.new(0, 110, 0, 34)
    m.Position = UDim2.new(0, 10, 1, -44.0)
    m.BackgroundColor3 = W.SurfaceHighlight
    m.Text = F or "OPEN"
    m.Font = Enum.Font.FredokaOne
    m.TextSize = 12
    m.TextColor3 = W.TextPrimary
    m.Visible = false
    m.AutoButtonColor = false
    m.Parent = E
    Instance.new("UICorner", m).CornerRadius = UDim.new(0, 10)
    MakeDraggable(m, m)
    A.MouseButton1Click:Connect(function()
        D.Visible = false
        m.Visible = true
    end)
    m.MouseButton1Click:Connect(function()
        D.Visible = true
        m.Visible = false
    end)
end
local function D(F, E, m)
    local A, H, _, Z
    F.InputBegan:Connect(function(k)
        if i.UILocked then
            return
        end
        if _G.ADMIN_PANEL_SLIDER_DRAG then
            return
        end
        if
            k.UserInputType == Enum.UserInputType.MouseButton1
            or k.UserInputType == Enum.UserInputType.Touch
        then
            A = true
            _ = k.Position
            Z = E.Position
            k.Changed:Connect(function()
                if k.UserInputState == Enum.UserInputState.End then
                    A = false
                    if m then
                        i.Positions[m] = {
                            X = E.Position.X.Scale,
                            Y = E.Position.Y.Scale,
                            OffsetX = E.Position.X.Offset,
                            OffsetY = E.Position.Y.Offset,
                        }
                        U()
                    end
                end
            end)
        end
    end)
    F.InputChanged:Connect(function(F)
        if
            F.UserInputType == Enum.UserInputType.MouseMovement
            or F.UserInputType == Enum.UserInputType.Touch
        then
            H = F
        end
    end)
    j.InputChanged:Connect(function(F)
        if _G.ADMIN_PANEL_SLIDER_DRAG then
            return
        end
        if F == H and A then
            local m = F.Position - _
            E.Position = UDim2.new(Z.X.Scale, Z.X.Offset + m.X, Z.Y.Scale, Z.Y.Offset + m.Y)
        end
    end)
end
local function F(E, m)
    local A = x:FindFirstChild("lMWjwEoSnCPj")
    if A then
        A:Destroy()
    end
    local A = Instance.new("ScreenGui", x)
    A.Name = "lMWjwEoSnCPj"
    A.ResetOnSpawn = false
    local H = Instance.new("Frame", A)
    H.Size = UDim2.new(0, 260, 0, 50)
    H.Position = UDim2.new(0.5, -130.0, 0, 80)
    H.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    H.BackgroundTransparency = 1
    H.BorderSizePixel = 0
    Instance.new("UICorner", H).CornerRadius = UDim.new(1, 0)
    local k = Instance.new("TextLabel", H)
    k.Size = UDim2.new(1, -30.0, 0, 18)
    k.Position = UDim2.new(0, 20, 0, 7)
    k.BackgroundTransparency = 1
    k.Text = E:upper()
    k.Font = Enum.Font.FredokaOne
    k.TextSize = 12
    k.TextColor3 = Color3.fromRGB(46, 204, 113)
    k.TextXAlignment = Enum.TextXAlignment.Left
    k.TextTransparency = 1
    local E = Instance.new("TextLabel", H)
    E.Size = UDim2.new(1, -30.0, 0, 15)
    E.Position = UDim2.new(0, 20, 0, 26)
    E.BackgroundTransparency = 1
    E.Text = m
    E.Font = Enum.Font.FredokaOne
    E.TextSize = 11
    E.TextColor3 = Color3.fromRGB(230, 230, 230)
    E.TextXAlignment = Enum.TextXAlignment.Left
    E.TextTransparency = 1
    local m = TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
    f:Create(H, m, { BackgroundTransparency = 0.05 }):Play()
    f:Create(k, m, { TextTransparency = 0 }):Play()
    f:Create(E, m, { TextTransparency = 0 }):Play()
    task.delay(2, function()
        if not A.Parent then
            return
        end
        local m = TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In)
        f:Create(H, m, { BackgroundTransparency = 1 }):Play()
        f:Create(k, m, { TextTransparency = 1 }):Play()
        local H = f:Create(E, m, { TextTransparency = 1 })
        H:Play()
        H.Completed:Wait()
        if A.Parent then
            A:Destroy()
        end
    end)
end
local function E(m)
    return r:GetPlayerFromCharacter(m) ~= nil
end
local function m(A)
    local H = A:FindFirstAncestorOfClass("Model")
    if H and E(H) then
        return
    end
    for H, H in pairs(A:GetPlayingAnimationTracks()) do
        H:Stop(0)
    end
    A.AnimationPlayed:Connect(function(A)
        A:Stop(0)
    end)
end
local A = nil
local function H(_)
    local Z = _:FindFirstAncestorOfClass("Model")
    local k = Z and E(Z)
    if _:IsA("Animator") then
        m(_)
    end
    if _:IsA("Accessory") or _:IsA("Clothing") then
        if _:FindFirstAncestorOfClass("Model") then
            _:Destroy()
        end
    end
    if not k then
        if
            _:IsA("ParticleEmitter")
            or _:IsA("Trail")
            or _:IsA("Beam")
            or _:IsA("Smoke")
            or _:IsA("Fire")
            or _:IsA("Sparkles")
            or _:IsA("Highlight")
        then
            _.Enabled = false
        end
        if _:IsA("PointLight") or _:IsA("SpotLight") or _:IsA("SurfaceLight") then
            _.Enabled = false
            _.Brightness = 0
            _.Range = 0
            task.defer(function()
                pcall(function()
                    _:Destroy()
                end)
            end)
        end
        if _:IsA("Explosion") then
            _:Destroy()
        end
        if _:IsA("MeshPart") then
            _.TextureID = ""
        end
    end
    if _:IsA("BasePart") then
        _.Material = Enum.Material.Plastic
        _.Reflectance = 0
        _.CastShadow = false
        pcall(function()
            if _.Material == Enum.Material.Glass or _.Material == Enum.Material.Neon then
                _.Material = Enum.Material.Plastic
            end
        end)
    end
    if _:IsA("SurfaceAppearance") or _:IsA("Texture") or _:IsA("Decal") then
        _:Destroy()
    end
end
local _ = game:GetService("Lighting")
getgenv().DayLock = getgenv().DayLock or { Enabled = false, Hour = 7, Refresh = 0.1 }
if getgenv().DayLock.Enabled == nil then
    getgenv().DayLock.Enabled = false
end
if type(getgenv().DayLock.Hour) ~= "number" then
    getgenv().DayLock.Hour = 7
end
if type(getgenv().DayLock.Refresh) ~= "number" then
    getgenv().DayLock.Refresh = 0.1
end
task.spawn(function()
    while true do
        local Z = getgenv().DayLock
        if Z and Z.Enabled then
            _.ClockTime = Z.Hour
            _.TimeOfDay = string.format("%02d:00:00", Z.Hour)
        end
        task.wait(Z and Z.Refresh or 0.1)
    end
end)
local function Z(k)
    i.FPSBoost = k
    U()
    if A then
        A:Disconnect()
        A = nil
    end
    getgenv().DayLock = getgenv().DayLock or {}
    getgenv().DayLock.Hour = 7
    getgenv().DayLock.Refresh = 0.1
    getgenv().DayLock.Enabled = k and true or false
    if k then
        _.ClockTime = getgenv().DayLock.Hour
        _.TimeOfDay = string.format("%02d:00:00", getgenv().DayLock.Hour)
        task.spawn(function()
            local k = a:GetDescendants()
            for u = 1, #k do
                H(k[u])
                if u % 30 == 0 then
                    task.wait()
                end
            end
        end)
        A = a.DescendantAdded:Connect(function(A)
            if not i.FPSBoost then
                return
            end
            task.defer(function()
                H(A)
            end)
        end)
        local A = a:FindFirstChildOfClass("Terrain")
        pcall(function()
            _.Brightness = 2
            _.GlobalShadows = false
            _.OutdoorAmbient = Color3.fromRGB(165, 165, 165)
            _.Ambient = Color3.fromRGB(20, 20, 20)
            _.FogEnd = 9000000000
            _.FogStart = 0
            _.EnvironmentDiffuseScale = 0
            _.EnvironmentSpecularScale = 0
            for H, H in pairs(_:GetChildren()) do
                if
                    H:IsA("BlurEffect")
                    or H:IsA("SunRaysEffect")
                    or H:IsA("BloomEffect")
                    or H:IsA("ColorCorrectionEffect")
                    or H:IsA("DepthOfFieldEffect")
                then
                    H:Destroy()
                end
            end
            if A then
                A.WaterWaveSize = 0
                A.WaterWaveSpeed = 0
                A.WaterReflectance = 0
                A.WaterTransparency = 1
            end
        end)
    end
end
if i.FPSBoost then
    task.spawn(function()
        task.wait(1)
        Z(true)
    end)
end
local A = nil
local function H(Z, k)
    local u = Z:FindFirstAncestorOfClass("Model")
    local g = u and E(u)
    if Z:IsA("Animator") then
        m(Z)
    end
    if Z:IsA("Accessory") or Z:IsA("Clothing") then
        if Z:FindFirstAncestorOfClass("Model") then
            Z:Destroy()
        end
    end
    if not g then
        if
            Z:IsA("ParticleEmitter")
            or Z:IsA("Trail")
            or Z:IsA("Beam")
            or Z:IsA("Smoke")
            or Z:IsA("Fire")
            or Z:IsA("Sparkles")
            or Z:IsA("Highlight")
        then
            Z.Enabled = false
        end
        if Z:IsA("Explosion") then
            Z:Destroy()
        end
        if Z:IsA("MeshPart") then
            Z.TextureID = ""
        end
        if k and (Z:IsA("PointLight") or Z:IsA("SpotLight") or Z:IsA("SurfaceLight")) then
            pcall(function()
                if Z.Parent ~= _ then
                    Z.Enabled = false
                    Z.Brightness = 0
                    Z.Range = 0
                    Z.Shadows = false
                    Z:Destroy()
                end
            end)
        end
    end
    if Z:IsA("BasePart") then
        Z.Material = Enum.Material.Plastic
        Z.Reflectance = 0
        Z.CastShadow = false
    end
    if Z:IsA("SurfaceAppearance") or Z:IsA("Texture") or Z:IsA("Decal") then
        Z:Destroy()
    end
end
local function E(m)
    i.FPSBoostV2 = m
    U()
    if A then
        A:Disconnect()
        A = nil
    end
    if m then
        _.GlobalShadows = false
        _.FogEnd = 1000000
        _.FogStart = 0
        _.EnvironmentDiffuseScale = 0
        _.EnvironmentSpecularScale = 0.1
        for m, m in pairs(_:GetChildren()) do
            if
                m:IsA("BloomEffect")
                or m:IsA("BlurEffect")
                or m:IsA("ColorCorrectionEffect")
                or m:IsA("SunRaysEffect")
                or m:IsA("DepthOfFieldEffect")
                or m:IsA("Atmosphere")
            then
                m:Destroy()
            end
        end
        task.spawn(function()
            local m = a:GetDescendants()
            for Z = 1, #m do
                H(m[Z], true)
                if Z % 30 == 0 then
                    task.wait()
                end
            end
        end)
        A = a.DescendantAdded:Connect(function(m)
            if not i.FPSBoostV2 then
                return
            end
            task.defer(function()
                H(m, false)
            end)
        end)
    end
end
if i.FPSBoostV2 then
    task.spawn(function()
        task.wait(1)
        E(true)
    end)
end
local m = {
    ProximityAPActive = false,
    carpetSpeedEnabled = false,
    infiniteJumpEnabled = i.TpSettings.InfiniteJump,
    antiRagdollMode = i.AntiRagdoll or 0,
    floatActive = i.Float == true,
    isTpMoving = false,
}
local A =
    { carpetSpeedConnection = nil, infiniteJumpConnection = nil, antiRagdollConn = nil, antiRagdollV2Task = nil }
local H = { carpetStatusLabel = nil, settingsGui = nil }
local Z = m.carpetSpeedEnabled
local k = A.carpetSpeedConnection
local u = H.carpetStatusLabel
local g = Vector3.new(7, 1, 7)
local h = { platform = nil, followConnection = nil, sawStealingDuringThisFloat = false }
local L = {}
local function s()
    local S = C.Character
    if not S then
        return nil, nil, nil
    end
    local N = S:FindFirstChild("HumanoidRootPart")
    local PG = S:FindFirstChildOfClass("Humanoid")
    return S, N, PG
end
local function S()
    if h.followConnection then
        h.followConnection:Disconnect()
        h.followConnection = nil
    end
    if h.platform then
        h.platform:Destroy()
        h.platform = nil
    end
end
local function N()
    local PG, PG, CG = s()
    if not PG or not CG or not h.platform then
        return
    end
    if CG.Health <= 0 then
        return
    end
    local CG = PG.Position - Vector3.new(0, 3.35, 0)
    if (h.platform.Position - CG).Magnitude > 0.05 then
        h.platform.CFrame = CFrame.new(CG)
    end
end
local function PG()
    S()
    local CG, CG, rG = s()
    if not CG or not rG then
        return
    end
    local rG = Instance.new("Part")
    rG.Name = "HAZE_FLOAT_PLATFORM"
    rG.Size = g
    rG.Anchored = true
    rG.CanCollide = true
    rG.CanTouch = false
    rG.CanQuery = false
    rG.Transparency = 1
    rG.CastShadow = false
    rG.Material = Enum.Material.SmoothPlastic
    rG.Color = Color3.new(1, 1, 1)
    rG.TopSurface = Enum.SurfaceType.Smooth
    rG.BottomSurface = Enum.SurfaceType.Smooth
    rG.Locked = true
    rG.Massless = true
    rG.CFrame = CFrame.new(CG.Position - Vector3.new(0, 3.35, 0))
    rG.Parent = a
    h.platform = rG
    h.followConnection = R.Heartbeat:Connect(function()
        if not m.floatActive then
            return
        end
        local g, CG, rG = s()
        if not g or not CG or not rG or rG.Health <= 0 then
            return
        end
        N()
    end)
    N()
end
local function g()
    if not m.floatActive then
        h.sawStealingDuringThisFloat = false
        return
    end
    h.sawStealingDuringThisFloat = (C:GetAttribute("Stealing") == true)
end
local function s(N, CG)
    N = not not N
    m.floatActive = N
    i.Float = N
    if N then
        g()
        PG()
    else
        S()
        h.sawStealingDuringThisFloat = false
    end
    U()
    if L and L.setFn then
        pcall(L.setFn, N)
    end
    if not CG and type(F) == "function" then
        F("FLOAT", N and "ENABLED" or "DISABLED")
    end
end
local function N(CG)
    s(not m.floatActive, CG)
end
_G.setFloatEnabled = s
_G.toggleFloat = N
C:GetAttributeChangedSignal("Stealing"):Connect(function()
    local CG = C:GetAttribute("Stealing") == true
    if not m.floatActive then
        return
    end
    if CG then
        h.sawStealingDuringThisFloat = true
        return
    end
    if h.sawStealingDuringThisFloat then
        s(false, false)
        if type(F) == "function" then
            F("FLOAT", "Disabled")
        end
    end
end)
C.CharacterAdded:Connect(function()
    task.wait(0.35)
    S()
    if m.floatActive then
        g()
        PG()
    end
end)
task.spawn(function()
    task.wait(0.35)
    if m.floatActive then
        g()
        PG()
    end
end)
local function g(h)
    m.carpetSpeedEnabled = h
    Z = m.carpetSpeedEnabled
    if A.carpetSpeedConnection then
        A.carpetSpeedConnection:Disconnect()
        A.carpetSpeedConnection = nil
    end
    k = A.carpetSpeedConnection
    if not h then
        return
    end
    if P.DisableStealSpeed then
        P.DisableStealSpeed()
    end
    A.carpetSpeedConnection = R.Heartbeat:Connect(function()
        k = A.carpetSpeedConnection
        local k = C.Character
        if not k then
            return
        end
        local h = k:FindFirstChild("Humanoid")
        local S = k:FindFirstChild("HumanoidRootPart")
        if not h or not S then
            return
        end
        local PG = i.TpSettings.Tool
        local CG = k:FindFirstChild(PG)
        if not CG then
            local k = C.Backpack:FindFirstChild(PG)
            if k then
                h:EquipTool(k)
            end
        end
        if CG then
            local k = h.MoveDirection
            if k.Magnitude > 0 then
                S.Velocity = Vector3.new(k.X * 140, S.Velocity.Y, k.Z * 140)
            else
                S.Velocity = Vector3.new(0, S.Velocity.Y, 0)
            end
        end
    end)
end
local k = { lastJumpTime = 0 }
local h = m.infiniteJumpEnabled
local S = A.infiniteJumpConnection
local function PG(CG)
    m.infiniteJumpEnabled = CG
    h = m.infiniteJumpEnabled
    i.TpSettings.InfiniteJump = CG
    U()
    if A.infiniteJumpConnection then
        A.infiniteJumpConnection:Disconnect()
        A.infiniteJumpConnection = nil
    end
    if A.infiniteJumpBtnBegin then
        A.infiniteJumpBtnBegin:Disconnect()
        A.infiniteJumpBtnBegin = nil
    end
    if A.infiniteJumpBtnEnd then
        A.infiniteJumpBtnEnd:Disconnect()
        A.infiniteJumpBtnEnd = nil
    end
    if A.infiniteJumpCharConn then
        A.infiniteJumpCharConn:Disconnect()
        A.infiniteJumpCharConn = nil
    end
    _ijMobileHeld = false
    S = A.infiniteJumpConnection
    if not CG then
        return
    end
    local CG = false
    local function rG()
        local RG = C:FindFirstChild("PlayerGui")
        if not RG then
            return
        end
        local dG = RG:FindFirstChild("TouchGui")
        if not dG then
            return
        end
        local RG = dG:FindFirstChild("TouchControlFrame")
        if not RG then
            return
        end
        local dG = RG:FindFirstChild("JumpButton")
        if not dG then
            return
        end
        if A.infiniteJumpBtnBegin then
            A.infiniteJumpBtnBegin:Disconnect()
        end
        if A.infiniteJumpBtnEnd then
            A.infiniteJumpBtnEnd:Disconnect()
        end
        A.infiniteJumpBtnBegin = dG.InputBegan:Connect(function(RG)
            if RG.UserInputType == Enum.UserInputType.Touch then
                CG = true
            end
        end)
        A.infiniteJumpBtnEnd = dG.InputEnded:Connect(function(RG)
            if RG.UserInputType == Enum.UserInputType.Touch then
                CG = false
            end
        end)
    end
    task.spawn(function()
        for RG = 1, 10 do
            local RG = C:FindFirstChild("PlayerGui")
            local dG = RG and RG:FindFirstChild("TouchGui")
            if
                dG
                and dG:FindFirstChild("TouchControlFrame")
                and dG.TouchControlFrame:FindFirstChild("JumpButton")
            then
                rG()
                break
            end
            task.wait(0.5)
        end
    end)
    A.infiniteJumpCharConn = C.CharacterAdded:Connect(function()
        CG = false
        task.delay(1, rG)
    end)
    A.infiniteJumpConnection = R.Heartbeat:Connect(function()
        S = A.infiniteJumpConnection
        if not j:IsKeyDown(Enum.KeyCode.Space) and not CG then
            return
        end
        local S = tick()
        if S - k.lastJumpTime < 0.1 then
            return
        end
        local CG = C.Character
        if not CG then
            return
        end
        local rG = CG:FindFirstChild("HumanoidRootPart")
        local RG = CG:FindFirstChild("Humanoid")
        if not rG or not RG or RG.Health <= 0 then
            return
        end
        k.lastJumpTime = S
        rG.Velocity = Vector3.new(rG.Velocity.X, 55, rG.Velocity.Z)
    end)
end
if h then
    PG(true)
end
local k = m.antiRagdollMode
local S = A.antiRagdollConn
local function CG()
    local rG = C.Character
    if not rG then
        return false
    end
    local RG = rG:FindFirstChildOfClass("Humanoid")
    if not RG then
        return false
    end
    local rG = RG:GetState()
    local RG = {
        [Enum.HumanoidStateType.Physics] = true,
        [Enum.HumanoidStateType.Ragdoll] = true,
        [Enum.HumanoidStateType.FallingDown] = true,
    }
    if RG[rG] then
        return true
    end
    local rG = C:GetAttribute("RagdollEndTime")
    if rG and rG - a:GetServerTimeNow() > 0 then
        return true
    end
    return false
end
local function rG()
    if A.antiRagdollConn then
        A.antiRagdollConn:Disconnect()
        A.antiRagdollConn = nil
    end
    S = A.antiRagdollConn
end
local function RG(dG)
    rG()
    if i.AntiRagdollV2 then
        stopAntiRagdollV2()
    end
    if dG == 0 then
        return
    end
    A.antiRagdollConn = R.Heartbeat:Connect(function()
        S = A.antiRagdollConn
        local A = C.Character
        if not A then
            return
        end
        local S = A:FindFirstChildOfClass("Humanoid")
        local dG = A:FindFirstChild("HumanoidRootPart")
        if not S or not dG then
            return
        end
        for _, desc in ipairs(A:GetDescendants()) do
            if desc:IsA("BallSocketConstraint") or desc:IsA("HingeConstraint") or desc.Name:find("RagdollAttachment") then
                pcall(function() desc:Destroy() end)
            elseif desc:IsA("Motor6D") then
                desc.Enabled = true
            end
        end
        pcall(function() S:BuildRigFromAttachments() end)
        if CG() then
            pcall(function()
                C:SetAttribute("RagdollEndTime", a:GetServerTimeNow())
            end)
            S:ChangeState(Enum.HumanoidStateType.Running)
            dG.Velocity = Vector3.zero
            if a.CurrentCamera.CameraSubject ~= S then
                a.CurrentCamera.CameraSubject = S
            end
            pcall(function()
                S.Jump = true
                local animator = S:FindFirstChild("Animator")
                if animator then
                    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                        track:Stop(0)
                    end
                end
                local animate = A:FindFirstChild("Animate")
                if animate and animate:IsA("LocalScript") then
                    task.spawn(function()
                        animate.Disabled = true
                        task.wait(0.05)
                        animate.Disabled = false
                    end)
                end
            end)
        end
    end)
end
local A = { antiRagdollConns = {} }
local S = A.antiRagdollConns
local CG = false
local function dG(nG)
    if not nG then
        return
    end
    local fG = false
    pcall(function()
        local JG = i.TpSettings.Tool or "Flying Carpet"
        local aG = nG:FindFirstChild(JG)
        if aG then
            local JG = nG:FindFirstChild("HumanoidRootPart")
            if JG then
                for eG, eG in ipairs(JG:GetChildren()) do
                    if eG:IsA("BodyVelocity") or eG:IsA("BodyPosition") or eG:IsA("BodyGyro") then
                        fG = true
                        break
                    end
                end
            end
            if not fG then
                for JG, JG in ipairs(aG:GetChildren()) do
                    if JG:IsA("BodyVelocity") or JG:IsA("BodyPosition") or JG:IsA("BodyGyro") then
                        fG = true
                        break
                    end
                end
            end
        end
    end)
    local JG = nG:GetDescendants()
    for aG, aG in ipairs(JG) do
        if
            aG:IsA("BallSocketConstraint")
            or aG:IsA("NoCollisionConstraint")
            or aG:IsA("HingeConstraint")
            or aG:IsA("Attachment") and (aG.Name == "A" or aG.Name == "B")
        then
            aG:Destroy()
        elseif (aG:IsA("BodyVelocity") or aG:IsA("BodyPosition") or aG:IsA("BodyGyro")) and not fG then
            aG:Destroy()
        end
    end
    for fG, fG in ipairs(JG) do
        if fG:IsA("Motor6D") then
            fG.Enabled = true
        end
    end
    local fG = nG:FindFirstChildOfClass("Humanoid")
    if fG then
        pcall(function() fG:BuildRigFromAttachments() end)
        fG:ChangeState(Enum.HumanoidStateType.Running)
        fG.Jump = true
        local animator = fG:FindFirstChild("Animator")
        if animator then
            for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                track:Stop(0)
            end
        end
        local animate = nG:FindFirstChild("Animate")
        if animate and animate:IsA("LocalScript") then
            task.spawn(function()
                animate.Disabled = true
                task.wait(0.05)
                animate.Disabled = false
            end)
        end
    end
    task.defer(function()
        pcall(function()
            local nG = C:FindFirstChild("PlayerScripts")
            if nG then
                nG = nG:FindFirstChild("PlayerModule")
            end
            if nG then
                require(nG):GetControls():Enable()
            end
        end)
    end)
end
local function nG(fG)
    if CG then
        return
    end
    CG = true
    task.defer(function()
        CG = false
        if fG and fG.Parent then
            dG(fG)
        end
    end)
end
local function fG(JG)
    if JG:IsA("BallSocketConstraint") or JG:IsA("NoCollisionConstraint") or JG:IsA("HingeConstraint") then
        return true
    end
    if JG:IsA("Attachment") and (JG.Name == "A" or JG.Name == "B") then
        return true
    end
    if JG:IsA("BodyVelocity") or JG:IsA("BodyPosition") or JG:IsA("BodyGyro") then
        return true
    end
    return false
end
local function JG(aG)
    for eG, eG in ipairs(S) do
        pcall(function()
            eG:Disconnect()
        end)
    end
    A.antiRagdollConns = {}
    S = A.antiRagdollConns
    local eG = aG:WaitForChild("Humanoid", 10)
    local xG = aG:WaitForChild("HumanoidRootPart", 10)
    if not eG or not xG then
        return
    end
    local QG = Vector3.new(0, 0, 0)
    local VG = eG.StateChanged:Connect(function()
        local iG = eG:GetState()
        if
            iG == Enum.HumanoidStateType.Physics
            or iG == Enum.HumanoidStateType.Ragdoll
            or iG == Enum.HumanoidStateType.FallingDown
            or iG == Enum.HumanoidStateType.GettingUp
        then
            local iG = false
            pcall(function()
                local OG = i.TpSettings.Tool or "Flying Carpet"
                local UG = aG:FindFirstChild(OG)
                if UG and xG then
                    for OG, OG in ipairs(xG:GetChildren()) do
                        if OG:IsA("BodyVelocity") or OG:IsA("BodyPosition") or OG:IsA("BodyGyro") then
                            iG = true
                        end
                    end
                end
            end)
            if not iG then
                eG:ChangeState(Enum.HumanoidStateType.Running)
            end
            dG(aG)
            pcall(function()
                a.CurrentCamera.CameraSubject = eG
            end)
            pcall(function()
                local iG = C:FindFirstChild("PlayerScripts")
                if iG then
                    iG = iG:FindFirstChild("PlayerModule")
                end
                if iG then
                    require(iG):GetControls():Enable()
                end
            end)
        end
    end)
    table.insert(S, VG)
    local VG = aG.DescendantAdded:Connect(function(iG)
        if fG(iG) then
            nG(aG)
        end
    end)
    table.insert(S, VG)
    pcall(function()
        local nG = n:FindFirstChild("Packages")
        if nG then
            local fG = nG:FindFirstChild("Net")
            if fG then
                local nG = fG:FindFirstChild("RE/CombatService/ApplyImpulse")
                if nG and nG:IsA("RemoteEvent") then
                    local fG = nG.OnClientEvent:Connect(function()
                        local nG = eG:GetState()
                        if
                            nG == Enum.HumanoidStateType.Physics
                            or nG == Enum.HumanoidStateType.Ragdoll
                            or nG == Enum.HumanoidStateType.FallingDown
                            or nG == Enum.HumanoidStateType.GettingUp
                        then
                            pcall(function()
                                xG.Velocity = Vector3.new(0, 0, 0)
                            end)
                        end
                    end)
                    table.insert(S, fG)
                end
            end
        end
    end)
    local nG = R.Heartbeat:Connect(function()
        local fG = eG:GetState()
        if
            fG == Enum.HumanoidStateType.Physics
            or fG == Enum.HumanoidStateType.Ragdoll
            or fG == Enum.HumanoidStateType.FallingDown
            or fG == Enum.HumanoidStateType.GettingUp
        then
            dG(aG)
            local fG = xG.Velocity
            if (fG - QG).Magnitude > 40 and fG.Magnitude > 25 then
                xG.Velocity = fG.Unit * math.min(fG.Magnitude, 15)
            end
        end
        QG = xG.Velocity
    end)
    table.insert(S, nG)
    dG(aG)
end
local function dG()
    CG = false
    for CG, CG in ipairs(S) do
        pcall(function()
            CG:Disconnect()
        end)
    end
    A.antiRagdollConns = {}
    S = A.antiRagdollConns
end
local function A(S)
    rG()
    dG()
    if not S then
        return
    end
    local S = C.Character
    if S then
        task.spawn(function()
            JG(S)
        end)
    end
    C.CharacterAdded:Connect(function(S)
        task.spawn(function()
            JG(S)
        end)
    end)
end
if k > 0 then
    RG(k)
end
if i.AntiRagdollV2 then
    A(true)
end
local k = {}
local S = false
local CG = nil
local rG = nil
local dG = nil
local function nG()
    S = false
    for fG, fG in ipairs(k) do
        if typeof(fG) == "RBXScriptConnection" then
            fG:Disconnect()
        end
    end
    k = {}
end
local function fG()
    if S then
        return
    end
    S = true
    local JG = C.Character
    if not JG then
        S = false
        return
    end
    local aG = JG:FindFirstChild("HumanoidRootPart")
    if not aG then
        for JG, JG in ipairs(workspace.CurrentCamera:GetChildren()) do
            if JG.Name == "HumanoidRootPart" then
                aG = JG
                break
            end
        end
    end
    if not aG then
        S = false
        return
    end
    local JG = aG.CFrame - aG.CFrame.Position
    CG = JG
    rG = aG.Position
    dG = aG
    local rG = R.Stepped:Connect(function()
        if not S then
            return
        end
        for eG, eG in ipairs(r:GetPlayers()) do
            if eG ~= C and eG.Character then
                for xG, xG in ipairs(eG.Character:GetChildren()) do
                    if xG:IsA("BasePart") then
                        xG.CanCollide = false
                    end
                end
            end
        end
    end)
    table.insert(k, rG)
    local rG = R.Stepped:Connect(function()
        if not S or not aG or not aG.Parent then
            return
        end
        aG.CFrame = CFrame.new(aG.Position) * JG
        aG.RotVelocity = Vector3.zero
    end)
    table.insert(k, rG)
    local rG = coroutine.create(function()
        if _G.invisibleStealEnabled then
            aG.CFrame = aG.CFrame * CFrame.new(0, 3, 0)
        end
        while S do
            R.Heartbeat:Wait()
            if not aG or not aG.Parent then
                break
            end
            local JG = aG.Velocity
            aG.Velocity = JG * 10000 + Vector3.new(0, 10000, 0)
            R.RenderStepped:Wait()
            if aG and aG.Parent then
                aG.Velocity = JG
            end
            R.Stepped:Wait()
            if aG and aG.Parent then
                aG.Velocity = JG + Vector3.new(0, 0.1, 0)
            end
        end
    end)
    coroutine.resume(rG)
    table.insert(k, rG)
end
local function k()
    if S then
        return
    end
    fG()
    task.delay(0.4, function()
        nG()
        task.wait(0.05)
        if dG and dG.Parent and CG then
            pcall(function()
                dG.CFrame = CFrame.new(dG.Position) * CG
                dG.RotVelocity = Vector3.zero
            end)
        end
    end)
end
_G.dropBrainrot = k
do
    local S = nil
    local CG = nil
    local rG = nil
    local function dG()
        local nG = workspace:FindFirstChild("Plots")
        if not nG then
            return nil
        end
        for fG, fG in ipairs(nG:GetChildren()) do
            local nG = fG:FindFirstChild("PlotSign")
            if nG then
                local JG = nG:FindFirstChildWhichIsA("SurfaceGui", true)
                if JG then
                    local nG = JG:FindFirstChildWhichIsA("TextLabel", true)
                    if nG then
                        local JG = nG.Text:lower()
                        if JG:find(C.DisplayName:lower(), 1, true) or JG:find(C.Name:lower(), 1, true) then
                            return fG
                        end
                    end
                end
            end
        end
        return nil
    end
    local function nG()
        if not i.LineToBase then
            return
        end
        local fG = dG()
        if not fG or not fG.Parent then
            return
        end
        local dG = C.Character
        if not dG or not dG.Parent then
            return
        end
        local JG = dG:FindFirstChild("HumanoidRootPart")
        if not JG or not JG.Parent then
            return
        end
        if S then
            pcall(function()
                S:Destroy()
            end)
        end
        if CG then
            pcall(function()
                CG:Destroy()
            end)
        end
        CG = JG:FindFirstChild("PlotBeamAttach_Player") or Instance.new("Attachment")
        CG.Name = "PlotBeamAttach_Player"
        CG.Position = Vector3.new(0, 0, 0)
        CG.Parent = JG
        local dG = fG:FindFirstChild("MainRootPart") or fG:FindFirstChildWhichIsA("BasePart")
        if not dG or not dG.Parent then
            return
        end
        rG = dG:FindFirstChild("PlotBeamAttach_Plot") or Instance.new("Attachment")
        rG.Name = "PlotBeamAttach_Plot"
        rG.Position = Vector3.new(0, 5, 0)
        rG.Parent = dG
        S = JG:FindFirstChild("PlotBeam") or Instance.new("Beam")
        S.Name = "PlotBeam"
        S.Attachment0 = CG
        S.Attachment1 = rG
        S.FaceCamera = true
        S.LightEmission = 0.5
        S.Color = ColorSequence.new(Color3.fromRGB(255, 255, 255))
        S.Transparency = NumberSequence.new(0)
        S.Width0 = 0.7
        S.Width1 = 0.7
        S.TextureMode = Enum.TextureMode.Wrap
        S.TextureSpeed = 0
        S.Parent = JG
    end
    local function dG()
        if S then
            pcall(function()
                S:Destroy()
            end)
        end
        if CG then
            pcall(function()
                CG:Destroy()
            end)
        end
        if rG then
            pcall(function()
                rG:Destroy()
            end)
        end
        S = nil
        CG = nil
        rG = nil
    end
    task.spawn(function()
        local rG = 0
        R.Heartbeat:Connect(function()
            if not i.LineToBase then
                return
            end
            rG = rG + 1
            if rG >= 30 then
                rG = 0
                if not S or not S.Parent or not CG or not CG.Parent then
                    pcall(nG)
                end
            end
        end)
    end)
    C.CharacterAdded:Connect(function(S)
        task.wait(0.5)
        if i.LineToBase and S then
            pcall(nG)
        end
    end)
    if C.Character then
        task.spawn(function()
            task.wait(0.2)
            if i.LineToBase then
                nG()
            end
        end)
    end
    _G.createPlotBeam = nG
    _G.resetPlotBeam = dG
end
task.spawn(function()
    local S = n:WaitForChild("Packages")
    local CG = n:WaitForChild("Datas")
    local rG = n:WaitForChild("Shared")
    local rG = n:WaitForChild("Utils")
    local rG = require(S:WaitForChild("Synchronizer"))
    local function S(dG)
        if not dG then
            return false
        end
        if typeof(dG) == "Instance" then
            return dG == C
        end
        if type(dG) == "string" then
            return dG == C.Name
        end
        return false
    end
    local function dG(nG)
        local fG, JG = pcall(function()
            local aG = getthreadidentity and getthreadidentity() or nil
            if setthreadidentity then
                setthreadidentity(8)
            end
            local eG = rG:GetTableFromChannel(nG)
            if aG and setthreadidentity then
                pcall(setthreadidentity, aG)
            end
            return eG
        end)
        if fG and type(JG) == "table" then
            return JG
        end
        return nil
    end
    local rG = require(CG:WaitForChild("Animals"))
    local nG = require(CG:WaitForChild("Mutations"))
    local fG = require(CG:WaitForChild("Traits"))
    local CG = {
        "",
        "K",
        "M",
        "B",
        "T",
        "Qa",
        "Qi",
        "Sx",
        "Sp",
        "Oc",
        "No",
        "Dc",
        "Ud",
        "Dd",
        "Td",
        "Qad",
        "Qid",
        "Sxd",
        "Spd",
        "Ocd",
        "Nod",
        "Vg",
        "Uvg",
        "Dvg",
        "Tvg",
    }
    local function JG(aG, eG)
        eG = eG or 1
        local xG = math.abs(aG)
        local QG = math.max(1, xG)
        local xG = math.floor(math.log(QG, 1000))
        local QG = CG[xG + 1] or "e+" .. xG
        local CG = aG * (10 ^ eG / 1000 ^ xG)
        local aG = math.floor(CG) / 10 ^ eG
        return (("%." .. eG .. "f"):format(aG)):gsub("%.?0+$", "") .. QG
    end
    local function CG(aG, eG, xG)
        local QG = rG[aG]
        if not QG then
            return 0
        end
        local aG = QG.Generation or QG.Price * 0.1
        local QG = 1
        if eG and eG ~= "None" then
            local VG = nG[eG]
            if VG and VG.Modifier then
                QG = QG + VG.Modifier
            end
        end
        local nG = false
        if type(xG) == "table" then
            for eG, eG in ipairs(xG) do
                if eG == "Sleepy" then
                    nG = true
                else
                    local xG = fG[eG]
                    if xG and xG.MultiplierModifier then
                        QG = QG + xG.MultiplierModifier
                    end
                end
            end
        end
        local fG = math.round(aG * QG)
        if nG then
            fG = math.round(fG * 0.5)
        end
        return fG
    end
    local nG = {}
    local fG = false
    local aG = nil
    local eG = {
        DeliveryHitbox = true,
        HumanoidRootPart = true,
        AnimalTarget = true,
        Spawn = true,
        Multiplier = true,
        MainRoot = true,
        StealHitbox = true,
    }
    local function xG(QG)
        if not QG:IsA("BasePart") then
            return false
        end
        if eG[QG.Name] then
            return false
        end
        local eG = QG.Name:lower()
        local VG = QG.Parent and QG.Parent.Name:lower() or ""
        return eG:find("base") or eG:find("claim") or VG:find("base") or VG:find("claim")
    end
    local function eG(QG)
        if not fG or not xG(QG) then
            return
        end
        if nG[QG] == nil then
            nG[QG] = QG.LocalTransparencyModifier
        end
        QG.LocalTransparencyModifier = 0.7
    end
    local xG
    xG = hookmetamethod(
        game,
        "__index",
        newcclosure(function(QG, VG)
            if not checkcaller() and VG == "LocalTransparencyModifier" and nG[QG] ~= nil then
                return nG[QG]
            end
            return xG(QG, VG)
        end)
    )
    local xG
    xG = hookmetamethod(
        game,
        "__newindex",
        newcclosure(function(QG, VG, iG)
            if not checkcaller() and VG == "LocalTransparencyModifier" and nG[QG] ~= nil then
                return
            end
            return xG(QG, VG, iG)
        end)
    )
    local function xG()
        fG = true
        if aG then
            aG:Disconnect()
        end
        local QG = a:GetDescendants()
        for VG = 1, #QG do
            eG(QG[VG])
        end
        aG = a.DescendantAdded:Connect(eG)
    end
    local function QG()
        fG = false
        if aG then
            aG:Disconnect()
            aG = nil
        end
        for aG, VG in pairs(nG) do
            pcall(function()
                aG.LocalTransparencyModifier = VG
            end)
        end
        nG = {}
    end
    _G._xrayEnable = xG
    _G._xrayDisable = QG
    C.CharacterAdded:Connect(function()
        task.wait(0.5)
        if not fG then
            return
        end
        local nG = a:GetDescendants()
        for fG = 1, #nG do
            eG(nG[fG])
        end
    end)
    if i.XrayBase then
        task.spawn(function()
            if not game:IsLoaded() then
                game.Loaded:Wait()
            end
            task.wait(2)
            if i.XrayBase then
                xG()
            end
        end)
    end
    local nG = true
    if i.DefaultToPriority and i.DefaultToHighest then
        i.DefaultToHighest = false
    end
    if i.DefaultToPriority and i.DefaultToNearest then
        i.DefaultToNearest = false
    end
    if i.DefaultToHighest and i.DefaultToNearest then
        i.DefaultToNearest = false
    end
    if not i.DefaultToPriority and not i.DefaultToHighest and not i.DefaultToNearest then
        i.DefaultToHighest = true
    end
    local fG = false
    local aG = false
    local eG = false
    if i.DefaultToNearest then
        fG = true
        i.StealNearest = true
        i.StealHighest = false
        i.StealPriority = false
        i.AutoTPPriority = true
    elseif i.DefaultToHighest then
        aG = true
        i.StealHighest = true
        i.StealNearest = false
        i.StealPriority = false
        i.AutoTPPriority = false
    elseif i.DefaultToPriority then
        eG = true
        i.StealPriority = true
        i.StealNearest = false
        i.StealHighest = false
        i.AutoTPPriority = true
    else
        fG = i.StealNearest
        aG = i.StealHighest
        eG = i.StealPriority
        if i.InstantSteal == nil then
            i.InstantSteal = false
        end
        if i.StealPriority then
            i.AutoTPPriority = true
        elseif i.StealNearest then
            i.AutoTPPriority = true
        elseif i.StealHighest then
            i.AutoTPPriority = false
        end
    end
    local xG = i.InstantSteal == true
    _G.NEAREST_INSTANT_MODE = (i.StealNearest == true and i.InstantSteal == true)
    local QG = false
    local VG = false
    local iG = 1
    local OG = nil
    local UG = nil
    local yG = {}
    local function KG()
        nG = fG == true or aG == true or eG == true
    end
    KG()
    local qG = {}
    local cG = {}
    local zG = nil
    local BG = nil
    local oG = {}
    local function YG(TG)
        if not TG or not TG.plot then
            return false
        end
        local IG = a:FindFirstChild("Plots")
        if not IG then
            return false
        end
        local pG = IG:FindFirstChild(TG.plot)
        if not pG then
            return false
        end
        local TG = dG(pG.Name)
        if TG then
            return S(TG.Owner)
        end
        return false
    end
    local function TG(IG)
        if not IG or IG == "None" then
            return ""
        end
        local pG = ""
        if IG == "Cursed" then
            pG = "<font color='rgb(200,0,0)'>Cur</font><font color='rgb(0,0,0)'>sed</font>"
        elseif IG == "Gold" then
            pG = "<font color='rgb(255,215,0)'>Gold</font>"
        elseif IG == "Diamond" then
            pG = "<font color='rgb(0,255,255)'>Diamond</font>"
        elseif IG == "YinYang" then
            pG = "<font color='rgb(255,255,255)'>Yin</font><font color='rgb(0,0,0)'>Yang</font>"
        elseif IG == "Candy" then
            pG = "<font color='rgb(255,105,180)'>Candy</font>"
        elseif IG == "Divine" then
            pG = "<font color='rgb(255,255,255)'>Divine</font>"
        elseif IG == "Rainbow" then
            local jG = {
                "rgb(255,0,0)",
                "rgb(255,127,0)",
                "rgb(255,255,0)",
                "rgb(0,255,0)",
                "rgb(0,0,255)",
                "rgb(75,0,130)",
                "rgb(148,0,211)",
            }
            for lG = 1, #IG do
                pG = pG .. "<font color='" .. jG[(lG - 1) % #jG + 1] .. "'>" .. IG:sub(lG, lG) .. "</font>"
            end
        elseif IG == "Radioactive" then
            pG = "<font color='rgb(132,255,0)'>Radioactive</font>"
        elseif IG == "Galaxy" then
            pG = "<font color='rgb(170,85,255)'>Galaxy</font>"
        else
            pG = IG
        end
        return "<font weight='800'>" .. pG .. " </font>"
    end
    local function IG(pG)
        if pG == "Gold" then
            return Color3.fromRGB(0, 178, 255)
        elseif pG == "Diamond" then
            return Color3.fromRGB(0, 255, 255)
        elseif pG == "Cursed" then
            return Color3.fromRGB(0, 140, 200)
        elseif pG == "YinYang" then
            return Color3.fromRGB(255, 255, 255)
        elseif pG == "Candy" then
            return Color3.fromRGB(180, 178, 255)
        elseif pG == "Divine" then
            return Color3.fromRGB(255, 255, 255)
        elseif pG == "Rainbow" then
            return Color3.fromRGB(148, 0, 211)
        elseif pG == "Radioactive" then
            return Color3.fromRGB(132, 255, 0)
        elseif pG == "Galaxy" then
            return Color3.fromRGB(170, 85, 255)
        end
        return Color3.fromRGB(120, 178, 255)
    end
    local function pG(jG)
        local lG = jG and jG.petName or "Unknown"
        return lG
    end
    local function jG(lG)
        local WG = lG and lG.mutation
        if WG and WG ~= "None" and WG ~= "" then
            return TG(WG), true
        end
        return "", false
    end
    local function TG()
        local lG = {}
        for WG, WG in ipairs(yG) do
            if WG.genValue >= 1 and not YG(WG) then
                table.insert(
                    lG,
                    {
                        petName = WG.name,
                        mpsText = WG.genText,
                        mpsValue = WG.genValue,
                        owner = WG.owner,
                        plot = WG.plot,
                        slot = WG.slot,
                        uid = WG.uid,
                        mutation = WG.mutation,
                        animalData = WG,
                    }
                )
            end
        end
        return lG
    end
    local lG = gethui and gethui() or game:GetService("CoreGui")
    local WG = Instance.new("ScreenGui")
    WG.Name = "cJPcVLFpwxgI"
    WG.ResetOnSpawn = false
    WG.Parent = lG
    local XG = Instance.new("Frame")
    local tG = d and 0.6 or 1
    local GG = {
        BG = Color3.fromRGB(10, 20, 35),
        SURF = Color3.fromRGB(15, 30, 50),
        SURF2 = Color3.fromRGB(16, 24, 44),
        TEXT = Color3.fromRGB(220, 240, 255),
        DIM = Color3.fromRGB(80, 126, 180),
        AQUA = Color3.fromRGB(60, 154, 220),
        AQUA2 = Color3.fromRGB(30, 119, 170),
        AQUA_STROKE = Color3.fromRGB(65, 120, 225),
    }
    XG.Size = UDim2.new(0, 300 * tG, 0, 335 * tG)
    XG.Position = UDim2.new(
        i.Positions.AutoSteal.X,
        i.Positions.AutoSteal.OffsetX or 0,
        i.Positions.AutoSteal.Y,
        i.Positions.AutoSteal.OffsetY or 0
    )
    XG.BackgroundColor3 = GG.BG
    XG.BackgroundTransparency = 0
    XG.BorderSizePixel = 0
    XG.ClipsDescendants = true
    XG.Parent = WG
    w(XG)
    b(XG, "AUTO STEAL")
    Instance.new("UICorner", XG).CornerRadius = UDim.new(0, 14)
    local WG = Instance.new("UIStroke", XG)
    WG.Color = GG.AQUA_STROKE
    WG.Thickness = 1.3
    WG.Transparency = 0.25
    local WG = Instance.new("Frame", XG)
    WG.Size = UDim2.new(1, 0, 0, 48)
    WG.BackgroundTransparency = 1
    D(WG, XG, "AutoSteal")
    local vG = Instance.new("TextLabel", WG)
    vG.Size = UDim2.new(1, -32.0, 0, 24)
    vG.Position = UDim2.new(0, 16, 0, 10)
    vG.BackgroundTransparency = 1
    vG.Text = "STEAL TARGET"
    vG.Font = Enum.Font.FredokaOne
    vG.TextSize = 20
    vG.TextColor3 = GG.TEXT
    vG.TextXAlignment = Enum.TextXAlignment.Center
    local WG = Instance.new("Frame", XG)
    WG.AnchorPoint = Vector2.new(0.5, 0)
    WG.Position = UDim2.new(0.5, 0, 0, 38)
    WG.Size = UDim2.new(0, 120, 0, 1)
    WG.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    WG.BackgroundTransparency = 0.15
    WG.BorderSizePixel = 0
    local WG = Instance.new("ScrollingFrame", XG)
    WG.Size = UDim2.new(1, -24.0, 0, 253)
    WG.Position = UDim2.new(0, 12, 0, 56)
    WG.BackgroundTransparency = 1
    WG.BorderSizePixel = 0
    WG.ClipsDescendants = true
    WG.ScrollingDirection = Enum.ScrollingDirection.Y
    WG.AutomaticCanvasSize = Enum.AutomaticSize.None
    WG.ScrollBarImageTransparency = 1
    WG.ScrollBarThickness = 0
    WG.CanvasSize = UDim2.new(0, 0, 0, 0)
    local XG = Instance.new("Frame", WG)
    XG.Name = "Holder"
    XG.BackgroundTransparency = 1
    XG.BorderSizePixel = 0
    XG.Position = UDim2.new(0, 0, 0, 0)
    XG.Size = UDim2.new(1, 0, 0, 0)
    XG.ClipsDescendants = false
    local vG = Instance.new("UIListLayout", XG)
    vG.Padding = UDim.new(0, 5)
    vG.SortOrder = Enum.SortOrder.LayoutOrder
    local MG = x:FindFirstChild("bRXgmsaEVfze")
    if MG then
        MG:Destroy()
    end
    local MG = Instance.new("ScreenGui")
    MG.Name = "bRXgmsaEVfze"
    MG.ResetOnSpawn = false
    MG.IgnoreGuiInset = true
    MG.DisplayOrder = 998
    MG.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    MG.Parent = x
    local wG = {
        PANEL = Color3.fromRGB(10, 14, 26),
        PANEL2 = Color3.fromRGB(16, 24, 44),
        TEXT = Color3.fromRGB(220, 240, 255),
        STROKE = Color3.fromRGB(50, 140, 200),
        GLOW = Color3.fromRGB(60, 154, 220),
        TRACK = Color3.fromRGB(14, 20, 36),
        TRACK2 = Color3.fromRGB(18, 27, 50),
        FILL1 = Color3.fromRGB(60, 147, 210),
        FILL2 = Color3.fromRGB(120, 178, 255),
    }
    local bG = Instance.new("Frame", MG)
    bG.Name = "CurrentTargetHUD"
    bG.AnchorPoint = Vector2.new(0.5, 1)
    bG.Size = UDim2.new(0, 230 * tG, 0, 46 * tG)
    bG.Position = UDim2.new(0.5, 0, 1, -220.0)
    bG.BackgroundColor3 = wG.PANEL
    bG.BackgroundTransparency = 0.02
    bG.BorderSizePixel = 0
    bG.ZIndex = 70
    Instance.new("UICorner", bG).CornerRadius = UDim.new(0, math.floor(12 * tG))
    local MG = Instance.new("UIStroke", bG)
    MG.Color = wG.STROKE
    MG.Thickness = 1
    MG.Transparency = 0.35
    local MG = Instance.new("UIStroke", bG)
    MG.Color = wG.GLOW
    MG.Thickness = 3
    MG.Transparency = 0.84
    MG.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local MG = Instance.new("ImageLabel", bG)
    MG.Name = "Shadow"
    MG.AnchorPoint = Vector2.new(0.5, 0.5)
    MG.Position = UDim2.new(0.5, 0, 0.5, 1)
    MG.Size = UDim2.new(1, 20, 1, 20)
    MG.BackgroundTransparency = 1
    MG.Image = "rbxassetid://6014261993"
    MG.ImageColor3 = Color3.new(0, 0, 0)
    MG.ImageTransparency = 0.72
    MG.ScaleType = Enum.ScaleType.Slice
    MG.SliceCenter = Rect.new(49, 49, 450, 450)
    MG.ZIndex = 69
    local MG = Instance.new("TextLabel", bG)
    MG.Name = "TargetName"
    MG.Size = UDim2.new(1, -12.0, 0, 13 * tG)
    MG.Position = UDim2.fromOffset(6 * tG, 3 * tG)
    MG.BackgroundTransparency = 1
    MG.Font = Enum.Font.FredokaOne
    MG.TextSize = 11 * tG
    MG.TextColor3 = wG.TEXT
    MG.TextXAlignment = Enum.TextXAlignment.Center
    MG.TextTruncate = Enum.TextTruncate.AtEnd
    MG.ZIndex = 72
    MG.Text = "No target"
    local DG = Instance.new("Frame", bG)
    DG.Name = "ProgressBg"
    DG.Size = UDim2.new(1, -10.0 * tG, 0, 18 * tG)
    DG.Position = UDim2.fromOffset(5 * tG, 18 * tG)
    DG.BackgroundColor3 = wG.TRACK
    DG.BorderSizePixel = 0
    DG.ZIndex = 72
    Instance.new("UICorner", DG).CornerRadius = UDim.new(0, math.floor(8 * tG))
    local bG = Instance.new("UIStroke", DG)
    bG.Color = wG.STROKE
    bG.Thickness = 1
    bG.Transparency = 0.55
    local bG = Instance.new("Frame", DG)
    bG.Name = "InnerTrack"
    bG.Size = UDim2.new(1, -2.0, 1, -2.0)
    bG.Position = UDim2.fromOffset(1, 1)
    bG.BackgroundColor3 = wG.TRACK2
    bG.BackgroundTransparency = 0.15
    bG.BorderSizePixel = 0
    bG.ZIndex = 72
    Instance.new("UICorner", bG).CornerRadius = UDim.new(0, math.floor(7 * tG))
    local bG = Instance.new("Frame", DG)
    bG.Name = "ProgressFill"
    bG.Size = UDim2.new(0, 0, 1, 0)
    bG.BackgroundColor3 = wG.FILL1
    bG.BorderSizePixel = 0
    bG.ZIndex = 73
    Instance.new("UICorner", bG).CornerRadius = UDim.new(0, math.floor(8 * tG))
    local FG = Instance.new("UIGradient", bG)
    FG.Color =
        ColorSequence.new({ ColorSequenceKeypoint.new(0, wG.FILL1), ColorSequenceKeypoint.new(1, wG.FILL2) })
    local FG = Instance.new("UIStroke", bG)
    FG.Color = Color3.fromRGB(130, 178, 255)
    FG.Thickness = 1
    FG.Transparency = 0.45
    local FG = Instance.new("TextLabel", DG)
    FG.Name = "Percent"
    FG.Size = UDim2.new(1, 0, 1, 0)
    FG.BackgroundTransparency = 1
    FG.Font = Enum.Font.FredokaOne
    FG.TextSize = 12 * tG
    FG.TextColor3 = wG.TEXT
    FG.TextStrokeTransparency = 0.7
    FG.TextXAlignment = Enum.TextXAlignment.Center
    FG.ZIndex = 74
    FG.Text = "0%"
    local wG = bG
    local DG = x:FindFirstChild("kzBUJxAKwhtf")
    if DG then
        DG:Destroy()
    end
    local DG = Instance.new("ScreenGui")
    DG.Name = "kzBUJxAKwhtf"
    DG.ResetOnSpawn = false
    DG.IgnoreGuiInset = true
    DG.DisplayOrder = 999
    DG.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    DG.Parent = lG
    local lG = Instance.new("Frame", DG)
    lG.Name = "TargetControlsFrame"
    lG.AutomaticSize = Enum.AutomaticSize.Y
    lG.Size = UDim2.new(0, 240 * tG, 0, 0)
    local tG = i.Positions.TargetControls and i.Positions.TargetControls.X
    local DG = i.Positions.TargetControls and i.Positions.TargetControls.Y
    local EG = i.Positions.TargetControls and i.Positions.TargetControls.OffsetX
    local mG = i.Positions.TargetControls and i.Positions.TargetControls.OffsetY
    if tG == nil or DG == nil then
        tG = i.Positions.AutoSteal.X + 0.24
        DG = i.Positions.AutoSteal.Y
        if tG > 0.76 then
            tG = math.max(0.02, i.Positions.AutoSteal.X - 0.24)
        end
        if DG > 0.72 then
            DG = 0.72
        end
        i.Positions.TargetControls = { X = tG, Y = DG }
    end
    lG.Position = UDim2.new(tG or 0.26, EG or 15, DG or 0.35, mG or 0)
    lG.BackgroundColor3 = Color3.fromRGB(10, 13, 22)
    lG.BackgroundTransparency = 0
    lG.BorderSizePixel = 0
    lG.ClipsDescendants = false
    lG.ZIndex = 100
    w(lG)
    b(lG, "TARGET CONTROLS")
    local b = {
        BG = Color3.fromRGB(10, 20, 35),
        SURF = Color3.fromRGB(15, 30, 50),
        SURF2 = Color3.fromRGB(16, 24, 44),
        TEXT = Color3.fromRGB(220, 240, 255),
        DIM = Color3.fromRGB(80, 126, 180),
        AQUA = Color3.fromRGB(60, 154, 220),
        AQUA2 = Color3.fromRGB(30, 119, 170),
        AQUA_STROKE = Color3.fromRGB(65, 120, 225),
        GREEN1 = Color3.fromRGB(18, 88, 58),
        GREEN2 = Color3.fromRGB(21, 120, 76),
        GREEN_STROKE = Color3.fromRGB(60, 185, 120),
        OFF_BG = Color3.fromRGB(18, 28, 52),
        OFF_TEXT = Color3.fromRGB(70, 112, 160),
    }
    local function tG(DG, EG)
        local mG = Instance.new("UICorner")
        if EG == 7 or EG == 9 then
            mG.CornerRadius = UDim.new(1, 0)
        else
            mG.CornerRadius = UDim.new(0, EG + 8)
        end
        mG.Parent = DG
        return mG
    end
    local function DG(EG, mG, AG, HG)
        local ZG = Instance.new("UIStroke")
        ZG.Color = mG
        ZG.Thickness = AG or 1
        ZG.Transparency = HG or 0
        ZG.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        ZG.Parent = EG
        return ZG
    end
    local function EG(mG, AG, HG, ZG, kG)
        f:Create(
            mG,
            TweenInfo.new(AG or 0.2, ZG or Enum.EasingStyle.Quint, kG or Enum.EasingDirection.Out),
            HG
        ):Play()
    end
    local function mG(AG, HG, ZG, kG)
        local uG = Instance.new("UIGradient")
        uG.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, HG), ColorSequenceKeypoint.new(1, ZG) })
        uG.Rotation = kG or 0
        uG.Parent = AG
        return uG
    end
    tG(lG, 18)
    DG(lG, b.AQUA_STROKE, 1.2, 0.4)
    local AG = Instance.new("ImageLabel")
    AG.Name = "Shadow"
    AG.AnchorPoint = Vector2.new(0.5, 0.5)
    AG.Position = UDim2.new(0.5, 0, 0.5, 2)
    AG.Size = UDim2.new(1, 24, 1, 24)
    AG.BackgroundTransparency = 1
    AG.Image = "rbxassetid://6014261993"
    AG.ImageColor3 = Color3.new(0, 0, 0)
    AG.ImageTransparency = 0.72
    AG.ScaleType = Enum.ScaleType.Slice
    AG.SliceCenter = Rect.new(49, 49, 450, 450)
    AG.ZIndex = 99
    AG.Parent = lG
    local AG = Instance.new("Frame", lG)
    AG.Size = UDim2.new(1, 0, 0, 44)
    AG.BackgroundTransparency = 1
    AG.ZIndex = 101
    D(AG, lG, "TargetControls")
    local HG = Instance.new("TextLabel", AG)
    HG.Size = UDim2.new(1, -28.0, 0, 24)
    HG.Position = UDim2.new(0, 14, 0, 10)
    HG.ZIndex = 102
    HG.BackgroundTransparency = 1
    HG.Text = "TARGET CONTROLS"
    HG.Font = Enum.Font.FredokaOne
    HG.TextSize = 18
    HG.TextColor3 = b.TEXT
    HG.TextXAlignment = Enum.TextXAlignment.Center
    local AG = Instance.new("Frame", lG)
    AG.AnchorPoint = Vector2.new(0.5, 0)
    AG.Position = UDim2.new(0.5, 0, 0, 38)
    AG.Size = UDim2.new(0, 124, 0, 1)
    AG.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    AG.BackgroundTransparency = 0.15
    AG.BorderSizePixel = 0
    AG.ZIndex = 101
    local AG = Instance.new("Frame", lG)
    AG.AutomaticSize = Enum.AutomaticSize.Y
    AG.Size = UDim2.new(1, -20.0, 0, 0)
    AG.Position = UDim2.fromOffset(10, 48)
    AG.BackgroundColor3 = b.SURF
    AG.BorderSizePixel = 0
    AG.ZIndex = 101
    tG(AG, 16)
    DG(AG, b.AQUA_STROKE, 1, 0.48)
    local lG = Instance.new("Frame", AG)
    lG.AutomaticSize = Enum.AutomaticSize.Y
    lG.Size = UDim2.new(1, -10.0, 0, 0)
    lG.Position = UDim2.fromOffset(5, 5)
    lG.BackgroundTransparency = 1
    lG.ZIndex = 102
    local AG = Instance.new("UIListLayout")
    AG.Padding = UDim.new(0, 8)
    AG.SortOrder = Enum.SortOrder.LayoutOrder
    AG.Parent = lG
    local AG = d and 1.3 or 1
    local function HG(ZG, kG, uG)
        local gG = Instance.new("Frame", ZG)
        gG.Name = kG:gsub("%s+", "") .. "Row"
        gG.Size = UDim2.new(1, 0, 0, math.floor(36 * AG))
        gG.BackgroundColor3 = b.SURF2
        gG.BackgroundTransparency = 0.02
        gG.BorderSizePixel = 0
        gG.LayoutOrder = math.floor((uG or 0) / math.max(1, 36 * AG)) + 1
        gG.ZIndex = 103
        tG(gG, 11)
        local ZG = DG(gG, b.AQUA_STROKE, 1, 0.52)
        local uG = Instance.new("TextLabel", gG)
        uG.BackgroundTransparency = 1
        uG.Position = UDim2.fromOffset(12, 0)
        uG.Size = UDim2.new(1, -100.0, 1, 0)
        uG.Font = Enum.Font.FredokaOne
        uG.Text = kG
        uG.TextColor3 = b.TEXT
        uG.TextSize = 12 * AG
        uG.TextXAlignment = Enum.TextXAlignment.Left
        uG.ZIndex = 104
        local hG = Instance.new("TextButton", gG)
        hG.Name = kG:gsub("%s+", "") .. "Toggle"
        hG.AutoButtonColor = false
        hG.Size = UDim2.fromOffset(math.floor(44 * AG), math.floor(24 * AG))
        hG.Position = UDim2.new(1, -math.floor(54 * AG), 0.5, -math.floor(12 * AG))
        hG.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        hG.BorderSizePixel = 0
        hG.Text = ""
        hG.ZIndex = 104
        tG(hG, 7)
        local LG = Instance.new("Frame", hG)
        LG.Size = UDim2.fromOffset(math.floor(20 * AG), math.floor(20 * AG))
        LG.Position = UDim2.new(0, math.floor(2 * AG), 0.5, -math.floor(10 * AG))
        LG.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        LG.BorderSizePixel = 0
        LG.ZIndex = 105
        tG(LG, 7)
        gG.MouseEnter:Connect(function()
            EG(gG, 0.14, { BackgroundColor3 = Color3.fromRGB(20, 31, 58) })
            EG(ZG, 0.14, { Transparency = 0.38 })
        end)
        gG.MouseLeave:Connect(function()
            EG(gG, 0.14, { BackgroundColor3 = b.SURF2 })
            EG(ZG, 0.14, { Transparency = 0.52 })
        end)
        return { row = gG, label = uG, button = hG, knob = LG, rowStroke = ZG }
    end
    local ZG = HG(lG, "Nearest", 0)
    local kG = HG(lG, "Highest", 36 * AG)
    local uG = HG(lG, "Priority", 72 * AG)
    local gG = HG(lG, "Auto Turret", 108 * AG)
    local hG = HG(lG, "Auto Kick", 144 * AG)
    local LG
    local function sG(SG, NG)
        KG()
        local function PB(CB, rB, RB)
            if rB then
                CB.button.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
                CB.knob.Position = UDim2.new(1, -math.floor(22 * AG), 0.5, -math.floor(10 * AG))
            else
                CB.button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                CB.knob.Position = UDim2.new(0, math.floor(2 * AG), 0.5, -math.floor(10 * AG))
            end
            if CB.rowStroke then
                CB.rowStroke.Transparency = rB and 0.38 or 0.52
            end
            if CB.label then
                CB.label.TextColor3 = rB and b.TEXT or b.TEXT
            end
        end
        PB(ZG, fG, W.Accent1)
        PB(kG, aG, W.Accent1)
        PB(uG, eG, W.Accent2)
        PB(gG, i.AutoDestroyTurrets, W.Accent1)
        PB(hG, i.AutoKickOnSteal, W.Accent1)
        if LG then
            if xG then
                LG.button.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
                LG.knob.Position = UDim2.new(1, -math.floor(22 * AG), 0.5, -math.floor(10 * AG))
            else
                LG.button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                LG.knob.Position = UDim2.new(0, math.floor(2 * AG), 0.5, -math.floor(10 * AG))
            end
            if LG.rowStroke then
                LG.rowStroke.Transparency = xG and 0.38 or 0.52
            end
        end
        if OG and NG then
            local PB = false
            for CB, rB in ipairs(NG) do
                if rB.uid == OG then
                    iG = CB
                    PB = true
                    break
                end
            end
        end
        if P.ListNeedsRedraw then
            for PB, PB in ipairs(XG:GetChildren()) do
                if PB:IsA("TextButton") then
                    PB:Destroy()
                end
            end
            oG = {}
            if NG and #NG > 0 then
                for PB = 1, #NG do
                    local CB = NG[PB]
                    local rB = Instance.new("TextButton")
                    rB.Size = UDim2.new(1, 0, 0, 38)
                    rB.BackgroundColor3 = GG.SURF2
                    rB.BorderSizePixel = 0
                    rB.Text = ""
                    rB.AutoButtonColor = false
                    rB.Parent = XG
                    rB.Position = UDim2.new(0, 0, 0, 0)
                    rB.ClipsDescendants = true
                    rB.ZIndex = 1
                    Instance.new("UICorner", rB).CornerRadius = UDim.new(1, 0)
                    local RB = Instance.new("Frame", rB)
                    RB.Name = "SelectedOverlay"
                    RB.Size = UDim2.new(1, 0, 1, 0)
                    RB.Position = UDim2.new(0, 0, 0, 0)
                    RB.BackgroundColor3 = Color3.fromRGB(8, 10, 12)
                    RB.BackgroundTransparency = 0.82
                    RB.BorderSizePixel = 0
                    RB.Visible = false
                    RB.ZIndex = 2
                    Instance.new("UICorner", RB).CornerRadius = UDim.new(1, 0)
                    local dB = Instance.new("UIGradient", RB)
                    dB.Rotation = 90
                    dB.Color = ColorSequence.new({
                        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
                        ColorSequenceKeypoint.new(0.45, Color3.fromRGB(18, 22, 18)),
                        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)),
                    })
                    dB.Transparency = NumberSequence.new({
                        NumberSequenceKeypoint.new(0, 0.12),
                        NumberSequenceKeypoint.new(0.18, 0.3),
                        NumberSequenceKeypoint.new(0.55, 0.52),
                        NumberSequenceKeypoint.new(1, 0.18),
                    })
                    local dB = Instance.new("Frame", rB)
                    dB.Size = UDim2.fromOffset(22, 22)
                    dB.Position = UDim2.fromOffset(8, 8)
                    dB.BackgroundColor3 = GG.SURF
                    dB.ZIndex = 3
                    dB.BorderSizePixel = 0
                    Instance.new("UICorner", dB).CornerRadius = UDim.new(0, 5)
                    local nB = Instance.new("UIStroke", dB)
                    nB.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
                    nB.Color = GG.AQUA_STROKE
                    nB.Thickness = 1
                    nB.Transparency = 0.45
                    local fB = Instance.new("TextLabel", dB)
                    fB.Size = UDim2.new(1, 0, 1, 0)
                    fB.BackgroundTransparency = 1
                    fB.Text = "#" .. PB
                    fB.Font = Enum.Font.FredokaOne
                    fB.TextSize = 11
                    fB.TextColor3 = GG.TEXT
                    fB.TextXAlignment = Enum.TextXAlignment.Center
                    fB.ZIndex = 4
                    local JB = CB and CB.petName or "Unknown"
                    local aB = CB and CB.mpsText or "$0/s"
                    local eB = Instance.new("TextLabel", rB)
                    eB.Size = UDim2.new(1, -108.0, 0, 16)
                    eB.Position = UDim2.fromOffset(38, 4)
                    eB.BackgroundTransparency = 1
                    eB.RichText = false
                    eB.Text = JB
                    eB.Font = Enum.Font.FredokaOne
                    eB.TextSize = 12
                    eB.TextColor3 = GG.TEXT
                    eB.TextXAlignment = Enum.TextXAlignment.Left
                    eB.TextTruncate = Enum.TextTruncate.None
                    eB.ClipsDescendants = false
                    eB.ZIndex = 4
                    local JB = Instance.new("UITextSizeConstraint", eB)
                    JB.MinTextSize = 8
                    JB.MaxTextSize = 12
                    local JB = Instance.new("TextLabel", rB)
                    JB.Size = UDim2.new(0, 88, 0, 16)
                    JB.Position = UDim2.new(1, -96.0, 0, 4)
                    JB.BackgroundTransparency = 1
                    JB.RichText = false
                    JB.Text = aB
                    JB.Font = Enum.Font.FredokaOne
                    JB.TextSize = 12
                    JB.TextColor3 = Color3.fromRGB(56, 214, 110)
                    JB.TextXAlignment = Enum.TextXAlignment.Right
                    JB.TextTruncate = Enum.TextTruncate.AtEnd
                    JB.ZIndex = 4
                    local aB = Instance.new("TextLabel", rB)
                    aB.Size = UDim2.new(1, -108.0, 0, 16)
                    aB.Position = UDim2.fromOffset(38, 18)
                    aB.BackgroundTransparency = 1
                    local xB, QB = jG(CB)
                    aB.RichText = QB
                    aB.Text = xB
                    aB.Font = Enum.Font.FredokaOne
                    aB.TextSize = 12
                    aB.TextColor3 = IG(CB.mutation)
                    aB.TextXAlignment = Enum.TextXAlignment.Left
                    aB.TextTruncate = Enum.TextTruncate.None
                    aB.ClipsDescendants = false
                    aB.ZIndex = 4
                    local QB = Instance.new("UITextSizeConstraint", aB)
                    QB.MinTextSize = 8
                    QB.MaxTextSize = 12
                    aB.Visible = xB ~= ""
                    oG[PB] = {
                        button = rB,
                        selectedOverlay = RB,
                        rankBox = dB,
                        rankBoxStroke = nB,
                        rank = fB,
                        info = eB,
                        rate = JB,
                        mutation = aB,
                        petData = CB,
                    }
                    rB.MouseButton1Click:Connect(function()
                        if UG == CB.uid then
                            UG = nil
                            OG = nil
                            _G.NEAREST_INSTANT_MODE = (fG and xG)
                        else
                            iG = PB
                            OG = CB.uid
                            UG = CB.uid
                            nG = true
                            _G.NEAREST_INSTANT_MODE = false
                        end
                        P.ListNeedsRedraw = true
                        if brainrotESPEnabled then
                            task.defer(function()
                                pcall(function()
                                    _espLastHash = nil
                                    refreshBrainrotESP()
                                end)
                            end)
                        end
                        sG(nG, TG())
                    end)
                end
            end
            P.ListNeedsRedraw = false
            local PB = #oG
            local CB = 0
            if PB > 0 then
                CB = PB * 38 + (PB - 1) * 5
            end
            XG.Size = UDim2.new(1, -0.0, 0, CB)
            WG.CanvasSize = UDim2.new(0, 0, 0, CB + 0)
        end
        for PB, PB in ipairs(oG) do
            local oG = PB.petData and UG and PB.petData.uid == UG
            PB.button.ZIndex = 1
            PB.button.BackgroundTransparency = 0
            PB.button.BackgroundColor3 = oG and Color3.fromRGB(44, 128, 79) or GG.SURF2
            if PB.selectedOverlay then
                PB.selectedOverlay.Visible = oG
                PB.selectedOverlay.ZIndex = 2
                PB.selectedOverlay.BackgroundColor3 = Color3.fromRGB(8, 10, 12)
                PB.selectedOverlay.BackgroundTransparency = oG and 0.9 or 1
            end
            if PB.rankBox then
                PB.rankBox.BackgroundColor3 = oG and Color3.fromRGB(33, 97, 60) or GG.SURF
                PB.rankBox.ZIndex = 3
            end
            if PB.rankBoxStroke then
                PB.rankBoxStroke.Color = oG and Color3.fromRGB(33, 97, 60) or GG.AQUA_STROKE
                PB.rankBoxStroke.Thickness = 1
                PB.rankBoxStroke.Transparency = oG and 1 or 0.45
            end
            if PB.rank then
                PB.rank.ZIndex = 4
                PB.rank.TextColor3 = oG and Color3.fromRGB(240, 255, 240) or GG.TEXT
            end
            if PB.info then
                PB.info.ZIndex = 4
                PB.info.RichText = false
                if PB.petData then
                    PB.info.Text = pG(PB.petData)
                end
                PB.info.TextColor3 = oG and Color3.fromRGB(0, 0, 0) or GG.TEXT
            end
            if PB.mutation then
                PB.mutation.ZIndex = 4
                if PB.petData then
                    local pG, GG = jG(PB.petData)
                    PB.mutation.RichText = GG
                    PB.mutation.Text = pG
                    PB.mutation.Visible = pG ~= ""
                end
                PB.mutation.TextColor3 = oG and Color3.fromRGB(0, 0, 0)
                    or IG(PB.petData and PB.petData.mutation)
            end
            if PB.rate then
                PB.rate.ZIndex = 4
                if PB.petData and PB.petData.mpsText then
                    PB.rate.Text = PB.petData.mpsText
                end
                PB.rate.TextColor3 = oG and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(56, 214, 110)
            end
        end
        local oG = NG and NG[iG]
        P.SelectedPetData = oG
        if SG then
            if not fG then
                if oG then
                    MG.Text = string.format("%s - %s", oG.petName or "Unknown", oG.mpsText or "")
                else
                    MG.Text = "Searching..."
                end
            end
        else
            MG.Text = "Disabled"
            if zG then
                zG:Cancel()
                zG = nil
            end
            bG.Size = UDim2.new(0, 0, 1, 0)
        end
        FG.Text = string.format("%d%%", math.clamp(math.floor(bG.Size.X.Scale * 100 + 0.5), 0, 100))
        XG.Size = UDim2.new(1, -0.0, 0, math.max(0, vG.AbsoluteContentSize.Y))
        WG.CanvasSize = UDim2.new(0, 0, 0, math.max(0, vG.AbsoluteContentSize.Y) + 0)
    end
    vG:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        XG.Size = UDim2.new(1, -0.0, 0, math.max(0, vG.AbsoluteContentSize.Y))
        WG.CanvasSize = UDim2.new(0, 0, 0, math.max(0, vG.AbsoluteContentSize.Y) + 0)
    end)
    P.UpdateAutoStealUI = function()
        sG(nG, TG())
    end
    task.spawn(function()
        while bG and bG.Parent do
            local oG = math.clamp(math.floor(bG.Size.X.Scale * 100 + 0.5), 0, 100)
            FG.Text = tostring(oG) .. "%"
            task.wait(0.05)
        end
    end)
    ZG.button.MouseButton1Click:Connect(function()
        fG = not fG
        if fG then
            aG = false
            eG = false
            UG = nil
        end
        i.StealNearest = fG
        i.StealHighest = aG
        i.StealPriority = eG
        KG()
        _G.NEAREST_INSTANT_MODE = (fG and xG)
        U()
        P.ListNeedsRedraw = false
        sG(nG, TG())
    end)
    kG.button.MouseButton1Click:Connect(function()
        aG = not aG
        if aG then
            fG = false
            eG = false
            UG = nil
        end
        i.StealNearest = fG
        i.StealHighest = aG
        i.StealPriority = eG
        KG()
        _G.NEAREST_INSTANT_MODE = (fG and xG)
        U()
        P.ListNeedsRedraw = false
        sG(nG, TG())
    end)
    uG.button.MouseButton1Click:Connect(function()
        eG = not eG
        if eG then
            fG = false
            aG = false
            UG = nil
        end
        i.StealNearest = fG
        i.StealHighest = aG
        i.StealPriority = eG
        KG()
        _G.NEAREST_INSTANT_MODE = (fG and xG)
        U()
        P.ListNeedsRedraw = false
        sG(nG, TG())
    end)
    gG.button.MouseButton1Click:Connect(function()
        i.AutoDestroyTurrets = not i.AutoDestroyTurrets
        U()
        P.ListNeedsRedraw = false
        sG(nG, TG())
    end)
    hG.button.MouseButton1Click:Connect(function()
        i.AutoKickOnSteal = not i.AutoKickOnSteal
        U()
        P.ListNeedsRedraw = false
        sG(nG, TG())
    end)
    local KG = Instance.new("TextButton", lG)
    KG.Name = "CustomizePriorityButton"
    KG.Size = UDim2.new(1, 0, 0, math.floor(38 * AG))
    KG.BackgroundColor3 = b.AQUA2
    KG.BackgroundTransparency = 0.02
    KG.Text = "CUSTOMIZE PRIORITY"
    KG.Font = Enum.Font.FredokaOne
    KG.TextSize = 12 * AG
    KG.TextColor3 = b.TEXT
    KG.BorderSizePixel = 0
    KG.AutoButtonColor = false
    KG.LayoutOrder = 7
    KG.ZIndex = 103
    tG(KG, 11)
    DG(KG, b.AQUA_STROKE, 1, 0.2)
    mG(KG, b.AQUA, b.AQUA2, 0)
    KG.Visible = false
    KG.MouseEnter:Connect(function()
        EG(KG, 0.14, { BackgroundColor3 = Color3.fromRGB(60, 132, 188) })
    end)
    KG.MouseLeave:Connect(function()
        EG(KG, 0.14, { BackgroundColor3 = b.AQUA2 })
    end)
    KG.MouseButton1Click:Connect(function()
        settingsGui.Enabled = true
        _G.SetHazeUIPage("Priority")
    end)
    LG = HG(lG, "Instant Steal", 180 * AG)
    if xG then
        LG.button.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
        LG.knob.Position = UDim2.new(1, -math.floor(22 * AG), 0.5, -math.floor(10 * AG))
    end
    LG.button.MouseButton1Click:Connect(function()
        xG = not xG
        if xG then
            QG = false
            VG = false
        else
            QG = false
            VG = false
        end
        i.InstantSteal = xG
        _G.NEAREST_INSTANT_MODE = (fG and xG)
        U()
        P.ListNeedsRedraw = false
        sG(nG, TG())
    end)
    task.spawn(function()
        while true do
            task.wait(1.5)
            if not xG then
                continue
            end
            xG = false
            QG = false
            VG = false
            i.InstantSteal = false
            _G.NEAREST_INSTANT_MODE = false
            task.wait(0.05)
            xG = true
            i.InstantSteal = true
            _G.NEAREST_INSTANT_MODE = (fG and true)
        end
    end)
    local function b(KG)
        if not KG then
            return nil
        end
        local oG = cG[KG.uid]
        if oG and oG.Parent then
            return oG
        end
        local oG = a.Plots:FindFirstChild(KG.plot)
        if not oG then
            return nil
        end
        local IG = oG:FindFirstChild("AnimalPodiums")
        if not IG then
            return nil
        end
        local pG = dG(oG.Name)
        local jG = pG and pG.AnimalList
        if not jG then
            local pG = IG:FindFirstChild(KG.slot)
            if pG then
                local lG = pG:FindFirstChild("Base")
                local pG = lG and lG:FindFirstChild("Spawn")
                if pG then
                    local lG = pG:FindFirstChild("PromptAttachment")
                    if lG then
                        for pG, pG in ipairs(lG:GetChildren()) do
                            if pG:IsA("ProximityPrompt") then
                                cG[KG.uid] = pG
                                return pG
                            end
                        end
                    end
                end
            end
            return nil
        end
        if not jG then
            return nil
        end
        local pG = KG.name and KG.name:lower() or ""
        local lG = KG.slot
        local WG = nil
        for XG, tG in pairs(jG) do
            if type(tG) == "table" and tostring(XG) == lG then
                local jG, lG = tG.Index, rG[tG.Index]
                if lG and (lG.DisplayName or jG):lower() == pG then
                    WG = IG:FindFirstChild(tostring(XG))
                    break
                end
            end
        end
        if not WG then
            WG = IG:FindFirstChild(KG.slot)
        end
        if WG then
            local IG = WG:FindFirstChild("Base")
            local jG = IG and IG:FindFirstChild("Spawn")
            if jG then
                local IG = jG:FindFirstChild("PromptAttachment")
                if IG then
                    for lG, lG in ipairs(IG:GetChildren()) do
                        if lG:IsA("ProximityPrompt") and lG.Enabled and lG.ActionText == "Steal" then
                            cG[KG.uid] = lG
                            return lG
                        end
                    end
                end
                local IG = jG.Position
                local jG, lG = IG.X, IG.Z
                local WG = nil
                local XG = math.huge
                for tG, tG in pairs(oG:GetDescendants()) do
                    if tG:IsA("ProximityPrompt") and tG.Enabled and tG.ActionText == "Steal" then
                        local oG = tG.Parent
                        local GG = nil
                        if oG and oG:IsA("BasePart") then
                            GG = oG.Position
                        elseif oG and oG:IsA("Attachment") and oG.Parent and oG.Parent:IsA("BasePart") then
                            GG = oG.Parent.Position
                        end
                        if GG then
                            local oG = IG.Y
                            if pG:find("la secret combinasion") then
                                oG = IG.Y - 5
                            end
                            local IG = math.sqrt((GG.X - jG) ^ 2 + (GG.Z - lG) ^ 2)
                            if IG < 5 and GG.Y > oG then
                                local IG = GG.Y - oG
                                if IG < XG then
                                    XG = IG
                                    WG = tG
                                end
                            end
                        end
                    end
                end
                if WG then
                    cG[KG.uid] = WG
                    return WG
                end
            end
        end
        return nil
    end
    local function KG(oG)
        if qG[oG] then
            return
        end
        local IG = { holdCallbacks = {}, triggerCallbacks = {}, holdEndCallbacks = {}, ready = true }
        local pG, jG = pcall(getconnections, oG.PromptButtonHoldBegan)
        if pG and type(jG) == "table" then
            for pG, pG in ipairs(jG) do
                if type(pG.Function) == "function" then
                    table.insert(IG.holdCallbacks, pG.Function)
                end
            end
        end
        local pG, jG = pcall(getconnections, oG.Triggered)
        if pG and type(jG) == "table" then
            for pG, pG in ipairs(jG) do
                if type(pG.Function) == "function" then
                    table.insert(IG.triggerCallbacks, pG.Function)
                end
            end
        end
        local pG, jG = pcall(getconnections, oG.PromptButtonHoldEnded)
        if pG and type(jG) == "table" then
            for pG, pG in ipairs(jG) do
                if type(pG.Function) == "function" then
                    table.insert(IG.holdEndCallbacks, pG.Function)
                end
            end
        end
        if #IG.holdCallbacks > 0 or #IG.triggerCallbacks > 0 or #IG.holdEndCallbacks > 0 then
            qG[oG] = IG
        end
    end
    local function oG(IG)
        for pG, pG in ipairs(IG) do
            task.spawn(pG)
        end
    end
    local function IG(pG)
        local jG = dG(pG)
        if jG then
            return S(jG.Owner)
        end
        return false
    end
    local function pG()
        local jG = C.Character and C.Character:FindFirstChild("HumanoidRootPart")
        if not jG then
            return nil, math.huge, nil
        end
        local lG = workspace:FindFirstChild("Plots")
        if not lG then
            return nil, math.huge, nil
        end
        local WG, XG, tG = nil, math.huge, nil
        for GG, GG in ipairs(lG:GetChildren()) do
            if IG(GG.Name) then
                continue
            end
            local IG = math.huge
            pcall(function()
                IG = (GG:GetPivot().Position - jG.Position).Magnitude
            end)
            if IG > 100 then
                continue
            end
            local IG = GG:FindFirstChild("AnimalPodiums")
            if not IG then
                continue
            end
            for lG, lG in ipairs(IG:GetChildren()) do
                local IG = lG:FindFirstChild("Base")
                local GG = IG and IG:FindFirstChild("Spawn")
                if not GG then
                    continue
                end
                local IG = (GG.Position - jG.Position).Magnitude
                if IG > 60 or IG >= XG then
                    continue
                end
                local jG = GG:FindFirstChild("PromptAttachment")
                if not jG then
                    continue
                end
                local GG = jG:FindFirstChildOfClass("ProximityPrompt")
                if GG and GG.Parent and GG.Enabled then
                    WG = GG
                    XG = IG
                    tG = lG.Name
                end
            end
        end
        return WG, XG, tG
    end
    local function IG(jG, lG)
        local WG = qG[jG]
        if not WG or not WG.ready then
            return false
        end
        WG.ready = false
        task.spawn(function()
            if BG ~= lG then
                if zG then
                    zG:Cancel()
                end
                wG.Size = UDim2.new(0, 0, 1, 0)
                BG = lG
            end
            if #WG.holdCallbacks > 0 then
                oG(WG.holdCallbacks)
            end
            wG.Size = UDim2.new(0, 0, 1, 0)
            wG.BackgroundTransparency = 0
            zG = f:Create(wG, TweenInfo.new(1.2, Enum.EasingStyle.Linear), { Size = UDim2.new(1, 0, 1, 0) })
            zG:Play()
            zG.Completed:Wait()
            if BG == lG and #WG.triggerCallbacks > 0 then
                oG(WG.triggerCallbacks)
            end
            WG.ready = true
        end)
        return true
    end
    local function oG(jG, lG)
        if not jG or not jG.Parent then
            return false
        end
        KG(jG)
        if not qG[jG] then
            return false
        end
        if BG ~= lG then
            if zG then
                zG:Cancel()
                zG = nil
            end
            wG.Size = UDim2.new(0, 0, 1, 0)
        end
        return IG(jG, lG)
    end
    local function qG()
        for BG, BG in pairs(cG) do
            if BG and BG.Parent then
                KG(BG)
            end
        end
    end
    task.spawn(function()
        while task.wait(2) do
            if nG then
                qG()
            end
        end
    end)
    local KG = {}
    local function qG(BG)
        if not BG then
            return ""
        end
        local IG = ""
        for jG, lG in pairs(BG) do
            if type(lG) == "table" then
                IG = IG .. tostring(jG) .. tostring(lG.Index) .. tostring(lG.Mutation)
            end
        end
        return IG
    end
    local function BG(IG)
        local jG = false
        pcall(function()
            local lG = dG(IG.Name)
            if not lG then
                return
            end
            local WG = lG.AnimalList
            local XG = lG.Owner
            if
                not XG
                or S(XG)
                or typeof(XG) == "Instance" and not r:FindFirstChild(XG.Name)
                or type(XG) == "string" and not r:FindFirstChild(XG)
            then
                KG[IG.Name] = nil
                for S = #yG, 1, -1.0 do
                    if yG[S].plot == IG.Name then
                        table.remove(yG, S)
                        jG = true
                    end
                end
                return
            end
            if not WG then
                KG[IG.Name] = nil
                for S = #yG, 1, -1.0 do
                    if yG[S].plot == IG.Name then
                        table.remove(yG, S)
                        jG = true
                    end
                end
                return
            end
            local S = typeof(XG) == "Instance" and XG.Name or tostring(XG)
            local lG = qG(WG, S)
            if KG[IG.Name] == lG then
                return
            end
            for qG = #yG, 1, -1.0 do
                if yG[qG].plot == IG.Name then
                    table.remove(yG, qG)
                end
            end
            for qG, XG in pairs(WG) do
                if type(XG) == "table" then
                    local WG, tG = XG.Index, rG[XG.Index]
                    if tG then
                        local rG = XG.Mutation or "None"
                        if rG == "Yin Yang" then
                            rG = "YinYang"
                        end
                        local GG = XG.Traits and #XG.Traits > 0 and table.concat(XG.Traits, ", ") or "None"
                        local vG = CG(WG, XG.Mutation, XG.Traits)
                        local CG = "$" .. JG(vG) .. "/s"
                        table.insert(
                            yG,
                            {
                                name = tG.DisplayName or WG,
                                genText = CG,
                                genValue = vG,
                                mutation = rG,
                                traits = GG,
                                owner = S,
                                plot = IG.Name,
                                slot = tostring(qG),
                                uid = IG.Name .. "_" .. tostring(qG),
                            }
                        )
                    end
                end
            end
            KG[IG.Name] = lG
            jG = true
        end)
        if jG then
            table.sort(yG, function(S, CG)
                return S.genValue > CG.genValue
            end)
            P.AllAnimalsCache = yG
            P.ListNeedsRedraw = true
            if P.UpdateAutoStealUI then
                P.UpdateAutoStealUI()
            end
            if not hasShownPriorityAlert and i.AlertsEnabled then
                task.spawn(function()
                    local S = nil
                    for CG = 1, #X do
                        local rG = X[CG]
                        local CG = rG:lower()
                        for rG, rG in ipairs(yG) do
                            if rG.name and rG.name:lower() == CG then
                                S = rG
                                break
                            end
                        end
                        if S then
                            break
                        end
                    end
                    if S then
                        local CG = S.owner
                        local rG = nil
                        local JG = a:FindFirstChild("Plots") and a.Plots:FindFirstChild(S.plot)
                        if JG then
                            local qG = dG(JG.Name)
                            if qG and qG.Owner then
                                local JG = qG.Owner
                                if typeof(JG) == "Instance" then
                                    rG = JG
                                    CG = JG.Name
                                elseif type(JG) == "string" then
                                    CG = JG
                                    rG = r:FindFirstChild(JG)
                                end
                            end
                        end
                        if not rG and CG then
                            rG = r:FindFirstChild(CG)
                        end
                        ShowPriorityAlert(S.name, S.genText, S.mutation, CG)
                    end
                end)
            end
        end
    end
    local function S(CG)
        local rG, JG = nil, 0
        while not rG and JG < 40 do
            rG = dG(CG.Name)
            if not rG then
                JG = JG + 1
                task.wait(0.07)
            end
        end
        if not rG then
            return
        end
        BG(CG)
        local function rG(JG)
            if not JG then
                return
            end
            JG.ChildAdded:Connect(function()
                task.wait(0.15)
                BG(CG)
            end)
            JG.ChildRemoved:Connect(function()
                for JG = #yG, 1, -1.0 do
                    if yG[JG].plot == CG.Name then
                        table.remove(yG, JG)
                    end
                end
                KG[CG.Name] = nil
                _getPetsCache = nil
                for JG in pairs(cG) do
                    if JG:sub(1, #CG.Name) == CG.Name then
                        cG[JG] = nil
                    end
                end
                P.ListNeedsRedraw = true
                if P.UpdateAutoStealUI then
                    P.UpdateAutoStealUI()
                end
                task.wait(0.15)
                BG(CG)
            end)
        end
        local JG = CG:FindFirstChild("AnimalPodiums")
        rG(JG)
        CG.ChildAdded:Connect(function(JG)
            if JG.Name == "AnimalPodiums" then
                rG(JG)
                BG(CG)
            end
        end)
        CG.ChildRemoved:Connect(function(rG)
            if rG.Name == "AnimalPodiums" then
                for rG = #yG, 1, -1.0 do
                    if yG[rG].plot == CG.Name then
                        table.remove(yG, rG)
                    end
                end
                KG[CG.Name] = nil
                _getPetsCache = nil
                P.ListNeedsRedraw = true
                if P.UpdateAutoStealUI then
                    P.UpdateAutoStealUI()
                end
            end
        end)
        task.spawn(function()
            while CG.Parent do
                task.wait(10)
                BG(CG)
            end
        end)
    end
    local CG = a:WaitForChild("Plots", 8)
    if CG then
        for rG, rG in ipairs(CG:GetChildren()) do
            S(rG)
        end
        CG.ChildAdded:Connect(function(rG)
            task.wait(0.5)
            S(rG)
        end)
        CG.ChildRemoved:Connect(function(S)
            KG[S.Name] = nil
            _getPetsCache = nil
            for CG = #yG, 1, -1.0 do
                if yG[CG].plot == S.Name then
                    table.remove(yG, CG)
                end
            end
            for CG in pairs(cG) do
                if CG:sub(1, #S.Name) == S.Name then
                    cG[CG] = nil
                end
            end
            P.ListNeedsRedraw = true
            if P.UpdateAutoStealUI then
                P.UpdateAutoStealUI()
            end
        end)
    end
    local S = false
    local function CG(rG, JG, KG, qG)
        if not i.AlertsEnabled then
            return
        end
        if S then
            return
        end
        local BG = qG and r:FindFirstChild(qG) or nil
        local qG = BG and BG:GetAttribute("__duels_block_steal") == true or false
        local BG = qG and "IN DUEL" or "NOT IN DUEL"
        local IG = qG and Color3.fromRGB(0, 178, 255) or Color3.fromRGB(0, 255, 0)
        local qG = {
            ["rainbow"] = Color3.fromRGB(255, 0, 255),
            ["bloodrot"] = Color3.fromRGB(0, 56, 139),
            ["candy"] = Color3.fromRGB(180, 178, 255),
            ["radioactive"] = Color3.fromRGB(0, 255, 0),
            ["cursed"] = Color3.fromRGB(50, 178, 255),
            ["gold"] = Color3.fromRGB(0, 178, 255),
            ["diamond"] = Color3.fromRGB(0, 255, 255),
            ["yinyang"] = Color3.fromRGB(255, 255, 255),
            ["lava"] = Color3.fromRGB(20, 178, 255),
        }
        local jG = KG and KG:gsub("%s+", ""):lower() or ""
        local KG = qG[jG] or Color3.fromRGB(255, 255, 255)
        local qG = gethui and gethui() or game:GetService("CoreGui")
        local jG = qG:FindFirstChild("XiPriorityAlertTest")
        if jG then
            jG:Destroy()
        end
        local jG = Instance.new("ScreenGui")
        jG.Name = "XiPriorityAlertTest"
        jG.ResetOnSpawn = false
        jG.DisplayOrder = 999
        jG.Parent = qG
        S = true
        local qG = Instance.new("Frame")
        qG.Size = UDim2.new(0, 410, 0, 62)
        qG.Position = UDim2.new(0.5, 0, 0, -76.0)
        qG.AnchorPoint = Vector2.new(0.5, 0)
        qG.BackgroundColor3 = Color3.fromRGB(12, 14, 20)
        qG.BackgroundTransparency = 0.02
        qG.BorderSizePixel = 0
        qG.Parent = jG
        Instance.new("UICorner", qG).CornerRadius = UDim.new(1, 0)
        local lG = Instance.new("Frame", qG)
        lG.Name = "InnerFrame"
        lG.Size = UDim2.new(1, -4.0, 1, -4.0)
        lG.Position = UDim2.new(0, 2, 0, 2)
        lG.BackgroundColor3 = Color3.fromRGB(16, 18, 26)
        lG.BackgroundTransparency = 0.08
        lG.BorderSizePixel = 0
        lG.ZIndex = 0
        Instance.new("UICorner", lG).CornerRadius = UDim.new(1, 0)
        local lG = Instance.new("UIStroke", qG)
        lG.Name = "GlowStroke"
        lG.Thickness = 2.5
        lG.Color = KG
        lG.Transparency = 1
        local WG = Instance.new("Frame", qG)
        WG.Name = "InnerGlow"
        WG.Size = UDim2.new(1, 6, 1, 6)
        WG.Position = UDim2.new(0.5, 0, 0.5, 0)
        WG.AnchorPoint = Vector2.new(0.5, 0.5)
        WG.BackgroundColor3 = KG
        WG.BackgroundTransparency = 1
        WG.ZIndex = 0
        Instance.new("UICorner", WG).CornerRadius = UDim.new(1, 0)
        local XG = Instance.new("TextLabel", qG)
        XG.Size = UDim2.new(1, -30.0, 0, 24)
        XG.Position = UDim2.new(0, 15, 0, 10)
        XG.BackgroundTransparency = 1
        XG.Text = rG .. " - " .. JG
        XG.Font = Enum.Font.FredokaOne
        XG.TextSize = 18
        XG.TextColor3 = KG
        XG.TextXAlignment = Enum.TextXAlignment.Center
        XG.TextStrokeTransparency = 0
        XG.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        local rG = Instance.new("TextLabel", qG)
        rG.Size = UDim2.new(1, -30.0, 0, 14)
        rG.Position = UDim2.new(0, 15, 0, 38)
        rG.BackgroundTransparency = 1
        rG.Text = BG
        rG.Font = Enum.Font.FredokaOne
        rG.TextSize = 17
        rG.TextColor3 = IG
        rG.TextXAlignment = Enum.TextXAlignment.Center
        rG.TextStrokeColor3 = KG
        f:Create(
            qG,
            TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
            { Position = UDim2.new(0.5, 0, 0, 22) }
        ):Play()
        if i.AlertSoundID and i.AlertSoundID ~= "" then
            local rG = Instance.new("Sound")
            rG.SoundId = i.AlertSoundID
            rG.Volume = 0.5
            rG.Parent = qG
            rG:Play()
            f:Create(
                lG,
                TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                { Transparency = 0 }
            ):Play()
            f:Create(
                WG,
                TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                { BackgroundTransparency = 0.85 }
            ):Play()
            task.delay(0.4, function()
                f:Create(
                    lG,
                    TweenInfo.new(0.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                    { Transparency = 0.6 }
                ):Play()
                f:Create(
                    WG,
                    TweenInfo.new(0.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                    { BackgroundTransparency = 1 }
                ):Play()
            end)
            rG.Ended:Connect(function()
                rG:Destroy()
            end)
        end
        task.delay(4, function()
            f:Create(
                qG,
                TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
                { Position = UDim2.new(0.5, 0, 0, -76.0) }
            ):Play()
            task.wait(0.35)
            jG:Destroy()
        end)
    end
    task.spawn(function()
        task.wait(0.5)
        while true do
            task.wait(0.5)
            if not S and i.AlertsEnabled and #yG > 0 then
                local S = nil
                for rG = 1, #X do
                    local JG = X[rG]
                    local rG = JG:lower()
                    for JG, JG in ipairs(yG) do
                        if JG.name and JG.name:lower() == rG then
                            S = JG
                            break
                        end
                    end
                    if S then
                        break
                    end
                end
                if S then
                    local rG = S.owner
                    local JG = nil
                    local KG = a:FindFirstChild("Plots") and a.Plots:FindFirstChild(S.plot)
                    if KG then
                        local qG = dG(KG.Name)
                        if qG and qG.Owner then
                            local dG = qG.Owner
                            if typeof(dG) == "Instance" then
                                JG = dG
                                rG = dG.Name
                            elseif type(dG) == "string" then
                                rG = dG
                                JG = r:FindFirstChild(dG)
                            end
                        end
                    end
                    if not JG and rG then
                        JG = r:FindFirstChild(rG)
                    end
                    CG(S.name, S.genText, S.mutation, rG)
                end
            end
        end
    end)
    local function S(CG)
        if #CG == 0 then
            return
        end
        if UG then
            for rG, dG in ipairs(CG) do
                if dG.uid == UG then
                    if iG ~= rG then
                        iG = rG
                        OG = dG.uid
                    end
                    MG.Text = string.format("%s - %s", dG.petName or "Unknown", dG.mpsText or "")
                    return
                end
            end
            UG = nil
        end
        if eG then
            for rG, rG in ipairs(X) do
                local dG = rG:lower()
                for rG, JG in ipairs(CG) do
                    if JG.petName and JG.petName:lower() == dG then
                        if iG ~= rG then
                            iG = rG
                            OG = JG.uid
                        end
                        return
                    end
                end
            end
            if iG ~= 1 then
                iG = 1
                OG = CG[1] and CG[1].uid
            end
        elseif fG then
            local rG = C.Character
            local dG = rG and rG:FindFirstChild("HumanoidRootPart")
            if dG then
                local rG, JG = 1, math.huge
                for eG, KG in ipairs(CG) do
                    local qG = KG.animalData and M(KG.animalData)
                    if qG and qG:IsA("BasePart") then
                        local KG = (dG.Position - qG.Position).Magnitude
                        if KG < JG then
                            JG = KG
                            rG = eG
                        end
                    end
                end
                if CG[rG] then
                    local dG = CG[rG]
                    MG.Text = string.format("%s - %s", dG.petName or "Unknown", dG.mpsText or "")
                end
                if not xG and iG ~= rG then
                    iG = rG
                    OG = CG[rG] and CG[rG].uid
                end
            end
        elseif aG then
            if iG ~= 1 then
                iG = 1
                OG = CG[1] and CG[1].uid
            end
        end
    end
    R.Heartbeat:Connect(function()
        if not nG then
            return
        end
        S(TG())
    end)
    task.spawn(function()
        while true do
            task.wait(0.5)
            if nG then
                local S = TG()
                if #S > 0 then
                    P.ListNeedsRedraw = false
                    sG(nG, S)
                end
            end
        end
    end)
    R.Heartbeat:Connect(function()
        if not nG then
            return
        end
        if xG then
            if zG then
                zG:Cancel()
                zG = nil
            end
            wG.Size = UDim2.new(1, 0, 1, 0)
            wG.BackgroundTransparency = 0
            if not VG then
                VG = true
                task.spawn(function()
                    if not game:IsLoaded() then
                        game.Loaded:Wait()
                    end
                    task.wait(0.5)
                    QG = true
                end)
            end
            if QG then
                if fG and not UG then
                    local S, CG, rG = pG()
                    if S and CG <= 60 then
                    end
                else
                    local S = TG()
                    if #S > 0 then
                        if iG > #S then
                            iG = #S
                        end
                        if iG < 1 then
                            iG = 1
                        end
                        local CG = S[iG]
                        if CG and not YG(CG.animalData) then
                            local S = cG[CG.uid]
                            if not S or not S.Parent then
                                S = b(CG.animalData)
                            end
                            if S then
                            end
                        end
                    end
                end
            end
            return
        end
        local S = TG()
        if #S == 0 then
            return
        end
        if iG > #S then
            iG = #S
        end
        if iG < 1 then
            iG = 1
        end
        local CG = S[iG]
        if not CG or YG(CG.animalData) then
            return
        end
        local S = cG[CG.uid]
        if not S or not S.Parent then
            S = b(CG.animalData)
        end
        if S then
            oG(S, CG.uid)
        end
    end)
    task.spawn(function()
        while task.wait(0.5) do
            sG(nG, TG())
        end
    end)
    task.spawn(function()
        task.wait(1)
        P.ListNeedsRedraw = true
        sG(nG, TG())
    end)
    task.spawn(function()
        while true do
            P.AllAnimalsCache = yG
            task.wait(0.5)
        end
    end)
    local b = nil
    local S = nil
    local CG = nil
    local function rG()
        if not nG or not i.TracerEnabled or i.StealNearest then
            if b then
                b:Destroy()
                b = nil
            end
            if S then
                S:Destroy()
                S = nil
            end
            if CG then
                CG:Destroy()
                CG = nil
            end
            return
        end
        local dG = TG()
        if #dG == 0 then
            if b then
                b.Enabled = false
            end
            return
        end
        if iG > #dG then
            iG = #dG
        end
        if iG < 1 then
            iG = 1
        end
        local nG = dG[iG] or dG[1]
        local dG = M(nG.animalData)
        local fG = C.Character
        local JG = fG and fG:FindFirstChild("HumanoidRootPart")
        if JG and dG then
            if not S or S.Parent ~= JG then
                if S then
                    S:Destroy()
                end
                S = Instance.new("Attachment", JG)
            end
            if not CG or CG.Parent ~= dG then
                if CG then
                    CG:Destroy()
                end
                CG = Instance.new("Attachment", dG)
            end
            if not b then
                b = Instance.new("Beam", JG)
                b.FaceCamera = true
                b.Width0 = 0.8
                b.Width1 = 0.8
                b.TextureMode = Enum.TextureMode.Static
                b.TextureSpeed = 3
            end
            b.Attachment0 = S
            b.Attachment1 = CG
            b.Enabled = true
            local S = {
                Cursed = Color3.fromRGB(0, 140, 200),
                Gold = Color3.fromRGB(0, 178, 255),
                Diamond = Color3.fromRGB(0, 255, 255),
                YinYang = Color3.fromRGB(220, 220, 220),
                Rainbow = Color3.fromRGB(200, 178, 255),
                Lava = Color3.fromRGB(20, 178, 255),
                Candy = Color3.fromRGB(180, 178, 255),
                Divine = Color3.fromRGB(255, 255, 255),
            }
            local CG = nG and nG.mutation and S[nG.mutation] or W.Accent1
            b.Color = ColorSequence.new(CG)
        else
            if b then
                b.Enabled = false
            end
        end
    end
    R.Heartbeat:Connect(rG)
end)
local b = {
    [1] = Vector3.new(-460.0, -6.0, 219),
    [5] = Vector3.new(-355.0, -6.0, 217),
    [2] = Vector3.new(-460.0, -6.0, 111),
    [6] = Vector3.new(-355.0, -6.0, 113),
    [3] = Vector3.new(-460.0, -6.0, 5),
    [7] = Vector3.new(-355.0, -6.0, 5),
    [4] = Vector3.new(-460.0, -6.0, -100.0),
    [8] = Vector3.new(-355.0, -6.0, -100.0),
}
local b = {
    [1] = Vector3.new(-476.474853515625, 20.732906341552734, 220.94090270996094),
    [5] = Vector3.new(-342.5367126464844, 17.69801902770996, 221.44737243652344),
    [2] = Vector3.new(-476.5684814453125, 20.70664405822754, 113.77315521240234),
    [6] = Vector3.new(-342.8604736328125, 17.669641494750977, 113.41409301757812),
    [3] = Vector3.new(-476.8675842285156, 20.74148178100586, 6.178487777709961),
    [7] = Vector3.new(-342.42108154296875, 17.687667846679688, 6.249461650848389),
    [4] = Vector3.new(-476.6324768066406, 20.744949340820312, -101.07275390625),
    [8] = Vector3.new(-342.7937927246094, 17.748071670532227, -99.73458862304688),
}
local b = {
    Vector3.new(-476.0, -4.0, 221),
    Vector3.new(-476.0, -4.0, 114),
    Vector3.new(-476.0, -4.0, 7),
    Vector3.new(-476.0, -4.0, -100.0),
    Vector3.new(-342.0, -4.0, -100.0),
    Vector3.new(-342.0, -4.0, 6),
    Vector3.new(-342.0, -4.0, 114),
    Vector3.new(-342.0, -4.0, 220),
}
local b = {
    Vector3.new(-519.0, -3.0, 221),
    Vector3.new(-519.0, -3.0, 114),
    Vector3.new(-518.0, -3.0, 7),
    Vector3.new(-519.0, -3.0, -100.0),
    Vector3.new(-301.0, -3.0, -100.0),
    Vector3.new(-301.0, -3.0, 7),
    Vector3.new(-302.0, -3.0, 114),
    Vector3.new(-300.0, -3.0, 220),
}
local b = { bodyController = nil }
local S = b.bodyController
local S = m.floatActive
R.Heartbeat:Connect(function()
    if not m.floatActive then
        return
    end
    local S = os.clock()
    if S - (_lastFloatTick or 0) < 0.05 then
        return
    end
    _lastFloatTick = S
    if b.bodyController and C.Character then
        local S = C.Character:FindFirstChild("HumanoidRootPart")
        if S then
            b.bodyController.Position = Vector3.new(S.Position.X, b.bodyController.Position.Y, S.Position.Z)
        end
    end
end)
local b = m.isTpMoving
_G._isTargetPlotUnlocked = function(S)
    local CG, rG = pcall(function()
        local dG = a:FindFirstChild("Plots")
        if not dG then
            return false
        end
        local nG = dG:FindFirstChild(S)
        if not nG then
            return false
        end
        local S = nG:FindFirstChild("Unlock")
        if not S then
            return true
        end
        local dG = {}
        for nG, nG in pairs(S:GetChildren()) do
            local S = nil
            if nG:IsA("Model") then
                pcall(function()
                    S = nG:GetPivot().Position
                end)
            elseif nG:IsA("BasePart") then
                S = nG.Position
            end
            if S then
                table.insert(dG, { Object = nG, Height = S.Y })
            end
        end
        table.sort(dG, function(S, nG)
            return S.Height < nG.Height
        end)
        if #dG == 0 then
            return true
        end
        local S = dG[1].Object
        for dG, dG in ipairs(S:GetDescendants()) do
            if dG:IsA("ProximityPrompt") and dG.Enabled then
                return false
            end
        end
        for dG, dG in ipairs(S:GetChildren()) do
            if dG:IsA("ProximityPrompt") and dG.Enabled then
                return false
            end
        end
        return true
    end)
    return CG and rG or false
end
local function S()
    if m.isTpMoving then
        return
    end
    if m.carpetSpeedEnabled then
        g(false)
        if u then
            u.Text = "OFF"
            u.TextColor3 = W.Error
        end
    end
    local CG = nil
    if i.AutoTPPriority then
        local rG = nil
        local dG = P.AllAnimalsCache
        if dG and type(dG) == "table" then
            for nG, nG in ipairs(X) do
                local fG = nG:lower()
                for nG, nG in ipairs(dG) do
                    if nG and nG.name and nG.name:lower() == fG and nG.owner ~= C.Name then
                        rG = nG
                        break
                    end
                end
                if rG then
                    break
                end
            end
            if not rG then
                for nG, nG in ipairs(dG) do
                    if nG and nG.owner ~= C.Name then
                        rG = nG
                        break
                    end
                end
            end
        end
        if rG then
            CG = rG
        else
            if not P.SelectedPetData then
                F("ERROR", "No target selected!")
                return
            end
            CG = P.SelectedPetData.animalData
        end
    else
        if not P.SelectedPetData then
            F("ERROR", "No target selected!")
            return
        end
        CG = P.SelectedPetData.animalData
    end
    if not CG then
        return
    end
    local rG = C.Character
    local dG = rG and rG:FindFirstChild("HumanoidRootPart")
    local nG = rG and rG:FindFirstChild("Humanoid")
    if not dG or not nG or nG.Health <= 0 then
        return
    end
    m.isTpMoving = true
    b = m.isTpMoving
    local fG, JG = pcall(function()
        local JG = M(CG)
        if not JG then
            error("targetPart nil")
        end
        local aG
        if JG:IsA("Attachment") then
            aG = JG.WorldCFrame
        elseif JG:IsA("BasePart") then
            aG = JG.CFrame
        else
            local eG, xG = pcall(function()
                return JG:GetPivot()
            end)
            if eG then
                aG = xG
            end
        end
        if not aG then
            error("targetCF nil")
        end
        local aG = JG.Position
        local eG = i.TpSettings.Tool
        local xG = C.Backpack:FindFirstChild(eG) or rG:FindFirstChild(eG)
        local eG = C.Backpack:FindFirstChild("Quantum Cloner") or rG:FindFirstChild("Quantum Cloner")
        I(nG)
        local rG = aG.Y > 10
        if i.TpSettings.TeleportV2 then
            local eG, xG = l(JG, nG, dG)
            if not eG then
                error("Teleport V2 failed: " .. tostring(xG))
            end
        else
            local l = Y(JG)
            if not l then
                error("signPart nil")
            end
            local Y = l.CFrame
            local eG = Y.RightVector
            local xG = -eG
            local eG = Y.LookVector
            local QG = -Y.LookVector
            local Y = G(dG.Position)
            if not Y then
                error("normal tp MED point nil")
            end
            dG.Velocity = Vector3.zero
            if nG and nG.Parent then
                nG:ChangeState(Enum.HumanoidStateType.Jumping)
            end
            task.wait(0.01)
            if not dG or not dG.Parent then
                error("hrp lost during normal jump start")
            end
            local Y
            if aG.Y <= 6.313370704650879 then
                local G = l.Position + eG * 20
                local aG = l.Position + QG * 20
                local VG = dG.Position
                local iG = (Vector3.new(G.X, 0, G.Z) - Vector3.new(VG.X, 0, VG.Z)).Magnitude
                local OG = (Vector3.new(aG.X, 0, aG.Z) - Vector3.new(VG.X, 0, VG.Z)).Magnitude
                local VG = iG < OG and G or aG
                Y = Vector3.new(VG.X, -4.8, VG.Z)
            else
                local G = l.Position + QG * 15
                local aG = l.Position + eG * 15
                local eG = dG.Position
                local QG = Vector3.new(eG.X, 0, eG.Z)
                local eG = (Vector3.new(G.X, 0, G.Z) - QG).Magnitude
                local VG = (Vector3.new(aG.X, 0, aG.Z) - QG).Magnitude
                local QG = eG < VG and G or aG
                Y = Vector3.new(QG.X, l.Position.Y + 4, QG.Z)
            end
            local l = dG.Position
            local G = math.max(l.Y, 14)
            local aG = Vector3.new(Y.X, G, Y.Z)
            if l.Y < 19 then
                local G = Vector3.new(l.X, 19, l.Z)
                local aG = math.clamp(math.ceil((19 - l.Y) / 1.5), 8, 25)
                for eG = 1, aG do
                    if not dG or not dG.Parent then
                        break
                    end
                    local QG = eG / aG
                    local aG = l:Lerp(G, QG)
                    dG.Velocity = Vector3.zero
                    dG.CFrame = CFrame.new(aG)
                    task.wait(0.03)
                end
                l = dG.Position
            end
            local G = Vector3.new(Y.X, 19, Y.Z)
            local aG = (Vector3.new(l.X, 0, l.Z) - Vector3.new(Y.X, 0, Y.Z)).Magnitude
            local eG = math.clamp(math.ceil(aG / 2), 20, 100)
            local QG = aG > 400 and 0.025 or aG > 250 and 0.018 or aG > 150 and 0.013 or 0.01
            for aG = 1, eG do
                if not dG or not dG.Parent then
                    break
                end
                local VG = aG / eG
                local aG = l:Lerp(G, VG)
                dG.Velocity = Vector3.zero
                dG.CFrame = CFrame.new(aG)
                task.wait(QG)
            end
            local l = dG.Position
            local G = math.clamp(math.ceil(math.abs(l.Y - Y.Y) / 1.5), 8, 25)
            for aG = 1, G do
                if not dG or not dG.Parent then
                    break
                end
                local eG = aG / G
                local G = l:Lerp(Y, eG)
                dG.Velocity = Vector3.zero
                dG.CFrame = CFrame.new(G)
                task.wait(0.01)
            end
            dG.Velocity = Vector3.zero
            dG.CFrame = CFrame.lookAt(Y, Y + xG)
            dG.Velocity = Vector3.zero
            o(function()
                return dG and dG.Parent and (dG.Position - Y).Magnitude <= 3
            end, 1.5)
            if rG or not _G._isTargetPlotUnlocked(CG.plot) then
                p(nG, dG)
                B(0.05)
                if rG then
                    dG.Velocity = Vector3.zero
                    B(0.01)
                    dG.Velocity = Vector3.zero
                    dG.CFrame = dG.CFrame * CFrame.new(0, 0, -2.5)
                    dG.Velocity = Vector3.zero
                else
                    dG.Velocity = Vector3.zero
                    dG.Velocity = Vector3.zero
                    B(0.03)
                    z(0.15)
                end
                B(0.07)
                local z = dG.Position
                B(0.01)
                local Y = o(function()
                    return dG and dG.Parent and (dG.Position - z).Magnitude <= 2
                end, 0.75)
                if not Y then
                    error("moved away from mini TP position before clone")
                end
                dG.Velocity = Vector3.zero
                dG.AssemblyAngularVelocity = Vector3.zero
                task.wait(0.1)
                q()
                local Y = o(function()
                    return dG and dG.Parent and (dG.Position - z).Magnitude >= 0.5
                end, 5)
                while _G.isCloning do
                    task.wait()
                end
                task.wait(0.2)
            end
        end
        task.wait(0.25)
        I(nG)
        local z = JG.Position
        local Y = dG.Position
        local I = z.Y - Y.Y
        local p = z.Y > 25
        local l = nil
        if I > 2 and not p then
            l = Instance.new("Part")
            l.Name = "XiTempPlatform"
            l.Size = Vector3.new(6, 1, 6)
            l.Position = Vector3.new(z.X, z.Y - 2.5, z.Z)
            l.Color = Color3.new(0, 0.5, 1)
            l.Material = Enum.Material.Neon
            l.Anchored = true
            l.CanCollide = true
            l.Transparency = 0.3
            l.Parent = a
            task.spawn(function()
                local I = tick()
                while tick() - I < 25 do
                    if C:GetAttribute("Stealing") then
                        break
                    end
                    task.wait(0.1)
                end
                if l and l.Parent then
                    l:Destroy()
                end
            end)
        end
        local I = l and Vector3.new(z.X, z.Y - 2, z.Z) or z
        if p then
            local p = Vector3.new(Y.X, z.Y - 2, Y.Z)
            local l = math.clamp(math.ceil(math.abs(p.Y - Y.Y) / 1.5), 6, 30)
            for G = 1, l do
                if not dG or not dG.Parent then
                    break
                end
                if C:GetAttribute("Stealing") then
                    break
                end
                local CG = G / l
                dG.Velocity = Vector3.zero
                dG.CFrame = CFrame.new(Y:Lerp(p, CG))
                task.wait(0.02)
            end
            local p = dG.Position
            local l = Vector3.new(z.X, z.Y - 2, z.Z)
            local z = (Vector3.new(p.X, 0, p.Z) - Vector3.new(l.X, 0, l.Z)).Magnitude
            local G = math.clamp(math.ceil(z / 2), 10, 60)
            local CG = z > 200 and 0.02 or 0.015
            for z = 1, G do
                if not dG or not dG.Parent then
                    break
                end
                if C:GetAttribute("Stealing") then
                    break
                end
                local rG = z / G
                dG.Velocity = Vector3.zero
                dG.CFrame = CFrame.new(p:Lerp(l, rG))
                task.wait(CG)
            end
        else
            local z = Vector3.new(Y.X, I.Y, Y.Z)
            local p = math.abs(z.Y - Y.Y)
            local l = math.clamp(math.ceil(p / 1.5), 6, 20)
            for p = 1, l do
                if not dG or not dG.Parent then
                    break
                end
                if C:GetAttribute("Stealing") then
                    break
                end
                local G = p / l
                dG.Velocity = Vector3.zero
                dG.CFrame = CFrame.new(Y:Lerp(z, G))
                task.wait(0.01)
            end
            local z = dG.Position
            local Y = Vector3.new(I.X, I.Y, I.Z)
            local p = (Vector3.new(z.X, 0, z.Z) - Vector3.new(I.X, 0, I.Z)).Magnitude
            local l = math.clamp(math.ceil(p / 2), 15, 80)
            local G = p > 200 and 0.02 or 0.01
            for p = 1, l do
                if not dG or not dG.Parent then
                    break
                end
                if C:GetAttribute("Stealing") then
                    break
                end
                local CG = p / l
                dG.Velocity = Vector3.zero
                dG.CFrame = CFrame.new(z:Lerp(Y, CG))
                task.wait(G)
            end
            local z = dG.Position
            local Y = math.abs(z.Y - I.Y)
            local p = math.clamp(math.ceil(Y / 1.5), 5, 15)
            for Y = 1, p do
                if not dG or not dG.Parent then
                    break
                end
                if C:GetAttribute("Stealing") then
                    break
                end
                local l = Y / p
                dG.Velocity = Vector3.zero
                dG.CFrame = CFrame.new(z:Lerp(I, l))
                task.wait(0.01)
            end
        end
        dG.Velocity = Vector3.zero
    end)
    m.isTpMoving = false
    b = m.isTpMoving
    if not fG then
    end
end
local function z(Y)
    if not Y then
        return
    end
    for I, I in ipairs(Y:GetChildren()) do
        if I:IsA("Tool") then
            pcall(function()
                I.Parent = C.Backpack
            end)
        end
    end
end
local Y = nil
local I = false
local function p()
    local function l(G)
        if not G then
            return nil
        end
        for b, b in ipairs(G:GetDescendants()) do
            if b:IsA("RemoteEvent") and b.Name:sub(1, 3) == "RE/" then
                return b
            end
        end
        return nil
    end
    return l(n) or l(game:GetService("CoreGui")) or l(workspace)
end
task.spawn(function()
    for l = 1, 20 do
        Y = p()
        if Y then
            break
        end
        task.wait(1)
    end
end)
local function l()
    if I then
        return
    end
    if not Y then
        Y = p()
    end
    if not Y then
        return
    end
    I = true
    local p = C.Character
    task.spawn(function()
        while C.Character == p do
            pcall(function()
                Y:FireServer("f888ee6e-c86d-46e1-93d7-0639d6635d42", C, "balloon")
            end)
            task.wait()
        end
        I = false
    end)
end
local resetCooldown = false
local resetThread = nil
local currentCharacter = nil
local resetSuccessful = false
local stopResetSequence = false
local cameraLocked = false
local lockedCameraCFrame = nil

local function StopResetSequence()
    stopResetSequence = true
    if resetThread then
        task.cancel(resetThread)
        resetThread = nil
    end
    resetCooldown = false
    currentCharacter = nil
    cameraLocked = false

    local character = C.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            pcall(function()
                humanoid.HipHeight = 2
                local rootPart = character:FindFirstChild("HumanoidRootPart")
                if rootPart then
                    rootPart.CanCollide = true
                end
                for _, part in ipairs(character:GetChildren()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = true
                    end
                end
            end)
        end
    end
end

C.CharacterAdded:Connect(function()
    StopResetSequence()
    resetCooldown = false
    currentCharacter = nil
    resetSuccessful = false
    stopResetSequence = false
    cameraLocked = false
end)

task.spawn(function()
    local camera = workspace.CurrentCamera
    while true do
        task.wait(0.016)
        if cameraLocked and lockedCameraCFrame and camera then
            camera.CFrame = lockedCameraCFrame
        end
    end
end)

local function Y(I)
    task.spawn(l)
    if m and type(m) == "table" and m.carpetSpeedEnabled then
        if type(g) == "function" then g(false) end
        if u and typeof(u) == "Instance" and u:IsA("TextLabel") then
            u.Text = "OFF"
            u.TextColor3 = W.Error
        end
    end
    
    if resetCooldown then return end
    resetCooldown = true
    resetSuccessful = false
    stopResetSequence = false
    cameraLocked = false

    local character = C.Character
    if not character then 
        resetCooldown = false 
        pcall(function() C:LoadCharacter() end)
        if I then
            local conn
            conn = C.CharacterAdded:Connect(function()
                if conn then conn:Disconnect() end
                task.defer(function() pcall(I) end)
            end)
        end
        return 
    end

    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then resetCooldown = false return end

    local camera = workspace.CurrentCamera
    if camera then
        lockedCameraCFrame = camera.CFrame
        cameraLocked = true
        camera.CFrame = lockedCameraCFrame
    end

    currentCharacter = character
    local isRespawning = false

    resetThread = task.spawn(function()
        local attempts = 0
        local maxAttempts = 40
        local originalHipHeight = humanoid.HipHeight

        while character and character.Parent and humanoid and humanoid.Health > 0 and not isRespawning and not stopResetSequence do
            if C.Character ~= character then
                isRespawning = true
                break
            end

            pcall(function()
                humanoid.HipHeight = 1e30
                humanoid.AutoRotate = true

                local rootPart = character:FindFirstChild("HumanoidRootPart")
                if rootPart then
                    rootPart.CanCollide = false
                end

                for _, part in ipairs(character:GetChildren()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = false
                    end
                end
            end)

            if not character or not character.Parent or not humanoid or humanoid.Health <= 0 or C.Character ~= character then
                resetSuccessful = true
                break
            end

            attempts = attempts + 1
            if attempts >= maxAttempts then break end
            task.wait(0.05)
        end

        if not resetSuccessful then
            if character and character.Parent and humanoid and humanoid.Health > 0 and not isRespawning then
                pcall(function()
                    humanoid.Health = 0
                end)
                task.wait(0.1)
                if not character.Parent or humanoid.Health <= 0 then
                    resetSuccessful = true
                end
            end
        end

        if not resetSuccessful and character and character.Parent and humanoid then
            pcall(function()
                humanoid.HipHeight = originalHipHeight
                local rootPart = character:FindFirstChild("HumanoidRootPart")
                if rootPart then
                    rootPart.CanCollide = true
                end
                for _, part in ipairs(character:GetChildren()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = true
                    end
                end
            end)
        end

        cameraLocked = false
        resetCooldown = false
        resetThread = nil
        currentCharacter = nil
        stopResetSequence = false
        
        task.defer(function()
            pcall(function()
                C:LoadCharacter()
            end)
        end)
    end)
    
    if I then
        local conn
        conn = C.CharacterAdded:Connect(function()
            if conn then conn:Disconnect() end
            task.defer(function() pcall(I) end)
        end)
    end
end
local function z(I, p)
    local l, G = pcall(function()
        if type(I) ~= "string" or I == "" then
            return p
        end
        local b = Enum.KeyCode[I]
        if b then
            return b
        end
        if #I == 1 then
            local CG = string.upper(I)
            b = Enum.KeyCode[CG]
            if b then
                return b
            end
        end
        return p
    end)
    return l and G or p
end
local I = 0
task.spawn(function()
    local p = 0
    local l = false
    while true do
        task.wait(1)
        if not i.AutoResetOnBalloon then
            continue
        end
        if l then
            continue
        end
        if tick() - p < 8 then
            continue
        end
        for G, G in ipairs(x:GetDescendants()) do
            local b = (G:IsA("TextLabel") or G:IsA("TextButton")) and G.Text
            if b and string.find(b, 'ran "balloon" on you') then
                p = tick()
                l = true
                task.spawn(function()
                    task.wait(25)
                    l = false
                end)
                Y(function()
                    l = false
                end)
                break
            end
        end
    end
end)
task.spawn(function()
    local p = setmetatable({}, { __mode = "k" })
    local function l(G)
        if typeof(G) ~= "string" then
            return false
        end
        return string.find(string.lower(G), "you stole", 1, true) ~= nil
    end
    local function G(b)
        if p[b] then
            return
        end
        p[b] = true
        if i.AutoKickOnSteal and l(b.Text) then
            c()
            return
        end
        b:GetPropertyChangedSignal("Text"):Connect(function()
            if i.AutoKickOnSteal and l(b.Text) then
                c()
            end
        end)
    end
    local function p(l)
        for b, b in ipairs(l:GetDescendants()) do
            if b:IsA("TextLabel") or b:IsA("TextButton") or b:IsA("TextBox") then
                G(b)
            end
        end
    end
    local function l(b)
        p(b)
        b.DescendantAdded:Connect(function(b)
            if b:IsA("TextLabel") or b:IsA("TextButton") or b:IsA("TextBox") then
                G(b)
            end
        end)
    end
    for G, G in ipairs(x:GetChildren()) do
        l(G)
    end
    x.ChildAdded:Connect(function(G)
        l(G)
    end)
    p(x)
end)
j.InputBegan:Connect(function(p, l)
    if p.UserInputType == Enum.UserInputType.Keyboard then
        local G = z(i.ResetKey, Enum.KeyCode.H)
        if p.KeyCode == G then
            if j:GetFocusedTextBox() == nil then
                local z = tick()
                if z - I >= 0.45 then
                    I = z
                    Y()
                end
            end
            return
        end
    end
    if l then
        return
    end
    local z = Enum.KeyCode[NormalizeKeyName(i.TpSettings.TpKey, "T")] or Enum.KeyCode.T
    local Y = Enum.KeyCode[NormalizeKeyName(i.TpSettings.CloneKey, "V")] or Enum.KeyCode.V
    if p.KeyCode == z then
        S()
    end
    if p.KeyCode == Y then
        q()
    end
    if p.KeyCode == (Enum.KeyCode[NormalizeKeyName(i.TpSettings.CarpetSpeedKey, "Q")] or Enum.KeyCode.Q) then
        Z = not Z
        g(Z)
        if u then
            u.Text = Z and "ON" or "OFF"
            u.TextColor3 = Z and W.Success or W.Error
        end
        F("CARPET SPEED", Z and "ON  |  " .. i.TpSettings.Tool .. "  |  140" or "OFF")
    end
    if p.KeyCode == (Enum.KeyCode[NormalizeKeyName(i.StealSpeedKey, "C")] or Enum.KeyCode.C) then
        if P.StealSpeedToggleFunc then
            P.StealSpeedToggleFunc()
        end
    end
    if p.KeyCode == (Enum.KeyCode[NormalizeKeyName(i.FloatKeybind, "Z")] or Enum.KeyCode.Z) then
        N(false)
    end
    if p.KeyCode == (Enum.KeyCode[NormalizeKeyName(i.RagdollSelfKey, "B")] or Enum.KeyCode.B) then
        task.spawn(function()
            pcall(k)
            F("DROP BRAINROT", "Triggered")
        end)
    end
end)
local z
local Y
local I
local p
local l = {}
local function G()
    z = H.settingsGui
    if d then
        local b = Instance.new("ScreenGui")
        b.Name = "XiMobileControls"
        b.ResetOnSpawn = false
        b.Parent = gethui and gethui() or game:GetService("CoreGui")
        local H = Instance.new("Frame")
        H.Size = UDim2.new(0, 50, 0, 260)
        H.Position = UDim2.new(1, -60.0, 0.5, -130.0)
        H.BackgroundColor3 = W.Background
        H.BackgroundTransparency = 0.2
        H.BorderSizePixel = 0
        H.Parent = b
        w(H)
        Instance.new("UICorner", H).CornerRadius = UDim.new(0, 10)
        local N = Instance.new("UIStroke", H)
        N.Color = W.Accent1
        N.Thickness = 1.5
        N.Transparency = 0.4
        D(H, H, "MobileControls")
        local N = Instance.new("UIListLayout", H)
        N.SortOrder = Enum.SortOrder.LayoutOrder
        N.Padding = UDim.new(0, 8)
        N.HorizontalAlignment = Enum.HorizontalAlignment.Center
        N.VerticalAlignment = Enum.VerticalAlignment.Center
        local function H(N, CG, rG, dG)
            local nG = Instance.new("TextButton")
            nG.Name = "MobileAction_" .. tostring(N)
            nG.Size = UDim2.new(0, 38, 0, 38)
            nG.BackgroundColor3 = W.SurfaceHighlight
            nG.BackgroundTransparency = 0.08
            nG.Text = N
            nG.TextColor3 = W.TextPrimary
            nG.Font = Enum.Font.FredokaOne
            nG.TextSize = N == "KICK" and 10 or 13
            nG.LayoutOrder = rG
            nG.AutoButtonColor = false
            nG.Visible = i.ShowMobileActionButtons ~= false
            nG.Parent = b
            local fG = "MobileBtn_" .. N
            if i.Positions[fG] then
                nG.Position = UDim2.new(
                    i.Positions[fG].X,
                    i.Positions[fG].OffsetX or 0,
                    i.Positions[fG].Y,
                    i.Positions[fG].OffsetY or 0
                )
            else
                local fG = (rG - 1) * 1.0471975511965976 - 1.5707963267948966
                nG.Position = UDim2.new(0.5, math.cos(fG) * 64 - 19, 0.5, math.sin(fG) * 64 - 19)
            end
            Instance.new("UICorner", nG).CornerRadius = UDim.new(0, 10)
            local rG = Instance.new("UIStroke", nG)
            rG.Color = CG
            rG.Thickness = 1.4
            rG.Transparency = 0.28
            table.insert(P.MobileActionButtons, nG)
            D(nG, nG, "MobileBtn_" .. N)
            nG.MouseButton1Click:Connect(function()
                local N = nG.BackgroundColor3
                nG.BackgroundColor3 = CG
                task.delay(0.1, function()
                    if nG and nG.Parent then
                        nG.BackgroundColor3 = N
                    end
                end)
                dG(nG)
            end)
            return nG
        end
        P.RefreshMobileActionButtons = function()
            local N = i.ShowMobileActionButtons ~= false
            for CG, CG in ipairs(P.MobileActionButtons) do
                if CG and CG.Parent then
                    CG.Visible = N
                end
            end
        end
        H("TP", W.Accent1, 1, function()
            if P.ForcePrioritySelection then
                P.ForcePrioritySelection()
                task.wait(0.1)
            end
            S()
            F("MOBILE", "Teleporting...")
        end)
        H("CL", W.Accent2, 2, function()
            q()
            F("MOBILE", "Cloning...")
        end)
        H("SP", W.Success, 3, function(q)
            Z = not Z
            g(Z)
            q.TextColor3 = Z and W.Success or W.TextPrimary
            F("MOBILE", Z and "Speed ON" or "Speed OFF")
        end)
        H("IV", Color3.fromRGB(50, 178, 255), 4, function(q)
            if _G.toggleInvisibleSteal then
                _G.toggleInvisibleSteal()
                task.delay(0.1, function()
                    local g = _G.invisibleStealEnabled
                    q.TextColor3 = g and Color3.fromRGB(0, 178, 255) or W.TextPrimary
                    F("MOBILE", g and "Invis ON" or "Invis OFF")
                end)
            end
        end)
        H("UI", Color3.fromRGB(255, 255, 255), 5, function()
            if z then
                z.Enabled = not z.Enabled
                F("MOBILE", z.Enabled and "Menu opened" or "Menu closed")
            end
            if P.RefreshMobileScale then
                P.RefreshMobileScale()
            end
        end)
        H("KICK", W.Error, 6, function()
            F("MOBILE", "Kicking...")
            c()
        end)
        if P.RefreshMobileActionButtons then
            P.RefreshMobileActionButtons()
        end
        local q = Instance.new("TextButton")
        q.Name = "MobileResetButton"
        q.Size = UDim2.new(0, 42, 0, 42)
        q.Position = UDim2.new(1, -58.0, 1, -105.0)
        q.BackgroundColor3 = Color3.fromRGB(50, 178, 255)
        q.AutoButtonColor = false
        q.Text = "ð§"
        q.Font = Enum.Font.FredokaOne
        q.TextSize = 20
        q.TextColor3 = Color3.new(0, 0, 0)
        q.Parent = b
        Instance.new("UICorner", q).CornerRadius = UDim.new(1, 0)
        local H = Instance.new("UIStroke", q)
        H.Color = Color3.fromRGB(0, 178, 255)
        H.Thickness = 1.5
        H.Transparency = 0.25
        D(q, q)
        q.MouseButton1Click:Connect(function()
            i.Positions = {
                SafePanel = { X = 0.1859375, Y = 0.5767123526556385, OffsetX = 0, OffsetY = 0 },
                StealSpeed = { X = 0.02, Y = 0.18 },
                Settings = { X = 0.5, Y = 0.5, OffsetX = 0, OffsetY = 0 },
                InvisPanel = { X = 0.8578125, Y = 0.17260276361454258, OffsetX = 0, OffsetY = 0 },
                AutoSteal = { X = 0.02, Y = 0.35, OffsetX = 0, OffsetY = 0 },
                MobileControls = { X = 0.9, Y = 0.4 },
                MobileBtn_TP = { X = 0.5, Y = 0.4 },
                MobileBtn_CL = { X = 0.5, Y = 0.4 },
                MobileBtn_SP = { X = 0.5, Y = 0.4 },
                MobileBtn_IV = { X = 0.5, Y = 0.4 },
                MobileBtn_UI = { X = 0.5, Y = 0.4 },
                MobileBtn_KICK = { X = 0.5, Y = 0.4 },
            }
            i.ShowMobileActionButtons = true
            i.MobileGuiScale = 0.5
            U()
            if P.RefreshMobileScale then
                P.RefreshMobileScale()
            end
            if b then
                b.Position = UDim2.new(i.Positions.MobileControls.X, 0, i.Positions.MobileControls.Y, 0)
            end
            F("RESET", "All GUI positions and scale reset")
        end)
        local q = Instance.new("TextButton")
        q.Name = "MobileSettingsButton"
        q.Size = UDim2.new(0, 42, 0, 42)
        q.Position = UDim2.new(1, -58.0, 1, -58.0)
        q.BackgroundColor3 = W.Accent1
        q.AutoButtonColor = false
        q.Text = "â"
        q.Font = Enum.Font.FredokaOne
        q.TextSize = 20
        q.TextColor3 = Color3.new(0, 0, 0)
        q.Parent = b
        Instance.new("UICorner", q).CornerRadius = UDim.new(1, 0)
        local b = Instance.new("UIStroke", q)
        b.Color = W.Accent2
        b.Thickness = 1.5
        b.Transparency = 0.25
        D(q, q)
        q.MouseButton1Click:Connect(function()
            if z then
                z.Enabled = not z.Enabled
            end
            if P.RefreshMobileScale then
                P.RefreshMobileScale()
            end
        end)
    end
    W.Background = Color3.fromRGB(10, 25, 10)
    W.Surface = Color3.fromRGB(15, 35, 15)
    W.SurfaceHighlight = Color3.fromRGB(25, 60, 25)
    W.Accent1 = Color3.fromRGB(0, 200, 0)
    W.Accent2 = Color3.fromRGB(0, 150, 0)
    W.TextPrimary = Color3.fromRGB(255, 255, 255)
    W.TextSecondary = Color3.fromRGB(170, 170, 170)
    W.Success = Color3.fromRGB(0, 255, 0)
    local q = {
        BG = Color3.fromRGB(10, 25, 10),
        SURF = Color3.fromRGB(15, 35, 15),
        SURF2 = Color3.fromRGB(12, 30, 12),
        TEXT = Color3.fromRGB(255, 255, 255),
        DIM = Color3.fromRGB(150, 150, 150),
        TOP = Color3.fromRGB(0, 150, 0),
        MID = Color3.fromRGB(0, 200, 0),
        BOT = Color3.fromRGB(0, 100, 0),
        ACCENT = Color3.fromRGB(0, 200, 0),
        ACCENT2 = Color3.fromRGB(0, 150, 0),
        STROKE = Color3.fromRGB(0, 100, 0),
        GLOW = Color3.fromRGB(0, 255, 0),
    }
    local function b(H, g)
        local N = Instance.new("UICorner")
        if g == 7 then
            N.CornerRadius = UDim.new(1, 0)
        else
            N.CornerRadius = UDim.new(0, g + 8)
        end
        N.Parent = H
        return N
    end
    local function H(g, N, CG, rG)
        local dG = Instance.new("UIStroke")
        dG.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        dG.Thickness = N or 1
        dG.Color = CG or q.STROKE
        dG.Transparency = rG or 0.5
        dG.Parent = g
        return dG
    end
    local function g(N, CG, rG, dG, nG)
        f
            :Create(
                N,
                TweenInfo.new(CG or 0.2, dG or Enum.EasingStyle.Quint, nG or Enum.EasingDirection.Out),
                rG
            )
            :Play()
    end
    z = Instance.new("ScreenGui")
    z.Name = "rqyubbJbyzOA"
    z.ResetOnSpawn = false
    z.IgnoreGuiInset = true
    z.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    z.Parent = gethui and gethui() or game:GetService("CoreGui")
    z.Enabled = false
    local function N(CG)
        return math.floor(CG * 1 + 0.5)
    end
    local CG = 94
    local rG = 808 - CG
    q.BG = Color3.fromRGB(8, 9, 14)
    q.SURF = Color3.fromRGB(15, 30, 50)
    q.SURF2 = Color3.fromRGB(16, 24, 44)
    q.SIDE = Color3.fromRGB(16, 26, 48)
    q.SIDE2 = Color3.fromRGB(22, 36, 68)
    q.TEXT = Color3.fromRGB(220, 240, 255)
    q.DIM = Color3.fromRGB(80, 126, 180)
    q.TOP = Color3.fromRGB(60, 154, 220)
    q.MID = Color3.fromRGB(30, 119, 170)
    q.BOT = Color3.fromRGB(22, 36, 68)
    q.ACCENT = Color3.fromRGB(60, 154, 220)
    q.ACCENT2 = Color3.fromRGB(30, 119, 170)
    q.STROKE = Color3.fromRGB(50, 140, 200)
    q.GLOW = Color3.fromRGB(100, 178, 255)
    W.Background = q.BG
    W.Surface = q.SURF
    W.SurfaceHighlight = q.SURF2
    W.Accent1 = q.ACCENT
    W.Accent2 = q.ACCENT2
    W.TextPrimary = q.TEXT
    W.TextSecondary = q.DIM
    W.Success = q.ACCENT2
    W.Error = q.ACCENT2
    local function CG(dG, nG, fG, JG, aG)
        local eG = Instance.new("UIPadding")
        eG.PaddingLeft = UDim.new(0, nG or 0)
        eG.PaddingTop = UDim.new(0, fG or 0)
        eG.PaddingRight = UDim.new(0, JG or 0)
        eG.PaddingBottom = UDim.new(0, aG or 0)
        eG.Parent = dG
        return eG
    end
    local function dG(nG)
        local fG = Instance.new("UIGradient")
        fG.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, q.ACCENT),
            ColorSequenceKeypoint.new(1, q.ACCENT2),
        })
        fG.Parent = nG
        return fG
    end
    local function nG(fG)
        local JG = Instance.new("UIGradient")
        JG.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(50, 133, 190)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 178, 255)),
        })
        JG.Parent = fG
        return JG
    end
    local function fG(JG)
        local aG = Instance.new("UIGradient")
        aG.Rotation = 0
        aG.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
            ColorSequenceKeypoint.new(0.5, Color3.fromRGB(180, 178, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255)),
        })
        aG.Parent = JG
        return aG
    end
    Y = Instance.new("Frame")
    Y.Name = "sFrame"
    Y.Size = UDim2.fromOffset(rG, 456)
    Y.AnchorPoint = Vector2.new(0.5, 0.5)
    Y.Position = UDim2.new(
        i.Positions.Settings.X,
        i.Positions.Settings.OffsetX or 0,
        i.Positions.Settings.Y,
        i.Positions.Settings.OffsetY or 0
    )
    Y.BackgroundColor3 = q.BG
    Y.BackgroundTransparency = 0.03
    Y.BorderSizePixel = 0
    Y.Parent = z
    b(Y, 18)
    local rG = H(Y, 2, Color3.fromRGB(50, 140, 200), 0)
    rG.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local rG = Instance.new("UIStroke")
    rG.Color = Color3.fromRGB(60, 154, 220)
    rG.Thickness = 4
    rG.Transparency = 0.75
    rG.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    rG.Parent = Y
    local rG = Instance.new("ImageLabel")
    rG.Name = "Shadow"
    rG.AnchorPoint = Vector2.new(0.5, 0.5)
    rG.Position = UDim2.new(0.5, 0, 0.5, 2)
    rG.Size = UDim2.new(1, 30, 1, 30)
    rG.BackgroundTransparency = 1
    rG.Image = "rbxassetid://6014261993"
    rG.ImageColor3 = Color3.new(0, 0, 0)
    rG.ImageTransparency = 0.6
    rG.ScaleType = Enum.ScaleType.Slice
    rG.SliceCenter = Rect.new(49, 49, 450, 450)
    rG.ZIndex = 0
    rG.Parent = Y
    w(Y)
    local w = Instance.new("Frame")
    w.Name = "Sidebar"
    w.Size = UDim2.fromOffset(163, 456)
    w.BackgroundColor3 = q.SURF
    w.BackgroundTransparency = 0
    w.BorderSizePixel = 0
    w.Parent = Y
    b(w, 18)
    H(w, 1, q.STROKE, 0.5)
    local rG = Instance.new("Frame")
    rG.Name = "Brand"
    rG.Size = UDim2.new(1, -16.0, 0, 60)
    rG.Position = UDim2.fromOffset(8, 8)
    rG.BackgroundColor3 = q.SURF2
    rG.BackgroundTransparency = 0
    rG.BorderSizePixel = 0
    rG.Parent = w
    b(rG, 14)
    H(rG, 1, q.STROKE, 0.45)
    local JG = Instance.new("Frame")
    JG.Name = "Orb"
    JG.Size = UDim2.fromOffset(38, 38)
    JG.AnchorPoint = Vector2.new(0.5, 0.5)
    JG.Position = UDim2.new(0.5, 0, 0.5, 0)
    JG.BackgroundColor3 = q.ACCENT2
    JG.BorderSizePixel = 0
    JG.Parent = rG
    b(JG, 12)
    dG(JG)
    H(JG, 1, q.GLOW, 0.45)
    task.spawn(function()
        while JG.Parent do
            g(JG, 1.6, { Rotation = 8 }, Enum.EasingStyle.Sine)
            task.wait(1.6)
            if not JG.Parent then
                break
            end
            g(JG, 1.6, { Rotation = -8.0 }, Enum.EasingStyle.Sine)
            task.wait(1.6)
        end
    end)
    local rG = Instance.new("Frame")
    rG.Name = "TabHolder"
    rG.BackgroundTransparency = 1
    rG.Position = UDim2.fromOffset(8, 76)
    rG.Size = UDim2.new(1, -16.0, 0, 312)
    rG.Parent = w
    local JG = Instance.new("UIListLayout")
    JG.Padding = UDim.new(0, 6)
    JG.SortOrder = Enum.SortOrder.LayoutOrder
    JG.Parent = rG
    local JG = Instance.new("Frame")
    JG.Name = "Footer"
    JG.Size = UDim2.new(1, -16.0, 0, 42)
    JG.Position = UDim2.new(0, 8, 1, -50.0)
    JG.BackgroundColor3 = q.SURF2
    JG.BackgroundTransparency = 0
    JG.BorderSizePixel = 0
    JG.Parent = w
    b(JG, 12)
    H(JG, 1, q.STROKE, 0.48)
    local w = Instance.new("TextLabel")
    w.BackgroundTransparency = 1
    w.Size = UDim2.new(1, -14.0, 1, 0)
    w.Position = UDim2.fromOffset(7, 0)
    w.Font = Enum.Font.FredokaOne
    w.Text = "V 1.0"
    w.TextSize = 11
    w.TextColor3 = q.DIM
    w.TextXAlignment = Enum.TextXAlignment.Left
    w.Parent = JG
    local w = Instance.new("Frame")
    w.Name = "Right"
    w.BackgroundTransparency = 1
    w.Position = UDim2.fromOffset(173, 10)
    w.Size = UDim2.new(0, 531, 1, -20.0)
    w.Parent = Y
    local JG = Instance.new("Frame")
    JG.Name = "Header"
    JG.Size = UDim2.new(1, 0, 0, 52)
    JG.BackgroundColor3 = q.SURF
    JG.BackgroundTransparency = 0
    JG.BorderSizePixel = 0
    JG.Parent = w
    b(JG, 16)
    H(JG, 1, q.STROKE, 0.42)
    local aG = Instance.new("TextLabel")
    aG.BackgroundTransparency = 1
    aG.Position = UDim2.fromOffset(16, 0)
    aG.Size = UDim2.new(1, -72.0, 1, 0)
    aG.Font = Enum.Font.FredokaOne
    aG.Text = "LACASA PRIVATEEEEEEEE"
    aG.TextColor3 = q.TEXT
    aG.TextSize = 21
    aG.TextXAlignment = Enum.TextXAlignment.Left
    aG.Parent = JG
    do
        local eG = fG(aG)
        eG.Offset = Vector2.new(-1.0, 0)
        task.spawn(function()
            while aG.Parent do
                eG.Offset = Vector2.new(-1.0, 0)
                f:Create(
                    eG,
                    TweenInfo.new(2.4, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
                    { Offset = Vector2.new(1, 0) }
                ):Play()
                task.wait(2.5)
            end
        end)
    end
    local fG = Instance.new("TextButton")
    fG.AutoButtonColor = false
    fG.Size = UDim2.fromOffset(30, 30)
    fG.Position = UDim2.new(1, -40.0, 0.5, -15.0)
    fG.BackgroundColor3 = q.SURF2
    fG.BackgroundTransparency = 0
    fG.BorderSizePixel = 0
    fG.Text = "X"
    fG.Font = Enum.Font.FredokaOne
    fG.TextSize = 13
    fG.TextColor3 = q.TEXT
    fG.Parent = JG
    b(fG, 10)
    H(fG, 1, q.STROKE, 0.36)
    fG.MouseEnter:Connect(function()
        g(fG, 0.16, { BackgroundTransparency = 0, BackgroundColor3 = Color3.fromRGB(30, 60, 120) })
    end)
    fG.MouseLeave:Connect(function()
        g(fG, 0.16, { BackgroundTransparency = 0, BackgroundColor3 = q.SURF2 })
    end)
    fG.MouseButton1Click:Connect(function()
        z.Enabled = false
    end)
    D(JG, Y, "Settings")
    local fG = Instance.new("Frame")
    fG.Name = "Content"
    fG.BackgroundTransparency = 1
    fG.Position = UDim2.fromOffset(0, 62)
    fG.Size = UDim2.new(1, 0, 1, -62.0)
    fG.Parent = w
    local function w(JG)
        local aG = Instance.new("Frame")
        aG.Name = JG
        aG.BackgroundTransparency = 1
        aG.Size = UDim2.fromScale(1, 1)
        aG.Visible = false
        aG.Parent = fG
        local fG = Instance.new("Frame")
        fG.Name = "Card"
        fG.BackgroundColor3 = q.SURF
        fG.BackgroundTransparency = 0
        fG.BorderSizePixel = 0
        fG.Size = UDim2.fromScale(1, 1)
        fG.Parent = aG
        b(fG, 16)
        H(fG, 1, q.STROKE, 0.48)
        local eG = Instance.new("ScrollingFrame")
        eG.Name = "Scroll"
        eG.Size = UDim2.new(1, -12.0, 1, -12.0)
        eG.Position = UDim2.fromOffset(6, 6)
        eG.BackgroundTransparency = 1
        eG.BorderSizePixel = 0
        eG.ScrollBarThickness = 4
        eG.ScrollBarImageColor3 = q.ACCENT2
        eG.CanvasSize = UDim2.new(0, 0, 0, 0)
        eG.Parent = fG
        local xG = Instance.new("UIPadding")
        xG.PaddingLeft = UDim.new(0, 6)
        xG.PaddingRight = UDim.new(0, 6)
        xG.PaddingTop = UDim.new(0, 6)
        xG.PaddingBottom = UDim.new(0, 10)
        xG.Parent = eG
        local xG = Instance.new("UIListLayout")
        xG.Padding = UDim.new(0, 8)
        xG.SortOrder = Enum.SortOrder.LayoutOrder
        xG.Parent = eG
        xG:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            eG.CanvasSize = UDim2.new(0, 0, 0, xG.AbsoluteContentSize.Y + 18)
        end)
        do
            local QG = Instance.new("Frame")
            QG.Name = "PageTitleRow"
            QG.Size = UDim2.new(1, 0, 0, N(30))
            QG.BackgroundTransparency = 1
            QG.Parent = eG
            local VG = Instance.new("Frame")
            VG.Size = UDim2.new(0, N(4), 0, N(18))
            VG.Position = UDim2.new(0, N(4), 0.5, -N(9))
            VG.BackgroundColor3 = q.ACCENT
            VG.BorderSizePixel = 0
            VG.Parent = QG
            b(VG, 3)
            dG(VG)
            local VG = Instance.new("TextLabel")
            VG.BackgroundTransparency = 1
            VG.Size = UDim2.new(0, 150, 1, 0)
            VG.Position = UDim2.new(0, N(16), 0, 0)
            VG.Text = tostring(JG)
            VG.TextColor3 = q.TEXT
            VG.TextSize = N(15)
            VG.Font = Enum.Font.FredokaOne
            VG.TextXAlignment = Enum.TextXAlignment.Left
            VG.Parent = QG
            local JG = Instance.new("Frame")
            JG.Size = UDim2.new(1, -N(160), 0, 1)
            JG.Position = UDim2.new(0, N(158), 0.5, 0)
            JG.AnchorPoint = Vector2.new(0, 0.5)
            JG.BackgroundColor3 = q.ACCENT2
            JG.BackgroundTransparency = 0.35
            JG.BorderSizePixel = 0
            JG.Parent = QG
            b(JG, 999)
        end
        return aG, fG, eG, xG
    end
    local fG = {}
    l = {}
    local JG, aG
    JG, aG, I, p = w("Settings")
    fG.Settings = JG
    l.Settings = aG
    _G.HazePageScrolls = _G.HazePageScrolls or {}
    _G.HazePageScrolls.Settings = I
    _G.HazeActiveScroll = I
    do
        local JG, aG, eG = w("Main")
        fG.Main = JG
        l.Main = aG
        _G.HazePageScrolls.Main = eG
    end
    do
        local JG, aG, eG = w("Misc")
        fG.Misc = JG
        l.Misc = aG
        _G.HazePageScrolls.Misc = eG
    end
    do
        local JG, aG, eG = w("Player")
        fG.Player = JG
        l.Player = aG
        _G.HazePageScrolls.Player = eG
    end
    do
        local JG, aG, eG = w("Priority")
        fG.Priority = JG
        l.Priority = aG
        _G.HazePageScrolls.Priority = eG
    end
    do
        local JG, aG, eG = w("Keybinds")
        fG.Keybinds = JG
        l.Keybinds = aG
        _G.HazePageScrolls.Keybinds = eG
    end
    do
        local JG, aG, eG = w("Credits")
        fG.Credits = JG
        l.Credits = aG
        _G.HazePageScrolls.Credits = eG
    end
    local w = {
        { key = "Main", label = "Main" },
        { key = "Misc", label = "Misc" },
        { key = "Player", label = "Player" },
        { key = "Priority", label = "Priority" },
        { key = "Keybinds", label = "Keybinds" },
        { key = "Settings", label = "Settings" },
        { key = "Credits", label = "Credits" },
    }
    local JG = {}
    local aG = nil
    local eG
    local function xG(QG, VG)
        local iG = Instance.new("TextButton")
        iG.Name = QG.key .. "Tab"
        iG.AutoButtonColor = false
        iG.Size = UDim2.new(1, 0, 0, 40)
        iG.BackgroundTransparency = 1
        iG.Text = ""
        iG.LayoutOrder = VG
        iG.Parent = rG
        local rG = Instance.new("Frame")
        rG.Size = UDim2.fromScale(1, 1)
        rG.BackgroundColor3 = q.SIDE2
        rG.BackgroundTransparency = 0.32
        rG.BorderSizePixel = 0
        rG.Parent = iG
        b(rG, 11)
        local VG = H(rG, 1, q.STROKE, 0.52)
        local OG = Instance.new("Frame")
        OG.Size = UDim2.fromOffset(4, 10)
        OG.Position = UDim2.new(0, 7, 0.5, 0)
        OG.AnchorPoint = Vector2.new(0, 0.5)
        OG.BackgroundColor3 = q.ACCENT
        OG.BorderSizePixel = 0
        OG.Parent = rG
        b(OG, 999)
        dG(OG)
        local UG = Instance.new("TextLabel")
        UG.BackgroundTransparency = 1
        UG.Position = UDim2.fromOffset(22, 0)
        UG.Size = UDim2.new(1, -32.0, 1, 0)
        UG.Font = Enum.Font.FredokaOne
        UG.Text = QG.label
        UG.TextColor3 = q.DIM
        UG.TextSize = 13
        UG.TextXAlignment = Enum.TextXAlignment.Left
        UG.Parent = rG
        local yG = Instance.new("Frame")
        yG.Size = UDim2.new(0.35, 0, 0, 1)
        yG.Position = UDim2.new(0, 22, 1, -5.0)
        yG.BackgroundColor3 = q.ACCENT
        yG.BorderSizePixel = 0
        yG.Parent = rG
        dG(yG)
        iG.MouseEnter:Connect(function()
            if aG ~= QG.key then
                g(rG, 0.16, { BackgroundTransparency = 0.09, BackgroundColor3 = Color3.fromRGB(22, 41, 80) })
                g(UG, 0.16, { TextColor3 = q.TEXT })
                g(yG, 0.16, { Size = UDim2.new(0.5, 0, 0, 1) })
            end
        end)
        iG.MouseLeave:Connect(function()
            if aG ~= QG.key then
                g(rG, 0.16, { BackgroundTransparency = 0.32, BackgroundColor3 = q.SIDE2 })
                g(UG, 0.16, { TextColor3 = q.DIM })
                g(yG, 0.16, { Size = UDim2.new(0.35, 0, 0, 1) })
            end
        end)
        iG.MouseButton1Click:Connect(function()
            eG(QG.key)
        end)
        JG[QG.key] = { ButtonBack = rG, Stroke = VG, Bar = OG, Label = UG, BottomLine = yG }
    end
    eG = function(rG)
        aG = rG
        for aG, QG in pairs(fG) do
            QG.Visible = (aG == rG)
        end
        for fG, aG in pairs(JG) do
            local JG = fG == rG
            g(
                aG.ButtonBack,
                0.2,
                {
                    BackgroundTransparency = JG and 0.03 or 0.18,
                    BackgroundColor3 = JG and Color3.fromRGB(28, 51, 100) or q.SIDE2,
                }
            )
            g(aG.Stroke, 0.2, { Transparency = JG and 0.18 or 0.52 })
            g(aG.Bar, 0.2, { Size = JG and UDim2.fromOffset(4, 24) or UDim2.fromOffset(4, 10) })
            g(
                aG.Label,
                0.2,
                {
                    TextColor3 = JG and q.TEXT or q.DIM,
                    Position = JG and UDim2.fromOffset(24, 0) or UDim2.fromOffset(22, 0),
                }
            )
            g(aG.BottomLine, 0.2, { Size = JG and UDim2.new(1, -26.0, 0, 1) or UDim2.new(0.35, 0, 0, 1) })
        end
        local fG = _G.HazePageScrolls
        if fG and fG[rG] then
            _G.HazeActiveScroll = fG[rG]
        else
            _G.HazeActiveScroll = I
        end
        if rG == "Priority" and _G.ReloadEmbeddedPriorityUi then
            _G.ReloadEmbeddedPriorityUi()
        end
    end
    for rG, fG in ipairs(w) do
        xG(fG, rG)
    end
    task.defer(function()
        task.wait()
        eG("Main")
    end)
    function _G.SetHazeUIPage(w)
        local rG = _G.HazePageScrolls
        if rG and rG[w] then
            _G.HazeActiveScroll = rG[w]
            eG(w)
        else
            _G.HazeActiveScroll = I
        end
    end
    local function w(rG, fG, JG)
        local aG = Instance.new("Frame")
        aG.Size = UDim2.new(0, 44, 0, 24)
        aG.Position = UDim2.new(1, -56.0, 0.5, -12.0)
        aG.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        aG.BackgroundTransparency = 0
        aG.BorderSizePixel = 0
        aG.Parent = rG
        b(aG, 7)
        
        local xG = Instance.new("Frame")
        xG.Size = fG and UDim2.new(1, 0, 1, 0) or UDim2.new(0, 0, 1, 0)
        xG.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
        xG.BackgroundTransparency = 0
        xG.BorderSizePixel = 0
        xG.Parent = aG
        b(xG, 7)
        
        local QG = Instance.new("Frame")
        QG.Size = UDim2.fromOffset(20, 20)
        QG.Position = fG and UDim2.new(1, -22.0, 0.5, -10.0) or UDim2.new(0, 2, 0.5, -10.0)
        QG.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        QG.BorderSizePixel = 0
        QG.Parent = aG
        b(QG, 7)
        
        local VG = Instance.new("TextButton")
        VG.Size = UDim2.new(1, 0, 1, 0)
        VG.BackgroundTransparency = 1
        VG.Text = ""
        VG.AutoButtonColor = false
        VG.Parent = aG
        
        local OG = fG == true
        local function UG(yG)
            OG = yG == true
            if OG then
                xG.Size = UDim2.new(1, 0, 1, 0)
                QG.Position = UDim2.new(1, -22.0, 0.5, -10.0)
            else
                xG.Size = UDim2.new(0, 0, 1, 0)
                QG.Position = UDim2.new(0, 2, 0.5, -10.0)
            end
        end
        if OG then
            xG.Size = UDim2.new(1, 0, 1, 0)
            QG.Position = UDim2.new(1, -22.0, 0.5, -10.0)
        else
            UG(false)
        end
        VG.MouseButton1Click:Connect(function()
            JG(not OG, function(JG)
                g(
                    xG,
                    0.22,
                    {
                        Size = JG and UDim2.new(1, 0, 1, 0) or UDim2.new(0, 0, 1, 0)
                    }
                )
                g(
                    QG,
                    0.22,
                    { Position = JG and UDim2.new(1, -22.0, 0.5, -10.0) or UDim2.new(0, 2, 0.5, -10.0) }
                )
                if JG then
                    OG = true
                else
                    OG = false
                end
            end)
        end)
        return { Set = UG, Container = aG }
    end
    local function g(rG, nG)
        local fG = Instance.new("Frame")
        fG.Size = UDim2.new(1, 0, 0, nG or N(46))
        fG.BackgroundColor3 = q.SURF2
        fG.BackgroundTransparency = 0.12
        fG.BorderSizePixel = 0
        fG.Parent = (_G.HazeActiveScroll or I)
        b(fG, N(10))
        H(fG, 1, q.STROKE, 0.55)
        CG(fG, N(12), N(8), N(12), N(8))
        local H = Instance.new("TextLabel")
        H.BackgroundTransparency = 1
        H.Size = UDim2.new(1, -(N(67) + N(14)), 1, 0)
        H.Text = rG
        H.Font = Enum.Font.FredokaOne
        H.TextSize = N(14)
        H.TextColor3 = q.TEXT
        H.TextXAlignment = Enum.TextXAlignment.Left
        H.Parent = fG
        return fG
    end
    local function H(CG)
        local rG = Instance.new("Frame")
        rG.Size = UDim2.new(1, 0, 0, N(30))
        rG.BackgroundTransparency = 1
        rG.Parent = (_G.HazeActiveScroll or I)
        local nG = Instance.new("Frame")
        nG.Size = UDim2.new(0, N(4), 0, N(18))
        nG.Position = UDim2.new(0, N(4), 0.5, -N(9))
        nG.BackgroundColor3 = q.ACCENT
        nG.BorderSizePixel = 0
        nG.Parent = rG
        b(nG, 3)
        dG(nG)
        local b = Instance.new("TextLabel")
        b.Size = UDim2.new(1, -N(24), 1, 0)
        b.Position = UDim2.new(0, N(16), 0, 0)
        b.BackgroundTransparency = 1
        b.Text = CG
        b.TextColor3 = q.TEXT
        b.TextSize = N(12)
        b.Font = Enum.Font.FredokaOne
        b.TextXAlignment = Enum.TextXAlignment.Left
        b.Parent = rG
        local b = Instance.new("Frame")
        b.Size = UDim2.new(1, -N(90), 0, 1)
        b.Position = UDim2.new(0, N(82), 0.5, 0)
        b.BackgroundColor3 = q.ACCENT
        b.BackgroundTransparency = 0.7
        b.BorderSizePixel = 0
        b.Parent = rG
        return rG
    end
    local function b(CG)
        local rG = Instance.new("Frame")
        rG.Size = UDim2.new(1, 0, 0, N(22))
        rG.BackgroundTransparency = 1
        rG.Parent = (_G.HazeActiveScroll or I)
        local dG = Instance.new("TextLabel")
        dG.Size = UDim2.new(1, -N(8), 1, 0)
        dG.Position = UDim2.new(0, N(2), 0, 0)
        dG.BackgroundTransparency = 1
        dG.Text = CG
        dG.TextColor3 = q.ACCENT
        dG.TextSize = N(12)
        dG.Font = Enum.Font.FredokaOne
        dG.TextXAlignment = Enum.TextXAlignment.Left
        dG.Parent = rG
        return rG
    end
    local function q(CG)
        local rG = Instance.new("Frame")
        rG.Size = UDim2.new(1, 0, 0, N(18))
        rG.BackgroundTransparency = 1
        rG.Parent = (_G.HazeActiveScroll or I)
        local dG = Instance.new("TextLabel")
        dG.Size = UDim2.new(1, -N(14), 1, 0)
        dG.Position = UDim2.new(0, N(10), 0, 0)
        dG.BackgroundTransparency = 1
        dG.Text = CG
        dG.TextColor3 = W.TextSecondary
        dG.TextSize = N(11)
        dG.Font = Enum.Font.FredokaOne
        dG.TextXAlignment = Enum.TextXAlignment.Left
        dG.Parent = rG
        return rG
    end
    do
        _G.SetHazeUIPage("Main")
        _G.HazeLastAutoTpLoadRow = g("Auto TP on Script Load")
        w(_G.HazeLastAutoTpLoadRow, i.TpSettings.TpOnLoad, function(N, CG)
            CG(N)
            i.TpSettings.TpOnLoad = N
            U()
            F("AUTO TP ON LOAD", N and "ENABLED" or "DISABLED")
        end)
        local N = g("Min Gen for Auto TP")
        local CG = Instance.new("TextBox", N)
        CG.Size = UDim2.new(0, 100, 0, 24)
        CG.Position = UDim2.new(1, -110.0, 0.5, -12.0)
        CG.BackgroundColor3 = W.SurfaceHighlight
        CG.Text = tostring(i.TpSettings.MinGenForTp or "")
        CG.Font = Enum.Font.FredokaOne
        CG.TextSize = 11
        CG.TextColor3 = W.TextPrimary
        CG.PlaceholderText = "e.g. 5k, 1m, 1b"
        Instance.new("UICorner", CG).CornerRadius = UDim.new(0, 4)
        CG.FocusLost:Connect(function()
            local N = CG.Text:gsub("%s", "")
            i.TpSettings.MinGenForTp = (N == "" and "" or N)
            U()
            F(
                "MIN GEN FOR TP",
                i.TpSettings.MinGenForTp == "" and "No minimum" or "Min: " .. (i.TpSettings.MinGenForTp or "")
            )
        end)
        _G.SetHazeUIPage("Misc")
        local function applyUnwalkState(state)
            local char = C.Character
            if not char then return end
            local animate = char:FindFirstChild("Animate")
            if animate and animate:IsA("LocalScript") then
                animate.Disabled = state
            end
            if state then
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    for _, track in ipairs(humanoid:GetPlayingAnimationTracks()) do
                        track:Stop()
                    end
                end
            end
        end
        C.CharacterAdded:Connect(function(char)
            if i.UnwalkEnabled then
                task.defer(function()
                    task.wait(0.5)
                    applyUnwalkState(true)
                end)
            end
        end)
        local uw_toggle = g("Unwalk")
        w(uw_toggle, i.UnwalkEnabled, function(val, setFn)
            setFn(val)
            i.UnwalkEnabled = val
            U()
            F("UNWALK", val and "ENABLED" or "DISABLED")
            applyUnwalkState(val)
        end)
        local N = g("FPS Boost")
        w(N, i.FPSBoostV2, function(N, CG)
            CG(N)
            E(N)
            F("FPS BOOST", N and "ENABLED" or "DISABLED")
        end)
        local E = g("Xray Base")
        w(E, i.XrayBase, function(E, N)
            N(E)
            i.XrayBase = E
            U()
            if E then
                if _G._xrayEnable then
                    _G._xrayEnable()
                end
            else
                if _G._xrayDisable then
                    _G._xrayDisable()
                end
            end
            F("XRAY BASE", E and "ENABLED" or "DISABLED")
        end)
        local E = g("Tracer Best Brainrot")
        w(E, i.TracerEnabled, function(E, N)
            N(E)
            i.TracerEnabled = E
            U()
            F("TRACER", E and "ENABLED" or "DISABLED")
        end)
        local E = g("Line to base")
        w(E, i.LineToBase, function(E, N)
            N(E)
            i.LineToBase = E
            U()
            if not E and _G.resetPlotBeam then
                pcall(_G.resetPlotBeam)
            end
            F("LINE TO BASE", E and "ENABLED" or "DISABLED")
        end)
    end
    autoTPPriorityToggleRef = autoTPPriorityToggleRef or { setFn = nil }
    do
        _G.SetHazeUIPage("Main")
        b("Teleport")
        local E = g("Teleport V2")
        w(E, i.TpSettings.TeleportV2 == true, function(E, N)
            N(E)
            i.TpSettings.TeleportV2 = E
            U()
            F("TELEPORT V2", E and "ENABLED" or "DISABLED")
        end)
        local E = { "Flying Carpet", "Cupid's Wings", "Santa's Sleigh", "Witch's Broom" }
        local N = {}
        for CG, CG in ipairs(E) do
            local E = g(CG)
            local rG = w(E, i.TpSettings.Tool == CG, function(E, dG)
                if E then
                    i.TpSettings.Tool = CG
                    U()
                    dG(true)
                    for E, nG in pairs(N) do
                        if E ~= CG then
                            nG.Set(false)
                        end
                    end
                    F("TP TOOL", CG)
                else
                    dG(i.TpSettings.Tool == CG)
                end
            end)
            N[CG] = rG
        end
        local E = g("Auto Snipe Delay")
        local N = Instance.new("Frame", E)
        N.Size = UDim2.new(0, 100, 0, 24)
        N.Position = UDim2.new(1, -110.0, 0.5, -12.0)
        N.BackgroundTransparency = 1
        local E = {}
        local CG = math.clamp(tonumber(i.TpSettings.Speed) or 1, 1, 4)
        i.TpSettings.Speed = CG
        for rG = 1, 4 do
            local dG = Instance.new("TextButton", N)
            dG.Size = UDim2.new(0.22, 0, 1, 0)
            dG.Position = UDim2.new((rG - 1) * 0.26, 0, 0, 0)
            local N = CG == rG
            dG.BackgroundColor3 = N and W.Accent1 or W.SurfaceHighlight
            dG.Text = tostring(rG)
            dG.TextColor3 = N and Color3.new(0, 0, 0) or W.TextPrimary
            dG.Font = Enum.Font.FredokaOne
            dG.TextSize = 12
            Instance.new("UICorner", dG).CornerRadius = UDim.new(0, 4)
            dG.MouseButton1Click:Connect(function()
                i.TpSettings.Speed = rG
                U()
                for N, CG in ipairs(E) do
                    local nG = N == rG
                    CG.BackgroundColor3 = nG and W.Accent1 or W.SurfaceHighlight
                    CG.TextColor3 = nG and Color3.new(0, 0, 0) or W.TextPrimary
                end
                F("AUTO SNIPE DELAY", string.format("%.2fs", O(rG)))
            end)
            table.insert(E, dG)
        end
        _G.SetHazeUIPage("Keybinds")
        b("Teleport")
        local O = g("TP Keybind")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = PrettyKeyName(i.TpSettings.TpKey)
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(N)
                if N.UserInputType == Enum.UserInputType.Keyboard then
                    i.TpSettings.TpKey = NormalizeKeyName(N.KeyCode.Name, i.TpSettings.TpKey or "T")
                    i.SavedKeybinds.TpKey = i.TpSettings.TpKey
                    E.Text = PrettyKeyName(i.TpSettings.TpKey)
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("TP KEYBIND", PrettyKeyName(i.TpSettings.TpKey))
                end
            end)
        end)
        local O = g("Auto Clone Keybind")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = PrettyKeyName(i.TpSettings.CloneKey)
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(N)
                if N.UserInputType == Enum.UserInputType.Keyboard then
                    i.TpSettings.CloneKey = NormalizeKeyName(N.KeyCode.Name, i.TpSettings.CloneKey or "V")
                    i.SavedKeybinds.CloneKey = i.TpSettings.CloneKey
                    E.Text = PrettyKeyName(i.TpSettings.CloneKey)
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("CLONE KEYBIND", PrettyKeyName(i.TpSettings.CloneKey))
                end
            end)
        end)
    end
    do
        _G.SetHazeUIPage("Keybinds")
        b("Carpet Speed")
        local O = g("Carpet Speed Keybind")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = PrettyKeyName(i.TpSettings.CarpetSpeedKey)
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(N)
                if N.UserInputType == Enum.UserInputType.Keyboard then
                    i.TpSettings.CarpetSpeedKey =
                        NormalizeKeyName(N.KeyCode.Name, i.TpSettings.CarpetSpeedKey or "Q")
                    i.SavedKeybinds.CarpetSpeedKey = i.TpSettings.CarpetSpeedKey
                    E.Text = PrettyKeyName(i.TpSettings.CarpetSpeedKey)
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("CARPET SPEED KEYBIND", PrettyKeyName(i.TpSettings.CarpetSpeedKey))
                end
            end)
        end)
        local O = g("Drop Brainrot Keybind")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = i.RagdollSelfKey ~= "" and PrettyKeyName(i.RagdollSelfKey) or "NONE"
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(N)
                if N.UserInputType == Enum.UserInputType.Keyboard then
                    i.RagdollSelfKey = NormalizeKeyName(N.KeyCode.Name, "")
                    i.SavedKeybinds.RagdollSelfKey = i.RagdollSelfKey
                    E.Text = i.RagdollSelfKey ~= "" and PrettyKeyName(i.RagdollSelfKey) or "NONE"
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F(
                        "DROP BRAINROT KEYBIND",
                        i.RagdollSelfKey ~= "" and PrettyKeyName(i.RagdollSelfKey) or "NONE"
                    )
                end
            end)
        end)
        local O = g("Carpet Speed Status")
        local E = Instance.new("TextLabel", O)
        E.Size = UDim2.new(0, 50, 0, 20)
        E.Position = UDim2.new(1, -60.0, 0.5, -10.0)
        E.BackgroundTransparency = 1
        E.Text = Z and "ON" or "OFF"
        E.TextColor3 = Z and W.Success or W.Error
        E.Font = Enum.Font.FredokaOne
        E.TextSize = 13
        E.TextXAlignment = Enum.TextXAlignment.Right
        u = E
    end
    do
        _G.SetHazeUIPage("Player")
        b("Movement")
        local O = g("Infinite Jump")
        w(O, h, function(O, E)
            E(O)
            PG(O)
            F("INFINITE JUMP", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Auto Steal Speed")
        w(O, i.AutoStealSpeed, function(O, E)
            E(O)
            i.AutoStealSpeed = O
            U()
            F("AUTO STEAL SPEED", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Float")
        local E = w(O, m.floatActive, function(O, m)
            m(O)
            s(O, true)
            F("FLOAT", O and "ENABLED" or "DISABLED")
        end)
        L.setFn = function(O)
            if E and E.Set then
                E.Set(O, true, true)
            end
        end
        local O = g("Auto reset on balloon")
        w(O, i.AutoResetOnBalloon, function(O, E)
            E(O)
            i.AutoResetOnBalloon = O
            U()
            F("AUTO RESET ON BALLOON", O and "ENABLED" or "DISABLED")
        end)
        _G.SetHazeUIPage("Keybinds")
        b("Movement")
        local O = g("Steal Speed Keybind")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = PrettyKeyName(i.StealSpeedKey)
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(m)
                if m.UserInputType == Enum.UserInputType.Keyboard then
                    i.StealSpeedKey = NormalizeKeyName(m.KeyCode.Name, i.StealSpeedKey or "C")
                    i.SavedKeybinds.StealSpeedKey = i.StealSpeedKey
                    E.Text = PrettyKeyName(i.StealSpeedKey)
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("STEAL SPEED KEYBIND", PrettyKeyName(i.StealSpeedKey))
                end
            end)
        end)
        local O = g("Float Keybind")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = PrettyKeyName(i.FloatKeybind or "Z")
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(m)
                if m.UserInputType == Enum.UserInputType.Keyboard then
                    i.FloatKeybind = NormalizeKeyName(m.KeyCode.Name, i.FloatKeybind or "Z")
                    i.SavedKeybinds.FloatKeybind = i.FloatKeybind
                    E.Text = PrettyKeyName(i.FloatKeybind)
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("FLOAT KEYBIND", PrettyKeyName(i.FloatKeybind))
                end
            end)
        end)
    end
    do
        _G.SetHazeUIPage("Main")
        b("AUTO UNLOCK")
        local O = g("Auto Unlock on Steal")
        w(O, i.AutoUnlockOnSteal, function(O, E)
            E(O)
            i.AutoUnlockOnSteal = O
            U()
            F("AUTO UNLOCK", O and "ENABLED" or "DISABLED")
        end)
        _G.SetHazeUIPage("Misc")
        local O = g("Show Unlock Buttons HUD")
        w(O, i.ShowUnlockButtonsHUD, function(O, E)
            E(O)
            i.ShowUnlockButtonsHUD = O
            U()
            toggleUnlockBaseMenu(O)
            F("UNLOCK BUTTONS", O and "VISIBLE" or "HIDDEN")
        end)
        local O = g("Show Mobile Buttons")
        w(O, i.ShowMobileActionButtons ~= false, function(O, E)
            E(O)
            i.ShowMobileActionButtons = O
            U()
            if P.RefreshMobileActionButtons then
                P.RefreshMobileActionButtons()
            end
            F("MOBILE BUTTONS", O and "VISIBLE" or "HIDDEN")
        end)
    end
    do
        _G.SetHazeUIPage("Player")
        b("ANTI-RAGDOLL")
        local O, E = {}, {}
        local m = g("V1")
        w(m, i.AntiRagdoll > 0, function(m, Z)
            O.fn = Z
            if m and i.AntiRagdollV2 then
                Z(false)
                F("ANTI-RAGDOLL", "DISABLE V2 FIRST")
                return
            end
            Z(m)
            local Z = m and 1 or 0
            i.AntiRagdoll = Z
            if m then
                i.AntiRagdollV2 = false
                if E.fn then
                    E.fn(false)
                end
            end
            U()
            RG(Z)
            if m then
                A(false)
            end
            F("ANTI-RAGDOLL V1", m and "ENABLED" or "DISABLED")
        end)
        local m = g("V2")
        w(m, i.AntiRagdollV2, function(m, Z)
            E.fn = Z
            if m and i.AntiRagdoll > 0 then
                Z(false)
                F("ANTI-RAGDOLL", "DISABLE V1 FIRST")
                return
            end
            Z(m)
            i.AntiRagdollV2 = m
            if m then
                i.AntiRagdoll = 0
                U()
                if O.fn then
                    O.fn(false)
                end
                RG(0)
                A(true)
            else
                U()
                A(false)
            end
            F("ANTI-RAGDOLL V2", m and "ENABLED" or "DISABLED")
        end)
    end
    do
        _G.SetHazeUIPage("Player")
        b("ESP")
        _G.SetHazeUIPage("Settings")
        _G.SetHazeUIPage("Player")
        espToggleRef = espToggleRef or { enabled = true, setFn = nil }
        local O = g("Brainrot ESP")
        local E = w(O, i.BrainrotESP, function(O, E)
            E(O)
            i.BrainrotESP = O
            U()
            if espToggleRef.setFn then
                espToggleRef.setFn(O)
            end
            F("BRAINROT ESP", O and "ENABLED" or "DISABLED")
        end)
        subspaceMineESPToggleRef = subspaceMineESPToggleRef or { setFn = nil }
        local O = g("Subspace Mine Esp")
        w(O, i.SubspaceMineESP, function(O, E)
            E(O)
            i.SubspaceMineESP = O
            U()
            if subspaceMineESPToggleRef.setFn then
                subspaceMineESPToggleRef.setFn(O)
            end
            F("SUBSPACE MINE ESP", O and "ENABLED" or "DISABLED")
        end)
    end
    do
        _G.SetHazeUIPage("Main")
        b("AUTO STEAL DEFAULTS")
        local O = {}
        local E = {}
        local m = {}
        autoTPPriorityToggleRef = autoTPPriorityToggleRef or { setFn = nil }
        local A = g("Default To Nearest")
        local Z = w(A, i.DefaultToNearest, function(A, u)
            if A then
                i.DefaultToNearest = true
                i.DefaultToHighest = false
                i.DefaultToPriority = false
                u(true)
                if E.setFn then
                    E.setFn(false)
                end
                if m.setFn then
                    m.setFn(false)
                end
                i.AutoTPPriority = true
                if autoTPPriorityToggleRef and autoTPPriorityToggleRef.setFn then
                    autoTPPriorityToggleRef.setFn(true)
                end
            else
                local h = i.DefaultToHighest or i.DefaultToPriority
                if not h then
                    u(true)
                    F("DEFAULT MODE", "At least one default must be enabled")
                    return
                end
                i.DefaultToNearest = false
                u(false)
            end
            U()
            F("DEFAULT TO NEAREST", A and "ENABLED" or "DISABLED")
        end)
        O.setFn = Z.Set
        local A = g("Default To Highest")
        local Z = w(A, i.DefaultToHighest, function(A, u)
            if A then
                i.DefaultToNearest = false
                i.DefaultToHighest = true
                i.DefaultToPriority = false
                u(true)
                if O.setFn then
                    O.setFn(false)
                end
                if m.setFn then
                    m.setFn(false)
                end
                i.AutoTPPriority = false
                if autoTPPriorityToggleRef and autoTPPriorityToggleRef.setFn then
                    autoTPPriorityToggleRef.setFn(false)
                end
            else
                local h = i.DefaultToNearest or i.DefaultToPriority
                if not h then
                    u(true)
                    F("DEFAULT MODE", "At least one default must be enabled")
                    return
                end
                i.DefaultToHighest = false
                u(false)
            end
            U()
            F("DEFAULT TO HIGHEST", A and "ENABLED" or "DISABLED")
        end)
        E.setFn = Z.Set
        local A = g("Default To Priority")
        local Z = w(A, i.DefaultToPriority, function(A, u)
            if A then
                i.DefaultToNearest = false
                i.DefaultToHighest = false
                i.DefaultToPriority = true
                u(true)
                if O.setFn then
                    O.setFn(false)
                end
                if E.setFn then
                    E.setFn(false)
                end
                i.AutoTPPriority = true
                if autoTPPriorityToggleRef and autoTPPriorityToggleRef.setFn then
                    autoTPPriorityToggleRef.setFn(true)
                end
            else
                local O = i.DefaultToNearest or i.DefaultToHighest
                if not O then
                    u(true)
                    F("DEFAULT MODE", "At least one default must be enabled")
                    return
                end
                i.DefaultToPriority = false
                u(false)
            end
            U()
            F("DEFAULT TO PRIORITY", A and "ENABLED" or "DISABLED")
        end)
        m.setFn = Z.Set
    end
    do
        _G.SetHazeUIPage("Main")
        b("AUTOMATION")
        local O = g("Auto Invis During Steal")
        local E = w(O, i.AutoInvisDuringSteal, function(O, m)
            m(O)
            i.AutoInvisDuringSteal = O
            _G.AutoInvisDuringSteal = O
            if _G.syncMiniInvisAutoStealToggle then
                pcall(_G.syncMiniInvisAutoStealToggle, O)
            end
            U()
            F("AUTO INVIS", O and "ENABLED" or "DISABLED")
        end)
        _G.syncMainAutoInvisToggle = E.Set
        _G.SetHazeUIPage("Main")
        local O = g("Auto TP on Failed Steal")
        w(O, i.AutoTpOnFailedSteal, function(O, E)
            E(O)
            i.AutoTpOnFailedSteal = O
            U()
            F("AUTO TP ON FAILED STEAL", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Auto TP Priority Mode")
        local E = w(O, i.AutoTPPriority, function(O, m)
            m(O)
            i.AutoTPPriority = O
            U()
            F("AUTO TP PRIORITY", O and "PRIORITY" or "HIGHEST")
        end)
        autoTPPriorityToggleRef.setFn = E.Set
        local O = g("Auto-Kick on Steal")
        w(O, i.AutoKickOnSteal, function(O, E)
            E(O)
            i.AutoKickOnSteal = O
            U()
            F("AUTO-KICK ON STEAL", O and "ENABLED" or "DISABLED")
        end)
    end
    do
        _G.SetHazeUIPage("Misc")
        b("Hide GUIs")
        local O = g("Hide Admin Panel GUI")
        w(O, i.HideAdminPanel, function(O, E)
            E(O)
            i.HideSafePanel = O
            U()
            local E = x:FindFirstChild("XiSafePanel")
            if E then
                E.Enabled = not O
            end
            F("HIDE ADMIN PANEL", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Hide Auto Steal GUI")
        w(O, i.HideAutoSteal, function(O, E)
            E(O)
            i.HideAutoSteal = O
            U()
            local E = x:FindFirstChild("cJPcVLFpwxgI")
            if E then
                E.Enabled = not O
            end
            F("HIDE AUTO STEAL", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Compact Auto Steal GUI")
        w(O, i.CompactAutoSteal, function(O, E)
            E(O)
            i.CompactAutoSteal = O
            U()
            local E = x:FindFirstChild("cJPcVLFpwxgI")
            if E and E:FindFirstChild("Frame") then
                local m = E.Frame
                local E = d and 0.6 or 1
                local A = O and 355 or 630 * E
                local E = f:Create(
                    m,
                    TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut),
                    { Size = UDim2.new(m.Size.X.Scale, m.Size.X.Offset, 0, A) }
                )
                E:Play()
                E.Completed:Connect(function()
                    P.ListNeedsRedraw = true
                    if P.UpdateAutoStealUI then
                        P.UpdateAutoStealUI()
                    end
                end)
            end
            F("COMPACT AUTO STEAL", O and "ENABLED" or "DISABLED")
        end)
    end
    do
        _G.SetHazeUIPage("Settings")
        b("EXTRAS")
        _G.SetHazeUIPage("Keybinds")
        local O = g("Reset")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = PrettyKeyName(i.ResetKey)
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(m)
                if m.UserInputType == Enum.UserInputType.Keyboard then
                    i.ResetKey = NormalizeKeyName(m.KeyCode.Name, i.ResetKey or "X")
                    i.SavedKeybinds.ResetKey = i.ResetKey
                    E.Text = PrettyKeyName(i.ResetKey)
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("RESET KEYBIND", PrettyKeyName(i.ResetKey))
                end
            end)
        end)
        _G.SetHazeUIPage("Keybinds")
        b("Extras")
        local O = g("Kick")
        local E = Instance.new("TextButton", O)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = i.KickKey ~= "" and PrettyKeyName(i.KickKey) or "NONE"
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            E.Text = "..."
            E.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(m)
                if m.UserInputType == Enum.UserInputType.Keyboard then
                    i.KickKey = NormalizeKeyName(m.KeyCode.Name, "")
                    i.SavedKeybinds.KickKey = i.KickKey
                    E.Text = i.KickKey ~= "" and PrettyKeyName(i.KickKey) or "NONE"
                    E.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("KICK KEYBIND", i.KickKey ~= "" and PrettyKeyName(i.KickKey) or "NONE")
                end
            end)
        end)
        _G.SetHazeUIPage("Misc")
        local O = g("Clean Error GUIs")
        w(O, i.CleanErrorGUIs, function(O, E)
            E(O)
            i.CleanErrorGUIs = O
            U()
            F("CLEAN ERROR GUIS", O and "ENABLED" or "DISABLED")
        end)
    end
    do
        _G.SetHazeUIPage("Misc")
        b("Admin Panel")
        local O = g("Click To Admin Panel")
        w(O, i.ClickToAP, function(O, E)
            E(O)
            i.ClickToAP = O
            U()
            F("CLICK TO AP", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Click To AP Single Command")
        w(O, i.ClickToAPSingleCommand, function(O, E)
            E(O)
            i.ClickToAPSingleCommand = O
            U()
            F("CLICK TO AP SINGLE", O and "ENABLED" or "DISABLED")
        end)
        q("Moby")
        local O = g("Disable Click To AP On Moby")
        w(O, i.DisableClickToAPOnMoby, function(O, E)
            E(O)
            i.DisableClickToAPOnMoby = O
            U()
            F("DISABLE CLICK TO AP ON MOBY", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Disable Proximity AP On Moby")
        w(O, i.DisableProximitySpamOnMoby, function(O, E)
            E(O)
            i.DisableProximitySpamOnMoby = O
            U()
            F("DISABLE PROXIMITY AP ON MOBY", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Cancel AP Panel on Moby")
        w(O, i.CancelAPPanelOnMoby, function(O, E)
            E(O)
            i.CancelAPPanelOnMoby = O
            U()
            F("CANCEL AP PANEL ON MOBY", O and "ENABLED" or "DISABLED")
        end)
        q("Kawaifu")
        local O = g("Disable Click To AP On Kawaifu")
        w(O, i.DisableClickToAPOnKawaifu, function(O, q)
            q(O)
            i.DisableClickToAPOnKawaifu = O
            U()
            F("DISABLE CLICK TO AP ON KAWAIFU", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Disable Proximity AP On Kawaifu")
        w(O, i.DisableProximitySpamOnKawaifu, function(O, q)
            q(O)
            i.DisableProximitySpamOnKawaifu = O
            U()
            F("DISABLE PROXIMITY AP ON KAWAIFU", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Cancel AP Panel on Kawaifu")
        w(O, i.CancelAPPanelOnKawaifu, function(O, q)
            q(O)
            i.CancelAPPanelOnKawaifu = O
            U()
            F("CANCEL AP PANEL ON KAWAIFU", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Speed Booster")
        w(O, false, function(state, setToggle)
            setToggle(state)
            if _G.ToggleSpeedBoosterGUI then
                _G.ToggleSpeedBoosterGUI(state)
            end
        end)
        _G.SetHazeUIPage("Keybinds")
        b("Admin Panel")
        local O = g("Click To AP Keybind")
        local q = Instance.new("TextButton", O)
        q.Size = UDim2.new(0, 60, 0, 24)
        q.Position = UDim2.new(1, -65.0, 0.5, -12.0)
        q.BackgroundColor3 = W.SurfaceHighlight
        q.Text = PrettyKeyName(i.ClickToAPKeybind or "L")
        q.Font = Enum.Font.FredokaOne
        q.TextColor3 = W.TextPrimary
        q.TextSize = 12
        Instance.new("UICorner", q).CornerRadius = UDim.new(0, 4)
        q.MouseButton1Click:Connect(function()
            q.Text = "..."
            q.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(E)
                if E.UserInputType == Enum.UserInputType.Keyboard then
                    i.ClickToAPKeybind = NormalizeKeyName(E.KeyCode.Name, i.ClickToAPKeybind or "L")
                    i.SavedKeybinds.ClickToAPKeybind = i.ClickToAPKeybind
                    q.Text = PrettyKeyName(i.ClickToAPKeybind)
                    q.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("CLICK TO AP KEYBIND", PrettyKeyName(i.ClickToAPKeybind))
                end
            end)
        end)
        local O = g("Proximity AP Keybind")
        local q = Instance.new("TextButton", O)
        q.Size = UDim2.new(0, 60, 0, 24)
        q.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        q.BackgroundColor3 = W.SurfaceHighlight
        q.Text = PrettyKeyName(i.ProximityAPKeybind or "P")
        q.Font = Enum.Font.FredokaOne
        q.TextColor3 = W.TextPrimary
        q.TextSize = 12
        Instance.new("UICorner", q).CornerRadius = UDim.new(0, 4)
        q.MouseButton1Click:Connect(function()
            q.Text = "..."
            q.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(E)
                if E.UserInputType == Enum.UserInputType.Keyboard then
                    i.ProximityAPKeybind = NormalizeKeyName(E.KeyCode.Name, i.ProximityAPKeybind or "P")
                    i.SavedKeybinds.ProximityAPKeybind = i.ProximityAPKeybind
                    q.Text = PrettyKeyName(i.ProximityAPKeybind)
                    q.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("PROXIMITY AP KEYBIND", PrettyKeyName(i.ProximityAPKeybind))
                end
            end)
        end)
    end
    do
        _G.SetHazeUIPage("Settings")
        b("ALERTS")
        local O = g("Enable Alerts")
        w(O, i.AlertsEnabled, function(O, q)
            q(O)
            i.AlertsEnabled = O
            U()
            F("PRIORITY ALERTS", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Alert Sound ID")
        local q = Instance.new("TextBox", O)
        q.Size = UDim2.new(0, 180, 0, 24)
        q.Position = UDim2.new(1, -185.0, 0.5, -12.0)
        q.BackgroundColor3 = W.SurfaceHighlight
        q.Text = i.AlertSoundID or "rbxassetid://6518811702"
        q.Font = Enum.Font.FredokaOne
        q.TextSize = 10
        q.TextColor3 = W.TextPrimary
        q.PlaceholderText = "Sound ID"
        Instance.new("UICorner", q).CornerRadius = UDim.new(0, 4)
        q.FocusLost:Connect(function()
            i.AlertSoundID = q.Text
            U()
            F("ALERT SOUND", "Updated")
        end)
    end
    do
        _G.SetHazeUIPage("Settings")
        b("JOB JOINER")
        local O = g("Job ID Joiner")
        w(O, i.ShowJobJoiner, function(O, q)
            q(O)
            i.ShowJobJoiner = O
            U()
            local q = gethui and gethui() or game:GetService("CoreGui")
            local E = q:FindFirstChild("XiJobJoiner")
            if E then
                E.Enabled = i.ShowJobJoiner
            end
            F("JOB ID JOINER", O and "ENABLED" or "DISABLED")
        end)
        _G.SetHazeUIPage("Keybinds")
        b("Job Joiner")
        local O = g("Job Joiner Keybind")
        local q = Instance.new("TextButton", O)
        q.Size = UDim2.new(0, 60, 0, 24)
        q.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        q.BackgroundColor3 = W.SurfaceHighlight
        q.Text = PrettyKeyName(i.JobJoinerKey or "J")
        q.Font = Enum.Font.FredokaOne
        q.TextColor3 = W.TextPrimary
        q.TextSize = 12
        Instance.new("UICorner", q).CornerRadius = UDim.new(0, 4)
        q.MouseButton1Click:Connect(function()
            q.Text = "..."
            q.TextColor3 = W.Accent1
            local O
            O = j.InputBegan:Connect(function(E)
                if E.UserInputType == Enum.UserInputType.Keyboard then
                    i.JobJoinerKey = NormalizeKeyName(E.KeyCode.Name, i.JobJoinerKey or "J")
                    i.SavedKeybinds.JobJoinerKey = i.JobJoinerKey
                    q.Text = PrettyKeyName(i.JobJoinerKey)
                    q.TextColor3 = W.TextPrimary
                    U()
                    O:Disconnect()
                    F("JOB JOINER KEYBIND", PrettyKeyName(i.JobJoinerKey))
                end
            end)
        end)
    end
    do
        _G.SetHazeUIPage("Player")
        b("Protection")
        local O = g("Anti-Bee & Anti-Disco")
        w(O, i.AntiBeeDisco, function(O, q)
            q(O)
            i.AntiBeeDisco = O
            U()
            if O then
                if _G.ANTI_BEE_DISCO and _G.ANTI_BEE_DISCO.Enable then
                    _G.ANTI_BEE_DISCO.Enable()
                end
            else
                if _G.ANTI_BEE_DISCO and _G.ANTI_BEE_DISCO.Disable then
                    _G.ANTI_BEE_DISCO.Disable()
                end
            end
            F("ANTI-BEE & DISCO", O and "ENABLED" or "DISABLED")
        end)
        local O = g("Auto-Destroy Turrets")
        w(O, i.AutoDestroyTurrets, function(O, q)
            q(O)
            i.AutoDestroyTurrets = O
            U()
            F("AUTO-DESTROY TURRETS", O and "ENABLED" or "DISABLED")
        end)
    end
    do
        _G.SetHazeUIPage("Settings")
        b("CAMERA")
        local O = g("FOV")
        local q = Instance.new("Frame", O)
        q.Size = UDim2.new(0, 140, 0, 5)
        q.Position = UDim2.new(1, -200.0, 0.5, -2.5)
        q.BackgroundColor3 = Color3.fromRGB(30, 32, 38)
        Instance.new("UICorner", q).CornerRadius = UDim.new(1, 0)
        local E = Instance.new("Frame", q)
        E.BackgroundColor3 = W.Accent1
        E.Size = UDim2.new(0, 0, 1, 0)
        Instance.new("UICorner", E).CornerRadius = UDim.new(1, 0)
        local m = Instance.new("Frame", q)
        m.Size = UDim2.new(0, 12, 0, 12)
        m.BackgroundColor3 = W.TextPrimary
        m.AnchorPoint = Vector2.new(0.5, 0.5)
        m.Position = UDim2.new(0, 0, 0.5, 0)
        Instance.new("UICorner", m).CornerRadius = UDim.new(1, 0)
        local A = Instance.new("UIStroke", m)
        A.Color = W.Accent1
        A.Thickness = 1.5
        A.Transparency = 0.2
        local A = Instance.new("TextLabel", O)
        A.Size = UDim2.new(0, 40, 0, 20)
        A.Position = UDim2.new(1, -50.0, 0.5, -10.0)
        A.BackgroundTransparency = 1
        A.Text = string.format("%.1f", i.FOV)
        A.TextColor3 = W.TextPrimary
        A.Font = Enum.Font.FredokaOne
        A.TextSize = 13
        local function O(Z)
            Z = math.clamp(Z, 30, 180)
            i.FOV = Z
            U()
            A.Text = string.format("%.1f", Z)
            local A = (Z - 30) / 150
            E.Size = UDim2.new(A, 0, 1, 0)
            m.Position = UDim2.new(A, 0, 0.5, 0)
            if a.CurrentCamera then
                a.CurrentCamera.FieldOfView = Z
            end
            F("FIELD OF VIEW", string.format("%.1f", Z))
        end
        O(i.FOV)
        local E = false
        q.InputBegan:Connect(function(m)
            if
                m.UserInputType == Enum.UserInputType.MouseButton1
                or m.UserInputType == Enum.UserInputType.Touch
            then
                E = true
            end
        end)
        j.InputEnded:Connect(function(m)
            if
                m.UserInputType == Enum.UserInputType.MouseButton1
                or m.UserInputType == Enum.UserInputType.Touch
            then
                E = false
            end
        end)
        j.InputChanged:Connect(function(m)
            if
                E
                and (
                    m.UserInputType == Enum.UserInputType.MouseMovement
                    or m.UserInputType == Enum.UserInputType.Touch
                )
            then
                local E = m.Position.X
                local m = q.AbsolutePosition.X
                local A = q.AbsoluteSize.X
                local q = (E - m) / A
                O(30 + q * 150)
            end
        end)
        local q = g("Reset FOV")
        local E = Instance.new("TextButton", q)
        E.Size = UDim2.new(0, 60, 0, 24)
        E.Position = UDim2.new(1, -70.0, 0.5, -12.0)
        E.BackgroundColor3 = W.SurfaceHighlight
        E.Text = "Reset"
        E.Font = Enum.Font.FredokaOne
        E.TextColor3 = W.TextPrimary
        E.TextSize = 12
        Instance.new("UICorner", E).CornerRadius = UDim.new(0, 4)
        E.MouseButton1Click:Connect(function()
            O(70)
            F("FIELD OF VIEW", "Reset to 70")
        end)
    end
    do
        _G.SetHazeUIPage("Keybinds")
        b("Menu")
        i.MenuKey = NormalizeKeyName(i.SavedKeybinds and i.SavedKeybinds.MenuKey or i.MenuKey, "LeftControl")
        if not d then
            local O = g("Menu Toggle Key")
            local q = Instance.new("TextButton", O)
            q.Size = UDim2.new(0, 80, 0, 24)
            q.Position = UDim2.new(1, -90.0, 0.5, -12.0)
            q.BackgroundColor3 = W.SurfaceHighlight
            q.Text = PrettyKeyName(i.MenuKey or "LeftControl")
            q.Font = Enum.Font.FredokaOne
            q.TextColor3 = W.TextPrimary
            q.TextSize = 12
            Instance.new("UICorner", q).CornerRadius = UDim.new(0, 4)
            q.MouseButton1Click:Connect(function()
                q.Text = "..."
                q.TextColor3 = W.Accent1
                local O
                O = j.InputBegan:Connect(function(E)
                    if E.UserInputType == Enum.UserInputType.Keyboard then
                        i.MenuKey = NormalizeKeyName(E.KeyCode.Name, i.MenuKey or "LeftControl")
                        i.SavedKeybinds.MenuKey = i.MenuKey
                        q.Text = PrettyKeyName(i.MenuKey)
                        q.TextColor3 = W.TextPrimary
                        U()
                        O:Disconnect()
                        F("MENU KEYBIND", PrettyKeyName(i.MenuKey))
                    end
                end)
            end)
        else
            _G.SetHazeUIPage("Settings")
            H("Settings")
            g("Menu Toggle: Touch Icon")
        end
    end
    do
        _G.SetHazeUIPage("Settings")
        b("UI CONTROLS")
        local O = g("Lock UI Dragging")
        w(O, i.UILocked, function(O, q)
            q(O)
            i.UILocked = O
            U()
            F("UI LOCK", O and "ENABLED" or "DISABLED")
        end)
        if d then
            local O = {}
            O.row = g("Mobile GUI Scale")
            O.bg = Instance.new("Frame", O.row)
            O.bg.Size = UDim2.new(0, 140, 0, 5)
            O.bg.Position = UDim2.new(1, -200.0, 0.5, -2.5)
            O.bg.BackgroundColor3 = Color3.fromRGB(30, 32, 38)
            Instance.new("UICorner", O.bg).CornerRadius = UDim.new(1, 0)
            O.fill = Instance.new("Frame", O.bg)
            O.fill.BackgroundColor3 = W.Accent1
            O.fill.BorderSizePixel = 0
            O.fill.Size = UDim2.new(0, 0, 1, 0)
            Instance.new("UICorner", O.fill).CornerRadius = UDim.new(1, 0)
            O.knob = Instance.new("Frame", O.bg)
            O.knob.Size = UDim2.new(0, 14, 0, 14)
            O.knob.AnchorPoint = Vector2.new(0.5, 0.5)
            O.knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            O.knob.BorderSizePixel = 0
            Instance.new("UICorner", O.knob).CornerRadius = UDim.new(1, 0)
            O.stroke = Instance.new("UIStroke", O.knob)
            O.stroke.Color = W.Accent1
            O.stroke.Thickness = 2
            O.readout = Instance.new("TextLabel", O.row)
            O.readout.Size = UDim2.new(0, 50, 0, 20)
            O.readout.Position = UDim2.new(1, -145.0, 0.5, -10.0)
            O.readout.BackgroundTransparency = 1
            O.readout.Text = string.format("%.1f", i.MobileGuiScale or 0.5)
            O.readout.Font = Enum.Font.FredokaOne
            O.readout.TextSize = 11
            O.readout.TextColor3 = W.Accent1
            O.readout.TextXAlignment = Enum.TextXAlignment.Right
            O.dragging = false
            O.min = 0.1
            O.max = 1
            O.scaleToT = function(q)
                return math.clamp((q - O.min) / (O.max - O.min), 0, 1)
            end
            O.updateScaleSlider = function(q)
                local w = math.clamp(q, O.min, O.max)
                i.MobileGuiScale = w
                U()
                O.fill.Size = UDim2.new(O.scaleToT(w), 0, 1, 0)
                O.knob.Position = UDim2.new(0, O.scaleToT(w) * O.bg.AbsoluteSize.X, 0.5, 0)
                O.readout.Text = string.format("%.2f", w)
                if P.RefreshMobileScale then
                    P.RefreshMobileScale()
                end
            end
            task.defer(function()
                O.updateScaleSlider(i.MobileGuiScale or 0.5)
            end)
            O.onScaleInput = function(q)
                O.updateScaleSlider(
                    O.min
                        + math.clamp((q.X - O.bg.AbsolutePosition.X) / O.bg.AbsoluteSize.X, 0, 1)
                            * (O.max - O.min)
                )
            end
            O.bg.InputBegan:Connect(function(q)
                if
                    q.UserInputType == Enum.UserInputType.MouseButton1
                    or q.UserInputType == Enum.UserInputType.Touch
                then
                    O.dragging = true
                    O.onScaleInput(q.Position)
                end
            end)
            O.knob.InputBegan:Connect(function(q)
                if
                    q.UserInputType == Enum.UserInputType.MouseButton1
                    or q.UserInputType == Enum.UserInputType.Touch
                then
                    O.dragging = true
                end
            end)
            j.InputEnded:Connect(function(q)
                if
                    q.UserInputType == Enum.UserInputType.MouseButton1
                    or q.UserInputType == Enum.UserInputType.Touch
                then
                    O.dragging = false
                end
            end)
            j.InputChanged:Connect(function(q)
                if
                    O.dragging
                    and (
                        q.UserInputType == Enum.UserInputType.MouseMovement
                        or q.UserInputType == Enum.UserInputType.Touch
                    )
                then
                    O.onScaleInput(q.Position)
                end
            end)
        end
        local O = g("Reset UI Positions")
        local q = Instance.new("TextButton", O)
        q.Size = UDim2.new(0, 80, 0, 24)
        q.Position = UDim2.new(1, -90.0, 0.5, -12.0)
        q.BackgroundColor3 = W.Error
        q.Text = "RESET"
        q.Font = Enum.Font.FredokaOne
        q.TextColor3 = W.TextPrimary
        q.TextSize = 12
        Instance.new("UICorner", q).CornerRadius = UDim.new(0, 4)
        q.MouseButton1Click:Connect(function()
            i.Positions = DeepCopy(V.Positions)
            U()
            F("UI RESET", "Positions restored")
            Y.Position = UDim2.new(
                V.Positions.Settings.X,
                V.Positions.Settings.OffsetX or 0,
                V.Positions.Settings.Y,
                V.Positions.Settings.OffsetY or 0
            )
            if x:FindFirstChild("cJPcVLFpwxgI") and x.AutoStealUI:FindFirstChild("Frame") then
                x.AutoStealUI.Frame.Position = UDim2.new(
                    V.Positions.AutoSteal.X,
                    V.Positions.AutoSteal.OffsetX or 0,
                    V.Positions.AutoSteal.Y,
                    V.Positions.AutoSteal.OffsetY or 0
                )
            end
            if x:FindFirstChild("XiSafePanel") and x.XiSafePanel:FindFirstChild("Frame") then
                x.XiSafePanel.Frame.Position = UDim2.new(
                    V.Positions.SafePanel.X,
                    V.Positions.SafePanel.OffsetX or 0,
                    V.Positions.SafePanel.Y,
                    V.Positions.SafePanel.OffsetY or 0
                )
            end
            if x:FindFirstChild("XiInvisPanel") and x.XiInvisPanel:FindFirstChild("Frame") then
                x.XiInvisPanel.Frame.Position = UDim2.new(
                    V.Positions.InvisPanel.X,
                    V.Positions.InvisPanel.OffsetX or 0,
                    V.Positions.InvisPanel.Y,
                    V.Positions.InvisPanel.OffsetY or 0
                )
            end
            if
                x:FindFirstChild("kzBUJxAKwhtf")
                and x.AutoStealTargetControls:FindFirstChild("TargetControlsFrame")
            then
                x.AutoStealTargetControls.TargetControlsFrame.Position = UDim2.new(
                    V.Positions.TargetControls.X,
                    V.Positions.TargetControls.OffsetX or 0,
                    V.Positions.TargetControls.Y,
                    V.Positions.TargetControls.OffsetY or 0
                )
            end
            F("UI RESET", "Positions restored to default")
        end)
    end
end
G()
local function V()
    local O = p.AbsoluteContentSize.Y
    I.CanvasSize = UDim2.new(0, 0, 0, math.max(O + 20, I.AbsoluteSize.Y))
end
p:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(V)
task.defer(V)
if d then
    I.ScrollBarThickness = 6
    I.ScrollingEnabled = true
    I.ElasticBehavior = Enum.ElasticBehavior.Always
end
if not d then
    j.InputBegan:Connect(function(V, O)
        if O then
            return
        end
        if
            V.KeyCode == (
                Enum.KeyCode[NormalizeKeyName(i.MenuKey, "LeftControl")] or Enum.KeyCode.LeftControl
            )
        then
            z.Enabled = not z.Enabled
            if P.RefreshMobileScale then
                P.RefreshMobileScale()
            end
        end
        if
            NormalizeKeyName(i.KickKey, "") ~= ""
            and V.KeyCode == Enum.KeyCode[NormalizeKeyName(i.KickKey, "")]
        then
            c()
        end
        if
            NormalizeKeyName(i.RagdollSelfKey, "B") ~= ""
            and V.KeyCode == Enum.KeyCode[NormalizeKeyName(i.RagdollSelfKey, "B")]
        then
            pcall(k)
            if type(F) == "function" then
                pcall(F, "DROP BRAINROT", "Triggered")
            end
        end
        if
            NormalizeKeyName(i.ProximityAPKeybind, "P")
            and V.KeyCode == Enum.KeyCode[NormalizeKeyName(i.ProximityAPKeybind, "P")]
        then
            ProximityAPActive = not ProximityAPActive
            if P.ProximityAPButton then
                updateProxButton()
            end
            F("PROXIMITY AP", ProximityAPActive and "ENABLED" or "DISABLED")
        end
        if V.KeyCode == (Enum.KeyCode[NormalizeKeyName(i.ClickToAPKeybind, "L")] or Enum.KeyCode.L) then
            i.ClickToAP = not i.ClickToAP
            U()
            F("CLICK TO AP", i.ClickToAP and "ENABLED" or "DISABLED")
        end
        if
            NormalizeKeyName(i.JobJoinerKey, "J")
            and V.KeyCode == Enum.KeyCode[NormalizeKeyName(i.JobJoinerKey, "J")]
        then
            local V = gethui and gethui() or game:GetService("CoreGui")
            local O = V:FindFirstChild("XiJobJoiner")
            if O then
                i.ShowJobJoiner = not i.ShowJobJoiner
                O.Enabled = i.ShowJobJoiner
                U()
                F("JOB ID JOINER", i.ShowJobJoiner and "OPENED" or "CLOSED")
            end
        end
    end)
end
task.spawn(function()
    task.wait(1)
    if i.HideAdminPanel then
        local V = x:FindFirstChild("XiSafePanel")
        if V then
            V.Enabled = false
        end
    end
    if i.HideAutoSteal then
        local V = x:FindFirstChild("cJPcVLFpwxgI")
        if V then
            V.Enabled = false
        end
    end
    if i.CompactAutoSteal then
        local V = x:FindFirstChild("cJPcVLFpwxgI")
        if V and V:FindFirstChild("Frame") then
            local O = V.Frame
            O.Size = UDim2.new(O.Size.X.Scale, O.Size.X.Offset, 0, 355)
        end
    end
end)
local function V(O)
    if not O or type(O) ~= "string" then
        return 0
    end
    O = O:gsub("%s", ""):lower()
    if O == "" then
        return 0
    end
    local q, c = O:match("^([%d%.]+)([kmb]?)$")
    if not q then
        return 0
    end
    q = tonumber(q)
    if not q or q < 0 then
        return 0
    end
    if c == "k" then
        return q * 1000
    elseif c == "m" then
        return q * 1000000
    elseif c == "b" then
        return q * 1000000000
    end
    return q
end
if i.TpSettings.TpOnLoad then
    task.spawn(function()
        local O = V(i.TpSettings.MinGenForTp)
        local V = tostring(i.TpSettings.MinGenForTp or "")
        local function q(c)
            if not c then
                return false
            end
            if not c.name or not c.uid then
                return false
            end
            if c.owner == C.Name then
                return false
            end
            if O > 0 and (tonumber(c.genValue) or 0) < O then
                return false
            end
            return true
        end
        local function c()
            local Y = P and P.AllAnimalsCache
            if not Y or #Y == 0 then
                return nil
            end
            for I = 1, #X do
                local p = X[I]:lower()
                for I, I in ipairs(Y) do
                    if q(I) and I.name:lower() == p then
                        return I
                    end
                end
            end
            return nil
        end
        local function Y()
            local I = P and P.AllAnimalsCache
            if not I or #I == 0 then
                return nil
            end
            for p, p in ipairs(I) do
                if q(p) then
                    return p
                end
            end
            return nil
        end
        local function q()
            return c() or Y()
        end
        local c = nil
        local Y = false
        local I = os.clock() + 6
        repeat
            local p = P and P.AllAnimalsCache
            if p and #p > 0 then
                Y = true
            end
            c = q()
            if c then
                break
            end
            R.Heartbeat:Wait()
        until os.clock() >= I
        if not c then
            if O > 0 then
                F("MIN GEN", "No target >= " .. V .. " after cache refresh.")
            elseif Y then
                F("TIMEOUT", "No valid target found.")
            else
                F("TIMEOUT", "No target cache found.")
            end
            return
        end
        P.SelectedPetData = {
            petName = c.name,
            name = c.name,
            mpsText = c.genText,
            mpsValue = c.genValue,
            genText = c.genText,
            genValue = c.genValue,
            owner = c.owner,
            plot = c.plot,
            slot = c.slot,
            uid = c.uid,
            mutation = c.mutation,
            animalData = c,
        }
        if _G.tpToBestBrainrot then
            pcall(_G.tpToBestBrainrot)
        else
            B(0.1)
            S()
        end
    end)
end
C:GetAttributeChangedSignal("Stealing"):Connect(function()
    local V = C:GetAttribute("Stealing")
    local O = not V
    if V then
        if i.AutoInvisDuringSteal and _G.toggleInvisibleSteal and not _G.invisibleStealEnabled then
            _G.toggleInvisibleSteal()
        end
        if i.AutoUnlockOnSteal then
            J()
        end
    elseif O then
        if i.AutoInvisDuringSteal and _G.toggleInvisibleSteal and _G.invisibleStealEnabled then
            _G.toggleInvisibleSteal()
        end
    end
end)
task.spawn(function()
    local J = false
    local V = i.StealSpeed or 25.5
    local O = nil
    local function q()
        J = false
        if O then
            O:Disconnect()
            O = nil
        end
    end
    P.GetStealSpeed = function()
        return V
    end
    P.SetStealSpeed = function(c, B)
        local Y = tonumber(c)
        if not Y then
            return V
        end
        V = math.clamp(Y, 5, 100)
        i.StealSpeed = V
        if not B then
            U()
        end
        if P._ssUpdateBtn then
            P._ssUpdateBtn()
        end
        return V
    end
    P.DisableStealSpeed = function()
        q()
        if P._ssUpdateBtn then
            P._ssUpdateBtn()
        end
    end
    local function c()
        J = true
        if O then
            O:Disconnect()
            O = nil
        end
        O = R.Heartbeat:Connect(function()
            local O = C.Character
            if not O then
                return
            end
            local B = O:FindFirstChildOfClass("Humanoid")
            local Y = O:FindFirstChild("HumanoidRootPart")
            if not B or not Y then
                return
            end
            local O = B.MoveDirection
            if O.Magnitude > 0 then
                Y.Velocity = Vector3.new(O.X * V, Y.Velocity.Y, O.Z * V)
            end
        end)
    end
    P._ssUpdateBtn = function() end
    P.StealSpeedToggleFunc = function()
        if J then
            q()
        else
            c()
        end
        if P._ssUpdateBtn then
            P._ssUpdateBtn()
        end
    end
    task.spawn(function()
        local V = nil
        while true do
            task.wait(0.3)
            if not i.AutoStealSpeed then
                V = nil
                continue
            end
            local O = C:GetAttribute("Stealing") == true
            if V == O then
                continue
            end
            V = O
            if O and not J then
                c()
                if P._ssUpdateBtn then
                    P._ssUpdateBtn()
                end
            elseif not O and J then
                q()
                if P._ssUpdateBtn then
                    P._ssUpdateBtn()
                end
            end
        end
    end)
end)
task.spawn(function()
    local J = i.BrainrotESP
    local V = nil
    local O = {}
    local q = Vector3.zero
    local function q()
        local c = P and P.AllAnimalsCache
        if type(c) ~= "table" then
            return ""
        end
        local B = {}
        for Y, I in ipairs(c) do
            if I and I.uid then
                B[Y] = tostring(I.uid) .. (I.genText or "")
            end
        end
        return table.concat(B, "|")
    end
    local function c()
        for B, B in ipairs(a:GetChildren()) do
            if B.Name == "XiBrainrotESP" then
                pcall(function()
                    B:Destroy()
                end)
            end
        end
        for B, B in ipairs(game:GetService("CoreGui"):GetChildren()) do
            if B:IsA("Highlight") and B.Name == "XiBrainrotESP_Highlight" then
                pcall(function()
                    B:Destroy()
                end)
            end
        end
        for B, B in ipairs(a:GetDescendants()) do
            if B:IsA("BillboardGui") and string.sub(B.Name, 1, 12) == "BrainrotESP_" then
                pcall(function()
                    B:Destroy()
                end)
            end
        end
    end
    c()
    local c = Instance.new("Folder")
    c.Name = "XiBrainrotESP"
    c.Parent = a
    local B = {}
    local Y = game:GetService("CoreGui")
    local Y = 0
    local I = nil
    local p = nil
    local G = C.DisplayName
    local w = {
        Cursed = Color3.fromRGB(50, 178, 255),
        Gold = Color3.fromRGB(0, 178, 255),
        Diamond = Color3.fromRGB(0, 255, 255),
        YinYang = Color3.fromRGB(220, 220, 220),
        Rainbow = Color3.fromRGB(200, 178, 255),
        Lava = Color3.fromRGB(20, 178, 255),
        Candy = Color3.fromRGB(180, 178, 255),
        Bloodrot = Color3.fromRGB(0, 56, 139),
        Radioactive = Color3.fromRGB(0, 255, 0),
        Divine = Color3.fromRGB(255, 255, 255),
    }
    local function b(E)
        if not E then
            return nil
        end
        local m = E:GetAttribute("_BR_UID")
        if m then
            return tostring(m)
        end
        Y += 1
        local m = "br_" .. tostring(Y)
        E:SetAttribute("_BR_UID", m)
        return m
    end
    local function Y(E)
        E = tostring(E or "")
        if E == "" then
            return nil
        end
        E = E:gsub("%s+", "")
        local m, A = E:match("%$?([%d%.]+)([KkMmBb]?)/s")
        if not m then
            return nil
        end
        local E = tonumber(m)
        if not E then
            return nil
        end
        A = (A or ""):upper()
        if A == "B" then
            return E * 1000, true
        elseif A == "M" then
            return E, false
        elseif A == "K" then
            return E / 1000, false
        else
            return E / 1000000, false
        end
    end
    local function E(m)
        local A = m or 0
        if A >= 1000 then
            local m = A / 1000
            if math.floor(m) == m then
                return string.format("$%dB/s", m)
            else
                return string.format("$%.1fB/s", m)
            end
        elseif A >= 1 then
            if math.floor(A) == A then
                return string.format("$%dM/s", A)
            else
                return string.format("$%.1fM/s", A)
            end
        elseif A >= 0.001 then
            local m = A * 1000
            if math.floor(m) == m then
                return string.format("$%dK/s", m)
            else
                return string.format("$%.1fK/s", m)
            end
        else
            local m = math.floor(A * 1000000 + 0.5)
            return string.format("$%d/s", m)
        end
    end
    local function m()
        local A = a:FindFirstChild("Plots")
        if not A then
            return nil
        end
        for H, H in ipairs(A:GetChildren()) do
            local A = H:FindFirstChild("PlotSign")
            if A then
                local Z = A:FindFirstChild("SurfaceGui")
                if Z then
                    local A = Z:FindFirstChild("Frame")
                    if A then
                        local Z = A:FindFirstChild("TextLabel")
                        if Z and Z:IsA("TextLabel") then
                            local A = tostring(Z.Text or "")
                            if A:find(G, 1, true) and A:find("'s Base", 1, true) then
                                return H
                            end
                        end
                    end
                end
            end
        end
        return nil
    end
    local function G(A)
        if not A then
            return nil
        end
        local H, Z, k = math.huge, math.huge, math.huge
        local u, g, h = -math.huge, -math.huge, -math.huge
        local L = false
        for s, s in ipairs(A:GetDescendants()) do
            if s:IsA("BasePart") then
                L = true
                local A = s.Position
                local N = s.Size / 2
                local s, PG = A.X - N.X, A.X + N.X
                local CG, rG = A.Y - N.Y, A.Y + N.Y
                local RG, dG = A.Z - N.Z, A.Z + N.Z
                if s < H then
                    H = s
                end
                if CG < Z then
                    Z = CG
                end
                if RG < k then
                    k = RG
                end
                if PG > u then
                    u = PG
                end
                if rG > g then
                    g = rG
                end
                if dG > h then
                    h = dG
                end
            end
        end
        if not L then
            return nil
        end
        return { minX = H, maxX = u, minY = Z, maxY = g, minZ = k, maxZ = h }
    end
    local function A(A, H, Z)
        if not H then
            return false
        end
        Z = Z or 0
        return A.X >= H.minX - Z
            and A.X <= H.maxX + Z
            and A.Y >= H.minY - Z
            and A.Y <= H.maxY + Z
            and A.Z >= H.minZ - Z
            and A.Z <= H.maxZ + Z
    end
    local function A()
        I = m()
        p = G(I)
    end
    local function p(G)
        return G and G ~= "None" and G ~= "N/A" and w[G] or Color3.fromRGB(0, 255, 150)
    end
    local function G(w)
        if w and w.kind == "CONVEYOR" then
            local m = Instance.new("BillboardGui")
            m.Name = "BrainrotESP_" .. tostring(w.uid)
            m.Size = UDim2.new(0, 148, 0, 54)
            m.StudsOffset = Vector3.new(0, 3, 0)
            m.AlwaysOnTop = true
            m.LightInfluence = 0
            m.MaxDistance = 100000
            m.ResetOnSpawn = false
            local H = Instance.new("Frame")
            H.Name = "Container"
            H.Parent = m
            H.Size = UDim2.new(1, 0, 1, 0)
            H.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
            H.BackgroundTransparency = 0.25
            H.BorderSizePixel = 0
            Instance.new("UICorner", H).CornerRadius = UDim.new(0, 6)
            local Z = Instance.new("UIStroke")
            Z.Name = "Stroke"
            Z.Parent = H
            Z.Color = Color3.fromRGB(60, 60, 60)
            Z.Thickness = 1.2
            local Z = Instance.new("TextLabel")
            Z.Name = "Tag"
            Z.Parent = H
            Z.Size = UDim2.new(1, -6.0, 0, 12)
            Z.Position = UDim2.new(0, 3, 0, 2)
            Z.BackgroundTransparency = 1
            Z.Text = "[CARPET]"
            Z.TextColor3 = Color3.fromRGB(0, 255, 150)
            Z.Font = Enum.Font.FredokaOne
            Z.TextSize = 9
            Z.TextXAlignment = Enum.TextXAlignment.Left
            local Z = Instance.new("TextLabel")
            Z.Name = "Name"
            Z.Parent = H
            Z.Size = UDim2.new(1, -6.0, 0, 13)
            Z.Position = UDim2.new(0, 3, 0, 13)
            Z.BackgroundTransparency = 1
            Z.Text = tostring(w.name or w.petName or "Brainrot")
            Z.TextColor3 = Color3.fromRGB(255, 255, 255)
            Z.Font = Enum.Font.FredokaOne
            Z.TextSize = 11
            Z.TextXAlignment = Enum.TextXAlignment.Left
            Z.TextTruncate = Enum.TextTruncate.AtEnd
            local Z = Instance.new("TextLabel")
            Z.Name = "Gen"
            Z.Parent = H
            Z.Size = UDim2.new(1, -6.0, 0, 14)
            Z.Position = UDim2.new(0, 3, 0, 25)
            Z.BackgroundTransparency = 1
            Z.Text = tostring(w.genText or "")
            Z.TextColor3 = Color3.fromRGB(180, 255, 180)
            Z.Font = Enum.Font.FredokaOne
            Z.TextSize = 14
            Z.TextXAlignment = Enum.TextXAlignment.Left
            Z.TextTruncate = Enum.TextTruncate.AtEnd
            local Z = Instance.new("TextLabel")
            Z.Name = "Info"
            Z.Parent = H
            Z.Size = UDim2.new(1, -6.0, 0, 10)
            Z.Position = UDim2.new(0, 3, 1, -12.0)
            Z.BackgroundTransparency = 1
            Z.Text = ""
            Z.TextColor3 = Color3.fromRGB(210, 210, 210)
            Z.Font = Enum.Font.FredokaOne
            Z.TextSize = 9
            Z.TextXAlignment = Enum.TextXAlignment.Right
            return m
        end
        local m = Instance.new("BillboardGui")
        m.Name = "BrainrotESP_" .. tostring(w.uid)
        m.Size = UDim2.new(0, 128, 0, 56)
        m.StudsOffset = Vector3.new(0, 1, 0)
        m.AlwaysOnTop = true
        m.LightInfluence = 0
        m.MaxDistance = 2500
        m.ResetOnSpawn = false
        local H = Instance.new("Frame")
        H.Name = "Container"
        H.Parent = m
        H.Size = UDim2.new(1, 0, 1, 0)
        H.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
        H.BackgroundTransparency = 0.45
        H.BorderSizePixel = 0
        Instance.new("UICorner", H).CornerRadius = UDim.new(0, 6)
        local Z = Color3.fromRGB(60, 60, 60)
        local k = Instance.new("UIStroke")
        k.Name = "Stroke"
        k.Parent = H
        k.Thickness = 1.2
        k.Color = Z
        local k = Instance.new("TextLabel")
        k.Name = "Tag"
        k.Parent = H
        k.Size = UDim2.new(1, -6.0, 0, 10)
        k.Position = UDim2.new(0, 3, 0, 2)
        k.BackgroundTransparency = 1
        k.Font = Enum.Font.FredokaOne
        k.TextSize = 10
        k.TextXAlignment = Enum.TextXAlignment.Left
        k.TextColor3 = Z
        k.Text = ""
        local Z = Instance.new("TextLabel")
        Z.Name = "Name"
        Z.Parent = H
        Z.Size = UDim2.new(1, -6.0, 0, 12)
        Z.Position = UDim2.new(0, 3, 0, 10)
        Z.BackgroundTransparency = 1
        Z.Font = Enum.Font.FredokaOne
        Z.TextSize = 11
        Z.TextWrapped = true
        Z.TextYAlignment = Enum.TextYAlignment.Top
        Z.TextXAlignment = Enum.TextXAlignment.Left
        Z.TextTruncate = Enum.TextTruncate.None
        Z.TextColor3 = Color3.fromRGB(255, 255, 255)
        Z.TextStrokeTransparency = 1
        Z.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        Z.Text = tostring(w.name or w.petName or "Brainrot")
        local Z = Instance.new("TextLabel")
        Z.Name = "Gen"
        Z.Parent = H
        Z.Size = UDim2.new(1, -6.0, 0, 14)
        Z.Position = UDim2.new(0, 3, 0, 25)
        Z.BackgroundTransparency = 1
        Z.Font = Enum.Font.FredokaOne
        Z.TextSize = 16
        Z.TextXAlignment = Enum.TextXAlignment.Left
        Z.TextTruncate = Enum.TextTruncate.AtEnd
        Z.TextColor3 = Color3.fromRGB(0, 255, 120)
        Z.TextStrokeTransparency = 0.82
        Z.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        Z.Text = tostring(w.genText or "")
        local w = Instance.new("TextLabel")
        w.Name = "Info"
        w.Parent = H
        w.Size = UDim2.new(1, -6.0, 0, 10)
        w.Position = UDim2.new(0, 3, 1, -12.0)
        w.BackgroundTransparency = 1
        w.Font = Enum.Font.FredokaOne
        w.TextSize = 9
        w.TextXAlignment = Enum.TextXAlignment.Right
        w.TextColor3 = Color3.fromRGB(210, 210, 210)
        w.Text = ""
        return m
    end
    local function w(m, H, Z)
        if not m or not m.Parent then
            return
        end
        local k = m:FindFirstChild("Container")
        if not k then
            return
        end
        local u = k:FindFirstChild("Stroke")
        local g = k:FindFirstChild("Tag")
        local h = k:FindFirstChild("Name")
        local L = k:FindFirstChild("Gen")
        local s = k:FindFirstChild("Info")
        if H and H.kind == "CONVEYOR" then
            if u then
                u.Color = hasMutation and color or Color3.fromRGB(60, 60, 60)
            end
            if g then
                g.Visible = true
                g.Text = "[CARPET]"
                g.TextColor3 = Color3.fromRGB(0, 255, 150)
            end
            if h then
                h.Text = tostring(H.name or H.petName or "Brainrot")
            end
            if L then
                L.Text = tostring(H.genText or "")
            end
            if s then
                if Z and H.adornee and H.adornee.Parent then
                    local k = math.floor((Z.Position - H.adornee.Position).Magnitude)
                    s.Text = tostring(k) .. " studs"
                else
                    s.Text = ""
                end
            end
            m.Adornee = H.adornee
            return
        end
        local k = H.mutation
        local N = k and k ~= "" and k ~= "None" and k ~= "N/A"
        local PG = p(k)
        if u then
            u.Color = N and PG or Color3.fromRGB(60, 60, 60)
        end
        if g then
            g.Visible = N
            g.Text = N and string.upper(tostring(k)) or ""
            g.TextColor3 = PG
        end
        if h then
            h.Text = tostring(H.name or H.petName or "Brainrot")
            h.TextWrapped = true
            h.TextTruncate = Enum.TextTruncate.None
            h.TextYAlignment = Enum.TextYAlignment.Top
            h.Position = N and UDim2.new(0, 3, 0, 12) or UDim2.new(0, 3, 0, 8)
            h.Size = UDim2.new(1, -6.0, 0, 24)
        end
        if L then
            L.Text = tostring(H.genText or "")
            L.Position = N and UDim2.new(0, 3, 0, 39) or UDim2.new(0, 3, 0, 36)
        end
        if s then
            if Z and H.adornee and H.adornee.Parent then
                local p = math.floor((Z.Position - H.adornee.Position).Magnitude)
                s.Text = tostring(p) .. " studs"
            else
                s.Text = ""
            end
        end
        m.Adornee = H.adornee
    end
    local function p()
        for m, H in pairs(B) do
            if H.bb then
                pcall(function()
                    H.bb:Destroy()
                end)
            end
            B[m] = nil
        end
        if c and c.Parent then
            for m, m in ipairs(c:GetChildren()) do
                pcall(function()
                    m:Destroy()
                end)
            end
        end
        for m, m in ipairs(a:GetDescendants()) do
            if m:IsA("BillboardGui") and string.sub(m.Name, 1, 12) == "BrainrotESP_" then
                pcall(function()
                    m:Destroy()
                end)
            end
        end
    end
    local m = {
        STOLEN = true,
        STEAL = true,
        PURCHASE = true,
        COMPRAR = true,
        BUY = true,
        COLLECT = true,
        COLETAR = true,
        CASH = true,
        VALUE = true,
        BASE = true,
        EMPTY = true,
        GENERATION = true,
        COMMON = true,
        UNCOMMON = true,
        RARE = true,
        EPIC = true,
        LEGENDARY = true,
        DIVINE = true,
        RAINBOW = true,
        CURSED = true,
        GOLD = true,
        DIAMOND = true,
        CANDY = true,
        MUTATION = true,
    }
    local function H(Z)
        if not Z or Z == "" then
            return false
        end
        local k = (Z:gsub("<[^>]+>", "")):match("^%s*(.-)%s*$") or ""
        if #k <= 1 then
            return false
        end
        local Z = k:upper()
        if Z:find("^%$") or Z:find("/S$") or Z:find("^[%d%.]+") then
            return false
        end
        return not m[Z]
    end
    local function m(Z)
        if type(Z) ~= "string" then
            return nil
        end
        local k = Z:gsub("<[^>]+>", ""):upper()
        if not k:find("%$") or not k:find("/S") then
            return nil
        end
        local Z = k:gsub("%$", ""):gsub("/S", ""):gsub("%s+", "")
        local k = tonumber(Z:match("[%d%.]+"))
        if not k then
            return nil
        end
        if Z:find("B") then
            return k * 1000000000
        elseif Z:find("M") then
            return k * 1000000
        elseif Z:find("K") then
            return k * 1000
        else
            return k
        end
    end
    local function Z(k)
        if not k then
            return nil, nil, 0
        end
        local u, g, h = nil, nil, 0
        for L, L in ipairs(k:GetDescendants()) do
            if L:IsA("BillboardGui") or L:IsA("SurfaceGui") then
                for k, k in ipairs(L:GetDescendants()) do
                    if k:IsA("TextLabel") and k.Text then
                        local L = m(k.Text)
                        if L and L > h then
                            h = L
                            g = k.Text:gsub("<[^>]+>", "")
                            local m = k.Parent
                            if m then
                                local k = nil
                                for L, L in ipairs(m:GetChildren()) do
                                    if L:IsA("TextLabel") and L.Name == "DisplayName" then
                                        local s = (L.Text or ""):gsub("<[^>]+>", ""):match("^%s*(.-)%s*$")
                                        if H(s) then
                                            k = s
                                            break
                                        end
                                    end
                                end
                                if not k then
                                    local L, s = nil, 0
                                    for N, N in ipairs(m:GetChildren()) do
                                        if N:IsA("TextLabel") then
                                            local m = (N.Text or ""):gsub("<[^>]+>", ""):match("^%s*(.-)%s*$")
                                                or ""
                                            if H(m) and #m > s then
                                                L, s = m, #m
                                            end
                                        end
                                    end
                                    if L then
                                        k = L
                                    end
                                end
                                if k then
                                    u = k
                                end
                            end
                        end
                    end
                end
            end
        end
        return u, g, h
    end
    local m = setmetatable({}, { __mode = "k" })
    local H = {}
    local function k(u)
        if not u or not u:IsA("ProximityPrompt") then
            return false
        end
        local g = string.lower(tostring(u.ActionText or ""))
        return g:find("purchase", 1, true) or g:find("comprar", 1, true) or g:find("buy", 1, true)
    end
    local function u(g)
        if k(g) then
            m[g] = true
        end
    end
    do
        for m, m in ipairs(a:GetDescendants()) do
            if m:IsA("ProximityPrompt") then
                u(m)
            end
        end
    end
    a.DescendantAdded:Connect(function(m)
        if m:IsA("ProximityPrompt") then
            u(m)
        end
    end)
    local function m()
        local k = {}
        local u = a:FindFirstChild("Debris") or a
        for g, g in ipairs(u:GetChildren()) do
            if g:IsA("Model") or g:IsA("BasePart") then
                local u, h, L = Z(g)
                if L and L >= 10000000 then
                    local s = g:IsA("BasePart") and g or g:IsA("Model") and g.PrimaryPart
                    if not s and g:IsA("Model") then
                        for N, N in ipairs(g:GetChildren()) do
                            if N:IsA("BasePart") then
                                s = N
                                break
                            end
                        end
                    end
                    if s then
                        table.insert(k, { name = u, gen = h, gv = L, part = s, model = g })
                    end
                end
            end
        end
        return k
    end
    local function k()
        local u = m()
        local m = {}
        local g = {}
        for h, h in ipairs(a:GetDescendants()) do
            if h:IsA("ProximityPrompt") and h.Enabled then
                local L = string.lower(tostring(h.ActionText or ""))
                if L:find("purchase", 1, true) or L:find("comprar", 1, true) or L:find("buy", 1, true) then
                    local L = h.Parent
                    if L and L:IsA("Attachment") then
                        L = L.Parent
                    end
                    if L and L:IsA("BasePart") then
                        local s, N, PG, CG = "Brainrot", "", 0, nil
                        local rG, RG = 15, nil
                        for dG, dG in ipairs(u) do
                            if dG.part and dG.part.Parent then
                                local u = (dG.part.Position - L.Position).Magnitude
                                if u < rG then
                                    rG = u
                                    RG = dG
                                end
                            end
                        end
                        if RG then
                            s = RG.name or "Brainrot"
                            N = RG.gen or ""
                            PG = RG.gv or 0
                            CG = RG.model
                        else
                            local u = L
                            local rG = L
                            while rG and rG.Parent and rG.Parent ~= a do
                                u = rG
                                rG = rG.Parent
                            end
                            local rG, RG, dG = Z(u)
                            if dG and dG >= 10000000 then
                                s = rG or s
                                N = RG or N
                                PG = dG
                                CG = u
                            end
                        end
                        if PG >= 10000000 then
                            local Z = b(L) or tostring(h)
                            local b = "conv_" .. tostring(Z)
                            if not g[b] then
                                g[b] = true
                                table.insert(
                                    m,
                                    {
                                        uid = b,
                                        kind = "CONVEYOR",
                                        name = s,
                                        petName = s,
                                        genText = N,
                                        genValue = PG,
                                        mutation = "None",
                                        adornee = L,
                                        model = CG,
                                    }
                                )
                            end
                        end
                    end
                end
            end
        end
        table.sort(m, function(b, Z)
            return (tonumber(b.genValue) or 0) > (tonumber(Z.genValue) or 0)
        end)
        local b = {}
        for Z = 1, math.min(3, #m) do
            b[Z] = m[Z]
        end
        return b
    end
    local function b()
        local m = {}
        local Z = P and P.AllAnimalsCache
        if type(Z) ~= "table" then
            return m
        end
        for u, u in ipairs(Z) do
            if u and u.uid and u.plot and u.slot then
                if not I or tostring(u.plot) ~= tostring(I.Name) then
                    local I = M(u)
                    if I and I:IsA("BasePart") and I.Parent then
                        local M = select(1, Y(u.genText or ""))
                        local Y = tonumber(u.genValue) or (M or 0) * 1000000
                        if M and M >= 35 or Y >= 35000000 then
                            table.insert(
                                m,
                                {
                                    uid = "plot_" .. tostring(u.uid),
                                    kind = "PLOT",
                                    name = u.name,
                                    petName = u.name,
                                    genText = u.genText or E((Y or 0) / 1000000),
                                    genValue = Y,
                                    mutation = u.mutation or "None",
                                    adornee = I,
                                    owner = u.owner,
                                    plot = u.plot,
                                    slot = u.slot,
                                }
                            )
                        end
                    end
                end
            end
        end
        table.sort(m, function(Y, I)
            return (tonumber(Y.genValue) or 0) > (tonumber(I.genValue) or 0)
        end)
        return m
    end
    local function Y(I)
        local M = {}
        local E = C.Character
        local m = E and E:FindFirstChild("HumanoidRootPart")
        local E = m and m.Position or Vector3.zero
        for Z, Z in ipairs(I) do
            if Z and Z.uid and Z.adornee and Z.adornee.Parent then
                M[Z.uid] = true
                local I = B[Z.uid]
                local u = not I or not I.bb or not I.bb.Parent or I.adornee ~= Z.adornee
                if u then
                    if I and I.bb then
                        pcall(function()
                            I.bb:Destroy()
                        end)
                    end
                    local u = G(Z)
                    u.Adornee = Z.adornee
                    u.Parent = c
                    B[Z.uid] = { bb = u, adornee = Z.adornee, kind = Z.kind, lastHash = "" }
                    I = B[Z.uid]
                else
                    I.adornee = Z.adornee
                    I.kind = Z.kind
                end
                local c = m and Z.adornee and math.floor((E - Z.adornee.Position).Magnitude) or 0
                local G = tostring(Z.genText) .. tostring(Z.mutation) .. tostring(Z.name) .. tostring(c)
                if I.lastHash ~= G then
                    I.lastHash = G
                    w(I.bb, Z, m)
                end
            end
        end
        for c, I in pairs(B) do
            if not M[c] then
                if I.bb then
                    pcall(function()
                        I.bb:Destroy()
                    end)
                end
                B[c] = nil
            end
        end
    end
    local function c()
        if not J then
            return
        end
        local I = C.Character
        local G = I and I:FindFirstChild("HumanoidRootPart")
        local I = G and G.Position or Vector3.zero
        local I = q()
        if I ~= V then
            V = I
            local V = {}
            local q = b()
            for I = 1, #q do
                V[#V + 1] = q[I]
            end
            for q = 1, #H do
                V[#V + 1] = H[q]
            end
            table.sort(V, function(q, I)
                return (tonumber(q.genValue) or 0) > (tonumber(I.genValue) or 0)
            end)
            O = V
        end
        Y(O)
    end
    task.spawn(function()
        while true do
            A()
            task.wait(2)
        end
    end)
    if not espToggleRef then
        espToggleRef = { enabled = true, setFn = nil }
    end
    espToggleRef.setFn = function(V)
        J = V
        i.BrainrotESP = V
        if V then
            task.spawn(function()
                A()
                pcall(function()
                    H = k()
                end)
                pcall(c)
            end)
        else
            p()
        end
    end
    task.spawn(function()
        while true do
            task.wait(1.2)
            if J then
                pcall(function()
                    H = k()
                end)
            end
        end
    end)
    task.spawn(function()
        while true do
            task.wait(2)
            if J then
                pcall(c)
            end
        end
    end)
    task.spawn(function()
        while true do
            task.wait(2)
            if J and next(B) == nil then
                pcall(function()
                    H = k()
                end)
                pcall(c)
            end
        end
    end)
end)
task.spawn(function()
    local J = false
    local V = {}
    local O, q, c, B
    local Y = {}
    local I = {}
    local p = 0
    local G = 0
    local M = 0
    local w = false
    local b = nil
    local E = nil
    local function m()
        if b and b.Parent then
            b:Destroy()
        end
        b = nil
        w = false
        if E then
            E:Disconnect()
            E = nil
        end
    end
    local function E()
        if w then
            return
        end
        w = true
        for A, A in pairs(I) do
            if A and A.Parent then
                A:Destroy()
            end
        end
        I = {}
        local A = Instance.new("ScreenGui")
        A.Name = "KbAtMQFpvsWR"
        A.ResetOnSpawn = false
        A.Parent = C:WaitForChild("PlayerGui")
        local H = Instance.new("Frame")
        H.Size = UDim2.new(0, 500, 0, 60)
        H.Position = UDim2.new(0.5, -250.0, 0.3, 0)
        H.BackgroundTransparency = 1
        H.BorderSizePixel = 0
        H.Parent = A
        local Z = Instance.new("TextLabel")
        Z.Size = UDim2.new(1, 0, 0.5, 0)
        Z.BackgroundTransparency = 1
        Z.Text = "ERROR CAUSED BY PLAYER DEATH"
        Z.TextColor3 = Color3.fromRGB(0, 178, 255)
        Z.TextStrokeTransparency = 0
        Z.TextStrokeColor3 = Color3.new(0, 0, 0)
        Z.Font = Enum.Font.FredokaOne
        Z.TextScaled = true
        Z.Parent = H
        local Z = Instance.new("TextLabel")
        Z.Size = UDim2.new(1, 0, 0.5, 0)
        Z.Position = UDim2.new(0, 0, 0.5, 0)
        Z.BackgroundTransparency = 1
        Z.Text = "MUST RESET TO FIX ERROR"
        Z.TextColor3 = Color3.fromRGB(0, 178, 255)
        Z.TextStrokeTransparency = 0
        Z.TextStrokeColor3 = Color3.new(0, 0, 0)
        Z.Font = Enum.Font.FredokaOne
        Z.TextScaled = true
        Z.Parent = H
        b = A
    end
    local function b(A)
        if w then
            return
        end
        local w = tick()
        if w - M < 0.05 then
            return
        end
        M = w
        if w - G > 1 then
            p = 0
            G = w
        end
        p = p + 1
        if p >= 7 then
            E()
            return
        end
        for G, G in pairs(I) do
            if G and G.Parent then
                G:Destroy()
            end
        end
        I = {}
        local G = Instance.new("ScreenGui")
        G.Name = "ErRbopUROzJw"
        G.ResetOnSpawn = false
        G.Parent = C:WaitForChild("PlayerGui")
        local w = Instance.new("TextLabel")
        w.Size = UDim2.new(0, 500, 0, 30)
        w.Position = UDim2.new(0.5, -250.0, 0.15, 0)
        w.BackgroundTransparency = 1
        w.Text = "LAGBACK DETECTED"
        w.TextColor3 = Color3.fromRGB(0, 178, 255)
        w.TextStrokeTransparency = 0
        w.TextStrokeColor3 = Color3.new(0, 0, 0)
        w.Font = Enum.Font.FredokaOne
        w.TextScaled = true
        w.Parent = G
        local w = Instance.new("TextLabel")
        w.Size = UDim2.new(0, 650, 0, 25)
        w.Position = UDim2.new(0.5, -325.0, 0.15, 32)
        w.BackgroundTransparency = 1
        w.Text = "DISABLE INVISIBLE STEAL NOW OR YOU WILL BE KILLED BY ANTICHEAT"
        w.TextColor3 = Color3.fromRGB(200, 200, 200)
        w.TextStrokeTransparency = 0
        w.TextStrokeColor3 = Color3.new(0, 0, 0)
        w.Font = Enum.Font.FredokaOne
        w.TextScaled = true
        w.Parent = G
        task.spawn(function()
            task.wait(1.5)
            if G and G.Parent then
                G:Destroy()
            end
        end)
        local G = Instance.new("Part")
        G.Name = "LagbackGhost"
        G.Shape = Enum.PartType.Ball
        G.Size = Vector3.new(3, 3, 3)
        G.Color = Color3.fromRGB(0, 178, 255)
        G.Material = Enum.Material.Glass
        G.Transparency = 0.3
        G.CanCollide = false
        G.Anchored = true
        G.CastShadow = false
        G.Position = A + Vector3.new(0, 5, 0)
        G.Parent = a.CurrentCamera
        local w = Instance.new("BillboardGui")
        w.Size = UDim2.new(0, 400, 0, 60)
        w.StudsOffset = Vector3.new(0, 4, 0)
        w.AlwaysOnTop = true
        w.Parent = G
        local E = Instance.new("TextLabel")
        E.Size = UDim2.new(1, 0, 0, 25)
        E.BackgroundTransparency = 1
        E.Text = "LAGBACK DETECTED"
        E.TextColor3 = Color3.fromRGB(0, 178, 255)
        E.TextStrokeTransparency = 0
        E.TextStrokeColor3 = Color3.new(0, 0, 0)
        E.Font = Enum.Font.FredokaOne
        E.TextScaled = true
        E.Parent = w
        local E = Instance.new("TextLabel")
        E.Size = UDim2.new(1, 0, 0, 25)
        E.Position = UDim2.new(0, 0, 0, 25)
        E.BackgroundTransparency = 1
        E.Text = "DISABLE INVISIBLE STEAL NOW OR YOU WILL BE KILLED BY ANTICHEAT"
        E.TextColor3 = Color3.fromRGB(200, 200, 200)
        E.TextStrokeTransparency = 0
        E.TextStrokeColor3 = Color3.new(0, 0, 0)
        E.Font = Enum.Font.FredokaOne
        E.TextScaled = true
        E.Parent = w
        table.insert(I, G)
    end
    local function G()
        for w, w in pairs(I) do
            pcall(function()
                if w and w.Parent then
                    w:Destroy()
                end
            end)
        end
        I = {}
        m()
        p = 0
        M = 0
        pcall(function()
            local I = C:FindFirstChild("PlayerGui")
            if I then
                for M, M in pairs(I:GetChildren()) do
                    if M.Name == "ErRbopUROzJw" then
                        M:Destroy()
                    end
                end
            end
        end)
        pcall(function()
            if a.CurrentCamera then
                for I, I in pairs(a.CurrentCamera:GetChildren()) do
                    if I.Name == "LagbackGhost" then
                        I:Destroy()
                    end
                end
            end
        end)
        pcall(function()
            for I, I in pairs(a:GetDescendants()) do
                if I.Name == "LagbackGhost" then
                    I:Destroy()
                end
            end
        end)
    end
    local function I()
        local M = a:FindFirstChild(C.Name)
        if not M then
            return
        end
        local w = M:FindFirstChild("DoubleRig")
        if w then
            local E = w:FindFirstChild("HumanoidRootPart") or w:FindFirstChildWhichIsA("BasePart")
            if E and true then
                b(E.Position)
            end
            w:Destroy()
        end
        local w = M:FindFirstChild("Constraints")
        if w then
            w:Destroy()
        end
        local w = M.ChildAdded:Connect(function(M)
            if M.Name == "DoubleRig" then
                task.defer(function()
                    local E = M:FindFirstChild("HumanoidRootPart") or M:FindFirstChildWhichIsA("BasePart")
                    if E and true then
                        b(E.Position)
                    end
                    M:Destroy()
                end)
            elseif M.Name == "Constraints" then
                M:Destroy()
            end
        end)
        table.insert(Y, w)
    end
    local function M()
        local w = C.Character
        if w and w:FindFirstChild("Humanoid") and w.Humanoid.Health > 0 then
            c = w.Humanoid.HipHeight
            q = w:FindFirstChild("HumanoidRootPart")
            if not q or not q.Parent then
                return false
            end
            for E, E in pairs(q:GetChildren()) do
                if E:IsA("Attachment") and (E.Name:find("Beam") or E.Name:find("Attach")) then
                    E:Destroy()
                end
            end
            for E, E in pairs(q:GetChildren()) do
                if E:IsA("Beam") then
                    E:Destroy()
                end
            end
            local E = Instance.new("Model")
            E.Parent = game
            w.Parent = E
            O = q:Clone()
            O.Parent = w
            q.Parent = a.CurrentCamera
            O.CFrame = q.CFrame
            w.PrimaryPart = O
            w.Parent = a
            for A, A in pairs(w:GetDescendants()) do
                if A:IsA("Weld") or A:IsA("Motor6D") then
                    if A.Part0 == q then
                        A.Part0 = O
                    end
                    if A.Part1 == q then
                        A.Part1 = O
                    end
                end
            end
            E:Destroy()
            return true
        end
        return false
    end
    local function w()
        local E = C.Character
        if not q or not q:IsDescendantOf(a) or not E or E.Humanoid.Health <= 0 then
            return
        end
        local A = Instance.new("Model")
        A.Parent = game
        E.Parent = A
        q.Parent = E
        E.PrimaryPart = q
        E.Parent = a
        q.CanCollide = true
        for A, A in pairs(E:GetDescendants()) do
            if A:IsA("Weld") or A:IsA("Motor6D") then
                if A.Part0 == O then
                    A.Part0 = q
                end
                if A.Part1 == O then
                    A.Part1 = q
                end
            end
        end
        if O then
            local A = O.CFrame
            O:Destroy()
            O = nil
            q.CFrame = A
        end
        q = nil
        if E and E.Humanoid then
            E.Humanoid.HipHeight = c
        end
        G()
    end
    local function c()
        local E = C.Character
        if E and E:FindFirstChild("Humanoid") and E.Humanoid.Health > 0 then
            local A = Instance.new("Animation")
            A.AnimationId = "http://www.roblox.com/asset/?id=18537363391"
            local H = E.Humanoid
            local E = H:FindFirstChild("Animator") or Instance.new("Animator", H)
            local H = E:LoadAnimation(A)
            H.Priority = Enum.AnimationPriority.Action4
            H:Play(0, 1, 0)
            A:Destroy()
            table.insert(V, H)
            H.Stopped:Connect(function()
                if J then
                    c()
                end
            end)
            task.delay(0, function()
                H.TimePosition = 0.7
                task.spawn(function()
                    task.wait(0.3)
                    if H then
                        H:AdjustSpeed(math.huge)
                    end
                end)
            end)
        end
    end
    local function E()
        G()
        if not J then
            return
        end
        local A = C.Character
        local H = A and A:FindFirstChildOfClass("Humanoid")
        J = false
        _G.invisibleStealEnabled = false
        for A, A in pairs(V) do
            pcall(function()
                A:Stop()
            end)
        end
        V = {}
        if B then
            B:Disconnect()
            B = nil
        end
        for A, A in ipairs(Y) do
            if A then
                A:Disconnect()
            end
        end
        Y = {}
        w()
        G()
        if H then
            pcall(function()
                H:ChangeState(Enum.HumanoidStateType.GettingUp)
            end)
        end
        if _G.updateMovementPanelInvisVisual then
            pcall(_G.updateMovementPanelInvisVisual, false)
        end
        if updateVisualState then
            updateVisualState(false)
        end
    end
    local function Y()
        if J then
            return
        end
        local w = C.Character
        if not w then
            return
        end
        local A = w:FindFirstChildOfClass("Humanoid")
        if not A then
            return
        end
        J = true
        _G.invisibleStealEnabled = true
        if _G.updateMovementPanelInvisVisual then
            pcall(_G.updateMovementPanelInvisVisual, true)
        end
        if updateVisualState then
            updateVisualState(true)
        end
        V = {}
        I()
        local V = M()
        if V then
            task.wait(0.05)
            c()
            task.defer(function()
                if _G.resetBrainrotBeam then
                    pcall(_G.resetBrainrotBeam)
                end
                if _G.resetPlotBeam then
                    pcall(_G.resetPlotBeam)
                end
                task.wait(0.1)
                if _G.updateBrainrotBeam then
                    pcall(_G.updateBrainrotBeam)
                end
                if _G.createPlotBeam then
                    pcall(_G.createPlotBeam)
                end
            end)
            local V = nil
            local c = 5
            B = R.PreSimulation:Connect(function()
                if w and w:FindFirstChild("Humanoid") and w.Humanoid.Health > 0 and q then
                    local B = w.PrimaryPart or w:FindFirstChild("HumanoidRootPart")
                    if B then
                        if c > 0 then
                            c = c - 1
                            V = nil
                        elseif V and true then
                            local c = q.Position
                            local I = (c - V).Magnitude
                            if I > 3 and not _G.RecoveryInProgress then
                                V = nil
                                b(c)
                                if _G.AutoRecoverLagback and _G.toggleInvisibleSteal then
                                    _G.RecoveryInProgress = true
                                    task.spawn(function()
                                        pcall(_G.toggleInvisibleSteal)
                                        task.wait(0.5)
                                        pcall(_G.toggleInvisibleSteal)
                                        _G.RecoveryInProgress = false
                                    end)
                                end
                            end
                        end
                        if O then
                            O.CanCollide = false
                        end
                        for c, c in pairs(q:GetChildren()) do
                            if c:IsA("Attachment") or c:IsA("Beam") then
                                c:Destroy()
                            end
                        end
                        local c = _G.InvisStealAngle or 180
                        local I = (_G.SinkSliderValue or 5) * 0.5
                        local M = B.CFrame - Vector3.new(0, I, 0)
                        q.CFrame = M * CFrame.Angles(math.rad(c), 0, 0)
                        q.Velocity = B.Velocity
                        q.CanCollide = false
                        V = q.Position
                    end
                end
            end)
        end
    end
    local V = Instance.new("ScreenGui")
    V.Name = "XiInvisPanel"
    V.ResetOnSpawn = false
    V.Parent = gethui and gethui() or game:GetService("CoreGui")
    V.Enabled = i.ShowInvisPanel
    local c = {
        BG = Color3.fromRGB(10, 20, 35),
        SURF = Color3.fromRGB(15, 30, 50),
        SURF2 = Color3.fromRGB(16, 24, 44),
        TEXT = Color3.fromRGB(220, 240, 255),
        DIM = Color3.fromRGB(80, 126, 180),
        AQUA_STROKE = Color3.fromRGB(65, 120, 225),
        GREEN1 = Color3.fromRGB(18, 88, 58),
        GREEN2 = Color3.fromRGB(21, 120, 76),
        GREEN_STROKE = Color3.fromRGB(60, 185, 120),
        OFF_BG = Color3.fromRGB(18, 28, 52),
        OFF_TEXT = Color3.fromRGB(70, 112, 160),
        SLIDER_BG = Color3.fromRGB(18, 28, 52),
        SLIDER_FILL1 = Color3.fromRGB(60, 154, 220),
        SLIDER_FILL2 = Color3.fromRGB(30, 119, 170),
    }
    local function B(I, M)
        local w = Instance.new("UICorner")
        w.CornerRadius = UDim.new(0, M)
        w.Parent = I
        return w
    end
    local function I(M, w, b, A)
        local H = Instance.new("UIStroke")
        H.Color = w
        H.Thickness = b or 1
        H.Transparency = A or 0
        H.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        H.Parent = M
        return H
    end
    local function M(w, b, A, H, Z)
        f:Create(w, TweenInfo.new(b or 0.2, H or Enum.EasingStyle.Quint, Z or Enum.EasingDirection.Out), A)
            :Play()
    end
    local function w(b, A, H, Z)
        local k = Instance.new("UIGradient")
        k.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, A), ColorSequenceKeypoint.new(1, H) })
        k.Rotation = Z or 0
        k.Parent = b
        return k
    end
    local b = Instance.new("Frame", V)
    b.Size = UDim2.new(0, 250, 0, 418)
    b.Position = UDim2.new(
        i.Positions.InvisPanel.X,
        i.Positions.InvisPanel.OffsetX or 0,
        i.Positions.InvisPanel.Y,
        i.Positions.InvisPanel.OffsetY or 0
    )
    b.BackgroundColor3 = c.BG
    b.BackgroundTransparency = 0
    b.BorderSizePixel = 0
    b.ClipsDescendants = true
    B(b, 18)
    I(b, c.AQUA_STROKE, 1.2, 0.28)
    local V = Instance.new("Frame", b)
    V.Size = UDim2.new(1, 0, 0, 44)
    V.BackgroundTransparency = 1
    D(V, b, "InvisPanel")
    local A = Instance.new("TextLabel", V)
    A.Size = UDim2.new(1, -28.0, 0, 24)
    A.Position = UDim2.new(0, 14, 0, 10)
    A.BackgroundTransparency = 1
    A.Text = "INVISIBLE STEAL"
    A.Font = Enum.Font.FredokaOne
    A.TextSize = 18
    A.TextColor3 = c.TEXT
    A.TextXAlignment = Enum.TextXAlignment.Center
    local V = Instance.new("Frame", b)
    V.AnchorPoint = Vector2.new(0.5, 0)
    V.Position = UDim2.new(0.5, 0, 0, 38)
    V.Size = UDim2.new(0, 108, 0, 1)
    V.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    V.BackgroundTransparency = 0.15
    V.BorderSizePixel = 0
    local V = Instance.new("Frame", b)
    V.Name = "ContentBox"
    V.Position = UDim2.fromOffset(10, 48)
    V.Size = UDim2.new(1, -20.0, 1, -60.0)
    V.BackgroundColor3 = c.SURF
    V.BorderSizePixel = 0
    B(V, 16)
    I(V, c.AQUA_STROKE, 1, 0.48)
    local b = Instance.new("Frame", V)
    b.BackgroundTransparency = 1
    b.Size = UDim2.new(1, -10.0, 1, -10.0)
    b.Position = UDim2.fromOffset(5, 5)
    local V = Instance.new("UIListLayout", b)
    V.Padding = UDim.new(0, 8)
    V.SortOrder = Enum.SortOrder.LayoutOrder
    local V = nil
    local function A(H, Z, k, u)
        local g = Instance.new("TextButton")
        g.AutoButtonColor = false
        g.Size = UDim2.new(1, 0, 0, 36)
        g.BackgroundColor3 = c.SURF2
        g.BackgroundTransparency = 0.02
        g.BorderSizePixel = 0
        g.Text = ""
        g.Parent = H
        B(g, 11)
        local H = I(g, c.AQUA_STROKE, 1, 0.52)
        local h = Instance.new("TextLabel")
        h.BackgroundTransparency = 1
        h.Position = UDim2.fromOffset(10, 0)
        h.Size = UDim2.new(1, -82.0, 1, 0)
        h.Font = Enum.Font.FredokaOne
        h.Text = Z
        h.TextColor3 = c.TEXT
        h.TextSize = 11
        h.TextXAlignment = Enum.TextXAlignment.Left
        h.Parent = g
        local Z = Instance.new("Frame")
        Z.Size = UDim2.fromOffset(58, 22)
        Z.Position = UDim2.new(1, -68.0, 0.5, -11.0)
        Z.BackgroundColor3 = c.OFF_BG
        Z.BorderSizePixel = 0
        Z.Parent = g
        B(Z, 7)
        local h = I(Z, c.AQUA_STROKE, 1, 0.55)
        local L = Instance.new("Frame")
        L.Size = UDim2.new(1, 0, 1, 0)
        L.BackgroundTransparency = 1
        L.BorderSizePixel = 0
        L.Parent = Z
        B(L, 7)
        w(L, c.GREEN1, c.GREEN2, 0)
        local s = Instance.new("TextLabel")
        s.BackgroundTransparency = 1
        s.Size = UDim2.fromScale(1, 1)
        s.Font = Enum.Font.FredokaOne
        s.TextSize = 10
        s.Parent = Z
        local N = k
        local k = false
        local function PG(CG)
            if N then
                if CG then
                    Z.BackgroundColor3 = c.GREEN1
                    L.BackgroundTransparency = 0
                    s.Text = "ON"
                    s.TextColor3 = Color3.fromRGB(232, 255, 240)
                    h.Color = c.GREEN_STROKE
                    h.Transparency = 0.22
                else
                    M(Z, 0.18, { BackgroundColor3 = c.GREEN1 })
                    M(L, 0.18, { BackgroundTransparency = 0 })
                    s.Text = "ON"
                    M(s, 0.12, { TextColor3 = Color3.fromRGB(232, 255, 240) })
                    M(h, 0.18, { Transparency = 0.22, Color = c.GREEN_STROKE })
                end
            else
                if CG then
                    Z.BackgroundColor3 = c.OFF_BG
                    L.BackgroundTransparency = 1
                    s.Text = "OFF"
                    s.TextColor3 = c.OFF_TEXT
                    h.Color = c.AQUA_STROKE
                    h.Transparency = 0.55
                else
                    M(Z, 0.18, { BackgroundColor3 = c.OFF_BG })
                    M(L, 0.18, { BackgroundTransparency = 1 })
                    s.Text = "OFF"
                    M(s, 0.12, { TextColor3 = c.OFF_TEXT })
                    M(h, 0.18, { Transparency = 0.55, Color = c.AQUA_STROKE })
                end
            end
        end
        local function Z(h, L, s)
            N = h and true or false
            PG(L)
            if u and not k and not s then
                u(N)
            end
        end
        PG(true)
        g.MouseEnter:Connect(function()
            M(g, 0.14, { BackgroundColor3 = Color3.fromRGB(20, 31, 58) })
            M(H, 0.14, { Transparency = 0.38 })
        end)
        g.MouseLeave:Connect(function()
            M(g, 0.14, { BackgroundColor3 = c.SURF2 })
            M(H, 0.14, { Transparency = 0.52 })
        end)
        g.MouseButton1Click:Connect(function()
            Z(not N, false, false)
        end)
        return {
            Set = function(H, u, g)
                k = true
                Z(H, u, g)
                k = false
            end,
            Get = function()
                return N
            end,
        }
    end
    local function H(Z, k, u, g)
        local h = Instance.new("Frame")
        h.Size = UDim2.new(1, 0, 0, 36)
        h.BackgroundColor3 = c.SURF2
        h.BackgroundTransparency = 0.02
        h.BorderSizePixel = 0
        h.Parent = Z
        B(h, 11)
        local Z = I(h, c.AQUA_STROKE, 1, 0.52)
        local L = Instance.new("TextLabel")
        L.BackgroundTransparency = 1
        L.Position = UDim2.fromOffset(10, 0)
        L.Size = UDim2.new(1, -76.0, 1, 0)
        L.Font = Enum.Font.FredokaOne
        L.Text = k
        L.TextColor3 = c.TEXT
        L.TextSize = 11
        L.TextXAlignment = Enum.TextXAlignment.Left
        L.Parent = h
        local k = Instance.new("TextButton")
        k.AutoButtonColor = false
        k.Size = UDim2.fromOffset(46, 22)
        k.Position = UDim2.new(1, -56.0, 0.5, -11.0)
        k.BackgroundColor3 = c.OFF_BG
        k.BorderSizePixel = 0
        k.Text = tostring(u)
        k.Font = Enum.Font.FredokaOne
        k.TextColor3 = c.TEXT
        k.TextSize = 10
        k.Parent = h
        B(k, 7)
        I(k, c.AQUA_STROKE, 1, 0.55)
        h.MouseEnter:Connect(function()
            M(h, 0.14, { BackgroundColor3 = Color3.fromRGB(20, 31, 58) })
            M(Z, 0.14, { Transparency = 0.38 })
        end)
        h.MouseLeave:Connect(function()
            M(h, 0.14, { BackgroundColor3 = c.SURF2 })
            M(Z, 0.14, { Transparency = 0.52 })
        end)
        k.MouseButton1Click:Connect(function()
            k.Text = "..."
            local Z
            Z = j.InputBegan:Connect(function(u)
                if u.UserInputType == Enum.UserInputType.Keyboard then
                    local h = u.KeyCode.Name
                    if g then
                        g(h, u.KeyCode)
                    end
                    k.Text = h
                    if Z then
                        Z:Disconnect()
                    end
                end
            end)
        end)
        return {
            Set = function(Z)
                k.Text = tostring(Z)
            end,
        }
    end
    local function Z(k, u, g, h, L, s, N)
        local PG = Instance.new("Frame")
        PG.Size = UDim2.new(1, 0, 0, 46)
        PG.BackgroundColor3 = c.SURF2
        PG.BackgroundTransparency = 0.02
        PG.BorderSizePixel = 0
        PG.Parent = k
        B(PG, 11)
        local k = I(PG, c.AQUA_STROKE, 1, 0.52)
        local CG = Instance.new("TextLabel")
        CG.BackgroundTransparency = 1
        CG.Position = UDim2.fromOffset(10, 3)
        CG.Size = UDim2.new(1, -56.0, 0, 16)
        CG.Font = Enum.Font.FredokaOne
        CG.Text = u
        CG.TextColor3 = c.TEXT
        CG.TextSize = 11
        CG.TextXAlignment = Enum.TextXAlignment.Left
        CG.Parent = PG
        local u = Instance.new("TextLabel")
        u.BackgroundTransparency = 1
        u.Position = UDim2.new(1, -34.0, 0, 3)
        u.Size = UDim2.fromOffset(24, 16)
        u.Font = Enum.Font.FredokaOne
        u.TextColor3 = c.TEXT
        u.TextSize = 10
        u.TextXAlignment = Enum.TextXAlignment.Right
        u.Parent = PG
        local CG = Instance.new("TextButton")
        CG.AutoButtonColor = false
        CG.Text = ""
        CG.Size = UDim2.new(1, -20.0, 0, 10)
        CG.Position = UDim2.fromOffset(10, 26)
        CG.BackgroundColor3 = c.SLIDER_BG
        CG.BorderSizePixel = 0
        CG.Parent = PG
        B(CG, 999)
        I(CG, c.AQUA_STROKE, 1, 0.65)
        local rG = Instance.new("Frame")
        rG.Size = UDim2.new(0, 0, 1, 0)
        rG.BackgroundColor3 = c.SLIDER_FILL1
        rG.BorderSizePixel = 0
        rG.Parent = CG
        B(rG, 999)
        w(rG, c.SLIDER_FILL1, c.SLIDER_FILL2, 0)
        local RG = Instance.new("Frame")
        RG.AnchorPoint = Vector2.new(0.5, 0.5)
        RG.Position = UDim2.new(0, 0, 0.5, 0)
        RG.Size = UDim2.fromOffset(12, 12)
        RG.BackgroundColor3 = c.TEXT
        RG.BorderSizePixel = 0
        RG.Parent = CG
        B(RG, 999)
        I(RG, c.AQUA_STROKE, 1, 0.3)
        local dG = L
        local function nG(fG)
            if s and s < 1 then
                return string.format("%.2f", fG)
            end
            if math.abs(fG - math.floor(fG)) < 0.001 then
                return tostring(math.floor(fG + 0.5))
            end
            return string.format("%.1f", fG)
        end
        local function fG(JG, aG)
            dG = math.clamp(JG, g, h)
            if s and s > 0 then
                dG = math.floor(dG / s + 0.5) * s
                dG = math.clamp(dG, g, h)
            end
            local s = (dG - g) / (h - g)
            rG.Size = UDim2.new(s, 0, 1, 0)
            RG.Position = UDim2.new(s, 0, 0.5, 0)
            u.Text = nG(dG)
            if N and not aG then
                N(dG)
            end
        end
        local function u(s)
            local N = CG.AbsolutePosition.X
            local rG = CG.AbsoluteSize.X
            local RG = math.clamp((s.Position.X - N) / rG, 0, 1)
            local s = g + (h - g) * RG
            fG(s, false)
        end
        fG(L, true)
        CG.InputBegan:Connect(function(g)
            if g.UserInputType == Enum.UserInputType.MouseButton1 then
                V = { setFromInput = u }
                u(g)
            end
        end)
        PG.MouseEnter:Connect(function()
            if not V then
                M(PG, 0.14, { BackgroundColor3 = Color3.fromRGB(20, 31, 58) })
                M(k, 0.14, { Transparency = 0.38 })
            end
        end)
        PG.MouseLeave:Connect(function()
            if not V then
                M(PG, 0.14, { BackgroundColor3 = c.SURF2 })
                M(k, 0.14, { Transparency = 0.52 })
            end
        end)
        return {
            Set = function(k, u)
                fG(k, u)
            end,
            Get = function()
                return dG
            end,
        }
    end
    local function k(u)
        local g = Instance.new("Frame")
        g.Name = "StealSpeedEmbedded"
        g.Size = UDim2.new(1, 0, 0, 64)
        g.BackgroundColor3 = c.SURF2
        g.BackgroundTransparency = 0.02
        g.BorderSizePixel = 0
        g.Parent = u
        B(g, 11)
        local u = I(g, c.AQUA_STROKE, 1, 0.52)
        local h = Instance.new("TextLabel")
        h.BackgroundTransparency = 1
        h.Position = UDim2.fromOffset(10, 6)
        h.Size = UDim2.new(1, -82.0, 0, 16)
        h.Font = Enum.Font.FredokaOne
        h.Text = "Speed"
        h.TextColor3 = c.TEXT
        h.TextSize = 11
        h.TextXAlignment = Enum.TextXAlignment.Left
        h.Parent = g
        local h = Instance.new("Frame")
        h.Size = UDim2.fromOffset(58, 22)
        h.Position = UDim2.new(1, -68.0, 0, 5)
        h.BackgroundColor3 = c.OFF_BG
        h.BorderSizePixel = 0
        h.Parent = g
        B(h, 7)
        I(h, c.AQUA_STROKE, 1, 0.55)
        local L = Instance.new("TextLabel")
        L.BackgroundTransparency = 1
        L.Size = UDim2.fromScale(1, 1)
        L.Font = Enum.Font.FredokaOne
        L.TextColor3 = c.TEXT
        L.TextSize = 10
        L.Parent = h
        local h = Instance.new("TextButton")
        h.AutoButtonColor = false
        h.Text = ""
        h.Size = UDim2.new(1, -20.0, 0, 10)
        h.Position = UDim2.fromOffset(10, 36)
        h.BackgroundColor3 = c.SLIDER_BG
        h.BorderSizePixel = 0
        h.Parent = g
        B(h, 999)
        I(h, c.AQUA_STROKE, 1, 0.65)
        local s = Instance.new("Frame")
        s.Size = UDim2.new(0, 0, 1, 0)
        s.BackgroundColor3 = c.SLIDER_FILL1
        s.BorderSizePixel = 0
        s.Parent = h
        B(s, 999)
        w(s, c.SLIDER_FILL1, c.SLIDER_FILL2, 0)
        local w = Instance.new("Frame")
        w.AnchorPoint = Vector2.new(0.5, 0.5)
        w.Position = UDim2.new(0, 0, 0.5, 0)
        w.Size = UDim2.fromOffset(12, 12)
        w.BackgroundColor3 = c.TEXT
        w.BorderSizePixel = 0
        w.Parent = h
        B(w, 999)
        I(w, c.AQUA_STROKE, 1, 0.3)
        local B, I = 5, 40
        local N =
            math.clamp(tonumber(P.GetStealSpeed and P.GetStealSpeed() or i.StealSpeed or 25.5) or 25.5, B, I)
        local function PG(CG)
            return string.format("%.1f", CG)
        end
        local function CG(rG)
            N = math.clamp(tonumber(rG) or N, B, I)
            local rG = (N - B) / (I - B)
            s.Size = UDim2.new(rG, 0, 1, 0)
            w.Position = UDim2.new(rG, 0, 0.5, 0)
            L.Text = PG(N)
        end
        local function w(L, s)
            local PG = math.clamp(tonumber(L) or N, B, I)
            CG(PG)
            if P.SetStealSpeed then
                P.SetStealSpeed(PG, s)
            else
                i.StealSpeed = PG
                if not s then
                    U()
                end
            end
        end
        local function L(s)
            local PG = h.AbsolutePosition.X
            local rG = h.AbsoluteSize.X
            if rG <= 0 then
                return
            end
            local RG = math.clamp((s.Position.X - PG) / rG, 0, 1)
            local s = B + (I - B) * RG
            w(s, false)
        end
        CG(N)
        h.InputBegan:Connect(function(B)
            if
                B.UserInputType == Enum.UserInputType.MouseButton1
                or B.UserInputType == Enum.UserInputType.Touch
            then
                V = { setFromInput = L }
                L(B)
            end
        end)
        g.MouseEnter:Connect(function()
            if not V then
                M(g, 0.14, { BackgroundColor3 = Color3.fromRGB(20, 31, 58) })
                M(u, 0.14, { Transparency = 0.38 })
            end
        end)
        g.MouseLeave:Connect(function()
            if not V then
                M(g, 0.14, { BackgroundColor3 = c.SURF2 })
                M(u, 0.14, { Transparency = 0.52 })
            end
        end)
        local c = {
            Set = function(B, I)
                if I then
                    CG(B)
                else
                    w(B, false)
                end
            end,
            Get = function()
                return N
            end,
        }
        P._ssUpdateBtn = function()
            if c then
                c.Set(P.GetStealSpeed and P.GetStealSpeed() or i.StealSpeed or N, true)
            end
        end
        return c
    end
    local c
    local function B(I)
        if c then
            c.Set(I, false, true)
        end
        if _G.updateMovementPanelInvisVisual then
            pcall(_G.updateMovementPanelInvisVisual, I)
        end
    end
    c = A(b, "Enabled", _G.invisibleStealEnabled or false, function()
        if _G.toggleInvisibleSteal then
            pcall(_G.toggleInvisibleSteal)
            B(_G.invisibleStealEnabled or false)
        end
    end)
    local c = H(b, "Keybind", i.InvisToggleKey, function(I, M)
        i.InvisToggleKey = I
        _G.INVISIBLE_STEAL_KEY = M
        U()
    end)
    local I = Z(b, "Rotation", 0, 360, i.InvisStealAngle, 1, function(M)
        i.InvisStealAngle = M
        _G.InvisStealAngle = M
        U()
    end)
    local M = Z(b, "Depth", 0, 18, i.SinkSliderValue, 1, function(w)
        i.SinkSliderValue = w
        _G.SinkSliderValue = w
        U()
    end)
    local w = A(b, "Auto Recover Lagback", _G.AutoRecoverLagback, function(H)
        _G.AutoRecoverLagback = H
        i.AutoRecoverLagback = H
        U()
    end)
    local H = A(b, "Auto Invis On Steal", i.AutoInvisDuringSteal, function(A)
        i.AutoInvisDuringSteal = A
        _G.AutoInvisDuringSteal = A
        if _G.syncMainAutoInvisToggle then
            pcall(_G.syncMainAutoInvisToggle, A)
        end
        U()
        F("AUTO INVIS", A and "ENABLED" or "DISABLED")
    end)
    local A = k(b)
    _G.syncMiniInvisAutoStealToggle = function(b)
        if H then
            H.Set(b, false, true)
        end
    end
    B(_G.invisibleStealEnabled or false)
    if c then
        c.Set(i.InvisToggleKey)
    end
    if I then
        I.Set(i.InvisStealAngle, true)
    end
    if M then
        M.Set(i.SinkSliderValue, true)
    end
    if w then
        w.Set(_G.AutoRecoverLagback, true, true)
    end
    if A then
        A.Set(i.StealSpeed or STEAL_SPEED, true)
    end
    if H then
        H.Set(i.AutoInvisDuringSteal, true, true)
    end
    j.InputChanged:Connect(function(c)
        if c.UserInputType == Enum.UserInputType.MouseMovement and V then
            V.setFromInput(c)
        end
    end)
    j.InputEnded:Connect(function(c)
        if c.UserInputType == Enum.UserInputType.MouseButton1 then
            V = nil
        end
    end)
    _G.toggleInvisibleSteal = function()
        if J then
            E()
        else
            Y()
        end
    end
    j.InputBegan:Connect(function(V)
        if j:GetFocusedTextBox() then
            return
        end
        if V.KeyCode == (_G.INVISIBLE_STEAL_KEY or Enum.KeyCode.V) then
            pcall(_G.toggleInvisibleSteal)
            if _G.updateMovementPanelInvisVisual then
                pcall(_G.updateMovementPanelInvisVisual, _G.invisibleStealEnabled or false)
            end
            if B then
                B(_G.invisibleStealEnabled or false)
            end
        end
    end)
    local function V(c)
        m()
        G()
        p = 0
        pcall(function()
            for B, B in pairs(a.CurrentCamera:GetChildren()) do
                if B:IsA("BasePart") and B.Name == "HumanoidRootPart" then
                    B:Destroy()
                end
            end
        end)
        if q then
            pcall(function()
                q:Destroy()
            end)
            q = nil
        end
        if O then
            pcall(function()
                O:Destroy()
            end)
            O = nil
        end
        J = false
        _G.invisibleStealEnabled = false
        if _G.updateMovementPanelInvisVisual then
            pcall(_G.updateMovementPanelInvisVisual, false)
        end
        task.wait(0.2)
        local J = a.CurrentCamera
        if J and c then
            local O = c:FindFirstChildOfClass("Humanoid")
            if O then
                J.CameraSubject = O
                J.CameraType = Enum.CameraType.Custom
            end
        end
    end
    C.CharacterAdded:Connect(V)
    local function J()
        local V = C.Character
        if V then
            local O = V:FindFirstChildOfClass("Humanoid")
            if O then
                O.Died:Connect(function()
                    m()
                    G()
                    p = 0
                end)
            end
        end
    end
    J()
    C.CharacterAdded:Connect(function()
        task.wait(0.1)
        J()
    end)
    task.spawn(function()
        local J = nil
        _G.AntiDieConnection = nil
        _G.AntiDieDisabled = false
        local function V()
            if _G.AntiDieDisabled then
                return
            end
            local O = C.Character
            if not O then
                return
            end
            local q = O:FindFirstChildOfClass("Humanoid")
            if not q then
                return
            end
            if J then
                pcall(function()
                    J:Disconnect()
                end)
            end
            J = q:GetPropertyChangedSignal("Health"):Connect(function()
                if _G.AntiDieDisabled then
                    return
                end
                if q.Health <= 0 then
                    q.Health = q.MaxHealth
                end
            end)
            _G.AntiDieConnection = J
        end
        _G.setupAntiDie = V
        V()
        C.CharacterAdded:Connect(function()
            task.wait(0.5)
            if not _G.AntiDieDisabled then
                V()
            end
        end)
    end)
end)
task.spawn(function()
    local J = false
    local V = false
    local O = false
    task.wait(1)
    while task.wait(0.1) do
        if _G.AutoInvisDuringSteal == false then
            J = false
            O = false
        else
            local q = C:GetAttribute("Stealing")
            if q and not J then
                V = _G.invisibleStealEnabled or false
                if not _G.invisibleStealEnabled and _G.toggleInvisibleSteal then
                    task.delay(0.25, function()
                        if C:GetAttribute("Stealing") and not _G.invisibleStealEnabled then
                            pcall(_G.toggleInvisibleSteal)
                            O = true
                        end
                    end)
                end
            end
            if not q and O and _G.invisibleStealEnabled and _G.toggleInvisibleSteal then
                pcall(_G.toggleInvisibleSteal)
                O = false
            end
            J = q
        end
    end
end)
task.spawn(function()
    local function J()
        local V = C.Character or C.CharacterAdded:Wait()
        local O = V:WaitForChild("HumanoidRootPart")
        local q = V:WaitForChild("Humanoid")
        return V, O, q
    end
    local function V(O)
        for q, q in ipairs(O:GetDescendants()) do
            if q:IsA("BillboardGui") then
                local O = q:FindFirstChildWhichIsA("TextLabel", true)
                if O and O.Text:find("!") then
                    return true
                end
            end
        end
        return false
    end
    local function O(q)
        for c, c in ipairs(q:GetDescendants()) do
            if c:IsA("BasePart") and c ~= q then
                c.Transparency = 0.5
                c.CanCollide = false
                c.CanTouch = false
                c.CanQuery = false
            elseif c:IsA("BillboardGui") and c.Name ~= "SentryLabel" then
                c:Destroy()
            elseif c:IsA("Decal") or c:IsA("Texture") then
                c.Transparency = 0.5
            end
        end
        if q:IsA("BasePart") and q.Name ~= "ProxyVisual" then
            q.Transparency = 1
            q.CanCollide = false
        end
    end
    local function q()
        local c, c = J()
        local B, Y = nil, math.huge
        for I, I in ipairs(a:GetDescendants()) do
            if I.Name:match("^Sentry_") then
                if V(I) then
                    local p = I:IsA("BasePart") and I or I:FindFirstChildWhichIsA("BasePart", true)
                    if p then
                        local G = (c.Position - p.Position).Magnitude
                        if G < Y then
                            Y = G
                            B = I
                        end
                    end
                end
            end
        end
        return B
    end
    while true do
        if i.AutoDestroyTurrets then
            if C:GetAttribute("Stealing") == true then
                task.wait(0.5)
            else
                local c = q()
                if c then
                    while c and c.Parent and C:GetAttribute("Stealing") ~= true do
                        local q, B, Y = J()
                        local J = C.Backpack:FindFirstChild("Bat") or q:FindFirstChild("Bat")
                        O(c)
                        local O = B.CFrame.LookVector * 4
                        local I = CFrame.new(B.Position + O, B.Position)
                        if c:IsA("Model") then
                            c:PivotTo(I)
                        elseif c:IsA("BasePart") then
                            c.CFrame = I
                        end
                        if J then
                            if J.Parent ~= q then
                                Y:EquipTool(J)
                            end
                            J:Activate()
                        end
                        task.wait(0.1)
                        if not V(c) then
                            break
                        end
                    end
                end
            end
        end
        task.wait(0.1)
    end
end)
P.FOV_MANAGER = { activeCount = 0, conn = nil, forcedFOV = 70 }
function P.FOV_MANAGER:Start()
    if self.conn then
        return
    end
    self.forcedFOV = i.FOV or 70
    self.conn = R.Heartbeat:Connect(function()
        local J = a.CurrentCamera
        if J then
            local V = i.FOV or self.forcedFOV
            if J.FieldOfView ~= V then
                J.FieldOfView = V
            end
        end
    end)
end
function P.FOV_MANAGER:Stop()
    if self.conn then
        self.conn:Disconnect()
        self.conn = nil
    end
end
function P.FOV_MANAGER:Push()
    self.activeCount = self.activeCount + 1
    self:Start()
end
function P.FOV_MANAGER:Pop()
    if self.activeCount > 0 then
        self.activeCount = self.activeCount - 1
    end
    if self.activeCount == 0 then
        self:Stop()
    end
end
P.ANTI_BEE_DISCO = {
    running = false,
    connections = {},
    originalMoveFunction = nil,
    controlsProtected = false,
    badLightingNames = { Blue = true, DiscoEffect = true, BeeBlur = true, ColorCorrection = true },
}
function P.ANTI_BEE_DISCO.nuke(J)
    if not J or not J.Parent then
        return
    end
    if P.ANTI_BEE_DISCO.badLightingNames[J.Name] then
        pcall(function()
            J:Destroy()
        end)
    end
end
function P.ANTI_BEE_DISCO.disconnectAll()
    for J, J in ipairs(P.ANTI_BEE_DISCO.connections) do
        if typeof(J) == "RBXScriptConnection" then
            J:Disconnect()
        end
    end
    P.ANTI_BEE_DISCO.connections = {}
end
function P.ANTI_BEE_DISCO.protectControls()
    if P.ANTI_BEE_DISCO.controlsProtected then
        return
    end
    pcall(function()
        local J = C.PlayerScripts
        local V = J:FindFirstChild("PlayerModule")
        if not V then
            return
        end
        local J = require(V):GetControls()
        if not J then
            return
        end
        local V = P.ANTI_BEE_DISCO
        if not V.originalMoveFunction then
            V.originalMoveFunction = J.moveFunction
        end
        local function O(q, c, B)
            if V.originalMoveFunction then
                V.originalMoveFunction(q, c, B)
            end
        end
        table.insert(
            V.connections,
            R.Heartbeat:Connect(function()
                if not V.running or not i.AntiBeeDisco then
                    return
                end
                local q = os.clock()
                if q - (V._lastCtrlTick or 0) < 0.2 then
                    return
                end
                V._lastCtrlTick = q
                if J.moveFunction ~= O then
                    J.moveFunction = O
                end
            end)
        )
        J.moveFunction = O
        V.controlsProtected = true
    end)
end
function P.ANTI_BEE_DISCO.restoreControls()
    if not P.ANTI_BEE_DISCO.controlsProtected then
        return
    end
    pcall(function()
        local J = C.PlayerScripts:FindFirstChild("PlayerModule")
        if not J then
            return
        end
        local V = require(J):GetControls()
        local J = P.ANTI_BEE_DISCO
        if V and J.originalMoveFunction then
            V.moveFunction = J.originalMoveFunction
            J.controlsProtected = false
        end
    end)
end
function P.ANTI_BEE_DISCO.blockBuzzingSound()
    pcall(function()
        local J = C.PlayerScripts:FindFirstChild("Bee", true)
        if J then
            local V = J:FindFirstChild("Buzzing")
            if V and V:IsA("Sound") then
                V:Stop()
                V.Volume = 0
            end
        end
    end)
end
function P.ANTI_BEE_DISCO.Enable()
    local J = P.ANTI_BEE_DISCO
    if J.running then
        return
    end
    J.running = true
    for V, V in ipairs(_:GetDescendants()) do
        J.nuke(V)
    end
    table.insert(
        J.connections,
        _.DescendantAdded:Connect(function(V)
            if not J.running or not i.AntiBeeDisco then
                return
            end
            J.nuke(V)
        end)
    )
    J.protectControls()
    table.insert(
        J.connections,
        R.Heartbeat:Connect(function()
            if not J.running or not i.AntiBeeDisco then
                return
            end
            local V = os.clock()
            if V - (J._lastBuzzTick or 0) < 0.5 then
                return
            end
            J._lastBuzzTick = V
            J.blockBuzzingSound()
        end)
    )
    P.FOV_MANAGER:Push()
    F("ANTI-BEE & DISCO", "Enabled")
end
function P.ANTI_BEE_DISCO.Disable()
    local J = P.ANTI_BEE_DISCO
    if not J.running then
        return
    end
    J.running = false
    J.restoreControls()
    J.disconnectAll()
    P.FOV_MANAGER:Pop()
    F("ANTI-BEE & DISCO", "Disabled")
end
_G.ANTI_BEE_DISCO = P.ANTI_BEE_DISCO
if i.AntiBeeDisco then
    task.spawn(function()
        task.wait(1)
        if P.ANTI_BEE_DISCO.Enable then
            P.ANTI_BEE_DISCO.Enable()
        end
    end)
end
local function J(V)
    if V and i.FOV and V.FieldOfView ~= i.FOV then
        V.FieldOfView = i.FOV
    end
end
local function V(O)
    if not O then
        return
    end
    J(O)
    O:GetPropertyChangedSignal("FieldOfView"):Connect(function()
        J(O)
    end)
end
V(a.CurrentCamera)
a:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
    V(a.CurrentCamera)
end)
task.spawn(function()
    if d then
        return
    end
    if x:FindFirstChild("IuCZFoVLhUds") then
        x.XiStatusHUD:Destroy()
    end
    local J = Instance.new("ScreenGui")
    J.Name = "IuCZFoVLhUds"
    J.ResetOnSpawn = false
    J.IgnoreGuiInset = true
    J.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    J.Parent = gethui and gethui() or game:GetService("CoreGui")
    local function V(O, q)
        local c = Instance.new(O)
        for O, B in pairs(q or {}) do
            c[O] = B
        end
        return c
    end
    local O = V(
        "Frame",
        {
            Name = "Outer",
            AnchorPoint = Vector2.new(0.5, 1),
            Size = UDim2.new(0, 470, 0, 44),
            Position = UDim2.new(0.5, 0, 1, -86.0),
            BackgroundColor3 = Color3.fromRGB(15, 16, 20),
            BorderSizePixel = 0,
            Parent = J,
        }
    )
    local J = Instance.new("UIScale")
    J.Name = "ResponsiveScale"
    J.Parent = O
    local function q()
        local c = Q and Q.ViewportSize or Vector2.new(1920, 1080)
        local B = math.min(c.X / 1920, c.Y / 1080)
        J.Scale = math.clamp(B, 0.72, 1)
    end
    q()
    if Q then
        Q:GetPropertyChangedSignal("ViewportSize"):Connect(q)
    end
    V("UICorner", { CornerRadius = UDim.new(0, 11), Parent = O })
    V(
        "UIStroke",
        {
            Color = Color3.fromRGB(40, 112, 160),
            Thickness = 1,
            Transparency = 0.18,
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            Parent = O,
        }
    )
    do
        local J = Instance.new("UIGradient")
        J.Rotation = 0
        J.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(22, 24, 30)),
            ColorSequenceKeypoint.new(0.35, Color3.fromRGB(17, 18, 23)),
            ColorSequenceKeypoint.new(0.72, Color3.fromRGB(13, 14, 18)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(18, 19, 24)),
        })
        J.Parent = O
    end
    local J = V(
        "Frame",
        {
            Name = "Sheen",
            Position = UDim2.new(0, 10, 0, 3),
            Size = UDim2.new(1, -20.0, 0, 1),
            BackgroundColor3 = Color3.fromRGB(255, 255, 255),
            BackgroundTransparency = 0.95,
            BorderSizePixel = 0,
            ZIndex = 3,
            Parent = O,
        }
    )
    local J = V(
        "Frame",
        {
            Name = "Dot",
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.new(0, 18, 0.5, 0),
            Size = UDim2.new(0, 8, 0, 8),
            BackgroundColor3 = Color3.fromRGB(50, 140, 200),
            BorderSizePixel = 0,
            ZIndex = 2,
            Parent = O,
        }
    )
    V("UICorner", { CornerRadius = UDim.new(1, 0), Parent = J })
    V(
        "UIStroke",
        {
            Color = Color3.fromRGB(80, 154, 220),
            Thickness = 1,
            Transparency = 0.35,
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            Parent = J,
        }
    )
    local J = V(
        "TextLabel",
        {
            Name = "Title",
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 30, 0, 0),
            Size = UDim2.new(0, 170, 1, 0),
            Font = Enum.Font.FredokaOne,
            Text = "LACASA PRIVATEEEEEEEE",
            TextSize = 15,
            TextColor3 = Color3.fromRGB(245, 246, 248),
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Center,
            ZIndex = 2,
            Parent = O,
        }
    )
    -- discord invite removed
    local function J(Q, q)
        local c = V(
            "Frame",
            {
                AnchorPoint = Vector2.new(0, 1),
                Position = UDim2.new(0, Q, 1, 0),
                Size = UDim2.new(0, 3, 0, q),
                BackgroundColor3 = Color3.fromRGB(50, 140, 200),
                BorderSizePixel = 0,
                ZIndex = 2,
                Parent = signalWrap,
            }
        )
        V("UICorner", { CornerRadius = UDim.new(0, 1), Parent = c })
    end
    J(0, 5)
    J(6, 9)
    J(12, 14)
    J(18, 18)
    local function J(Q)
        V(
            "Frame",
            {
                Position = UDim2.new(0, Q, 0.5, -10.0),
                Size = UDim2.new(0, 1, 0, 20),
                BackgroundColor3 = Color3.fromRGB(40, 112, 160),
                BackgroundTransparency = 0.15,
                BorderSizePixel = 0,
                ZIndex = 2,
                Parent = O,
            }
        )
    end
    J(310)
    J(382)
    local J = V(
        "TextLabel",
        {
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 324, 0, 6),
            Size = UDim2.new(0, 42, 0, 10),
            Font = Enum.Font.FredokaOne,
            Text = "FPS",
            TextSize = 9,
            TextColor3 = Color3.fromRGB(80, 126, 180),
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Center,
            ZIndex = 2,
            Parent = O,
        }
    )
    J.AutoLocalize = false
    local J = V(
        "TextLabel",
        {
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 324, 0, 17),
            Size = UDim2.new(0, 46, 0, 16),
            Font = Enum.Font.FredokaOne,
            Text = "0",
            TextSize = 14,
            TextColor3 = Color3.fromRGB(80, 154, 220),
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Center,
            ZIndex = 2,
            Parent = O,
        }
    )
    J.AutoLocalize = false
    local Q = V(
        "TextLabel",
        {
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 396, 0, 6),
            Size = UDim2.new(0, 50, 0, 10),
            Font = Enum.Font.FredokaOne,
            Text = "PING",
            TextSize = 9,
            TextColor3 = Color3.fromRGB(80, 126, 180),
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Center,
            ZIndex = 2,
            Parent = O,
        }
    )
    Q.AutoLocalize = false
    local Q = V(
        "TextLabel",
        {
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 396, 0, 17),
            Size = UDim2.new(0, 60, 0, 16),
            Font = Enum.Font.FredokaOne,
            Text = "0ms",
            TextSize = 14,
            TextColor3 = Color3.fromRGB(80, 154, 220),
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Center,
            ZIndex = 2,
            Parent = O,
        }
    )
    Q.AutoLocalize = false
    local V = 0
    local q = tick()
    R.Heartbeat:Connect(function()
        V += 1
        local c = tick()
        if c - q >= 1 then
            J.Text = tostring(V)
            if V >= 60 then
                J.TextColor3 = Color3.fromRGB(56, 214, 110)
            elseif V >= 30 then
                J.TextColor3 = Color3.fromRGB(0, 178, 255)
            else
                J.TextColor3 = Color3.fromRGB(60, 154, 220)
            end
            V = 0
            q = c
        end
    end)
    task.spawn(function()
        while O and O.Parent do
            local J = math.floor(C:GetNetworkPing() * 1000)
            Q.Text = tostring(J) .. "ms"
            if J < 80 then
                Q.TextColor3 = Color3.fromRGB(56, 214, 110)
            elseif J < 150 then
                Q.TextColor3 = Color3.fromRGB(0, 178, 255)
            else
                Q.TextColor3 = Color3.fromRGB(60, 154, 220)
            end
            task.wait(0.25)
        end
    end)
    if i.ShowUnlockButtonsHUD then
        task.defer(function()
            toggleUnlockBaseMenu(true)
        end)
    end
end)
task.spawn(function()
    if z and z:FindFirstChild("sFrame", true) then
        local J = z.sFrame:FindFirstChild("sList")
        if J then
            for Q, Q in ipairs(J:GetChildren()) do
                local J = Q:FindFirstChildOfClass("TextLabel")
                if J and J.Text == "Subspace Mine Esp" then
                    local J = Q:FindFirstChildWhichIsA("Frame")
                    if J then
                        local Q = J:FindFirstChildOfClass("TextButton")
                        if Q then
                            getgenv().subspaceMineESPToggleRef = subspaceMineESPToggleRef
                        end
                    end
                    break
                end
            end
        end
    end
    local J = {}
    local function Q(V)
        local O = V:match("SubspaceTripmine(.+)")
        if not O then
            return "Unknown"
        end
        local V = r:FindFirstChild(O)
        local q = V and V.DisplayName or O
        return q
    end
    local function V(O)
        local q = Q(O.Name)
        local Q = Instance.new("SelectionBox")
        Q.Name = "ESP_Hitbox"
        Q.Adornee = O
        Q.Color3 = Color3.fromRGB(50, 140, 200)
        Q.LineThickness = 0.05
        Q.Parent = O
        local c = Instance.new("BillboardGui")
        c.Name = "ESP_Label"
        c.Adornee = O
        c.Size = UDim2.new(0, 250, 0, 50)
        c.StudsOffset = Vector3.new(0, 2.5, 0)
        c.AlwaysOnTop = false
        c.Parent = O
        local z = Instance.new("TextLabel", c)
        z.Size = UDim2.new(1, 0, 1, 0)
        z.BackgroundTransparency = 1
        z.Text = q .. "'s Subspace Mine"
        z.TextColor3 = Color3.fromRGB(80, 154, 220)
        z.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        z.TextStrokeTransparency = 0
        z.Font = Enum.Font.FredokaOne
        z.TextSize = 16
        return { selectionBox = Q, billboardGui = c, mine = O }
    end
    local function Q()
        if not i.SubspaceMineESP then
            for O, q in pairs(J) do
                if q.selectionBox and q.selectionBox.Parent then
                    q.selectionBox:Destroy()
                end
                if q.billboardGui and q.billboardGui.Parent then
                    q.billboardGui:Destroy()
                end
                J[O] = nil
            end
            return
        end
        local O = a:FindFirstChild("ToolsAdds")
        if not O then
            return
        end
        local q = {}
        for c, c in pairs(O:GetChildren()) do
            if c.Name:match("^SubspaceTripmine") and c:IsA("BasePart") then
                q[c] = true
                if not J[c] then
                    J[c] = V(c)
                end
            end
        end
        for V, O in pairs(J) do
            if not q[V] or not V.Parent then
                if O.selectionBox and O.selectionBox.Parent then
                    O.selectionBox:Destroy()
                end
                if O.billboardGui and O.billboardGui.Parent then
                    O.billboardGui:Destroy()
                end
                J[V] = nil
            end
        end
    end
    if subspaceMineESPToggleRef then
        subspaceMineESPToggleRef.setFn = function(V)
            i.SubspaceMineESP = V
            if not V then
                for V, V in pairs(J) do
                    if V.selectionBox and V.selectionBox.Parent then
                        V.selectionBox:Destroy()
                    end
                    if V.billboardGui and V.billboardGui.Parent then
                        V.billboardGui:Destroy()
                    end
                end
                table.clear(J)
            end
        end
    end
    while true do
        task.wait(0.5)
        local J, J = pcall(Q)
    end
end)
task.spawn(function()
    local J = n:WaitForChild("Datas")
    local Q = require(J:WaitForChild("Animals"))
    local J = { "Strawberry Elephant", "Meowl", "Skibidi Toilet", "Headless Horseman" }
    local V = {}
    for O, O in ipairs(J) do
        V[string.lower(O)] = true
    end
    local function O(q, c)
        local z = Instance.new("UICorner")
        z.CornerRadius = UDim.new(0, c)
        z.Parent = q
        return z
    end
    local function q(c, z, B, Y, I)
        local p = Instance.new("UIPadding")
        p.PaddingLeft = UDim.new(0, z or 0)
        p.PaddingRight = UDim.new(0, B or 0)
        p.PaddingTop = UDim.new(0, Y or 0)
        p.PaddingBottom = UDim.new(0, I or 0)
        p.Parent = c
        return p
    end
    local function c()
        local z = {}
        local B = {}
        local Y = {}
        for I, I in ipairs(J) do
            table.insert(B, I)
            z[string.lower(I)] = true
        end
        for J, I in pairs(Q) do
            if type(J) == "string" and J ~= "" and type(I) == "table" then
                local Q = string.lower(J)
                local p = tostring(I.Rarity or "")
                if p == "Secret" and not z[Q] and not string.find(J, "Lucky Block", 1, true) then
                    z[Q] = true
                    table.insert(Y, J)
                end
            end
        end
        table.sort(Y, function(J, Q)
            return string.lower(J) < string.lower(Q)
        end)
        return B, Y, z
    end
    local J, Q, z = c()
    local c = Instance.new("ScreenGui")
    c.Name = "ejfsHpLRCcrc"
    c.ResetOnSpawn = false
    c.IgnoreGuiInset = true
    c.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    c.Parent = gethui and gethui() or game:GetService("CoreGui")
    c.Enabled = false
    local B = Instance.new("BindableEvent")
    B.Name = "ReloadPriorityList"
    B.Parent = c
    local function c(Y)
        for I = #X, 1, -1.0 do
            X[I] = nil
        end
        for I, p in ipairs(Y) do
            X[I] = p
        end
        v()
        U()
    end
    local Y = {
        BG = Color3.fromRGB(10, 25, 10),
        PANEL = Color3.fromRGB(15, 35, 15),
        TEXT = Color3.fromRGB(240, 240, 240),
        DIM = Color3.fromRGB(150, 150, 150),
        DIM2 = Color3.fromRGB(100, 100, 100),
        ROW = Color3.fromRGB(18, 45, 18),
        ROW2 = Color3.fromRGB(15, 40, 15),
        ROW_HOVER = Color3.fromRGB(25, 60, 25),
        CHIP = Color3.fromRGB(20, 80, 20),
        LINE = Color3.fromRGB(0, 150, 0),
        LINE_SOFT = Color3.fromRGB(0, 100, 0),
        SEARCH = Color3.fromRGB(10, 15, 10),
    }
    local I = { { "OGs", J }, { "Secrets", Q } }
    for J, J in ipairs(I) do
        J.open = true
    end
    local J = {}
    local p = {}
    local function G()
        table.clear(J)
        table.clear(p)
        local v = {}
        for M, M in ipairs(X) do
            if type(M) == "string" and M ~= "" then
                local X = string.lower(M)
                if not v[X] then
                    v[X] = true
                    p[M] = true
                    table.insert(J, M)
                    if not z[X] and not V[X] then
                        z[X] = true
                        table.insert(Q, M)
                    end
                end
            end
        end
        table.sort(Q, function(Q, V)
            return string.lower(Q) < string.lower(V)
        end)
    end
    local function Q(V)
        return p[V] == true
    end
    local function V(z)
        if p[z] then
            return
        end
        p[z] = true
        table.insert(J, z)
        c(J)
    end
    local function z(X)
        if not p[X] then
            return
        end
        p[X] = nil
        for p, v in ipairs(J) do
            if v == X then
                table.remove(J, p)
                break
            end
        end
        c(J)
    end
    local p = nil
    repeat
        task.wait()
        p = l and l.Priority
    until p and p.Parent
    local l = p:FindFirstChild("Scroll")
    if l then
        l:Destroy()
    end
    local l = p:FindFirstChild("EmbeddedPriorityManager")
    if l then
        l:Destroy()
    end
    l = Instance.new("Frame")
    l.Name = "EmbeddedPriorityManager"
    l.BackgroundColor3 = Y.BG
    l.BorderSizePixel = 0
    l.Size = UDim2.new(1, -12.0, 1, -12.0)
    l.Position = UDim2.fromOffset(6, 6)
    l.Parent = p
    O(l, 16)
    local p = Instance.new("Frame")
    p.BackgroundTransparency = 1
    p.Size = UDim2.new(1, -16.0, 1, -16.0)
    p.Position = UDim2.fromOffset(8, 8)
    p.Parent = l
    local l = Instance.new("Frame")
    l.Size = UDim2.new(0.5, -4.0, 1, 0)
    l.BackgroundColor3 = Y.PANEL
    l.BorderSizePixel = 0
    l.Parent = p
    O(l, 16)
    local X = Instance.new("Frame")
    X.Position = UDim2.new(0.5, 4, 0, 0)
    X.Size = UDim2.new(0.5, -4.0, 1, 0)
    X.BackgroundColor3 = Y.PANEL
    X.BorderSizePixel = 0
    X.Parent = p
    O(X, 16)
    local p = Instance.new("TextLabel")
    p.BackgroundTransparency = 1
    p.Size = UDim2.new(1, -20.0, 0, 16)
    p.Position = UDim2.fromOffset(10, 8)
    p.Font = Enum.Font.FredokaOne
    p.Text = "All Animals"
    p.TextSize = 11
    p.TextColor3 = Y.TEXT
    p.TextXAlignment = Enum.TextXAlignment.Left
    p.Parent = l
    local p = Instance.new("TextBox")
    p.ClearTextOnFocus = false
    p.Text = ""
    p.PlaceholderText = "Search brainrot..."
    p.Font = Enum.Font.FredokaOne
    p.TextSize = 11
    p.TextColor3 = Y.TEXT
    p.PlaceholderColor3 = Y.DIM2
    p.TextXAlignment = Enum.TextXAlignment.Left
    p.Size = UDim2.new(1, -20.0, 0, 26)
    p.Position = UDim2.fromOffset(10, 28)
    p.BackgroundColor3 = Y.SEARCH
    p.BorderSizePixel = 0
    p.Parent = l
    O(p, 9)
    q(p, 10, 10, 0, 0)
    local q = Instance.new("ScrollingFrame")
    q.BackgroundTransparency = 1
    q.BorderSizePixel = 0
    q.Position = UDim2.fromOffset(10, 60)
    q.Size = UDim2.new(1, -20.0, 1, -70.0)
    q.AutomaticCanvasSize = Enum.AutomaticSize.Y
    q.CanvasSize = UDim2.new()
    q.ScrollBarThickness = 2
    q.ScrollBarImageColor3 = Y.LINE
    q.Parent = l
    local l = Instance.new("Frame")
    l.BackgroundTransparency = 1
    l.Size = UDim2.new(1, -6.0, 0, 0)
    l.AutomaticSize = Enum.AutomaticSize.Y
    l.Position = UDim2.fromOffset(3, 0)
    l.Parent = q
    local q = Instance.new("UIListLayout")
    q.Padding = UDim.new(0, 5)
    q.SortOrder = Enum.SortOrder.LayoutOrder
    q.Parent = l
    local q = Instance.new("TextLabel")
    q.BackgroundTransparency = 1
    q.Size = UDim2.new(1, -20.0, 0, 16)
    q.Position = UDim2.fromOffset(10, 8)
    q.Font = Enum.Font.FredokaOne
    q.Text = "My Order"
    q.TextSize = 11
    q.TextColor3 = Y.TEXT
    q.TextXAlignment = Enum.TextXAlignment.Left
    q.Parent = X
    local q = Instance.new("TextLabel")
    q.BackgroundTransparency = 1
    q.Size = UDim2.fromOffset(42, 16)
    q.Position = UDim2.new(1, -48.0, 0, 8)
    q.Font = Enum.Font.FredokaOne
    q.Text = "#0"
    q.TextSize = 11
    q.TextColor3 = Y.DIM
    q.Parent = X
    local v = Instance.new("ScrollingFrame")
    v.BackgroundTransparency = 1
    v.BorderSizePixel = 0
    v.Position = UDim2.fromOffset(10, 30)
    v.Size = UDim2.new(1, -20.0, 1, -40.0)
    v.AutomaticCanvasSize = Enum.AutomaticSize.Y
    v.CanvasSize = UDim2.new()
    v.ScrollBarThickness = 2
    v.ScrollBarImageColor3 = Y.LINE
    v.Parent = X
    local X = Instance.new("Frame")
    X.BackgroundTransparency = 1
    X.Size = UDim2.new(1, -6.0, 0, 0)
    X.AutomaticSize = Enum.AutomaticSize.Y
    X.Position = UDim2.fromOffset(3, 0)
    X.Parent = v
    local v = Instance.new("UIListLayout")
    v.Padding = UDim.new(0, 4)
    v.SortOrder = Enum.SortOrder.LayoutOrder
    v.Parent = X
    local function v(M)
        local w = Instance.new("Frame")
        w.Size = UDim2.fromOffset(18, 18)
        w.BackgroundColor3 = Y.SEARCH
        w.BorderSizePixel = 0
        w.Parent = M
        O(w, 6)
        local M = Instance.new("UIStroke")
        M.Thickness = 1
        M.Color = Y.LINE_SOFT
        M.Transparency = 0.35
        M.Parent = w
        local b = Instance.new("TextLabel")
        b.BackgroundTransparency = 1
        b.Size = UDim2.fromScale(1, 1)
        b.Font = Enum.Font.FredokaOne
        b.Text = "✓"
        b.TextSize = 12
        b.TextColor3 = Y.TEXT
        b.Visible = false
        b.Parent = w
        local function E(m)
            if m then
                w.BackgroundColor3 = Y.CHIP
                M.Color = Y.LINE
                M.Transparency = 0.08
            else
                w.BackgroundColor3 = Y.SEARCH
                M.Color = Y.LINE_SOFT
                M.Transparency = 0.35
            end
            b.Visible = m
        end
        return E
    end
    local M = {}
    local w = {}
    local b
    local E
    local m
    local A
    b = function()
        for H, H in ipairs(M) do
            H()
        end
    end
    A = function()
        q.Text = "#" .. tostring(#J)
        for q, q in ipairs(w) do
            local H = 0
            for _, _ in ipairs(q.items) do
                if Q(_) then
                    H += 1
                end
            end
            q.label.Text = tostring(H) .. "/" .. tostring(#q.items)
        end
    end
    m = function()
        for q, q in ipairs(X:GetChildren()) do
            if not q:IsA("UIListLayout") then
                q:Destroy()
            end
        end
        for q, H in ipairs(J) do
            local _ = Instance.new("Frame")
            _.BackgroundColor3 = Y.ROW
            _.BorderSizePixel = 0
            _.Size = UDim2.new(1, 0, 0, 30)
            _.Parent = X
            O(_, 9)
            local X = Instance.new("TextLabel")
            X.BackgroundTransparency = 1
            X.Size = UDim2.fromOffset(22, 30)
            X.Position = UDim2.fromOffset(8, 0)
            X.Font = Enum.Font.FredokaOne
            X.Text = tostring(q)
            X.TextSize = 10
            X.TextColor3 = Y.DIM
            X.Parent = _
            local X = Instance.new("TextLabel")
            X.BackgroundTransparency = 1
            X.Position = UDim2.fromOffset(30, 0)
            X.Size = UDim2.new(1, -90.0, 1, 0)
            X.Font = Enum.Font.FredokaOne
            X.Text = H
            X.TextSize = 10
            X.TextColor3 = Y.TEXT
            X.TextXAlignment = Enum.TextXAlignment.Left
            X.TextTruncate = Enum.TextTruncate.AtEnd
            X.Parent = _
            local X = Instance.new("TextButton")
            X.AutoButtonColor = false
            X.Text = "x"
            X.Font = Enum.Font.FredokaOne
            X.TextSize = 10
            X.TextColor3 = Color3.fromRGB(240, 178, 255)
            X.Size = UDim2.fromOffset(16, 16)
            X.Position = UDim2.new(1, -54.0, 0.5, -8.0)
            X.BackgroundColor3 = Color3.fromRGB(58, 54, 78)
            X.BorderSizePixel = 0
            X.Parent = _
            O(X, 5)
            local Z = Instance.new("TextButton")
            Z.AutoButtonColor = false
            Z.Text = "^"
            Z.Font = Enum.Font.FredokaOne
            Z.TextSize = 10
            Z.TextColor3 = Color3.fromRGB(80, 154, 220)
            Z.Size = UDim2.fromOffset(16, 16)
            Z.Position = UDim2.new(1, -36.0, 0.5, -8.0)
            Z.BackgroundColor3 = Color3.fromRGB(14, 23, 44)
            Z.BorderSizePixel = 0
            Z.Parent = _
            O(Z, 5)
            local k = Instance.new("TextButton")
            k.AutoButtonColor = false
            k.Text = "v"
            k.Font = Enum.Font.FredokaOne
            k.TextSize = 10
            k.TextColor3 = Color3.fromRGB(80, 154, 220)
            k.Size = UDim2.fromOffset(16, 16)
            k.Position = UDim2.new(1, -18.0, 0.5, -8.0)
            k.BackgroundColor3 = Color3.fromRGB(14, 23, 44)
            k.BorderSizePixel = 0
            k.Parent = _
            O(k, 5)
            X.MouseButton1Click:Connect(function()
                z(H)
                A()
                m()
                b()
            end)
            Z.MouseButton1Click:Connect(function()
                if q > 1 then
                    J[q], J[q - 1] = J[q - 1], J[q]
                    c(J)
                    m()
                end
            end)
            k.MouseButton1Click:Connect(function()
                if q < #J then
                    J[q], J[q + 1] = J[q + 1], J[q]
                    c(J)
                    m()
                end
            end)
        end
    end
    E = function()
        for J, J in ipairs(l:GetChildren()) do
            if not J:IsA("UIListLayout") then
                J:Destroy()
            end
        end
        table.clear(M)
        table.clear(w)
        local J = string.lower(p.Text or "")
        for q, q in ipairs(I) do
            local c = {}
            for I, I in ipairs(q[2]) do
                if J == "" or string.find(string.lower(I), J, 1, true) then
                    table.insert(c, I)
                end
            end
            if #c > 0 or J == "" then
                local J = Instance.new("Frame")
                J.BackgroundTransparency = 1
                J.Size = UDim2.new(1, 0, 0, 0)
                J.AutomaticSize = Enum.AutomaticSize.Y
                J.Parent = l
                local I = Instance.new("UIListLayout")
                I.Padding = UDim.new(0, 4)
                I.SortOrder = Enum.SortOrder.LayoutOrder
                I.Parent = J
                local I = Instance.new("TextButton")
                I.AutoButtonColor = false
                I.Text = ""
                I.Size = UDim2.new(1, 0, 0, 30)
                I.BackgroundColor3 = Y.ROW
                I.BorderSizePixel = 0
                I.Parent = J
                O(I, 10)
                local l = Instance.new("TextLabel")
                l.BackgroundTransparency = 1
                l.Position = UDim2.fromOffset(12, 0)
                l.Size = UDim2.new(1, -90.0, 1, 0)
                l.Font = Enum.Font.FredokaOne
                l.Text = q[1]
                l.TextSize = 11
                l.TextColor3 = Y.TEXT
                l.TextXAlignment = Enum.TextXAlignment.Left
                l.TextTruncate = Enum.TextTruncate.AtEnd
                l.Parent = I
                local l = Instance.new("TextLabel")
                l.BackgroundTransparency = 1
                l.Position = UDim2.new(1, -58.0, 0, 0)
                l.Size = UDim2.fromOffset(28, 30)
                l.Font = Enum.Font.FredokaOne
                l.Text = "0/" .. tostring(#q[2])
                l.TextSize = 9
                l.TextColor3 = Y.DIM
                l.Parent = I
                table.insert(w, { label = l, items = q[2] })
                local l = Instance.new("TextLabel")
                l.BackgroundTransparency = 1
                l.Position = UDim2.new(1, -20.0, 0, 0)
                l.Size = UDim2.fromOffset(12, 30)
                l.Font = Enum.Font.FredokaOne
                l.Text = q.open and "v" or ">"
                l.TextSize = 12
                l.TextColor3 = Y.DIM
                l.Parent = I
                local l = Instance.new("Frame")
                l.BackgroundTransparency = 1
                l.Visible = q.open
                l.Size = UDim2.new(1, 0, 0, 0)
                l.AutomaticSize = Enum.AutomaticSize.Y
                l.Parent = J
                local J = Instance.new("UIListLayout")
                J.Padding = UDim.new(0, 3)
                J.SortOrder = Enum.SortOrder.LayoutOrder
                J.Parent = l
                for J, J in ipairs(c) do
                    local c = Instance.new("TextButton")
                    c.AutoButtonColor = false
                    c.Text = ""
                    c.Size = UDim2.new(1, 0, 0, 30)
                    c.BackgroundColor3 = Y.ROW2
                    c.BorderSizePixel = 0
                    c.Parent = l
                    O(c, 9)
                    local O = Instance.new("Frame")
                    O.BackgroundTransparency = 1
                    O.Size = UDim2.fromOffset(18, 18)
                    O.Position = UDim2.fromOffset(9, 6)
                    O.Parent = c
                    local l = v(O)
                    local O = Instance.new("TextLabel")
                    O.BackgroundTransparency = 1
                    O.Position = UDim2.fromOffset(34, 0)
                    O.Size = UDim2.new(1, -40.0, 1, 0)
                    O.Font = Enum.Font.FredokaOne
                    O.Text = J
                    O.TextSize = 10
                    O.TextColor3 = Y.TEXT
                    O.TextXAlignment = Enum.TextXAlignment.Left
                    O.TextTruncate = Enum.TextTruncate.AtEnd
                    O.Parent = c
                    local function O()
                        local X = Q(J)
                        l(X)
                        c.BackgroundColor3 = X and Y.CHIP or Y.ROW2
                    end
                    table.insert(M, O)
                    c.MouseButton1Click:Connect(function()
                        if Q(J) then
                            z(J)
                        else
                            V(J)
                        end
                        A()
                        m()
                        b()
                    end)
                    O()
                end
                I.MouseButton1Click:Connect(function()
                    q.open = not q.open
                    E()
                    A()
                end)
            end
        end
        A()
    end
    local function J()
        t()
        G()
        E()
        m()
        b()
        A()
    end
    _G.ReloadEmbeddedPriorityUi = J
    G()
    E()
    m()
    A()
    p:GetPropertyChangedSignal("Text"):Connect(function()
        E()
    end)
    B.Event:Connect(function()
        J()
    end)
end)
task.spawn(function()
    local J = n:WaitForChild("Packages")
    local Q = n:WaitForChild("Datas")
    local V = n:WaitForChild("Shared")
    local V = n:WaitForChild("Utils")
    local n = require(J:WaitForChild("Synchronizer"))
    local n = require(Q:WaitForChild("Animals"))
    local J = require(Q:WaitForChild("Mutations"))
    local V = require(Q:WaitForChild("Traits"))
    local Q = {
        "",
        "K",
        "M",
        "B",
        "T",
        "Qa",
        "Qi",
        "Sx",
        "Sp",
        "Oc",
        "No",
        "Dc",
        "Ud",
        "Dd",
        "Td",
        "Qad",
        "Qid",
        "Sxd",
        "Spd",
        "Ocd",
        "Nod",
        "Vg",
        "Uvg",
        "Dvg",
        "Tvg",
    }
    local function O(q, c)
        c = c or 1
        local z = math.abs(q)
        local B = math.max(1, z)
        local z = math.floor(math.log(B, 1000))
        local B = Q[z + 1] or "e+" .. z
        local Q = q * (10 ^ c / 1000 ^ z)
        local q = math.floor(Q) / 10 ^ c
        return (("%." .. c .. "f"):format(q)):gsub("%.?0+$", "") .. B
    end
    local function Q(q, c, z)
        local B = n[q]
        if not B then
            return 0
        end
        local q = B.Generation or B.Price * 0.1
        local B = 1
        if c and c ~= "None" then
            local Y = J[c]
            if Y and Y.Modifier then
                B = B + Y.Modifier
            end
        end
        local J = false
        if type(z) == "table" then
            for c, c in ipairs(z) do
                if c == "Sleepy" then
                    J = true
                else
                    local z = V[c]
                    if z and z.MultiplierModifier then
                        B = B + z.MultiplierModifier
                    end
                end
            end
        end
        local V = math.round(q * B)
        if J then
            V = math.round(V * 0.5)
        end
        return V
    end
    local J = false
    local V = {}
    local q = 0
    local c = Vector3.new(0, 0, 0)
    local function z()
        for B, B in ipairs(a.Plots:GetChildren()) do
            local Y = _getPlotData(B.Name)
            if Y and _isMyPlot(Y.Owner) then
                return B
            end
        end
        return nil
    end
    local function B(Y)
        local I = {}
        if not Y then
            return I
        end
        local p = _getPlotData(Y.Name)
        local Y = p and p.AnimalList or nil
        if not Y then
            return I
        end
        for p, l in pairs(Y) do
            if type(l) == "table" then
                I[p] = { Index = l.Index, Mutation = l.Mutation, Traits = l.Traits }
            end
        end
        return I
    end
    local function Y(I)
        local p = n[I.Index]
        local n = p and p.DisplayName or I.Index
        local p = Q(I.Index, I.Mutation, I.Traits)
        local Q = "$" .. O(p) .. "/s"
        return n, Q, I.Mutation
    end
    C:GetAttributeChangedSignal("Stealing"):Connect(function()
        local n = C:GetAttribute("Stealing")
        if n then
            J = true
            V = B(z())
            q = tick()
            local n = C.Character
            local Q = n and n:FindFirstChild("HumanoidRootPart")
            if Q then
                c = Q.Position
            end
        else
            if not J then
                return
            end
            J = false
            local n = tick() - q
            local J = 0
            local Q = C.Character
            local O = Q and Q:FindFirstChild("HumanoidRootPart")
            if O then
                J = (O.Position - c).Magnitude
            end
            task.wait(0.6)
            local Q = B(z())
            local O = nil
            for q, c in pairs(Q) do
                local Q = V[q]
                if not Q or (Q.Index ~= c.Index or Q.Mutation ~= c.Mutation) then
                    O = c
                    break
                end
            end
            if O then
                local Q, Q, Q = Y(O)
            else
                if i.AutoTpOnFailedSteal and n > 3 and J > 60 then
                    F("STEAL FAILED", string.format("Auto TPing... (%.1fs, %d studs)", n, J))
                    task.spawn(S)
                end
            end
        end
    end)
end)
P.FPSFunctions = {}
P.FPSFunctions.removeMeshes = function(n)
    if not n:IsA("Tool") then
        return
    end
    local J = n:FindFirstChild("Handle")
    if not J then
        return
    end
    local n = J:GetDescendants()
    for J = 1, #n do
        local Q = n[J]
        if Q:IsA("SpecialMesh") or Q:IsA("Mesh") or Q:IsA("FileMesh") then
            Q:Destroy()
        end
    end
end
P.FPSFunctions.onCharacterAdded = function(n)
    local J = P.FPSFunctions
    n.ChildAdded:Connect(function(Q)
        if Q:IsA("Tool") and i.FPSBoost then
            J.removeMeshes(Q)
        end
    end)
    local Q = n:GetChildren()
    for n = 1, #Q do
        if Q[n]:IsA("Tool") then
            J.removeMeshes(Q[n])
        end
    end
end
P.FPSFunctions.onPlayerAdded = function(n)
    local J = P.FPSFunctions
    n.CharacterAdded:Connect(J.onCharacterAdded)
    if n.Character then
        J.onCharacterAdded(n.Character)
    end
end
P.FPSFunctions.initPlayerTracking = function()
    local n = P.FPSFunctions
    local J = r:GetPlayers()
    for Q = 1, #J do
        n.onPlayerAdded(J[Q])
    end
    r.PlayerAdded:Connect(n.onPlayerAdded)
end
P.FPSFunctions.initPlayerTracking()
if i.CleanErrorGUIs then
    task.spawn(function()
        local n = cloneref and cloneref(game:GetService("GuiService")) or game:GetService("GuiService")
        while true do
            if i.CleanErrorGUIs then
                pcall(function()
                    n:ClearError()
                end)
            end
            task.wait(0.005)
        end
    end)
end
task.spawn(function()
    local n = {
        Background = Color3.fromRGB(15, 17, 22),
        Accent1 = Color3.fromRGB(60, 154, 220),
        Accent2 = Color3.fromRGB(30, 119, 170),
        White = Color3.fromRGB(235, 172, 245),
        Gray = Color3.fromRGB(100, 98, 145),
        Success = Color3.fromRGB(30, 150, 90),
        Error = Color3.fromRGB(80, 178, 255),
    }
    local J = d and 0.65 or 1
    local d = 50 * J
    local Q = Instance.new("ScreenGui")
    Q.Name = "XiJobJoiner"
    Q.ResetOnSpawn = false
    Q.Enabled = i.ShowJobJoiner
    Q.Parent = gethui and gethui() or game:GetService("CoreGui")
    local V = Instance.new("Frame")
    V.Name = "Main"
    V.Size = UDim2.new(0, 500 * J, 0, d)
    local d = i.Positions.JobJoiner or { X = 0.5, Y = 0.85 }
    V.AnchorPoint = Vector2.new(0.5, 0)
    V.Position = UDim2.new(d.X, 0, d.Y, 0)
    V.BackgroundColor3 = Color3.fromRGB(20, 22, 28)
    V.BackgroundTransparency = 0.15
    V.BorderSizePixel = 0
    V.Parent = Q
    Instance.new("UICorner", V).CornerRadius = UDim.new(0, 12)
    local d = Instance.new("UIGradient", V)
    d.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 22, 28)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(25, 27, 35)),
    })
    d.Rotation = 45
    local d = Instance.new("UIStroke", V)
    d.Thickness = 2
    d.Transparency = 0.3
    local Q = Instance.new("UIGradient", d)
    Q.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, n.Accent1),
        ColorSequenceKeypoint.new(0.5, n.Accent2),
        ColorSequenceKeypoint.new(1, n.Accent1),
    })
    task.spawn(function()
        while d.Parent do
            Q.Rotation = Q.Rotation + 1
            task.wait(0.05)
        end
    end)
    D(V, V, "JobJoiner")
    local d = Instance.new("Frame", V)
    d.Size = UDim2.new(1, -20.0 * J, 1, 0)
    d.Position = UDim2.new(0, 10 * J, 0, 0)
    d.BackgroundTransparency = 1
    local Q = Instance.new("UIListLayout", d)
    Q.FillDirection = Enum.FillDirection.Horizontal
    Q.HorizontalAlignment = Enum.HorizontalAlignment.Center
    Q.VerticalAlignment = Enum.VerticalAlignment.Center
    Q.Padding = UDim.new(0, 8 * J)
    local function Q(V, O, q)
        local c = Instance.new("Frame")
        c.BackgroundTransparency = 1
        c.Size = UDim2.new(0, O * J, 0, 32 * J)
        local O = Instance.new("TextLabel", c)
        O.Size = UDim2.new(1, 0, 0, 10 * J)
        O.Position = UDim2.new(0, 0, 0, -10.0 * J)
        O.BackgroundTransparency = 1
        O.Text = V
        O.TextColor3 = n.Accent1
        O.Font = Enum.Font.FredokaOne
        O.TextSize = 9 * J
        local O = Instance.new("TextBox", c)
        O.Size = UDim2.new(1, 0, 1, 0)
        O.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
        O.BackgroundTransparency = 0.5
        O.Text = q or ""
        O.PlaceholderText = V
        O.TextColor3 = n.White
        O.Font = Enum.Font.FredokaOne
        O.TextSize = 12 * J
        O.ClearTextOnFocus = false
        Instance.new("UICorner", O).CornerRadius = UDim.new(0, 6)
        local V = Instance.new("UIStroke", O)
        V.Color = n.Gray
        V.Thickness = 0.1
        V.Transparency = 0.6
        O.Focused:Connect(function()
            f:Create(V, TweenInfo.new(0.2), { Color = n.Accent1, Transparency = 0 }):Play()
        end)
        O.FocusLost:Connect(function()
            f:Create(V, TweenInfo.new(0.2), { Color = n.Gray, Transparency = 0.6 }):Play()
        end)
        return c, O
    end
    local function V(O, q, c)
        local z = Instance.new("TextButton")
        z.Size = UDim2.new(0, q * J, 0, 32 * J)
        z.BackgroundColor3 = c
        z.BackgroundTransparency = 0.2
        z.Text = O
        z.Font = Enum.Font.FredokaOne
        z.TextSize = 12 * J
        z.TextColor3 = n.White
        z.AutoButtonColor = false
        Instance.new("UICorner", z).CornerRadius = UDim.new(0, 6)
        local J = Instance.new("UIStroke", z)
        J.Color = c
        J.Thickness = 1.5
        J.Transparency = 0.4
        z.MouseEnter:Connect(function()
            f:Create(z, TweenInfo.new(0.2), { BackgroundTransparency = 0 }):Play()
            f:Create(J, TweenInfo.new(0.2), { Transparency = 0.1 }):Play()
        end)
        z.MouseLeave:Connect(function()
            f:Create(z, TweenInfo.new(0.2), { BackgroundTransparency = 0.2 }):Play()
            f:Create(J, TweenInfo.new(0.2), { Transparency = 0.4 }):Play()
        end)
        return z
    end
    local f = V("JOIN", 60, n.Success)
    f.Parent = d
    local J, O = Q("", 180, "")
    O.PlaceholderText = ""
    J.Parent = d
    O.TextTruncate = Enum.TextTruncate.AtEnd
    local J = V("CLEAR", 50, Color3.fromRGB(60, 60, 70))
    J.Parent = d
    local V, q = Q("Attempts", 60, "2000")
    V.Parent = d
    local V, c = Q("Delay", 50, "0.05")
    V.Parent = d
    local d = false
    f.MouseButton1Click:Connect(function()
        if d then
            d = false
            f.Text = "JOIN"
            f.BackgroundColor3 = n.Success
            F("JOINER", "Process Cancelled")
            return
        end
        local Q = O.Text:gsub("%s+", "")
        local V = tonumber(q.Text) or 10
        local q = tonumber(c.Text) or 0.5
        if Q == "" or #Q < 5 then
            F("ERROR", "Invalid JobID")
            return
        end
        d = true
        f.Text = "STOP"
        f.BackgroundColor3 = n.Error
        task.spawn(function()
            for c = 1, V do
                if not d then
                    break
                end
                F("JOINING", string.format("Attempt %d/%d...", c, V))
                local V, c = pcall(function()
                    e:TeleportToPlaceInstance(game.PlaceId, Q, C)
                end)
                if not V then
                end
                task.wait(q)
            end
            d = false
            if f and f.Parent then
                f.Text = "JOIN"
                f.BackgroundColor3 = n.Success
            end
        end)
    end)
    J.MouseButton1Click:Connect(function()
        O.Text = ""
    end)
end)
do
    local function d(n)
        if not n then
            return false
        end
        local f = pcall(function()
            if typeof(firesignal) == "function" then
                if n.MouseButton1Click then
                    firesignal(n.MouseButton1Click)
                end
                if n.MouseButton1Down then
                    firesignal(n.MouseButton1Down)
                end
                if n.Activated then
                    firesignal(n.Activated)
                end
            else
                local J = n.AbsolutePosition.X + n.AbsoluteSize.X / 2
                local e = n.AbsolutePosition.Y + n.AbsoluteSize.Y / 2 + 58
                local n = Instance.new("VirtualInputManager")
                n:SendMouseButtonEvent(J, e, 0, true, game, 0)
                n:SendMouseButtonEvent(J, e, 0, false, game, 0)
            end
        end)
        return f
    end
    _G.fireClick = d
    local function n(f, J)
        if not f or not J or J == "" then
            return false
        end
        local e = x:WaitForChild("SafePanel", 5)
        if not e then
            return false
        end
        local Q, V = pcall(function()
            return e.SafePanel:WaitForChild("Content"):WaitForChild("ScrollingFrame")
        end)
        if not Q or not V then
            return false
        end
        local Q = V:FindFirstChild(J)
        if not Q then
            return false
        end
        if not d(Q) then
            return false
        end
        task.wait(0.05)
        local J, Q = pcall(function()
            return e:WaitForChild("SafePanel"):WaitForChild("Profiles"):WaitForChild("ScrollingFrame")
        end)
        if not J or not Q then
            return false
        end
        local J = Q:FindFirstChild(f.Name)
        if not J then
            return false
        end
        if not d(J) then
            return false
        end
        return true
    end
    _G.runAdminCommand = n
end
do
    local d =
        { "balloon", "inverse", "jail", "jumpscare", "morph", "nightvision", "ragdoll", "rocket", "tiny" }
    local n = Instance.new("Highlight")
    n.Name = "DebrokClickAPHighlight"
    n.FillColor = W.Accent1
    n.FillTransparency = 0.3
    n.OutlineColor = W.Accent1
    n.OutlineTransparency = 0
    n.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    n.Adornee = nil
    n.Parent = game:GetService("CoreGui")
    local function f(J)
        local e = x:FindFirstChild("SafePanel")
        if e then
            local Q, V = pcall(function()
                local O = e.SafePanel.Content.ScrollingFrame:FindFirstChild(J)
                local e = O and O:FindFirstChild("Timer")
                return e and e.Visible
            end)
            if Q and V then
                return true
            end
        end
        if type(lastActionUse) == "table" then
            local e = lastActionUse[J]
            local Q = ACTION_COOLDOWNS and ACTION_COOLDOWNS[J] or 0
            if e and Q > 0 and tick() - e < Q then
                return true
            end
        end
        return false
    end
    local function J(e)
        if type(lastActionUse) == "table" then
            lastActionUse[e] = tick()
        end
        if P.AdminButtonCache and P.AdminButtonCache[e] then
            for Q, Q in ipairs(P.AdminButtonCache[e]) do
                if Q and Q.Parent then
                    pcall(function()
                        Q.BackgroundColor3 = W.Error
                    end)
                    task.delay(ACTION_COOLDOWNS and ACTION_COOLDOWNS[e] or 5, function()
                        if Q and Q.Parent then
                            pcall(function()
                                local V = e == "balloon"
                                    and P.BalloonedPlayers
                                    and next(P.BalloonedPlayers) ~= nil
                                Q.BackgroundColor3 = V and W.Error or W.SurfaceHighlight
                            end)
                        end
                    end)
                end
            end
        end
    end
    local function e()
        local Q = P.BalloonedPlayers and next(P.BalloonedPlayers) ~= nil
        if P.AdminButtonCache and P.AdminButtonCache.balloon then
            for V, V in ipairs(P.AdminButtonCache.balloon) do
                if V and V.Parent then
                    pcall(function()
                        V.BackgroundColor3 = Q and W.Error or W.SurfaceHighlight
                    end)
                end
            end
        end
    end
    local function Q()
        local V = { "ragdoll", "balloon", "rocket", "jail" }
        local O = {}
        for q, q in ipairs(V) do
            O[q] = true
            if not f(q) then
                return q
            end
        end
        for V, V in ipairs(d) do
            if not O[V] and not f(V) then
                return V
            end
        end
        return nil
    end
    local function V(O, q)
        q = tonumber(q) or 0.1
        local c = 0
        for z, z in ipairs(d) do
            if not f(z) then
                task.delay(c * q, function()
                    if not O or not O.Parent or O == C then
                        return
                    end
                    local q, B = pcall(_G.runAdminCommand, O, z)
                    if q and B then
                        J(z)
                        if z == "balloon" then
                            P.BalloonedPlayers[O.UserId] = true
                            e()
                        end
                    end
                end)
                c = c + 1
            end
        end
    end
    local O = 0
    local function q()
        local c = {}
        local z = C.Character
        local B = z and z:FindFirstChild("HumanoidRootPart")
        if not B then
            return c
        end
        local z = tonumber(i.ProximityRange) or 15
        for Y, Y in ipairs(r:GetPlayers()) do
            if Y ~= C and Y.Parent and Y.Character and Y.Character:FindFirstChild("HumanoidRootPart") then
                local I = Y.Character.HumanoidRootPart
                local p = (I.Position - B.Position).Magnitude
                if p <= z then
                    if
                        not (
                            i.DisableProximitySpamOnMoby and y(Y)
                            or i.DisableProximitySpamOnKawaifu and K(Y)
                        )
                    then
                        table.insert(c, Y)
                    end
                end
            end
        end
        table.sort(c, function(z, B)
            return z.UserId < B.UserId
        end)
        return c
    end
    local function c()
        local z = q()
        if #z <= 0 then
            return
        end
        local q = Q()
        if not q then
            return
        end
        O = O % #z + 1
        local B = z[O]
        if not B then
            return
        end
        local O, z = pcall(_G.runAdminCommand, B, q)
        if O and z then
            J(q)
            if q == "balloon" then
                P.BalloonedPlayers[B.UserId] = true
                e()
            end
        end
    end
    local function O(q, z, B, Y)
        local I = Vector3.new(Y / 2, Y / 2, Y / 2)
        local Y = B - I
        local p = B + I
        local function B(I, l)
            if math.abs(l) < 1e-8 then
                if I >= 0 then
                    return math.huge
                else
                    return -math.huge
                end
            end
            return I / l
        end
        local I = B(Y.X - q.X, z.X)
        local l = B(p.X - q.X, z.X)
        if I > l then
            I, l = l, I
        end
        local W = B(Y.Y - q.Y, z.Y)
        local X = B(p.Y - q.Y, z.Y)
        if W > X then
            W, X = X, W
        end
        if I > X or W > l then
            return false
        end
        if W > I then
            I = W
        end
        if X < l then
            l = X
        end
        local W = B(Y.Z - q.Z, z.Z)
        local Y = B(p.Z - q.Z, z.Z)
        if W > Y then
            W, Y = Y, W
        end
        if I > Y or W > l then
            return false
        end
        return true
    end
    R.RenderStepped:Connect(function()
        if i.ClickToAP then
            local q = a.CurrentCamera
            local z = j:GetMouseLocation()
            local B = q:ViewportPointToRay(z.X, z.Y)
            local q = nil
            local z = math.huge
            for Y, Y in ipairs(r:GetPlayers()) do
                if Y ~= C and Y.Character and Y.Character:FindFirstChild("HumanoidRootPart") and Y.Parent then
                    local I = Y.Character.HumanoidRootPart
                    local p = I.Position
                    if O(B.Origin, B.Direction, p, 8) then
                        local I = (B.Origin - p).Magnitude
                        if I < z then
                            z = I
                            q = Y
                        end
                    end
                end
            end
            n.Adornee = q and q.Character or nil
        else
            n.Adornee = nil
        end
    end)
    j.InputBegan:Connect(function(n, q)
        if q then
            return
        end
        if n.UserInputType ~= Enum.UserInputType.MouseButton1 then
            return
        end
        if not i.ClickToAP then
            return
        end
        local n = a.CurrentCamera
        local q = j:GetMouseLocation()
        local z = n:ViewportPointToRay(q.X, q.Y)
        local n = nil
        local q = math.huge
        for B, B in ipairs(r:GetPlayers()) do
            if B ~= C and B.Character and B.Character:FindFirstChild("HumanoidRootPart") and B.Parent then
                local Y = B.Character.HumanoidRootPart
                local I = Y.Position
                if O(z.Origin, z.Direction, I, 8) then
                    local O = (z.Origin - I).Magnitude
                    if O < q then
                        q = O
                        n = B
                    end
                end
            end
        end
        if not n then
            return
        end
        if i.DisableClickToAPOnMoby and y(n) then
            F("CLICK TO AP", "Disabled on Moby users")
            return
        end
        if i.DisableClickToAPOnKawaifu and K(n) then
            F("CLICK TO AP", "Disabled on Kawaifu users")
            return
        end
        local O = false
        for q, q in ipairs(d) do
            if not f(q) then
                O = true
                break
            end
        end
        if O then
            if i.ClickToAPSingleCommand then
                local d = Q()
                if d then
                    local f, Q = pcall(_G.runAdminCommand, n, d)
                    if f and Q then
                        J(d)
                        if d == "balloon" then
                            P.BalloonedPlayers[n.UserId] = true
                            e()
                        end
                        F("CLICK AP", "Sent " .. d .. " to " .. n.Name)
                    else
                        F("CLICK AP", "Failed to send " .. d .. " to " .. n.Name)
                    end
                else
                    F("CLICK AP", "All commands on cooldown")
                end
            else
                V(n)
                F("CLICK AP", "Triggered on " .. n.Name)
            end
        else
            local d = x:WaitForChild("SafePanel", 5)
            if d then
                local f = d:WaitForChild("SafePanel"):WaitForChild("Profiles"):WaitForChild("ScrollingFrame")
                local d = f:FindFirstChild(n.Name)
                if d then
                    fireClick(d)
                    F("CLICK AP", "Selected " .. n.Name)
                end
            end
        end
    end)
    local d = false
    local function n()
        if d then
            return
        end
        d = true
        task.spawn(function()
            while ProximityAPActive do
                c()
                task.wait(0.1)
            end
            d = false
        end)
    end
    local d = 0
    R.Heartbeat:Connect(function()
        local R = os.clock()
        if R - d < 0.2 then
            return
        end
        d = R
        if ProximityAPActive then
            n()
        end
    end)
end
local function R()
    local function d(n)
        local f, J = pcall(function()
            return string.lower(tostring(n.Text or ""))
        end)
        return f and J or ""
    end
    local function n(f)
        if not f then
            return false
        end
        local J = false
        local e = false
        local Q = false
        local V = 0
        for O, O in ipairs(f:GetDescendants()) do
            if O:IsA("TextLabel") or O:IsA("TextButton") then
                local f = d(O)
                if f:find("admin panel", 1, true) then
                    J = true
                end
                if f:find("player list", 1, true) then
                    e = true
                end
                if f == "tp" then
                    Q = true
                end
                if f == "🏃" or f == "🔒" or f == "🚀" or f == "🎈" then
                    V += 1
                end
            end
        end
        return J and e and V >= 4 and not Q
    end
    local function d(f)
        if not f or not f:IsA("ScreenGui") then
            return false
        end
        if f.Name == "XiSafePanel" then
            return true
        end
        if n(f) then
            return true
        end
        return false
    end
    local n = {}
    pcall(function()
        table.insert(n, x)
    end)
    pcall(function()
        local f = game:GetService("CoreGui")
        if f then
            table.insert(n, f)
        end
    end)
    for f, f in ipairs(n) do
        for n, n in ipairs(f:GetChildren()) do
            if d(n) then
                pcall(function()
                    n:Destroy()
                end)
            end
        end
    end
end
local function d()
    R()
    local R = {
        Text = Color3.fromRGB(240, 240, 240),
        SubText = Color3.fromRGB(150, 150, 150),
        Accent = Color3.fromRGB(0, 255, 0),
        AccentSoft = Color3.fromRGB(20, 80, 20),
        AccentBright = Color3.fromRGB(50, 200, 50),
        PanelTop = Color3.fromRGB(15, 35, 15),
        PanelTop2 = Color3.fromRGB(10, 25, 10),
        PanelInner = Color3.fromRGB(12, 30, 12),
        ControlsBar = Color3.fromRGB(12, 30, 12),
        RowA = Color3.fromRGB(20, 45, 20),
        RowB = Color3.fromRGB(15, 40, 15),
        RowHover = Color3.fromRGB(30, 60, 30),
        RowLine = Color3.fromRGB(0, 200, 0),
        ButtonBg = Color3.fromRGB(10, 14, 10),
        ButtonStroke = Color3.fromRGB(0, 100, 0),
        LockedFill = Color3.fromRGB(15, 30, 15),
        Danger = Color3.fromRGB(220, 60, 80),
        Yellow = Color3.fromRGB(255, 180, 50),
        White = Color3.fromRGB(255, 255, 255),
        PanelCorner = 3,
        RowCorner = 3,
        ButtonCorner = 3,
        AvatarCorner = 3,
    }
    R.RowHeight = 54
    R.ButtonSize = 38
    R.BoxSize = 32
    R.IconSize = 17
    R.ActionsWidth = 5 * R.ButtonSize
    R.SafeGap = 12
    R.NameSize = 16
    R.UserSize = 12
    R.StatusSize = 11
    local n = {
        { label = "🏃", action = "ragdoll" },
        { label = "🔒", action = "jail" },
        { label = "🚀", action = "rocket" },
        { label = "🎈", action = "balloon" },
        { label = "TP", action = "tp" },
    }
    local f = { ragdoll = 30, jail = 60, rocket = 120, balloon = 30 }
    local function J(e, Q)
        local V = Instance.new(e)
        for e, O in pairs(Q or {}) do
            V[e] = O
        end
        return V
    end
    local function e(Q, V)
        local O = Instance.new("UICorner")
        O.CornerRadius = UDim.new(0, V or 10)
        O.Parent = Q
        return O
    end
    local function Q(V, O, q, c)
        local z = Instance.new("UIStroke")
        z.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        z.LineJoinMode = Enum.LineJoinMode.Round
        z.Color = O
        z.Thickness = q or 1
        z.Transparency = c or 0
        z.Parent = V
        return z
    end
    local function V(O, q, c, z)
        local B = Instance.new("UIGradient")
        B.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, q), ColorSequenceKeypoint.new(1, c) })
        B.Rotation = z or 0
        B.Parent = O
        return B
    end
    local O = {}
    local q = { ragdoll = {}, jail = {}, rocket = {}, balloon = {}, tp = {} }
    local c = {}
    local z = {}
    local function B(Y)
        return f[Y] or 0
    end
    local function f(Y)
        local I = O[Y]
        local p = B(Y)
        if not I or p <= 0 then
            return false
        end
        return tick() - I < p
    end
    local function Y(I, p)
        local l = I:FindFirstChild("Box")
        local I = l and l:FindFirstChild("Stroke")
        local W = l and l:FindFirstChild("Fill")
        if not l or not I or not W then
            return
        end
        if p == "normal" then
            W.BackgroundColor3 = R.ButtonBg
            W.BackgroundTransparency = 0.08
            I.Color = R.ButtonStroke
            I.Transparency = 1
        elseif p == "hover" then
            W.BackgroundColor3 = R.RowHover
            W.BackgroundTransparency = 0
            I.Color = R.AccentBright
            I.Transparency = 1
        elseif p == "locked" then
            W.BackgroundColor3 = Color3.fromRGB(34, 72, 145)
            W.BackgroundTransparency = 0.02
            I.Color = Color3.fromRGB(120, 178, 255)
            I.Transparency = 0.35
        end
    end
    local function I(p, l)
        for W, W in ipairs(q[p] or {}) do
            if W and W.Parent then
                local p = W:GetAttribute("TargetPlayerName")
                local X = p and r:FindFirstChild(p) or nil
                local p = X and rpIsAdminPanelBlockedForPlayer and rpIsAdminPanelBlockedForPlayer(X) or false
                local X = l or p
                W.Active = not X
                W:SetAttribute("Locked", X)
                if X then
                    Y(W, "locked")
                else
                    Y(W, "normal")
                end
            end
        end
    end
    local function p(l)
        return i.CancelAPPanelOnMoby and y and y(l) or i.CancelAPPanelOnKawaifu and K and K(l)
    end
    local function l(W)
        if not W then
            return
        end
        local X = z and z[W.UserId]
        if not X or not X.Parent then
            return
        end
        local t = X:FindFirstChild("Actions")
        if not t then
            return
        end
        local X = p(W)
        for W, W in ipairs(t:GetChildren()) do
            if W:IsA("TextButton") then
                local t = W:GetAttribute("ActionName")
                local G = X or t and f(t) or false
                W.Active = not G
                W:SetAttribute("Locked", G)
                if G then
                    Y(W, "locked")
                else
                    Y(W, "normal")
                end
            end
        end
    end
    local function W(X)
        if B(X) <= 0 then
            return
        end
        O[X] = tick()
        I(X, true)
        task.spawn(function()
            while f(X) do
                task.wait(0.2)
            end
            I(X, false)
        end)
    end
    local function O(B, I)
        if _G.runAdminCommand then
            local X, t = pcall(_G.runAdminCommand, B, I)
            return X and t
        end
        if type(runAdminCommand) == "function" then
            local X, t = pcall(runAdminCommand, B, I)
            return X and t
        end
        return false
    end
    local function B(I)
        if not I then
            return nil
        end
        local X = a:FindFirstChild("Plots")
        if not X then
            return nil
        end
        for t, t in ipairs(X:GetChildren()) do
            local G = _getPlotData(t.Name)
            if G and G.Owner then
                local v = G.Owner
                if typeof(v) == "Instance" and v == I then
                    return t
                end
                if type(v) == "string" and v == I.Name then
                    return t
                end
            end
        end
        for t, t in ipairs(X:GetChildren()) do
            local X = t:FindFirstChild("PlotSign")
            local G = X
                and X:FindFirstChild("SurfaceGui")
                and X.SurfaceGui:FindFirstChild("Frame")
                and X.SurfaceGui.Frame:FindFirstChild("TextLabel")
            if G then
                local X = tostring(G.Text or "")
                local G = X:match("^(.-)'") or X
                if G == I.DisplayName or G == I.Name then
                    return t
                end
            end
        end
        return nil
    end
    local function I(X)
        if not X then
            return nil
        end
        local t = X:FindFirstChild("PlotSign")
        if not t then
            return nil
        end
        if t:IsA("BasePart") then
            return t
        end
        if t:IsA("Model") then
            return t.PrimaryPart or t:FindFirstChildWhichIsA("BasePart", true)
        end
        return t:FindFirstChildWhichIsA("BasePart", true)
    end
    local function X(t)
        local G = C.Character
        local v = G and G:FindFirstChild("HumanoidRootPart")
        local M = G and G:FindFirstChild("Humanoid")
        if not v or not M or M.Health <= 0 then
            return false, "No character"
        end
        local w = B(t)
        if not w then
            return false, "Base not found"
        end
        local B = I(w)
        if not B then
            return false, "Sign not found"
        end
        local I = i.TpSettings.Tool
        local t = C.Backpack:FindFirstChild(I) or G:FindFirstChild(I)
        if t then
            M:EquipTool(t)
            task.wait(0.01)
        end
        local I = B.CFrame
        local t = I.LookVector
        local G = -I.LookVector
        T(v, 40)
        local T = B.Position + t * 20
        local I = B.Position + G * 20
        local B = v.Position
        local t = Vector3.new(B.X, 0, B.Z)
        local B = (Vector3.new(T.X, 0, T.Z) - t).Magnitude
        local G = (Vector3.new(I.X, 0, I.Z) - t).Magnitude
        local t = B < G and T or I
        local B = Vector3.new(t.X, -4.8, t.Z)
        v.Velocity = Vector3.zero
        v.CFrame = CFrame.new(B)
        v.Velocity = Vector3.zero
        o(function()
            return v
                and v.Parent
                and (v.Position - B).Magnitude <= 2
                and M
                and M.FloorMaterial ~= Enum.Material.Air
        end, 3)
        v.Velocity = Vector3.zero
        return true
    end
    c.adminGui = J(
        "ScreenGui",
        {
            Name = "XiSafePanel",
            ResetOnSpawn = false,
            IgnoreGuiInset = true,
            ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
            Parent = x,
        }
    )
    c.outer = J(
        "Frame",
        {
            Name = "Frame",
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Size = UDim2.fromOffset(530, 44),
            AutomaticSize = Enum.AutomaticSize.Y,
            Position = UDim2.new(
                i.Positions.SafePanel.X,
                i.Positions.SafePanel.OffsetX or 0,
                i.Positions.SafePanel.Y,
                i.Positions.SafePanel.OffsetY or 0
            ),
            ZIndex = 10,
            Parent = c.adminGui,
        }
    )
    c.backdrop = J(
        "Frame",
        {
            Name = "Backdrop",
            BackgroundColor3 = Color3.fromRGB(8, 9, 12),
            BackgroundTransparency = 0.5,
            BorderSizePixel = 0,
            Position = UDim2.fromOffset(-3.0, -2.0),
            Size = UDim2.new(1, 6, 1, 4),
            AutomaticSize = Enum.AutomaticSize.Y,
            ZIndex = 0,
            Parent = c.outer,
        }
    )
    e(c.backdrop, 8)
    Q(c.backdrop, R.AccentSoft, 1, 0.82)
    c.topCard = J(
        "Frame",
        {
            Name = "TopCard",
            BackgroundColor3 = R.PanelTop,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Size = UDim2.new(1, 0, 0, 42),
            Parent = c.outer,
        }
    )
    e(c.topCard, R.PanelCorner)
    Q(c.topCard, R.AccentSoft, 1.2, 1)
    V(c.topCard, R.PanelTop, R.PanelTop2, 90)
    c.refreshBtn = J(
        "TextButton",
        {
            Size = UDim2.fromOffset(72, 24),
            Position = UDim2.fromOffset(10, 8),
            BackgroundColor3 = Color3.fromRGB(24, 27, 33),
            AutoButtonColor = false,
            Text = "Refresh",
            Font = Enum.Font.FredokaOne,
            TextSize = 10,
            TextColor3 = R.Text,
            Visible = false,
            Visible = false,
            Parent = c.topCard,
        }
    )
    c.closeBtn = J(
        "TextButton",
        {
            Size = UDim2.fromOffset(18, 18),
            Position = UDim2.new(1, -28.0, 0, 10),
            BackgroundColor3 = Color3.fromRGB(22, 24, 30),
            AutoButtonColor = false,
            Text = "X",
            Font = Enum.Font.FredokaOne,
            TextSize = 9,
            TextColor3 = R.Text,
            Visible = false,
            Parent = c.topCard,
        }
    )
    c.title = J(
        "TextLabel",
        {
            BackgroundTransparency = 1,
            AnchorPoint = Vector2.new(0.5, 0),
            Position = UDim2.new(0.5, 0, 0, 8),
            Size = UDim2.new(0, 220, 0, 22),
            TextXAlignment = Enum.TextXAlignment.Center,
            Text = "LACASA PRIVATEEEEEEEEEEE",
            Font = Enum.Font.FredokaOne,
            TextSize = 16,
            TextColor3 = R.Text,
            Visible = false,
            Parent = c.topCard,
        }
    )
    c.controlsBar = J(
        "Frame",
        {
            Name = "ControlsBar",
            BackgroundColor3 = Color3.fromRGB(8, 9, 12),
            BackgroundTransparency = 0,
            BorderSizePixel = 0,
            Position = UDim2.fromOffset(6, 6),
            Size = UDim2.new(1, -12.0, 0, 40),
            Parent = c.topCard,
        }
    )
    e(c.controlsBar, 8)
    c.controls = J(
        "Frame",
        {
            Name = "Controls",
            BackgroundTransparency = 1,
            Position = UDim2.fromOffset(8, 4),
            Size = UDim2.new(1, -16.0, 1, -8.0),
            Parent = c.controlsBar,
        }
    )
    c.proxBtn = J(
        "TextButton",
        {
            Name = "ProximityAPButton",
            Size = UDim2.fromOffset(52, 24),
            Position = UDim2.fromOffset(2, 7),
            BackgroundColor3 = Color3.fromRGB(20, 22, 28),
            AutoButtonColor = false,
            Text = "Prox",
            Font = Enum.Font.FredokaOne,
            TextSize = 10,
            TextColor3 = R.Text,
            Parent = c.controls,
        }
    )
    e(c.proxBtn, 3)
    c.proxBtnStroke = Q(c.proxBtn, R.ButtonStroke, 1, 0.28)
    c.spamBaseBtn = J(
        "TextButton",
        {
            Size = UDim2.fromOffset(90, 24),
            Position = UDim2.fromOffset(62, 7),
            BackgroundColor3 = Color3.fromRGB(20, 22, 28),
            AutoButtonColor = false,
            Text = "Spam Base Owner",
            Font = Enum.Font.FredokaOne,
            TextSize = 10,
            TextColor3 = R.Text,
            Parent = c.controls,
        }
    )
    e(c.spamBaseBtn, 3)
    c.spamStroke = Q(c.spamBaseBtn, R.ButtonStroke, 1, 0.28)
    c.rangeLabel = J(
        "TextLabel",
        {
            BackgroundTransparency = 1,
            Position = UDim2.fromOffset(160, 7),
            Size = UDim2.fromOffset(38, 24),
            Text = tostring(math.floor(i.ProximityRange or 15)),
            Font = Enum.Font.FredokaOne,
            TextSize = 12,
            TextColor3 = R.Text,
            Parent = c.controls,
        }
    )
    c.proxSliderBg = J(
        "Frame",
        {
            BackgroundColor3 = Color3.fromRGB(18, 31, 60),
            BorderSizePixel = 0,
            Position = UDim2.fromOffset(198, 17),
            Size = UDim2.fromOffset(170, 6),
            Parent = c.controls,
        }
    )
    e(c.proxSliderBg, 3)
    c.proxFill = J(
        "Frame",
        { BackgroundColor3 = R.White, BorderSizePixel = 0, Size = UDim2.new(0, 0, 1, 0), Parent = c.proxSliderBg }
    )
    e(c.proxFill, 3)
    c.proxKnob = J(
        "Frame",
        {
            BackgroundColor3 = R.White,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.new(0, 0, 0.5, 0),
            Size = UDim2.fromOffset(16, 16),
            Parent = c.proxSliderBg,
        }
    )
    e(c.proxKnob, 3)
    c.proxKnobStroke = Q(c.proxKnob, Color3.fromRGB(18, 21, 27), 1.2, 0.35)
    c.listHolder = J(
        "Frame",
        {
            Name = "ListHolder",
            BackgroundColor3 = R.PanelInner,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 0, 46),
            Size = UDim2.new(1, 0, 0, 0),
            AutomaticSize = Enum.AutomaticSize.Y,
            Parent = c.outer,
        }
    )
    e(c.listHolder, R.PanelCorner)
    c.pad = Instance.new("UIPadding")
    c.pad.PaddingTop = UDim.new(0, 2)
    c.pad.PaddingBottom = UDim.new(0, 2)
    c.pad.PaddingLeft = UDim.new(0, 4)
    c.pad.PaddingRight = UDim.new(0, 4)
    c.pad.Parent = c.listHolder
    local x = J(
        "UIListLayout",
        { SortOrder = Enum.SortOrder.LayoutOrder, Padding = UDim.new(0, 2), Parent = c.listHolder }
    )
    D(c.topCard, c.outer, "SafePanel")
    local x = {}
    local V = {}
    local B = 0
    local function o()
        local T = ProximityAPActive == true
        c.proxBtn.BackgroundColor3 = T and Color3.fromRGB(24, 42, 80) or Color3.fromRGB(8, 10, 18)
        c.proxBtn.TextColor3 = R.Text
        c.proxBtnStroke.Color = T and Color3.fromRGB(80, 154, 220) or R.ButtonStroke
    end
    local T = nil
    local function I()
        if ProximityAPActive then
            if not T then
                T = Instance.new("Part")
                T.Name = "XiProxViz"
                T.Anchored = true
                T.CanCollide = false
                T.Shape = Enum.PartType.Cylinder
                T.Color = R.AccentBright
                T.Transparency = 0.6
                T.CastShadow = false
                T.Parent = a
            end
            local t = C.Character
            local G = t and t:FindFirstChild("HumanoidRootPart")
            if G then
                T.Size = Vector3.new(0.5, (i.ProximityRange or 15) * 2, (i.ProximityRange or 15) * 2)
                T.CFrame = G.CFrame * CFrame.Angles(0, 0, 1.5707963267948966)
            end
        elseif T then
            T:Destroy()
            T = nil
        end
    end
    local function T(t)
        local G, v = 5, 50
        t = math.clamp(t or 15, G, v)
        i.ProximityRange = t
        U()
        local U = (t - G) / (v - G)
        c.proxFill.Size = UDim2.new(U, 0, 1, 0)
        c.proxKnob.Position = UDim2.new(U, 0, 0.5, 0)
        c.rangeLabel.Text = tostring(math.floor(t + 0.5))
        I()
    end
    T(i.ProximityRange or 15)
    o()
    P.ProximityAPButton = c.proxBtn
    P.ProximityAPButtonStroke = c.proxBtnStroke
    P.AdminProxBtn = c.proxBtn
    local P = false
    c.proxSliderBg.InputBegan:Connect(function(U)
        if
            U.UserInputType == Enum.UserInputType.MouseButton1
            or U.UserInputType == Enum.UserInputType.Touch
        then
            P = true
            _G.ADMIN_PANEL_SLIDER_DRAG = true
        end
    end)
    j.InputEnded:Connect(function(U)
        if
            U.UserInputType == Enum.UserInputType.MouseButton1
            or U.UserInputType == Enum.UserInputType.Touch
        then
            P = false
            _G.ADMIN_PANEL_SLIDER_DRAG = false
        end
    end)
    j.InputChanged:Connect(function(U)
        if
            P
            and (
                U.UserInputType == Enum.UserInputType.MouseMovement
                or U.UserInputType == Enum.UserInputType.Touch
            )
        then
            local P = U.Position.X
            local U = c.proxSliderBg.AbsolutePosition.X
            local j = c.proxSliderBg.AbsoluteSize.X
            local t = math.clamp((P - U) / j, 0, 1)
            T(5 + t * 45)
        end
    end)
    c.proxBtn.MouseButton1Click:Connect(function()
        ProximityAPActive = not ProximityAPActive
        o()
        I()
        F("PROXIMITY AP", ProximityAPActive and "ENABLED" or "DISABLED")
    end)
    c.spamBaseBtn.MouseButton1Click:Connect(function()
        local P = C.Character
        local U = P and P:FindFirstChild("HumanoidRootPart")
        if not U then
            F("SPAM OWNER", "No character found")
            return
        end
        local P = nil
        local o = math.huge
        local T = a:FindFirstChild("Plots")
        if T then
            for a, a in ipairs(T:GetChildren()) do
                local T = a:FindFirstChild("PlotSign")
                if T then
                    local j = T:FindFirstChild("YourBase")
                    if not j or not j.Enabled then
                        local j = T:IsA("BasePart") and T.Position or T.PrimaryPart and T.PrimaryPart.Position
                        if not j then
                            local t = T:FindFirstChildWhichIsA("BasePart", true)
                            j = t and t.Position
                        end
                        if j then
                            local T = (U.Position - j).Magnitude
                            if T < o then
                                o = T
                                P = a
                            end
                        end
                    end
                end
            end
        end
        if not P then
            F("SPAM OWNER", "No nearby base found")
            return
        end
        local a = nil
        local U = _getPlotData(P.Name)
        if U and U.Owner then
            local o = U.Owner
            if typeof(o) == "Instance" then
                a = o
            elseif type(o) == "string" then
                a = r:FindFirstChild(o)
            end
        end
        if not a then
            local U = P:FindFirstChild("PlotSign")
            local P = U
                and U:FindFirstChild("SurfaceGui")
                and U.SurfaceGui:FindFirstChild("Frame")
                and U.SurfaceGui.Frame:FindFirstChild("TextLabel")
            if P then
                local U = P.Text
                local P = U and U:match("^(.-)'") or U
                if P then
                    for U, U in ipairs(r:GetPlayers()) do
                        if U.DisplayName == P or U.Name == P then
                            a = U
                            break
                        end
                    end
                end
            end
        end
        if not a or a == C then
            F("SPAM OWNER", "Owner not found or is you")
            return
        end
        c.spamBaseBtn.BackgroundColor3 = Color3.fromRGB(24, 42, 80)
        c.spamBaseBtn.TextColor3 = R.Text
        F("SPAM OWNER", "Spamming " .. a.DisplayName)
        task.spawn(function()
            local P = {
                "balloon",
                "inverse",
                "jail",
                "jumpscare",
                "morph",
                "nightvision",
                "ragdoll",
                "rocket",
                "tiny",
            }
            local U = 0
            for o, o in ipairs(P) do
                local P = O(a, o)
                if P then
                    U = U + 1
                end
                task.wait(0.15)
            end
            task.wait(0.2)
            c.spamBaseBtn.BackgroundColor3 = Color3.fromRGB(20, 22, 28)
            c.spamBaseBtn.TextColor3 = R.Text
            F("SPAM OWNER", "Sent " .. U .. " commands to " .. a.DisplayName)
        end)
    end)
    local function P()
        local a = {}
        for U, U in ipairs(c.listHolder:GetChildren()) do
            if U:IsA("Frame") and U.Name:match("^Row_") then
                local o = tonumber(U:GetAttribute("CreateIndex")) or math.huge
                local T = U:GetAttribute("PlayerName") or ""
                table.insert(a, { row = U, createIndex = o, playerName = T })
            end
        end
        table.sort(a, function(U, o)
            if U.createIndex ~= o.createIndex then
                return U.createIndex < o.createIndex
            end
            return U.playerName < o.playerName
        end)
        for U, o in ipairs(a) do
            o.row.LayoutOrder = U
        end
    end
    local function a(U)
        local o = x[U.UserId]
        if not o or not o.Parent then
            return
        end
        local T = U:GetAttribute("Stealing")
        local j = U:GetAttribute("StealingIndex")
        if T then
            local U = tostring(j or "Unknown")
            o.Text = "STEALING - " .. U
            o.TextColor3 = R.Yellow
        else
            o.Text = ""
        end
    end
    local function U(o, T, j)
        local t = T == "kawaifu" and "KAWAIFU" or "MOBY"
        local G = T == "kawaifu" and Color3.fromRGB(176, 160, 228) or Color3.fromRGB(50, 126, 180)
        local v = T == "kawaifu" and 58 or 38
        local T = o:FindFirstChild("UserTag_MOBY") or o:FindFirstChild("UserTag_KAWAIFU")
        if T then
            T:Destroy()
        end
        local T = J(
            "TextLabel",
            {
                Name = "UserTag_" .. t,
                BackgroundColor3 = G,
                BorderSizePixel = 0,
                Position = UDim2.fromOffset(j, 8),
                Size = UDim2.fromOffset(v, 16),
                Text = t,
                Font = Enum.Font.FredokaOne,
                TextSize = 9,
                TextColor3 = Color3.fromRGB(255, 255, 255),
                TextXAlignment = Enum.TextXAlignment.Center,
                ZIndex = 11,
                Parent = o,
            }
        )
        e(T, 5)
        return T
    end
    local function o(T)
        if K and K(T) then
            return "kawaifu"
        end
        if y and y(T) then
            return "moby"
        end
        return nil
    end
    local function y(K)
        local T = z[K.UserId]
        if not T or not T.Parent then
            return
        end
        l(K)
        local j = T:FindFirstChild("UserTag_MOBY") or T:FindFirstChild("UserTag_KAWAIFU")
        local t = o(K)
        if not t then
            if j then
                j:Destroy()
            end
            return
        end
        local function G(v)
            local M = v and v:FindFirstChild("DisplayNameLabel")
            local v = 90
            if M and M:IsA("TextLabel") then
                local w = M.TextBounds.X
                if w and w > 0 then
                    v = w
                end
            end
            return 50 + v + 8
        end
        local v = t == "kawaifu" and "UserTag_KAWAIFU" or "UserTag_MOBY"
        local M = G(T)
        if j and j.Name == v then
            j.Position = UDim2.fromOffset(M, 8)
        else
            if j then
                j:Destroy()
            end
            U(T, t, M)
        end
        task.defer(function()
            local T = z[K.UserId]
            if not T or not T.Parent then
                return
            end
            local j = o(K)
            local K = T:FindFirstChild("UserTag_MOBY") or T:FindFirstChild("UserTag_KAWAIFU")
            if not j then
                if K then
                    K:Destroy()
                end
                return
            end
            local o = j == "kawaifu" and "UserTag_KAWAIFU" or "UserTag_MOBY"
            local t = G(T)
            if K and K.Name == o then
                K.Position = UDim2.fromOffset(t, 8)
            else
                if K then
                    K:Destroy()
                end
                U(T, j, t)
            end
        end)
    end
    local function U(K, o, T, j)
        local t = J(
            "TextButton",
            {
                Size = UDim2.fromOffset(R.ButtonSize, R.ButtonSize),
                BackgroundTransparency = 1,
                AutoButtonColor = false,
                Text = "",
                LayoutOrder = T,
                Parent = K,
            }
        )
        local K = J(
            "Frame",
            {
                Name = "Box",
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.new(0.5, 0, 0.5, 0),
                Size = UDim2.fromOffset(R.BoxSize, R.BoxSize),
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                Parent = t,
            }
        )
        e(K, 6)
        local T = J(
            "Frame",
            {
                Name = "Fill",
                BackgroundColor3 = R.ButtonBg,
                BackgroundTransparency = 0.08,
                BorderSizePixel = 0,
                Size = UDim2.fromScale(1, 1),
                Parent = K,
            }
        )
        e(T, 6)
        local T = Q(K, R.ButtonStroke, 1.1, 1)
        T.Name = "Stroke"
        J(
            "TextLabel",
            {
                BackgroundTransparency = 1,
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.new(0.5, 0, 0.5, 0),
                Size = UDim2.fromOffset(R.BoxSize, R.BoxSize),
                Text = o.label,
                Font = Enum.Font.FredokaOne,
                TextSize = R.IconSize,
                TextColor3 = R.Text,
                TextXAlignment = Enum.TextXAlignment.Center,
                TextYAlignment = Enum.TextYAlignment.Center,
                Parent = t,
            }
        )
        t:SetAttribute("Locked", false)
        t:SetAttribute("ActionName", o.action)
        t:SetAttribute("TargetPlayerName", j and j.Name or "")
        Y(t, "normal")
        t.MouseEnter:Connect(function()
            if t:GetAttribute("Locked") then
                return
            end
            Y(t, "hover")
        end)
        t.MouseLeave:Connect(function()
            if t:GetAttribute("Locked") then
                return
            end
            Y(t, "normal")
        end)
        table.insert(q[o.action], t)
        local K = f(o.action)
        local q = p(j)
        local T = K or q
        t.Active = not T
        t:SetAttribute("Locked", T)
        if T then
            Y(t, "locked")
        end
        t.MouseButton1Click:Connect(function()
            if p(j) then
                F("ADMIN", "AP Panel blocked on Moby users")
                l(j)
                return
            end
            if o.action == "tp" then
                local K, q = X(j)
                if not K then
                    F("ADMIN", "Failed TP: " .. tostring(q or (j and j.Name or "target")))
                    return
                end
                F("ADMIN", "TP'd to first floor of " .. (j and j.Name or "target"))
                return
            end
            if f(o.action) then
                return
            end
            local K = j and j.Name or "target"
            F("ADMIN", "Attempting " .. o.action .. " on " .. K)
            W(o.action)
            local q = O(j, o.action)
            if q == false then
                F("ADMIN", "Failed to send " .. o.action .. " to " .. K)
            else
                F("ADMIN", "Sent " .. o.action .. " to " .. K)
            end
        end)
        return t
    end
    local function K(q)
        if not q or q == C then
            return
        end
        if z[q.UserId] and z[q.UserId].Parent then
            return
        end
        local o = 0
        for Y, Y in ipairs(c.listHolder:GetChildren()) do
            if Y:IsA("Frame") and Y.Name:match("^Row_") then
                o = o + 1
            end
        end
        local Y = o % 2 == 0
        local o = Y and R.RowA or R.RowB
        local Y = J(
            "Frame",
            {
                Name = ("Row_%d"):format(q.UserId),
                BackgroundColor3 = o,
                BackgroundTransparency = 0.4,
                BorderSizePixel = 0,
                Size = UDim2.new(1, 0, 0, R.RowHeight),
                ZIndex = 5,
                Parent = c.listHolder,
            }
        )
        B = B + 1
        Y:SetAttribute("CreateIndex", B)
        Y:SetAttribute("PlayerName", q.Name)
        e(Y, R.RowCorner)
        Q(Y, R.AccentSoft, 1, 0.98)
        Y.ClipsDescendants = true
        z[q.UserId] = Y
        Y.MouseEnter:Connect(function()
            Y.BackgroundColor3 = R.RowHover
        end)
        Y.MouseLeave:Connect(function()
            Y.BackgroundColor3 = o
        end)
        local B = J(
            "Frame",
            {
                BackgroundColor3 = Color3.fromRGB(9, 10, 14),
                BorderSizePixel = 0,
                Size = UDim2.fromOffset(34, 34),
                Position = UDim2.fromOffset(8, 10),
                Parent = Y,
            }
        )
        e(B, 8)
        Q(B, R.AccentSoft, 1, 0.72)
        local Q = J(
            "ImageLabel",
            {
                BackgroundTransparency = 1,
                Size = UDim2.fromScale(1, 1),
                Position = UDim2.fromOffset(0, 0),
                Image = "rbxthumb://type=AvatarHeadShot&id=" .. q.UserId .. "&w=150&h=150",
                ZIndex = 10,
                Parent = B,
            }
        )
        e(Q, 8)
        local e = -(R.ActionsWidth + R.SafeGap)
        local Q = J(
            "TextLabel",
            {
                Name = "DisplayNameLabel",
                BackgroundTransparency = 1,
                Position = UDim2.fromOffset(50, 6),
                Size = UDim2.new(1, e, 0, 20),
                TextXAlignment = Enum.TextXAlignment.Left,
                Text = q.DisplayName,
                Font = Enum.Font.FredokaOne,
                TextSize = R.NameSize,
                TextColor3 = R.Text,
                ZIndex = 10,
                Parent = Y,
            }
        )
        y(q)
        J(
            "TextLabel",
            {
                BackgroundTransparency = 1,
                Position = UDim2.fromOffset(50, 23),
                Size = UDim2.new(1, e, 0, 16),
                TextXAlignment = Enum.TextXAlignment.Left,
                Text = "@" .. q.Name,
                Font = Enum.Font.FredokaOne,
                TextSize = R.UserSize,
                TextColor3 = R.SubText,
                ZIndex = 10,
                Parent = Y,
            }
        )
        local Q = J(
            "TextLabel",
            {
                Name = "Status",
                BackgroundTransparency = 1,
                Position = UDim2.fromOffset(50, 37),
                Size = UDim2.new(1, e, 0, 14),
                TextXAlignment = Enum.TextXAlignment.Left,
                Text = "",
                Font = Enum.Font.FredokaOne,
                TextSize = R.StatusSize,
                TextColor3 = R.Yellow,
                ZIndex = 10,
                Parent = Y,
            }
        )
        x[q.UserId] = Q
        local e = J(
            "Frame",
            {
                Name = "Actions",
                BackgroundTransparency = 1,
                AnchorPoint = Vector2.new(1, 0.5),
                Position = UDim2.new(1, -8.0, 0.5, 0),
                Size = UDim2.fromOffset(R.ActionsWidth, R.ButtonSize),
                ZIndex = 12,
                Parent = Y,
            }
        )
        local Q = Instance.new("UIListLayout")
        Q.FillDirection = Enum.FillDirection.Horizontal
        Q.SortOrder = Enum.SortOrder.LayoutOrder
        Q.HorizontalAlignment = Enum.HorizontalAlignment.Left
        Q.VerticalAlignment = Enum.VerticalAlignment.Center
        Q.Padding = UDim.new(0, 0)
        Q.Parent = e
        for Q, B in ipairs(n) do
            U(e, B, Q, q)
        end
        local n = J(
            "Frame",
            {
                Name = "Line",
                BackgroundColor3 = R.RowLine,
                BackgroundTransparency = 0.93,
                BorderSizePixel = 0,
                Position = UDim2.new(0, 16, 1, -1.0),
                Size = UDim2.new(1, -32.0, 0, 1),
                Parent = Y,
            }
        )
        Y.InputBegan:Connect(function(R)
            if R.UserInputType ~= Enum.UserInputType.MouseButton1 then
                return
            end
            if p(q) then
                F("ADMIN", "AP Panel blocked on Moby users")
                l(q)
                return
            end
            local R = { "ragdoll", "jail", "rocket", "balloon", "jumpscare", "inverse", "tiny", "morph" }
            local n = false
            for J, J in ipairs(R) do
                if not f(J) then
                    n = true
                    break
                end
            end
            if n then
                local n = false
                for J, J in ipairs(R) do
                    if f(J) then
                        continue
                    end
                    W(J)
                    if p(q) then
                        F("ADMIN", "AP Panel blocked on Moby users")
                        l(q)
                        break
                    end
                    O(q, J)
                    n = true
                    task.wait(0.01)
                end
                if n then
                    F("ADMIN", "Triggered ALL on " .. q.Name)
                end
            end
        end)
        local R = {}
        table.insert(
            R,
            q:GetAttributeChangedSignal("Stealing"):Connect(function()
                a(q)
            end)
        )
        table.insert(
            R,
            q:GetAttributeChangedSignal("StealingIndex"):Connect(function()
                a(q)
            end)
        )
        table.insert(
            R,
            q.CharacterAdded:Connect(function(n)
                task.defer(function()
                    y(q)
                    local f = pcall(function()
                        n.DescendantAdded:Connect(function()
                            task.defer(function()
                                y(q)
                            end)
                        end)
                        n.DescendantRemoving:Connect(function()
                            task.defer(function()
                                y(q)
                            end)
                        end)
                    end)
                end)
            end)
        )
        if q.Character then
            table.insert(
                R,
                q.Character.DescendantAdded:Connect(function()
                    task.defer(function()
                        y(q)
                    end)
                end)
            )
            table.insert(
                R,
                q.Character.DescendantRemoving:Connect(function()
                    task.defer(function()
                        y(q)
                    end)
                end)
            )
        end
        V[q.UserId] = R
        a(q)
        y(q)
        l(q)
        P()
    end
    local function R(n)
        local f = z[n.UserId]
        if f then
            f:Destroy()
            z[n.UserId] = nil
        end
        x[n.UserId] = nil
        local f = V[n.UserId]
        if f then
            for J, J in ipairs(f) do
                pcall(function()
                    J:Disconnect()
                end)
            end
            V[n.UserId] = nil
        end
    end
    c.refreshBtn.MouseButton1Click:Connect(function()
        for n, n in ipairs(r:GetPlayers()) do
            if n ~= C then
                K(n)
                a(n)
            end
        end
        P()
        F("ADMIN PANEL", "Player list refreshed")
    end)
    for P, P in ipairs(r:GetPlayers()) do
        if P ~= C then
            K(P)
        end
    end
    r.PlayerAdded:Connect(function(P)
        task.defer(function()
            K(P)
        end)
    end)
    r.PlayerRemoving:Connect(R)
    task.spawn(function()
        while c.adminGui.Parent do
            I()
            for P, P in ipairs(r:GetPlayers()) do
                if P ~= C then
                    y(P)
                end
            end
            task.wait(0.25)
        end
    end)
    if i.HideAdminPanel then
        c.adminGui.Enabled = false
    end
end
d()
-- =========================================
-- ACTIONS MENU (ALWAYS VISIBLE & BINDABLE) & SPEED BOOSTER
-- =========================================
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local actionGuiParent = CoreGui
pcall(function() if gethui then actionGuiParent = gethui() end end)

-- === ACTION MENU ===
local theme = {
    bg = Color3.fromRGB(18, 27, 50),
    btnBg = Color3.fromRGB(25, 35, 60),
    text = Color3.fromRGB(255, 255, 255),
    subText = Color3.fromRGB(150, 150, 150)
}

local actionGui = Instance.new("ScreenGui")
actionGui.Name = "ActionMenuUI_V6"
actionGui.ResetOnSpawn = false
pcall(function() if syn and syn.protect_gui then syn.protect_gui(actionGui) end end)
actionGui.Parent = actionGuiParent

local actionFrame = Instance.new("Frame", actionGui)
actionFrame.Size = UDim2.new(0, 160, 0, 155)
actionFrame.Position = UDim2.new(0, 20, 0.5, -80)
actionFrame.BackgroundColor3 = theme.bg
actionFrame.BorderSizePixel = 0
actionFrame.Active = true
actionFrame.Draggable = true
Instance.new("UICorner", actionFrame).CornerRadius = UDim.new(0, 8)

local shadow = Instance.new("UIStroke", actionFrame)
shadow.Color = Color3.fromRGB(0, 178, 255)
shadow.Thickness = 1.5
shadow.Transparency = 0.5

local title = Instance.new("TextLabel", actionFrame)
title.Size = UDim2.new(1, 0, 0, 22)
title.Position = UDim2.new(0, 0, 0, 8)
title.BackgroundTransparency = 1
title.Text = "ACTIONS"
title.Font = Enum.Font.GothamBlack
title.TextSize = 14
title.TextColor3 = theme.text

local subTitle = Instance.new("TextLabel", actionFrame)
subTitle.Size = UDim2.new(1, 0, 0, 14)
subTitle.Position = UDim2.new(0, 0, 0, 26)
subTitle.BackgroundTransparency = 1
subTitle.Text = "Menu"
subTitle.Font = Enum.Font.Gotham
subTitle.TextSize = 10
subTitle.TextColor3 = theme.subText

local btnContainer = Instance.new("Frame", actionFrame)
btnContainer.Size = UDim2.new(1, -24, 1, -55)
btnContainer.Position = UDim2.new(0, 12, 0, 45)
btnContainer.BackgroundTransparency = 1

local layout = Instance.new("UIListLayout", btnContainer)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Padding = UDim.new(0, 6)

local keybinds = {}
local isBinding = false

local function createActionBtn(text, order, callback)
    local btn = Instance.new("TextButton", btnContainer)
    btn.Size = UDim2.new(1, 0, 0, 30)
    btn.BackgroundColor3 = theme.btnBg
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 12
    btn.TextColor3 = theme.text
    btn.LayoutOrder = order
    btn.AutoButtonColor = false
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    local originalText = text
    local boundKey = nil

    local function updateText()
        if boundKey then
            btn.Text = originalText .. " (" .. boundKey.Name .. ")"
        else
            btn.Text = originalText
        end
    end

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 45, 75)}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = theme.btnBg}):Play()
    end)
    
    btn.MouseButton1Click:Connect(function()
        if not isBinding then
            callback()
        end
    end)
    
    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton2 then
            if isBinding then return end
            isBinding = true
            btn.Text = "Press key..."
            
            local conn
            conn = UserInputService.InputBegan:Connect(function(inp)
                if inp.UserInputType == Enum.UserInputType.Keyboard then
                    conn:Disconnect()
                    task.wait(0.1)
                    isBinding = false
                    
                    if inp.KeyCode == Enum.KeyCode.Escape or inp.KeyCode == Enum.KeyCode.Backspace then
                        if boundKey and keybinds[boundKey] then
                            keybinds[boundKey] = nil
                        end
                        boundKey = nil
                    elseif inp.KeyCode ~= Enum.KeyCode.Unknown then
                        if boundKey and keybinds[boundKey] then
                            keybinds[boundKey] = nil
                        end
                        boundKey = inp.KeyCode
                        keybinds[boundKey] = callback
                    end
                    updateText()
                end
            end)
        end
    end)
end

TeleportService.TeleportInitFailed:Connect(function()
    pcall(function()
        TeleportService:Teleport(game.PlaceId)
    end)
end)

createActionBtn("Rejoin", 1, function()
    pcall(function()
        local qot = queue_on_teleport or queueonteleport or (syn and syn.queue_on_teleport)
        if qot then
            qot("repeat task.wait() until game:IsLoaded()")
        end
    end)

    local p = LocalPlayer or Players.LocalPlayer or game:GetService("Players").LocalPlayer
    if p then
        p:Kick("\n[Rejoining...]")
    end

    task.spawn(function()
        task.wait(0.02)
        local placeId = game.PlaceId
        local jobId = game.JobId
        pcall(function()
            if #Players:GetPlayers() > 1 and jobId and jobId ~= "" then
                TeleportService:TeleportToPlaceInstance(placeId, jobId)
            else
                TeleportService:Teleport(placeId)
            end
        end)
    end)
end)

createActionBtn("Reset", 2, function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.Health = 0
    end
end)

createActionBtn("Kick", 3, function()
    LocalPlayer:Kick("\n[Kicked by User]")
end)


UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe or isBinding then return end
    if input.UserInputType == Enum.UserInputType.Keyboard then
        if keybinds[input.KeyCode] then
            keybinds[input.KeyCode]()
        end
    end
end)


-- === SPEED BOOSTER ===
local DEFAULT_SPEED = 100
local currentSpeed = DEFAULT_SPEED
local boostEnabled = false
local boostConn = nil
local lvAttachment = nil
local linearVelocity = nil

local boosterGui = Instance.new("ScreenGui")
boosterGui.Name = "SpeedBoosterUI"
boosterGui.ResetOnSpawn = false
pcall(function() if syn and syn.protect_gui then syn.protect_gui(boosterGui) end end)
boosterGui.Parent = actionGuiParent

local bFrame = Instance.new("Frame")
bFrame.Name = "MainFrame"
bFrame.Size = UDim2.new(0, 160, 0, 110)
bFrame.Position = UDim2.new(1, -340, 0, 50)
bFrame.BackgroundColor3 = Color3.fromRGB(18, 27, 50) 
bFrame.BackgroundTransparency = 0.05
bFrame.BorderSizePixel = 0
bFrame.Active = true
bFrame.Draggable = true
bFrame.Visible = false
bFrame.Parent = boosterGui
Instance.new("UICorner", bFrame).CornerRadius = UDim.new(0, 10)

local bStroke = Instance.new("UIStroke")
bStroke.Color = Color3.fromRGB(0, 178, 255)
bStroke.Thickness = 1.5
bStroke.Parent = bFrame

local bTitle = Instance.new("TextLabel")
bTitle.Text = "Speed Booster"
bTitle.Font = Enum.Font.FredokaOne
bTitle.TextColor3 = Color3.fromRGB(240, 240, 240)
bTitle.TextSize = 13
bTitle.BackgroundTransparency = 1
bTitle.Size = UDim2.new(1, -16, 0, 25)
bTitle.Position = UDim2.new(0, 8, 0, 2)
bTitle.TextXAlignment = Enum.TextXAlignment.Center
bTitle.Parent = bFrame

local bContent = Instance.new("Frame")
bContent.Name = "ContentFrame"
bContent.Position = UDim2.new(0, 0, 0, 30)
bContent.Size = UDim2.new(1, 0, 1, -30)
bContent.BackgroundTransparency = 1
bContent.Parent = bFrame

local bListLayout = Instance.new("UIListLayout")
bListLayout.Padding = UDim.new(0, 8)
bListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
bListLayout.Parent = bContent
local bPad = Instance.new("UIPadding")
bPad.PaddingTop = UDim.new(0, 5)
bPad.Parent = bContent

local bSpeedRow = Instance.new("Frame")
bSpeedRow.Size = UDim2.new(1, -20, 0, 24)
bSpeedRow.BackgroundTransparency = 1
bSpeedRow.Parent = bContent

local bSpeedLabel = Instance.new("TextLabel")
bSpeedLabel.Text = "Speed"
bSpeedLabel.Font = Enum.Font.GothamBold
bSpeedLabel.TextSize = 11
bSpeedLabel.Size = UDim2.new(0.4, 0, 1, 0)
bSpeedLabel.BackgroundTransparency = 1
bSpeedLabel.TextColor3 = Color3.fromRGB(240, 240, 240)
bSpeedLabel.Parent = bSpeedRow

local bSpeedInput = Instance.new("TextBox")
bSpeedInput.Size = UDim2.new(0.6, 0, 1, 0)
bSpeedInput.Position = UDim2.new(0.4, 0, 0, 0)
bSpeedInput.BackgroundColor3 = Color3.fromRGB(10, 15, 30)
bSpeedInput.TextColor3 = Color3.fromRGB(240, 240, 240)
bSpeedInput.Font = Enum.Font.GothamBold
bSpeedInput.TextSize = 11
bSpeedInput.ClearTextOnFocus = false
bSpeedInput.Text = tostring(DEFAULT_SPEED)
bSpeedInput.Parent = bSpeedRow
Instance.new("UICorner", bSpeedInput).CornerRadius = UDim.new(0, 4)
Instance.new("UIStroke", bSpeedInput).Color = Color3.fromRGB(0, 178, 255)

local bActivateBtn = Instance.new("TextButton")
bActivateBtn.Size = UDim2.new(1, -20, 0, 26)
bActivateBtn.Font = Enum.Font.FredokaOne
bActivateBtn.TextSize = 12
bActivateBtn.Text = "ACTIVATE"
bActivateBtn.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
bActivateBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
bActivateBtn.AutoButtonColor = false
bActivateBtn.Parent = bContent
Instance.new("UICorner", bActivateBtn).CornerRadius = UDim.new(0, 6)

local function destroyLV()
    if linearVelocity then linearVelocity:Destroy() end
    if lvAttachment then lvAttachment:Destroy() end
    linearVelocity, lvAttachment = nil, nil
end

local function setupLV(hrp)
    destroyLV()
    lvAttachment = Instance.new("Attachment", hrp)
    linearVelocity = Instance.new("LinearVelocity")
    linearVelocity.Attachment0 = lvAttachment
    linearVelocity.VelocityConstraintMode = Enum.VelocityConstraintMode.Plane
    linearVelocity.PrimaryTangentAxis = Vector3.new(1, 0, 0)
    linearVelocity.SecondaryTangentAxis = Vector3.new(0, 0, 1)
    linearVelocity.MaxForce = math.huge
    linearVelocity.PlaneVelocity = Vector2.zero
    linearVelocity.RelativeTo = Enum.ActuatorRelativeTo.World
    linearVelocity.Parent = hrp
end

local function toggleBoost()
    boostEnabled = not boostEnabled
    if boostEnabled then
        bActivateBtn.Text = "ACTIVATED"
        bActivateBtn.BackgroundColor3 = Color3.fromRGB(39, 174, 96)
        if boostConn then boostConn:Disconnect() end
        boostConn = RunService.Heartbeat:Connect(function()
            local char = Players.LocalPlayer.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hrp or not hum then return end
            if not linearVelocity or linearVelocity.Parent ~= hrp then
                setupLV(hrp)
            end
            
            -- Only boost when stealing
            if char:GetAttribute("Stealing") ~= true then
                linearVelocity.PlaneVelocity = Vector2.zero
                return
            end

            local md = hum.MoveDirection
            if md.Magnitude > 0.1 then
                local flat = Vector3.new(md.X, 0, md.Z).Unit
                linearVelocity.PlaneVelocity = Vector2.new(flat.X * currentSpeed, flat.Z * currentSpeed)
            else
                linearVelocity.PlaneVelocity = Vector2.zero
            end
        end)
    else
        bActivateBtn.Text = "ACTIVATE"
        bActivateBtn.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
        if boostConn then boostConn:Disconnect() end
        destroyLV()
    end
end

bActivateBtn.MouseButton1Click:Connect(toggleBoost)
bSpeedInput.FocusLost:Connect(function()
    local num = tonumber(bSpeedInput.Text)
    currentSpeed = num and math.max(1, num) or DEFAULT_SPEED
    bSpeedInput.Text = tostring(currentSpeed)
end)
bSpeedInput:GetPropertyChangedSignal("Text"):Connect(function()
    local num = tonumber(bSpeedInput.Text)
    if num then currentSpeed = math.max(1, num) end
end)

_G.ToggleSpeedBoosterGUI = function(state)
    bFrame.Visible = state
end
